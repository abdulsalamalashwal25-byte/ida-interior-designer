# واجهات الوكيل — Agent Interfaces
**Interior Designer Agent · واجهات التشغيل الموحدة (API + CLI + Viewer)**
**التاريخ:** 2026-09-15 · **الحالة:** عاملة ومختبرة (45/45)

> هذا المستند يوثق ما بُني بعد فحص ملفات المستودع: طبقة واجهات فوق المحركات
> القائمة دون إعادة تنفيذ أي قاعدة. القرارات المعمارية المقفلة لم تُمس.

---

## 1. نتائج فحص الملفات (قبل البناء)

### 1.1 مخزون المستودع

| المكوّن | المحتوى | الحالة عند الفحص |
|---|---|---|
| `*.md` (~50 ملفاً) | المواصفات المعتمدة R01، خرائط المسؤولية، تقارير الإغلاق | مكتملة ومقفلة |
| `project_master.schema.json` | مخطط الطبقة A (Draft 2020-12) | سليم |
| `project_master.template.json` | القالب R00 (12 مجهولاً مسجلاً) | سليم |
| `scripts/` (41 محركاً) | A + B1..B11 + C1..C6 + D + E + F + Phase01 + SA | سليمة ومنفصلة الملكية |
| `tests/` (23 حزمة + `run_all.py`) | عقود الاختبار لكل مرحلة | 17 خضراء، 6 حمراء (سبب بيئي، §6) |
| **واجهات التشغيل** | **CLI / API / مساحة مشروع / عارض** | **غير موجودة — وهذا ما بُني** |

### 1.2 الفجوة التي عالجها هذا العمل

كانت المحركات تُستدعى فردياً من بايثون فقط: لا أمر موحد، لا إدارة لمساحة
مشروع، لا تسمية ملفات موحدة، لا مسار حوكمة عملي (اقتراح ← قرار ← CR)، ولا
عارض للمخرجات. أي تشغيل حقيقي كان يتطلب معرفة داخلية بكل محرك.

### 1.3 اكتشافات أثناء الفحص (مثبتة بالاختبار)

1. **فجوة ناقل G3 (مهمة):** `p1_validate.run_schema_gate` يتوقع كائنات
   `Finding`، لكن `schema_gate.validate_master` يُرجع نصوصاً عادية، فتصل
   المراجع فارغة (`rule/location/message/severity = null`) و`error_count`
   صفر دائماً — أي أن ملفاً مرفوضاً مخططياً كان سيُعلن `PASS`.
   **المعالجة:** إكمال في طبقة الواجهة فقط (§5.1) — لم يُمس أي محرك.
2. **محرك C5 لا يعمل على Python 3.11:** سطر واحد بصياغة f-string الخاصة
   بـ 3.12+. **المعالجة:** إصلاح نحوي مكافئ تماماً (§5.2) أعاد حزمة D
   كلها للعمل (81/81).
3. **6 حزم اختبار حمراء:** كلها `SyntaxError` داخل ملفات الاختبار نفسها
   (صياغة 3.12 في بيئة 3.11) — ليست فشلاً منطقياً، ولم تُمس.
4. **مجهولات القالب تبقى مفتوحة بعد G2:** يملأ G2 الحقول لكنه لا يُغلق
   بنود `registers.unknowns` المقابلة، فتبقى الجاهزية محجوبة. سلوك
   محركات (نطاق Phase 01 مقفل) — الواجهة تُظهره بصدق ولا تتحايل عليه.

---

## 2. ما تم بناؤه

```
ida-interior-designer/
├── ida.py                  # واجهة سطر الأوامر الموحدة (20 أمراً)
├── scripts/
│   ├── ida_api.py          # الواجهة البرمجية عالية المستوى (JSON-safe)
│   └── ida_workspace.py    # مساحة المشروع + التسمية + الشارة + الهوية
├── examples/
│   ├── demo_intake.json        # إجابات تجريبية (اختبارية فقط)
│   ├── demo_field_specs.json   # تصنيف الحقول المعلن
│   ├── make_demo_master.py     # مولّد المثال (خط أنابيب حقيقي + جدران ثابتة)
│   └── demo_master.json        # مخرج المولّد (PRJ-90، موسوم TEST)
├── viewer/
│   └── index.html          # عارض قراءة فقط (عربي RTL، ملف واحد، بلا اعتماديات)
├── tests/
│   └── test_interfaces.py  # حزمة دخان الواجهات (45 اختباراً، Python 3.11)
├── requirements.txt        # jsonschema (طبقة المخطط فقط)
└── AGENT-INTERFACES.md     # هذا المستند
```

### 2.1 الأوامر — `python3 ida.py <command>`

| الأمر | العملية | المحرك المالك |
|---|---|---|
| `init PRJ-01` | إنشاء مساحة مشروع (استبيان + تصنيف + مجلدات A/B/C) | — (اتفاقيات ملفات) |
| `intake` | بوابة G1: هل يجوز بدء التعبئة؟ | p1_intake |
| `build` | خط Phase 01 الكامل: استبيان ← master مُتحقق | p1_pipeline (G1..G5) |
| `validate` | A + B1..B11 بتقارير محمولة كما صدرت | schema_gate + B1..B11 |
| `readiness` | الجاهزية للاعتماد النهائي (الحجب معلن كاملاً) | schema_gate + B7 |
| `status` | لوحة قراءة فقط (عناصر/سجلات/حضور/جاهزية) | قارئ + readiness |
| `plan` | مخطط Class A: بوابة C1 ← باعث C3 ← حكم D | C1 + C3 + D |
| `quantities` | جدول الكميات Q-01..Q-04 | C4 |
| `present` | عرض Class B من تجميع A (يرفض خارج العقد) | C5 |
| `manifest` | تجميع أدلة C6 (بلا أي حكم) | C6 |
| `fidelity` | حكم D على SVG مقابل تجميع GA | D |
| `existing-state` | تحليل الوضع الراهن O-1 (دليل تحليلي، ليس A) | SA Unit 1 |
| `propose` | تسجيل اقتراح [P] (OPEN، ليس قراراً أبداً) | registers |
| `decide` | قرار USER بسجل كامل (يربط الاقتراح ذهاباً وإياباً) | registers + B1/B10 |
| `object` | اعتراض OBJ مفتوح (RL-003 يشتعل ضد قرار قائم — مقصود) | registers + B10 |
| `cr` | فتح CR (MINOR/MAJOR سواء، PENDING_APPROVAL) | registers |
| `approve-cr` | اعتماد CR (USER حصراً، بنداء صريح) | — (GAP-02 موثق) |
| `apply-change` | تطبيق CR معتمد على مسار واحد (الكاتب الوحيد لـ [C]) | registers + B9/B10 |
| `caption` | صياغة/تدقيق ختم الضمان (سياسة اللغة A/B/C) | schema_gate lint |
| `test` | تشغيل الانحدار الكامل أو حزمة واحدة | tests/run_all.py |
| `serve` | معاينة HTTP للمشروع (يربط 0.0.0.0) | http.server |

كل أمر يقبل `--json` للمخرجات الآلية. رموز الخروج:
`0` نجاح · `1` خطأ · `2` استخدام خاطئ · **`3` امتناع/حجب** (نتيجة
نطاقية مشروعة، ليست عطلاً: امتناع C1/C3، حجب G1، جاهزية سالبة).

### 2.2 مساحة المشروع

```
projects/PRJ-01/
├── intake.json            # إجاباتك (فارغ = [U]، لا تخمين)
├── field_specs.json       # تصنيف BLOCKING/NON_BLOCKING المعلن
├── project_master.json    # الحقيقة الوحيدة (يُبنى بـ build، لا باليد)
├── 05_VIEWS/A_accurate/   # Class A — ملزِم هندسياً
├── 05_VIEWS/B_presentation/ # Class B — هندسة من A، تجميل مختلف
├── 05_VIEWS/C_ai_mood/    # Class C — إلهام غير ملزِم
└── reports/               # تحقق/جاهزية/كميات/أمانة/دليل — JSON
```

التسمية: `PRJ-01_R01_A_FloorPlan.svg` (المواصفة §10). معرف المشروع يُفحص
عند `init` ضد نمط المخطط `PRJ-NN` — فشل سريع بدل عمل ضائع.

### 2.3 العارض — `viewer/index.html`

ملف واحد، بلا اعتماديات، عربي RTL، **قراءة فقط**: يحمّل
`project_master.json` (بطاقات + وسوم + مجهولات + سجل تغييرات)، ويعرض
SVG الفئتين A/B، ويعرض أي تقرير JSON. يعمل بفتح الملف مباشرة أو عبر
`ida serve --dir <project>` مع بارامترات `?master=&svgA=&svgB=`.

---

## 3. التشغيل السريع (نص مُجرّب)

```bash
pip install -r requirements.txt

# 1. مشروع جديد من المثال
python3 ida.py init PRJ-90 --name "Demo" --dir projects/PRJ-90 --test
cp examples/demo_intake.json examples/demo_field_specs.json projects/PRJ-90/

# 2. بناء النموذج
python3 ida.py build --project projects/PRJ-90
# → BLOCKED_DECLARED خروج 3 (جاهزية سالبة مشروعة: مجهولات حاجبة مفتوحة)

# 3. تحقق ولوحة
python3 ida.py validate --master projects/PRJ-90/project_master.json
python3 ida.py status   --master projects/PRJ-90/project_master.json

# 4. توليد (بعد إضافة جدران مؤكدة من رفع مساحي)
python3 ida.py plan --master <master> --project projects/PRJ-90
# → 05_VIEWS/A_accurate/PRJ-90_R00_A_FloorPlan.svg + fidelity VERIFIED
python3 ida.py quantities --master <master>
python3 ida.py present --assembly reports/<bundle>.json --project projects/PRJ-90
python3 ida.py manifest --record C3=... --record C5=... --record C4=...

# 5. حوكمة
python3 ida.py propose "نص الاقتراح" --master <m> --in-place
python3 ida.py decide "العنوان" --proposal P-001 --rationale "لماذا…" \
    --revision R01 --master <m> --in-place
python3 ida.py cr --target-element space/ceiling_height --current 2800 \
    --proposed 2900 --reason "…" --impact "…" --master <m> --in-place
python3 ida.py approve-cr CR-001 --master <m> --in-place
python3 ida.py apply-change CR-001 --path space/ceiling_height \
    --fact '{...tagged fact...}' --master <m> --in-place

# 6. معاينة
python3 ida.py serve --dir projects/PRJ-90 --port 8000
```

دورة كاملة جاهزة للتجربة فوراً على `examples/demo_master.json`
(استوديو 6000×4000، 4 جدران، تحقق PASS بصفر أخطاء).

---

## 4. حدود الحوكمة في الواجهات (مفروضة، لا شعارات)

1. **لا اختراع:** الواجهة لا تبني أي قيمة — `build` يستهلك خط Phase 01،
   و`apply-change` يكتب فقط ما أُعطي صراحة في `--fact`.
2. **لا ترقية ذاتية:** لا مسار في الواجهة يحوّل [P]/[A] إلى [C] إلا عبر
   قرار USER صريح (`decide`) أو CR معتمد (`apply-change`).
3. **No Auto-Apply:** رفض مثبت بالاختبار — CR غير معتمد لا يغيّر شيئاً،
   والاعتماد بنداء صريح فقط (`approve-cr`)، ولا شيء في الخط يستدعيه.
4. **USER وحده يعتمد:** أي `actor` غير USER مرفوض؛ و`approved_by` لا يقبل
   إلا USER. (GAP-02 تبقى موثقة: لا يوجد دفتر اعتماد خارجي بعد.)
5. **سجلات مطابقة للمخطط حرفياً:** الحقول والحالات والقيم الدنيا تُفحص
   عند الحدود، فلا تكتب الواجهة بنداً سيرفضه المخطط (مثبت: master بعد
   سلسلة حوكمة كاملة يتحقق PASS).
6. **SVG المُقاس لا يُلمس:** نص SVG يُحفظ كما بعثه C3/C5 حرفياً — أي ختم
   بشري يعيش في التقرير الجانبي، لأن العبث بالنص يخرجه عن عقد القياس D.
7. **امتناع صادق:** C1/C3/G1 تمتنع بأكواد وأسباب وروافع، والخروج 3 يميّز
   الامتناع عن العطل.

---

## 5. إصلاحان أثناء البناء (موثقان، بأقل لمس ممكن)

### 5.1 إكمال صدق التحقق (طبقة الواجهة فقط — صفر تعديل في المحركات)

**المشكلة:** ناقل G3 في `p1_validate` يُسقط أخطاء المخطط (نصوص بلا
`severity`) فيُعلن PASS على ملف مرفوض مخططياً (مثبت: سماكة ‎-200‎ كانت
تمر). **الحل:** `ida_api.validate_all` يستهلك أيضاً حكم المحرك المباشر
`schema_gate.validate_master() → (ok, errors)` ويُفشل الملخص عند `ok=False`.
هذا استهلاك لحكم المحرك نفسه (وموثق فيه أنه "hard error") — لا إعادة
تأويل ولا تخفيف. المراجع المحمولة تبقى حرفية في التقرير للتدقيق.
**مطلوب من أعلى:** إصلاح ناقل G3 في Phase 01 ليحوّل نصوص المخطط إلى مراجع
مكتملة (قضية صاعدة، خارج نطاق الواجهات).

### 5.2 توافق C5 مع Python 3.11 (تعديل نحوي صفري الدلالة)

**المشكلة:** سطر واحد في `scripts/c5_presentation.py` يستخدم backslash
داخل تعبير f-string (صياغة 3.12+ فقط) فيتعذر استيراد المحرك كله على 3.11.
**الحل:** إخراج النص خارج التعبير إلى متغير (`_empty_stroke`) — النص
المُبعث متطابق حرفياً. موسوم `COMPAT-3.11` في موضعه.
**الأثر الجانبي الإيجابي:** حزمة D (81 اختباراً) كانت تستورد C5 فعادت
خضراء كاملة.

---

## 6. حالة الاختبارات

```
run_all.py بعد العمل: 19/24 خضراء (كانت 17/23)
├── كل ما كان أخضر بقي أخضر (صفر انحدار)
├── D عادت خضراء (81/81) بفضل إصلاح C5
├── IF الجديدة خضراء (45/45): مساحة/خط/تحقق/توليد/حوكمة/CLI
└── 5 حمراء: SyntaxError داخل ملفات الاختبار نفسها (صياغة f-string
    الخاصة بـ 3.12 في بيئة 3.11) — C2/C3/C5/C6/SA. المحركات المقابلة
    تعمل وتُغطيها حزمة IF (بوابة C1، باعث C3، عرض C5، دليل C6، O-1).
```

التوصية: الترقية إلى Python 3.12+ في بيئة CI تجعل الكل أخضر دون تغيير
أي سطر؛ أو إصلاح نحوي لملفات الاختبار الخمسة (خارج النطاق المقفل).

---

## 7. خطوات تالية مقترحة (غير منفذة عمداً)

1. إصلاح ناقل G3 صعوداً في Phase 01 (§5.1).
2. دفتر اعتماد خارجي (append-only) لإغلاق GAP-02 وتمكين الاعتماد النهائي.
3. تعبئة الجدران/الفتحات من الاستبيان (G2 اليوم لا يعبئها — نطاق مقفل).
4. إغلاق بنود المجهولات عند تعبئة حقولها (سياسة تسوية صريحة).
5. باعثا DXF/GLB عند توفر البيئة (C3 اليوم SVG فقط — قرار معماري).
6. ترقية بيئة الاختبار إلى Python 3.12+.

---

*بُني بعد فحص كامل ملفات المستودع. المحركات تحكم، والواجهات تنقل وتُظهر —
والملف هو الحقيقة.*
