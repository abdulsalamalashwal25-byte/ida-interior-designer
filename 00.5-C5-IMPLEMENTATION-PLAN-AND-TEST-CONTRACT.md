# 00.5-C5 — IMPLEMENTATION PLAN & TEST CONTRACT
**Gate 1 — تخطيط وعقد اختبار فقط. لا كود · لا اختبارات منفَّذة · لا تعديل Schema/Master/C1–C4/validators · لا C6/D/E · لا معمارية جديدة.**

> **المرجع المغلق:** C5 Architecture CLOSED · DEC-C5-01/02/05/06/08 CLOSED · DEC-C5-07 APPROVED · GAP-C-09 APPROVED WITH CONDITION · **PP-8 إلزامي**.
> **الأساس:** Regression = **850/850**.

---

## 1. الملفات المزمع إنشاؤها (لاحقًا — لم تُنشأ)

| الملف | الحالة المزمعة | المسؤولية | الملكية |
|---|---|---|---|
| `scripts/c5_presentation.py` | **سيُنشأ** | قبول/رفض المعاملات (PP-1…PP-8) · ربط الستايل · إنتاج SVG فئة B · سجل الأدلة | **C5** |
| `tests/test_00_5_C5.py` | **سيُنشأ** | عقد الاختبار أدناه | **C5** |
| `tests/run_all.py` | **سيُعدَّل — سطر واحد** | تسجيل C5 | مشترك |

**لن يُمسّ:** `c3_*` · `c4_*` · `c1_*` · `c2_*` · أي validator · Schema · Template · Master.

---

## 2. GA HANDOFF — خطة التنفيذ

**المدخل الوحيد:** `record["assembly"]` الناتج فعليًا من تشغيل مسار A.

**سطح الـartifact (مقاس فعليًا، لا مفترض):**

| المستوى | المفاتيح |
|---|---|
| الجذر | `contract_tiers` · `outline` · `walls` · `openings` · `not_represented` |
| الجدار | `id` · `start` · `end` · `thickness` · `tier` |
| الفتحة | `id` · `kind` · `host_wall` · `offset` · `width` · `p0` · `p1` · `provenance` · `placement_convention` · `tier` |

**قواعد التنفيذ:**

| # | القاعدة |
|---|---|
| **G-1** | C5 **لا يستورد ولا يستدعي** `assemble()` — يُثبَت بفحص المصدر + اختبار عزل |
| **G-2** | C5 **لا يقرأ الماستر** ولا أي مسار هندسي آخر |
| **G-3** | C5 **لا يقرأ نص SVG** الناتج من C3 كمصدر قياس (منع AS-C4-05) |
| **G-4** | C5 يعامل الـartifact **قراءة فقط**؛ يعمل على نسخة ولا يعدّل المُمرَّر (H-2) |
| **G-5** | **A قبل B:** بلا artifact لا إنتاج — امتناع معلن |
| **G-6** | الإحداثيات **تُنسخ حرفيًا** كما وردت — بلا إعادة حساب ولا تقريب (وراثة DEC-C3-01) |

---

## 3. ⚠️ ملاحظتان تقنيتان مسجَّلتان — لا اجتهاد فيهما

### 3.1 H-4 — لا معرّف instance فريد اليوم

**القياس:** سجل C3 يحمل `format` · `contract` · `tiers` · `status` — **ولا يحمل معرّف نسخة فريدًا** (لا `artifact_id` ولا hash). والهوية (`project_id`/`master_revision`) **تُمرَّر من المستدعي** ولا يولّدها C3.

**الأثر:** H-4 (ربط B بـA بالمعرّف) **لا يمكن تحقيقه بمعرّف يولّده C5 من عنده** — وتوليده اجتهاد، وطلبه يعني تعديل C3 المغلق.

**الحل المقترح بلا اختراع ولا تعديل:** C5 يسجّل في أدلته **الهوية المُمرَّرة** (`project_id` · `master_revision`) + **`contract_tiers`** + **بصمة تشخيصية للـartifact عبر `c2.provisional_digest`** موسومة `PROVISIONAL / usable_as_evidence: False`. هذه **قرينة ربط لا معرّف رسمي**، وD يبقى الحاكم.
⇒ **يُسجَّل كـ`OBS-C5-01`** (ملاحظة، ليست blocker): *ربط A↔B اليوم بالقرينة لا بمعرّف رسمي.*

### 3.2 التمييز بين «جزئي» و«خارج النطاق»

**القياس:** `not_represented` تحوي نوعين مختلفين جوهريًا:
- **NR-01/02/03/04/06** — إغفال **عنصر** بسبب أهلية/نقص ⇒ **جزئية حقيقية**.
- **NR-05** — **خارج عقد النطاق** (dimensions · Tier 4) ⇒ **تظهر دائمًا حتى في الحالة الكاملة** (مُثبت: الحالة الكاملة أعطت `['NR-05','NR-05']`).

**الأثر:** لو عدّ التنفيذ كل `not_represented` جزئيةً، لصار **كل مخرج PARTIAL دائمًا** — وهو تشويش يُفرغ الوسم من معناه.

**القاعدة المقررة:** **PARTIAL يُشتق من رموز العناصر (NR-01/02/03/04/06) فقط. NR-05 خارج النطاق ولا تُنتج جزئية.** هذا **اشتقاق من رموز قائمة**, لا قاعدة جديدة.
⇒ **`OBS-C5-02`**.

---

## 4. PRESENTATION PARAMETER — خطة التنفيذ

**الستة المعتمدة حصرًا:** `color` · `line_weight` · `shadow` · `label` · `sheet_order` · `print_scale`.

| القاعدة | كيف تُنفَّذ |
|---|---|
| **PP-1** | غياب معامل ⇒ **لا تطبيق**؛ العنصر يُرسم بلا ستايل مُطبَّق — **لا قيمة بديلة** |
| **PP-2** | خارج الستة ⇒ `REJECTED_OUT_OF_CONTRACT` |
| **PP-3** | غير معروف ⇒ **رفض معلن**, لا تجاهل |
| **PP-4** | الرفض يُعلَن ويستمر؛ **إلا** إن كان جوهريًا ⇒ امتناع |
| **PP-5** | كل مطبَّق/مرفوض يُسجَّل بسببه |
| **PP-6** | **لا يُقرأ معامل من الماستر** ولا يُكتب فيه |
| **PP-7** | اجتياز §5-B شرط لازم لا كافٍ — القائمة هي الحد الأعلى |
| **PP-8** | **لا اختراع** `semantic`/`unit`/`default`/`interpretation`؛ قيمة بمعنى غير محدد ⇒ **رفض معلن** |

**قواعد خاصة مقررة:**
- **`label`** نص تقديمي فقط؛ **وسم يحمل بُعدًا أو رقمًا محسوبًا ⇒ رفض** (خطر Tier 3).
- **`print_scale`** بحدود **SC-1**: حاوية/viewport/framing فقط؛ وأي أثر على الإحداثيات ⇒ **رفض**.
- **`sheet_order`** مقبول عقديًا؛ وبما أن Boards مؤجَّل، **يُسجَّل بلا اختراع سلوك** (PP-4/PP-5) — **ولا يُفتح نظام لوحات**.
- **`line_weight`** إن وصل بلا وحدة والوحدة لازمة لمعناه ⇒ **رفض بـPP-8**, لا افتراض وحدة.

---

## 5. B OUTPUT — خطة التنفيذ

**SVG فقط · Class B · Tier 0+1 · لا PDF/PPTX/Boards/Tier جديد.**

المخرج يحمل: ختم **`CLASS B — PRESENTATION VISUAL`** · النطاق · وسم **PARTIAL** عند الاقتضاء · `fidelity: NOT_ASSESSED — owned by D` · `engineering_fingerprint: NOT_ISSUED` · `D1 NOT_ESTABLISHED / D2 NOT_CLAIMED` · قائمة المعاملات المطبَّقة والمرفوضة.

**تسمية موروثة (Phase 00 §10):** `PRJ01_R01_B_*` · مجلد `05_VIEWS/B_presentation/`.
**ملاحظة:** C5 **يُعيد نصًا ولا يكتب ملفًا** — الحفظ فعل المستدعي، والإدراج في Manifest ملك C6.

---

## 6. GEOMETRY IMMUTABILITY — كيف ستُثبَت

**الاستراتيجية: مقارنة قبل/بعد على الـartifact نفسه + استخراج عكسي من SVG.**

| ما يجب إثباته | طريقة الإثبات |
|---|---|
| **coordinates** | مقارنة عميقة `deepcopy(ga) == ga` بعد التشغيل + مطابقة كل `x/y` مستخرج من SVG مع GA |
| **positions** | `p0/p1` للفتحات مطابقة حرفيًا |
| **units** | لا تحويل؛ الوحدات كما وردت |
| **dimensions** | `thickness`/`width` كما وردت بلا مساس |
| **topology** | `host_wall` وعلاقات العناصر بلا تغيير |
| **element IDs** | مجموعة المعرّفات في SVG = مجموعتها في GA |
| **element count** | عدد العناصر المرسومة = عدد المُدرجة في GA |

**الأداة:** `c2_fingerprint.provisional_digest` على **إسقاط هندسي** قبل وبعد — **تشخيصي وموسوم `usable_as_evidence: False`**, ولا يُقدَّم كبصمة هندسية (محجوبة) ولا كدليل fidelity.

**الحد الصريح:** هذا يثبت أن **C5 لم يغيّر ما تسلّمه**. ولا يثبت أن الهندسة **صحيحة**، ولا أن B **تطابق** A — **ذلك حكم D**.

---

## 7. PARTIAL — خطة التنفيذ

| # | القاعدة |
|---|---|
| **PT-1** | الجزئية تُشتق من **NR-01/02/03/04/06 فقط** (§3.2) |
| **PT-2** | عند الجزئية: **وسم `PARTIAL` بارز داخل SVG** + في سجل الأدلة |
| **PT-3** | إدراج **قائمة المُغفَل** بمعرّفاتها وأسبابها |
| **PT-4** | نص صريح: **ليس تصميمًا مكتملًا** |
| **PT-5** | **PARTIAL لا يمنع فئة B** (اتساقًا مع A-Q7) — ولا أضيف قاعدة عكسية |
| **PT-6** | **لا ترقية تلقائية** إلى COMPLETE · **لا معاملة الغياب كصفر** |
| **PT-7** | GA ممتنعة/بلا عناصر مؤهلة ⇒ **امتناع C5**, لا مخرج فارغ |

---

## 8. EVIDENCE BOUNDARY

**ينتجه C5 ✅:** مصدر GA والهوية المُمرَّرة · المعاملات المقبولة · المرفوضة بأسبابها · حالة التطبيق · الإغفالات · حالة الاكتمال · بيئة C2 (دليل).

**لا ينتجه ❌:** حكم fidelity · شهادة «B لم تنحرف» · بصمة هندسية · ادعاء D1/D2 · حكم على صحة الماستر أو الهندسة.

**نص يرافق كل مخرج:** *«C5 يسجّل ما تسلّمه وما طبّقه. لا يثبت أن B تطابق A هندسيًا — الحكم لـD. Generator ≠ Independent Verifier.»*

---

## 9. NO-WRITE BOUNDARY

**سلوكي + فحص مسارات** (شرط إغلاق C3 رقم 4، ومنهج C4-NW-05):

| الهدف | الإثبات |
|---|---|
| Master | لا كتابة · الملف غير موجود · القالب/Schema بـsha256 ثابت |
| `outputs[]` | **لا قراءة ولا كتابة ولا ذكر** |
| C3 | بصمة `c3_*.py` ثابتة قبل/بعد |
| أي مصدر هندسي | جرد ملفات قبل/بعد ⇒ **لا ملف جديد** |
| مسارات الكتابة | صفر من: `open(` · `.write(` · `json.dump` · `write_text` · `write_bytes` · `os.replace` · `shutil` · `pathlib` · `tempfile` · `pickle` |

---

## 10. TEST MATRIX

### 10.1 Positive (مقترح ~8)
GA سليمة ⇒ SVG فئة B · ختم B ظاهر · المعاملات الستة تُطبَّق وتُسجَّل · الهندسة مطابقة للـGA · نطاق Tier 0+1 موروث · الفتحات بمواضعها · سجل أدلة كامل · `fidelity = NOT_ASSESSED`.

### 10.2 Negative — **كل بند مطلوب مغطّى صراحةً**

| # | الحالة | النتيجة المتوقعة |
|---|---|---|
| N-01 | **parameter absent** | **لا تطبيق ولا default** (PP-1) |
| N-02 | **parameter unknown** | رفض معلن (PP-3) |
| N-03 | **parameter خارج الستة** | `REJECTED_OUT_OF_CONTRACT` (PP-2) |
| N-04 | **parameter بلا semantic محدد** | رفض (PP-8) |
| N-05 | **parameter بلا unit والوحدة لازمة** | رفض (PP-8) — **لا افتراض وحدة** |
| N-06 | **silent default injection** | **يستحيل**: لا قيمة بديلة في الكود |
| N-07 | **disguised geometry scaling** عبر `print_scale` | رفض (SC-1) + الهندسة ثابتة |
| N-08 | **second GA assembly** | **لا استدعاء `assemble()`** إطلاقًا |
| N-09 | **alternate geometry source** (ماستر/SVG) | مرفوض — المدخل artifact فقط |
| N-10 | **GA mutation** | الـartifact المُمرَّر **مطابق قبل/بعد** |
| N-11 | **A/P/U كمدخل هندسي** | لا يصل أصلًا (مستبعد في C3)؛ ولو حُقن ⇒ **لا يُرقّى ولا يُرسم** |
| N-12 | **NOT_PRESENT** | غياب معلن ⇒ لا يُرسم وجود |
| N-13 | **UNKNOWN** | مستبعد معلن ⇒ جزئية |
| N-14 | **UNCOMPUTABLE** | مستهلَك من B6/B7 ⇒ لا إعادة اشتقاق |
| N-15 | **PARTIAL** | مخرج موسوم لا مكتمل |
| N-16 | **Fidelity self-claim** | **لا مفتاح حكم** في أي مخرج |
| N-17 | GA ممتنعة/فارغة | امتناع معلن |
| N-18 | `label` يحمل رقمًا محسوبًا | رفض |

### 10.3 Boundary (~6)
صفر معاملات · الستة مجتمعة · معامل بقيمة حدية · GA بعنصر واحد · GA بلا فتحات · جزئية بعنصر واحد مُغفَل.

### 10.4 Mutation (~7) — **تحت AS-C2-16/17/18**
السماح بمعامل خارج الستة ⇒ تموت N-03 · حقن default ⇒ تموت N-01 · قبول `print_scale` هندسي ⇒ تموت N-07 · إسقاط وسم PARTIAL ⇒ تموت PT · السماح بتعديل GA ⇒ تموت N-10 · إضافة مفتاح fidelity ⇒ تموت N-16 · **+ CONTROL**.
**الانضباط:** الطفرات عبر الوحدة · التوقعات **قيم حرفية** · **لا default-argument binding** · **عزل الدفاعات المكرّرة** قبل أي طفرة.

### 10.5 Isolation (~6)
لا import لأي validator · لا import/استدعاء `assemble` · لا قراءة ماستر · لا قراءة نص SVG كمصدر · لا مفتاح حكم · لا طلب بصمة هندسية.

### 10.6 Anti-Smuggling (~8)
AS-C5-01 معامل يغيّر الهندسة · -02 هندسة بديلة · -05 default injection · -07 فئة C تغذّي B · -09 B تُقدَّم كـA · -10 partial→complete · -19 ادعاء عدم الانحراف · -20/21/22 (re-assembly drift · artifact mutation · disguised scale).

### 10.7 No-Write (~6) · 10.8 PARTIAL (~6) · 10.9 Regression
كما في §9 · كما في §7 · **الأساس 850/850 بلا انحدار**.

**الحجم التقديري: ~55–65 اختبارًا** (تقدير تخطيطي لا التزام رقمي؛ **العدد ليس دليل جودة**).

---

## 11. ما لا يستطيع C5 إثباته — يبقى لـD

| البند | المالك |
|---|---|
| **B لم تنحرف هندسيًا عن A** | **D** |
| صحة الهندسة نفسها | B2–B5 عبر الماستر |
| Output-vs-Master fidelity | **D** |
| صلاحية المخرج للتسليم | **E** |
| أن العرض «أمين» جماليًا | **خارج 00.5** |

---

## 12. BLOCKERS

## **لا توجد blockers حقيقية.**

**ولا يوجد تعارض بين المعمارية المغلقة ومتطلبات التنفيذ.** الملاحظتان في §3 (`OBS-C5-01` معرّف الارتباط · `OBS-C5-02` تمييز الجزئية) **قابلتان للمعالجة داخل العقد المغلق** بلا اجتهاد وبلا تعديل C3 — ولذلك سجّلتهما **ملاحظتين لا حاجبين**. وإن رأيتم غير ذلك في `OBS-C5-01` فهو **قرار ربط A↔B** يخصّكم.

---

## 13. C5 IMPLEMENTATION GATE CARD — Gate 1

| البند | الحالة |
|---|---|
| GA Handoff | ✅ مخطَّط — `record["assembly"]` · G-1…G-6 |
| Parameter Contract | ✅ الستة + PP-1…PP-8 |
| B Output | ✅ SVG · Class B · Tier 0+1 |
| Geometry Immutability | ✅ سبعة محاور بطريقة إثبات لكل منها |
| PARTIAL | ✅ PT-1…PT-7 + قاعدة اشتقاق مقاسة |
| Evidence Boundary | ✅ دليل بلا حكم |
| No-Write | ✅ سلوكي + 10 مسارات |
| Test Matrix | ✅ تسع مجموعات · 18 سلبيًا يغطي كل بند مطلوب |
| Ownership | ✅ لكل جزء مالك واحد |
| ما يبقى لـD | ✅ §11 |
| Blockers | ✅ **لا شيء** |
| ملاحظات مسجَّلة | ⚠️ OBS-C5-01 · OBS-C5-02 |
| كود مكتوب | ✅ **صفر** |
| ملفات مُنشأة | ✅ **صفر** (البصمات ثابتة · الانحدار 850/850) |

### الحالة: **GATE 1 — جاهز للمراجعة**

**لم أكتب أي implementation code. لم أنشئ `c5_presentation.py` ولا `test_00_5_C5.py`. لم أمسّ Schema/Master/C1–C4/validators. لم أفتح C6/D/E.**

**بانتظار اعتمادكم الصريح لـGate 1 قبل كتابة أي سطر.**
