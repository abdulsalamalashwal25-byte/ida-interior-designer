# 00.5-C6 — ARCHITECTURE & CONTRACT DESIGN
**معمارية فقط — لا Python · لا tests · لا Schema · لا Master · لا تعديل C1–C5 · لا بدء D/E · لا Manifest implementation.**

> **المرجع:** C-STEP-01 §12 (**C6 — Output Manifest خارجي · لا يكتب `outputs[]`**) · §13 (**البيان خارجي · الماستر غير معدَّل**) · C1…C5 مغلقة.
> **الحالة السابقة:** C5 IMPLEMENTATION = CLOSED WITH CONDITIONS · Regression = **931/931**.

---

## 0. قياسات أولية — ما ستستهلكه C6 فعلًا (لا افتراض)

فحصت السجلات الحقيقية التي تُنتجها الطبقات المغلقة:

| الطبقة | مفاتيح السجل |
|---|---|
| **A (C3)** | `status` · `svg` · `assembly` · `contract` · `tiers` · `format` · `capability` · `determinism` · `engineering_fingerprint` · `fidelity` · `abstention` |
| **B (C5)** | `status` · `class` · `svg` · `coverage` · `omissions` · `parameters_admitted` · `parameters_rejected` · `lineage_evidence` · `geometry_source` · `contract` · `determinism` · `engineering_fingerprint` · `fidelity` · `abstention` |
| **Q (C4)** | `schedule_type` · `result_kind` · `items` · `exclusions` · `completeness` · `class_a` · `scope` · `out_of_scope` · `partial_notice` · `written_to_master` · `provenance_caveat` · `fidelity` |

### ثلاث لا-تماثلات مقاسة — تُسجَّل ولا تُسوّى باجتهاد

| # | الاكتشاف | الأثر على C6 |
|---|---|---|
| **1** | **لا طبقة تحمل `output_id` ولا `file`** — القياس: `output_id? False` في A وB وQ | C6 **لا يخترع معرّفًا** ⇒ **DEC-C6-01** |
| **2** | **حالة الاكتمال غير موحّدة:** A **بلا حقل اكتمال إطلاقًا** (`status=EMITTED` رغم وجود `NR-01/NR-03`) · B لديه `coverage=PARTIAL` · Q لديه `completeness=PARTIAL` | C6 **لا يوحّدها باجتهاد** ⇒ **DEC-C6-02** |
| **3** | **A لا يحمل حقل `class`** (يضع الفئة في تعليق داخل SVG)، بينما B يحمل `class="B"` | C6 **لا يستنتج فئة A من غيابها** ⇒ **DEC-C6-03** |

**هذه ليست عيوبًا في C3/C4/C5** — كل طبقة أوفت بعقدها المغلق. لكنها **فجوات تكامل** تظهر فقط عند التجميع، وC6 هو أول من يراها.

---

## 1. SCOPE — مسؤولية C6

> **C6 — Output Manifest / Evidence Assembly (خارجي)**
>
> **المسؤولية الوحيدة:** **تجميع** بطاقات هوية المخرجات وأدلتها في **بيان خارجي مستقل**, **نقلًا أمينًا** عن الطبقات المنتِجة، **بلا حكم وبلا اختراع**.

**C6 مُجمِّع (assembler) لا مُقيِّم (evaluator).** جملة واحدة حاكمة: **ما لم تقله الطبقة، لا يقوله البيان.**

| يملكه C6 ✅ | لا يملكه ❌ |
|---|---|
| تجميع سجلات A/B/Q في بيان واحد | إنتاج مخرج أو هندسة |
| نقل الحالات والأدلة **كما صرّحت بها الطبقات** | إعادة تفسير حالة أو severity |
| إعلان ما هو **غائب أو غير متاح** | اختراع قيمة غائبة |
| ربط المخرجات بمصادرها المعلنة | إثبات صحة الربط |
| تسجيل حدود كل مخرج | إصدار حكم fidelity |

---

## 2. NON-SCOPE — ممنوعات قاطعة

**C6 ليس:** Fidelity validator · Geometry validator · Design decision maker · Approval authority · **Engineering fingerprint issuer** (صلاحية fingerprint **لم تُغلق في C2**) · Master editor · **`outputs[]` owner**.

وكذلك: لا يعيد تنفيذ B-series · لا يعيد تقييم بوابة C1 · لا يحسب كمية · لا يطبّق معامل عرض · لا يقرأ الماستر كمصدر هندسي.

---

## 3. OWNERSHIP

| الجهة | ما يبقى لها | ما يُمنع على C6 |
|---|---|---|
| **Master** | SSOT | **لا كتابة** · ولا قراءة هندسية (يقرأ الهوية فقط إن مُرِّرت) |
| **C1** | الإذن والنطاق | لا إعادة تقييم |
| **C2** | التقنين · البصمات · ENV-FP · D0/D1/D2 | **لا إصدار بصمة** · لا ادعاء D1/D2 · لا اختراع عقد بصمة |
| **C3** | الهندسة وA وGA Artifact | لا تعديل · لا إعادة حساب |
| **C4** | Quantity Schedule | **لا إعادة حساب كمية** · لا تحويلها إلى BOQ |
| **C5** | B والمعاملات | لا تعديل · لا إعادة قبول معامل |
| **D** | **Fidelity وعدم الانحراف** | **لا حكم** |
| **E** | **`outputs[]` · الحوكمة · الاعتماد · الهوية** | **لا كتابة ولا ملكية** |

---

## 4. DATA FLOW

```
project_master ──► C1 (إذن)
       │
       ▼
   C3 ──► GA Artifact ──┬──► A output (record)
                        │
                        └──► C5 (+params) ──► B output (record)
   C4 ──► Quantity Schedule (external derived result)
   C2 ──► ENV-FP · determinism classification (دليل)
                        │
                        ▼
              ┌──────────────────────┐
              │   C6 — MANIFEST      │  تجميع أمين · بلا حكم
              └──────────┬───────────┘
                         ▼
                    D (الحكم) ──► E (الحوكمة · outputs[])

   ✗ C6 ──╳──► Master      ✗ C6 ──╳──► outputs[]      ✗ C6 ──╳──► verdict
```

**C6 لا يشغّل الطبقات؛ يستقبل سجلاتها.** ولا يصنع مخرجًا غائبًا.

---

## 5. MANIFEST CONTRACT

### 5.1 رأس البيان
`manifest_kind: EXTERNAL_OUTPUT_MANIFEST` · `written_to_master: false` · `generated_by: C6` · `manifest_contract_version` · `project_id` · `master_revision` · **`fidelity: NOT_ASSESSED — owned by D`** · **`approval: NOT_APPLICABLE — owned by E`**.

### 5.2 سجل المخرج الواحد

| المجموعة | الحقول |
|---|---|
| **الهوية** | `output_ref` (مُصرَّح من المستدعي أو `NOT_SET`) · `class` · `format` · `contract` |
| **النسب** | `parent_ref` · `geometry_source` · `generator` (المكوّن ونسخته) · `master_revision` |
| **الحالة** | `status` · `completeness` · `class_a_eligibility` (لـQ فقط) |
| **الأدلة** | `parameters_admitted` · `parameters_rejected` · `omissions` · `exclusions` · `lineage_evidence` |
| **البصمة** | `engineering_fingerprint: NOT_ISSUED` · `provisional_digest` (إن وُجد، موسومًا) |
| **الحتمية** | `D0` · `D1: NOT_ESTABLISHED` · `D2: NOT_CLAIMED` |
| **البيئة** | `environment_evidence` (من C2) أو `NOT_SET` |
| **القدرة** | `capability` لحظية أو `NOT_SET` |
| **الحدود** | `limitations[]` · `fidelity: NOT_ASSESSED` |

### 5.3 تحذير الهوية — قاعدة ملزمة

> **`project_id + master_revision + provisional_digest` لا تتحول تلقائيًا إلى `artifact_id` ولا engineering fingerprint ولا unique GA identity.**

تُسجَّل **مجتمعةً تحت `lineage_evidence`** بوسم `PROVISIONAL / usable_as_evidence: false`، **وبلا مفتاح باسم `artifact_id`**. أي ترقية تحتاج **قرارًا معماريًا صريحًا** (**DEC-C6-01**) — وهذا استمرار مباشر لـCONDITION-01 المعتمد في C5.

---

## 6. IDENTITY MODEL

| المستوى | الحالة |
|---|---|
| **هوية المشروع** | `project_id` · `master_revision` — **مُمرَّرة لا مولَّدة** |
| **هوية المخرج** | 🔴 **غير موجودة** — لا طبقة تُنتج `output_id` (مقاس) ⇒ `NOT_SET` ما لم يصرّح المستدعي |
| **هوية الـartifact** | 🔴 **غير موجودة** — قرينة lineage فقط |
| **هوية المكوّن** | اسم المكوّن ونسخة عقده — **مُصرَّح لا مستنتَج** |

**قاعدة:** **C6 لا يولّد معرّفًا.** غياب المعرّف يُسجَّل `NOT_SET` صراحةً؛ و`$defs.output.output_id` **ملك E** ولا يملؤه C6.

---

## 7. PROVENANCE MODEL

يسجّل C6 **نقلًا**: المصدر · الأب · المولّد · النسخة · العقد · المعاملات (مقبولة/مرفوضة) · الإغفالات · الاستبعادات · حالة الجزئية · دليل البيئة · القدرة · الحدود.

**قاعدتان تُطبعان في البيان نفسه:**
> **provenance ≠ fidelity** — النسب يثبت من أين جاءت القيمة، لا أنها صحيحة.
> **lineage ≠ engineering identity** — القرينة ليست هوية هندسية ولا بصمة.

**وقاعدة ثالثة:** **وجود Manifest ليس دليل fidelity** — البيان يثبت أن مخرجًا سُجِّل، لا أنه صحيح.

---

## 8. STATUS MODEL

**كل حالة منقولة من مالكها. لم أضف حالة لملء القائمة.**

| الحالة | المعنى | المصدر | Owner | شرط الدخول | ممنوع |
|---|---|---|---|---|---|
| `GENERATED` | المخرج أُنتج | `status=EMITTED` من C3/C5 | C3/C5 | تصريح الطبقة | أن تعني «صحيح» |
| `PARTIAL` | تغطية ناقصة معلنة | `coverage`/`completeness` | C5/C4 | تصريح الطبقة | الترقية إلى COMPLETE |
| `ABSTAINED` | امتناع معلن | `status=ABSTAIN` | C1/C3/C4/C5 | تصريح الطبقة | تحويله إلى نجاح |
| `REJECTED` | معامل مرفوض | `parameters_rejected` | C5 | تصريح الطبقة | إخفاؤه |
| `NOT_ASSESSED` | لم يُحكم عليه | fidelity | **D** | دائمًا لـfidelity | تحويله إلى PASS |
| `NOT_ISSUED` | لم يُصدر | fingerprint | **C2** | البصمات محجوبة | استنتاج بصمة |
| `NOT_ESTABLISHED` | العقد غير قائم | D1 · SEC | C2 | دائمًا اليوم | ادعاء D1 |
| `NOT_CLAIMED` | لا ادعاء | D2 | C2 | دائمًا | ادعاء D2 |
| `UNCOMPUTABLE` | تعذّر الحساب | حكم B6/B7 عبر C4 | B6/B7 | تصريح المالك | إعادة اشتقاق السبب |
| `UNKNOWN` | غير معروف | `[U]` عبر الطبقات | B7 | تصريح المالك | **تحويله إلى NOT_PRESENT** |
| `NOT_SET` | غير متاح | C6 نفسه | C6 | المعلومة غير مُمرَّرة | اختراع قيمة |

**`NOT_SET` هي الحالة الوحيدة التي يُنشئها C6** — وهي **إعلان غياب**, لا قيمة.

---

## 9. A / B / C HANDLING

| الفئة | تعامل C6 |
|---|---|
| **A — DESIGN-ACCURATE** | يسجّله بفئته المصرَّحة · **لا يستنتجها** (DEC-C6-03) |
| **B — PRESENTATION VISUAL** | يسجّل `class="B"` كما صرّح C5 · مع `geometry_source` |
| **C — AI MOOD** | **قسم منفصل موسوم غير موثوق** · **لا يغذّي A/B** · ولا يُدرج بجوارهما بلا فاصل (Phase 00 §10) |

**ثلاث قواعد:** **لا تحويل فئة** · **لا ترقية C إلى A/B** · **وجود البيان ليس دليل fidelity ولا صلاحية**. والفئة **تُنقل من الطبقة**؛ وإن لم تُصرَّح ⇒ **`NOT_SET`** لا استنتاج.

---

## 10. PARTIAL HANDLING

يسجّل C6 لكل مخرج جزئي: **لماذا** · **ما لم يُمثَّل** بمعرّفاته · **سبب كل إغفال** مصنَّفًا (`UNKNOWN/[U]` · `contract limitation` · `capability limitation` · `eligibility`).

**أربعة محظورات:** ❌ تحويل omission إلى `NOT_PRESENT` · ❌ ترقية PARTIAL إلى COMPLETE · ❌ معاملة الغياب كصفر · ❌ إخفاء الجزئية عند التجميع.

**قاعدة تجميع مقترحة:** بيان يضم مخرجًا جزئيًا واحدًا **يُوسم جزئيًا على مستوى البيان** — فلا يوحي التجميع باكتمال لا وجود له ⇒ **DEC-C6-04**.

---

## 11. FINGERPRINT BOUNDARY — إلزامي

**مراجعة حدود C2 كما أُغلقت:**

| البند | الحالة في C2 | سلوك C6 |
|---|---|---|
| **Engineering fingerprint** | **محجوب** بـDEC-C2-01/02/03/05/06/08 | يسجّل **`NOT_ISSUED`** ولا يستنتج |
| **Provisional digest** | تشخيصي · `usable_as_evidence: False` | يُنقل **بوسمه** ولا يُرقّى |
| **Environment fingerprint** | دليل سياقي · **ليس بصمة هندسية** | يُنقل كما هو + قاعدتا التفسير |
| **D0** | `OBSERVED_IN_MEASURED_CASE` فقط | يُنقل بلا تعميم |
| **D1** | `NOT_ESTABLISHED` (GAP-C-04/14/02) | كما هو |
| **D2** | `NOT_CLAIMED` | كما هو |
| **Canonicalization** | Spec **غير معتمدة** | **لا يخترع C6 صورة قانونية للبيان** ⇒ **DEC-C6-05** |

> **قاعدة قاطعة:** **C6 لا يخترع fingerprint contract غير مغلق.** وغير المتاح يُسجَّل `NOT_ISSUED` / `NOT_ESTABLISHED`, **ولا يُستنتج**.

---

## 12. EVIDENCE vs VERDICT

| السؤال | المالك |
|---|---|
| هل المخرج يطابق الماستر؟ | **D** |
| هل التصميم معتمد؟ | **E / الحوكمة البشرية** |
| هل الهندسة صحيحة؟ | **B2–B5 عبر الماستر** |
| هل الماستر صادق؟ | **حوكمة الماستر** |
| **ماذا أُنتج وبأي حدود؟** | **C6** ✅ |

**C6 لا يغيّر نتيجة أي طبقة ولا يعيد تفسير `severity`** — وهذا امتداد مباشر لانضباط C1 (`severity != ERROR` لا يُرفع).

---

## 13. IMMUTABILITY / READ-ONLY

| المصدر | الحكم |
|---|---|
| `project_master.json` | **لا كتابة** · ولا قراءة هندسية |
| **GA Artifact** | read-only · لا تعديل |
| **A / B records** | read-only · **لا تعديل نص المخرج** |
| **C4 evidence** | read-only · لا إعادة حساب |
| **قرارات C1** | تُستهلك ولا تُعدَّل |
| **`outputs[]`** | **لا قراءة ولا كتابة** — ملك E |

**C6 يعمل على نسخ**, ويُعيد بيانًا **ولا يحفظ ملفًا بنفسه** (نفس نمط C3/C4/C5) ⇒ **DEC-C6-06**.

---

## 14. FAILURE / ABSTENTION

| الحالة | سلوك C6 |
|---|---|
| **A غير موجود** | `A: NOT_SET` + **إعلان**؛ ولا يُشتق وجوده من وجود B |
| **B غير موجود** | `B: NOT_SET` — ليس فشلًا (B اختياري) |
| **C5 ABSTAINED** | يُنقل `ABSTAINED` بسببه · **لا يُحوَّل إلى نجاح** |
| **C4 غير قابل للحساب** | `UNCOMPUTABLE`/`ABSTAIN` كما صرّح · بلا إعادة اشتقاق |
| **provenance ناقص** | يُعلَن النقص · **لا يُكمَّل** |
| **fingerprint غير متاح** | `NOT_ISSUED` |
| **environment evidence ناقص** | `NOT_SET` صراحةً |
| **output contract غير معروف** | **امتناع عن تصنيف المخرج** ⇒ `contract: NOT_SET` |
| **output PARTIAL** | يُسجَّل جزئيًا بأسبابه |

> **C6 لا يحوّل الفشل إلى نجاح.** وبيان بلا أي مخرج صالح ⇒ **بيان امتناع معلن**, لا بيان فارغ يوحي بالنجاح.

---

## 15. E BOUNDARY

| **C6 يجهّز لـE** | **يبقى ملك E** |
|---|---|
| سجل مخرجات خارجي بالحالات والأدلة | **`outputs[]`** — الكتابة والملكية |
| النسب والحدود والإغفالات | **الاعتماد وهوية المعتمِد** (GAP-02) |
| بطاقة بيئة وقدرة | **دورة الحياة والحوكمة** |
| **تصريح بما هو `NOT_SET`** | **توليد `output_id` الرسمي** |

> **C6 = Manifest/Evidence assembly** · **E = Governance / lifecycle / `outputs[]` ownership.**
> **إنتاج البيان لا ينقل ملكية `outputs[]` إلى C6** — وGAP-C-06 تبقى مفتوحة لـE.

---

## 16. ATTACK SURFACE

| # | الخطر | الحدّ المانع |
|---|---|---|
| **AS-C6-01** | **fabricated identity** | §6 — لا توليد معرّف · `NOT_SET` |
| **AS-C6-02** | **fabricated fingerprint** | §11 — `NOT_ISSUED` ولا استنتاج |
| **AS-C6-03** | **fabricated approval** | §15 — الاعتماد ملك E |
| **AS-C6-04** | **fabricated completeness** | §10 — الحالة منقولة |
| **AS-C6-05** | **fabricated fidelity** | §12 — `NOT_ASSESSED` |
| **AS-C6-06** | **source substitution** | §13 — المصادر المعلنة فقط |
| **AS-C6-07** | **provenance spoofing** | §7 — نقل بلا إعادة بناء |
| **AS-C6-08** | **status promotion** | §8 — لكل حالة مالك |
| **AS-C6-09** | **PARTIAL → COMPLETE** | §10 |
| **AS-C6-10** | **UNKNOWN → NOT_PRESENT** | §8/§10 — التمييز الثلاثي |
| **AS-C6-11** | **evidence → verdict smuggling** | §12 |
| **AS-C6-12** | **A/B/C class confusion** | §9 — لا استنتاج فئة |
| **AS-C6-13** | **cross-revision contamination** | مطابقة `master_revision` وإعلان أي اختلاف ⇒ **DEC-C6-07** |
| **AS-C6-14** | **stale evidence** | إعلان زمن/نسخة الدليل بلا اختراع طابع |
| **AS-C6-15** | **missing parent** | `parent_ref: NOT_SET` معلن |
| **AS-C6-16** | **orphan output** | مخرج B بلا A ⇒ **يُعلَن يتيمًا** لا يُخفى |
| **AS-C6-17** | **manifest/master divergence** | البيان **لا يدّعي** تمثيل الماستر؛ ويعلن `master_revision` الذي بُني عليه |
| **AS-C6-18** ⭐ | **completeness normalisation** — توحيد حالة A/B/Q باجتهاد رغم اختلاف مصادرها (§0) | **DEC-C6-02** — يُنقل كل مصدر بحالته |
| **AS-C6-19** ⭐ | **manifest-as-proof** — تقديم وجود البيان كدليل صحة | §7 — نص صريح في البيان |

---

## 17. DECISIONS — DEC-C6-XX

| # | المشكلة | الخيارات | المقترح | السبب | المالك | Dependencies | أثر D/E | Blocker؟ |
|---|---|---|---|---|---|---|---|---|
| **DEC-C6-01** | **لا `output_id` في أي طبقة** (مقاس) | (أ) `NOT_SET` ما لم يصرّح المستدعي · (ب) C6 يولّد معرّفًا · (ج) ترقية `provisional_digest` | **(أ)** | (ب) اختراع هوية · (ج) يخرق CONDITION-01 | **E** | `$defs.output.output_id` | **E يملأه لاحقًا** | 🔴 **نعم** |
| **DEC-C6-02** | **حالة الاكتمال غير موحّدة** — A بلا حقل · B `coverage` · Q `completeness` | (أ) نقل كل مصدر بحالته + `NOT_SET` لـA · (ب) توحيد باجتهاد · (ج) طلب تعديل C3 | **(أ)** | (ب) AS-C6-18 · (ج) C3 مغلق | **C6 يعرض / C3 يملك المصدر** | §0-2 | D يرى الحالة الحقيقية | 🔴 **نعم** |
| **DEC-C6-03** | **A لا يحمل حقل `class`** | (أ) `NOT_SET` ما لم يصرّح المستدعي · (ب) استنتاجها من غياب `class` · (ج) قراءة تعليق SVG | **(أ)** | (ب) اختراع · (ج) قراءة مخرج كمصدر بيانات | **C3/المستدعي** | §0-3 | تصنيف E | 🟡 |
| **DEC-C6-04** | هل بيان يضم جزئيًا يُوسم جزئيًا؟ | (أ) نعم · (ب) لا | **(أ)** | يمنع إيحاء الاكتمال | C6 | §10 | D/E يريان الحد | 🟡 |
| **DEC-C6-05** | صورة البيان القانونية | (أ) بلا تقنين معتمد · (ب) استعمال صورة C2 | **(أ) مع تسجيل الحد** | **Canonicalization Spec غير معتمدة (GAP-C-04)** | C2 | GAP-C-04 | D | 🟡 |
| **DEC-C6-06** | هل يحفظ C6 ملف البيان؟ | (أ) يُعيد بنية والمستدعي يحفظ · (ب) يكتب ملفًا | **(أ)** | اتساقًا مع C3/C4/C5 | C6 | — | E يقرر التخزين | 🟡 |
| **DEC-C6-07** | تعارض `master_revision` بين المخرجات | (أ) إعلان التعارض بلا حكم · (ب) رفض البيان · (ج) اختيار الأحدث | **(أ)** | (ج) قرار حوكمي · (ب) حكم | **D/E** | AS-C6-13 | **D يحكم** | 🟡 |
| **DEC-C6-08** | هل يُدرج Quantity Schedule في البيان؟ | (أ) نعم كنوعه المصرَّح `QUANTITY_SCHEDULE` · (ب) لا · (ج) كـBOQ | **(أ)** | **BOQ وCost Estimate خارج النطاق** (GAP-C-08) | C4 | §0 | E | 🟡 |
| **DEC-C6-09** | عقد المخرج الأول للبيان | (أ) JSON خارجي · (ب) Markdown · (ج) كلاهما | **(أ)** | قابل للتدقيق آليًا | C6 | — | D/E | 🔴 **نعم** |

**تبعيات مسجَّلة لا مخترعة:** ملكية `outputs[]` (**E** · GAP-C-06) · حجية الجداول (**GAP-C-08**) · التقنين (**GAP-C-04**) · صدق الاعتماد (**GAP-02 ⇒ E**).

---

## 18. OPEN GAPS

**لم تُغلق أي ثغرة.**

| # | الحالة |
|---|---|
| **GAP-C-06** ملكية `outputs[]` | 🔴 ⇒ **E** |
| **GAP-C-04** التقنين · **GAP-C-14** متطايرات DXF | 🔴 |
| **GAP-C-02** المكتبات/البيئة | 🔴 نشطة |
| **GAP-C-08** حجية الجداول | 🔴 (فُصلت اتجاهيًا في C4) |
| **GAP-C-09** | ✅ معتمدة بشرط (C5) |
| GAP-C-01 · 03 · 05 · 07 · 10 · 11 · 12 · 13 | 🔴 كما هي |
| **DEC-C2-01…08** (ومنها 04/07) | 🔴 مفتوحة |
| **GAP-C6-01** ⭐ جديدة | **لا مصدر موحّد لحالة الاكتمال عبر A/B/Q** (§0-2) |
| **GAP-C6-02** ⭐ جديدة | **لا هوية مخرج رسمية في أي طبقة** (§0-1) |
| **OBS-C5-01** | قائمة — الربط قرينة لا هوية |

---

## 19. CAPABILITY REQUIREMENTS

| المستوى | الحالة |
|---|---|
| **1 — Format** | ✅ JSON/نص — **المكتبة القياسية فقط**, لا تبعية خارجية |
| **2 — Approved output contract** | 🔴 **مفقود** — **DEC-C6-09** (صيغة البيان) و**DEC-C6-01/02** غير معتمدة |
| **3 — Runtime** | ✅ متاح |

**قاعدة موروثة:** القدرة **دليل لحظي** لكل runtime، لا تُعمَّم ولا تُخزَّن كحقيقة (GAP-C-02 نشطة).

---

## 20. GATE CARD

| البند | الحالة |
|---|---|
| Scope / Non-Scope | ✅ §1/§2 — مُجمِّع لا مُقيِّم |
| Ownership بلا تداخل | ✅ §3 |
| Data Flow | ✅ §4 — ثلاثة أسهم مقطوعة عمدًا |
| Manifest Contract | ✅ §5 |
| Identity Model | ✅ §6 — **لا توليد معرّف** |
| Provenance | ✅ §7 — provenance ≠ fidelity · lineage ≠ engineering identity |
| Status Model | ✅ §8 — 11 حالة، لكل منها مالك وشروط |
| A/B/C | ✅ §9 — لا تحويل فئة |
| PARTIAL | ✅ §10 — أربعة محظورات |
| Fingerprint Boundary | ✅ §11 — **لا اختراع عقد غير مغلق** |
| Evidence vs Verdict | ✅ §12 |
| Immutability | ✅ §13 |
| Failure/Abstention | ✅ §14 — الفشل لا يصير نجاحًا |
| E Boundary | ✅ §15 — `outputs[]` لا ينتقل |
| Attack Surface | ✅ 19 |
| Decisions | ✅ 9، **بلا إغلاق ذاتي** |
| Open Gaps | ✅ بلا إغلاق · **+2 جديدتان** |
| Capability | ⚠️ **المستوى 2 مفقود** |
| كود / tests / تعديلات | ✅ **صفر** |

### C6 Architecture Status: 🔴 **HOLD**

**ثلاثة حواجب — كلها قرارات لا اجتهادات:**

1. **DEC-C6-01** — لا هوية مخرج في أي طبقة (مقاس). توليدها اختراع، وملكيتها **E**.
2. **DEC-C6-02** — حالة الاكتمال غير موحّدة عبر A/B/Q. توحيدها باجتهاد هو **AS-C6-18**.
3. **DEC-C6-09** — عقد مخرج البيان غير معتمد ⇒ **المستوى 2 من Capability مفقود**، وهو نفس الحاجب الذي أوقف C3 وC4 وC5 حتى اعتمدتم عقودها.

**ما يرفع الحجب:** اعتماد **DEC-C6-01** و**DEC-C6-02** و**DEC-C6-09**، وتثبيت **DEC-C6-03/04/05/06/07/08**.

**تنويه:** هذا Gate Card **ليس موافقة** — الإغلاق بمراجعتكم المستقلة.

**لم أكتب كودًا ولا اختبارات، ولم أمسّ Schema/Master/C1–C5، ولم أبدأ D/E ولا أي Manifest implementation.**
