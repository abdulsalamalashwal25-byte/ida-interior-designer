# 00.5-C — RESPONSIBILITY & ATTACK-SURFACE MAP — **R02**
**C-STEP-01 R02 — خريطة فقط. لا كود · لا تعديل schema · لا تعديل validator · لا تشغيل اختبارات · لا بدء C1.**

> **الاسم المقترح:** *Deterministic Generation* (تقلّص عن R01: **أُزيل ادعاء إثبات المطابقة** — انظر §2).
> **سجل التعديل R01 → R02:** حُلّت دائرة C↔B8 بدليل تنفيذي · نُزعت ملكية fidelity من C إلى تحقق مستقل ⇒ D · عُرِّفت الحتمية بدل ادعائها · أُعيدت ملكية `outputs[]` قرارًا مفتوحًا لـE · فُصّلت الأهلية حسب فئة المخرج · فُكّك BOQ إلى أربعة أنواع · ثُبّتت قاعدة الهندسة الواحدة.

---

## 1. C SCOPE — ما يدخل تحت مسؤولية C

| # | المسؤولية | الحد |
|---|---|---|
| C-S1 | **التوليد الحتمي** للمخرجات من `project_master.json` | قراءة فقط |
| C-S2 | **إعادة الإنتاجية المعرَّفة** تحت بيئة مثبَّتة (§5-B) | ليست «بايت مطابق» مطلقًا |
| C-S3 | **حساب** البصمات الست من المحتوى | يسدّ CG-B9-01 |
| C-S4 | **إنتاج Output Manifest خارجي** (ملف مستقل) | **لا يكتب في الـMaster** — §4 |
| C-S5 | **بوابة ما قبل التوليد**: استهلاك قرار B7 | لا يعيد تنفيذه |
| C-S6 | **فصل مسارات A/B/C** وتسميتها وختمها | Phase 00 §10 |
| C-S7 | **إنتاج أدلة التحقق** (Verification Evidence) لتستهلكها D | **دليل لا حكم** |
| C-S8 | **التصريح بالعجز** عمّا لا يمكن توليده | لا اختلاق |

**الجملة الحاكمة:** *C يحوّل ما هو موجود. **C لا يعرف شيئًا لا يعرفه النموذج، ولا يشهد لنفسه.***

## 2. C NON-SCOPE

| ليس من C | المالك |
|---|---|
| شكل البيانات ومجال القيمة | **A** |
| وجود المراجع · تماسك البصمة المصرَّحة مع meta (XR-014…018) | **B1** |
| الطوبولوجيا · الأثاث · الحركة · الأبواب | **B2–B5** |
| صحة حساب `[D]` المصرَّح | **B6** |
| بوابة المجهول الحاجب | **B7** |
| **ادعاءات المخرج عن مصدره** (class/guarantee/binding) | **B8** |
| الإصدار · الحالات · الزمن | **B9/B10/B11** |
| **إثبات مطابقة الملف المنتَج للنموذج (fidelity verification)** | **⇒ D** (تصحيح R02) |
| صدق الاعتماد والهوية | **00.5-E** |
| ملكية الكتابة في `outputs[]` | **قرار مفتوح ⇒ 00.5-E** |

### تصحيح R02 (نقطة المشرف 2) — C **ليس** مالك إثبات المطابقة
| السؤال | المالك |
|---|---|
| هل وُلِّد الملف بعملية حتمية معرَّفة؟ | **C** |
| هل أنتج C الأدلة اللازمة (بصمة · سجل تشغيل · جدول إحداثيات)؟ | **C** |
| **هل الملف يطابق الـMaster هندسيًا فعلًا؟** | **D — تحقق مستقل** |

**المبرر:** المولِّد لا يزكّي نفسه. لو ملك C إثبات مطابقته لصار خصمًا وحكمًا، وهو نفس النمط الذي رُفض في CG-B9-02 («السجل إفادة ذاتية»). **C يقدّم الدليل · D يصدر الحكم.**

---

## 3. DEPENDENCY GRAPH — دورة الحياة (تصحيح النقطة 1)

**الدليل التنفيذي الذي حسم الدائرة:** بتشغيل القالب و`outputs = []`:
`A ok · B1 ok · B8 ok · B9 ok · B10 ok · B11 ok` — **صفر findings**.
⇒ قواعد المخرجات **صادقة خوائيًا قبل التوليد**؛ فهي تحكم على شيء لم يوجد بعد. **لا دائرة**: B8 ليس شرطًا سابقًا للتوليد، بل **مرحلة لاحقة له**.

**دليل ثانٍ:** `grep '"outputs"'` في B9/B10/B11 ⇒ **صفر** في الثلاثة. وB11 يستثني `outputs[].generated_on` صراحة (DEF-B11-02).

```
┌─ المرحلة 1: PRE-GENERATION GATES (على النموذج وحده) ─────────┐
│  A · B1(model refs) · B2 · B3 · B4 · B5 · B6 · B7            │
│  B9 · B10 · B11   ← model-scoped بالكامل (صفر قراءة outputs) │
│  البوابة: final_approval_readiness ⇒ blockers?               │
└───────────────────────────┬──────────────────────────────────┘
                            │ كلها خضراء + لا حاجب
                            ▼
┌─ المرحلة 2: C — GENERATION ──────────────────────────────────┐
│  يقرأ النموذج · يكتب ملفات + Output Manifest خارجي           │
│  ينتج: البصمات المحسوبة · أدلة التحقق                        │
│  لا يكتب في الـMaster                                        │
└───────────────────────────┬──────────────────────────────────┘
                            ▼
┌─ المرحلة 3: POST-GENERATION VERIFICATION ────────────────────┐
│  B8: صدق ادعاءات المخرج (class/guarantee/binding/provenance) │
│  B1: XR-014…018 على قيود outputs[] بعد تسجيلها               │
│  B11: generated_on مقابل created_on (يملكه B8 فعليًا)         │
└───────────────────────────┬──────────────────────────────────┘
                            ▼
┌─ المرحلة 4: D — QA / CONSISTENCY AUDIT ──────────────────────┐
│  الفحوص الـ14 · **output-vs-master fidelity** (تملكه D)       │
│  يستهلك أدلة C ولا يثق بادعائه                                │
└──────────────────────────────────────────────────────────────┘
```

### تصنيف B9/B10/B11 (مطلوب صراحةً)
| الطبقة | precondition؟ | post-generation؟ | الدليل |
|---|---|---|---|
| **B9** | ✅ **نعم بالكامل** — الإصدار والتجميد يجب أن يستقرا قبل التوليد | ⚠️ **حوكمة لاحقة**: قيد changelog عن التوليد يُراجَع بعده | صفر قراءة `outputs` |
| **B10** | ✅ **نعم بالكامل** — لا تولّد من قرار قائم على اقتراح مرفوض | — | صفر قراءة `outputs` |
| **B11** | ✅ **نعم** للتواريخ داخل النموذج | ⚠️ `generated_on` **ليس ملكه** (DEF-B11-02) ⇒ B8 | صفر قراءة `outputs` |

---

## 4. `outputs[]` — قرار معماري **مفتوح** (تصحيح النقطة 4)

**التناقض المعترف به في R01:** `outputs[]` **جزء من الـMaster**، فـ«C لا يكتب في النموذج» + «C يقترح `outputs[]`» لا يجتمعان.

**موقف R02:** **لا حسم الآن.** المسجَّل:
- ✅ **مسموح لـC:** إنتاج **Output Manifest خارجي** (ملف مستقل خارج الـMaster) يحوي بطاقات الهوية والبصمات.
- ⛔ **غير مفترض:** أن C يكتب أو يقترح قيودًا داخل `outputs[]`.
- 📌 **GAP-C-06 ⇒ 00.5-E:** من يملك الكتابة في `outputs[]`؟ (المستخدم · E · أم آلية اعتماد؟) **لا يُغلق هنا.**

---

## 5. INPUTS — الأهلية حسب فئة المخرج (تصحيح النقطة 5)

قاعدة R01 «كل المدخلات `[C]`/`[D]`» كانت فضفاضة. البديل:

### 5-A · أهلية المدخلات
| الفئة | ما يجوز استهلاكه | ما لا يجوز |
|---|---|---|
| **A — DESIGN-ACCURATE** | `[C]` و`[D]` **فقط**، بعد اجتياز B6، وبلا حاجب | `[A]` `[P]` `[U]` · أي `AI_IMAGE` · أي معيار غير معتمد |
| **B — PRESENTATION** | **نفس هندسة A حرفيًا** + **presentation parameters** (§5-C) | أي تعديل في الهندسة · أي مصدر هندسي بديل |
| **C — AI MOOD** | مسار **غير موثوق** معزول | **ممنوع أن يغذّي A أو B** · ممنوع أن يصبح مدخلًا لأي اشتقاق |

**حكم قاطع:** لا تتحول `[A]`/`[P]`/`[U]` إلى `[C]`/`[D]` بالتوليد أبدًا. عنصر `[A]`/`[P]` إما **يُستبعد** من فئة A أو **يُوسَم داخل المخرج** بحالته — ولا يُرقّى.

### 5-B · Presentation Parameters المسموحة للفئة B
**تعريف:** معامل لا يغيّر أي إحداثي ولا بُعد ولا معرّفًا — **ألوان · سماكة خطوط · ظلال · وسوم · ترتيب اللوحة · مقياس الطباعة**.
**اختبار الأهلية:** لو غُيِّر المعامل، هل تتغير هندسة مستخرَجة من الملف؟ **نعم ⇒ محظور.**
**مصدرها:** إعدادات عرض معلنة، **ليست حقائق في الـMaster** ولا تُكتب فيه.

### 5-C · مدخلات إلزامية أخرى
`meta.revision` · `geometry_version` · `cameras[]` · مخرجات B7 (خريطة اليقين) · نتيجة `final_approval_readiness`.

---

## 6. الحتمية — تعريف بدل ادعاء (تصحيح النقطة 3)

### الدليل: «بايت مطابق» **فشل تجريبيًا**
تشغيلان لنفس الهندسة (ezdxf 1.4.4) بفارق ثانية ⇒ **ستة اختلافات**:

| # | مصدر الاختلاف | مثال |
|---|---|---|
| 1–2 | **طوابع زمنية** `$TDCREATE`/`$TDUPDATE` | `2461297.995613426` ≠ `2461297.995625` |
| 3–4 | **GUIDs** `$FINGERPRINTGUID`/`$VERSIONGUID` | `{CB8226B3…}` ≠ `{506868CE…}` |
| 5–6 | **نسخة المكتبة + وقتها مضمَّنان** | `1.4.4 @ 2026-09-13T23:53:41…` |

وحتى بعد تطبيع الترويستين المعروفتين **بقي الملف مختلفًا**. ⇒ **الادعاء المطلق باطل، والصياغة الصحيحة شرطية.**

### التعريف المقترح (ثلاث طبقات)
| الطبقة | التعريف | ملاحظة |
|---|---|---|
| **D0 — Semantic determinism** | نفس المدخل ⇒ **نفس المحتوى الهندسي** (إحداثيات ومعرّفات ومواد) | **الأقوى عمليًا والملزم** |
| **D1 — Canonical-form determinism** | نفس المدخل ⇒ **بايت مطابق بعد التطبيع** (إزالة الطوابع والGUIDs ونصوص الأدوات) | يتطلب Canonicalization Spec |
| **D2 — Raw byte determinism** | بايت مطابق بلا تطبيع | **غير قابل للتحقيق مع DXF — لا يُوعد به** |

### ما يجب تثبيته وإعلانه قبل ادعاء أي حتمية
| المصدر | الإجراء المطلوب (توصيف لا تنفيذ) |
|---|---|
| **Serialization** | تنسيق كتابة مثبَّت (فواصل · ترميز · نهايات أسطر) |
| **Ordering** | ترتيب كلي صريح للعناصر — **ممنوع الاعتماد على `set`** |
| **Timestamps** | تحييد/تثبيت الطوابع المضمَّنة |
| **Floating point** | سياسة تقريب وتمثيل معلنة (`0.1+0.2 = 0.30000000000000004`) |
| **Library/toolchain** | **تثبيت النسخ وتسجيلها** مع كل مخرج |
| **Runtime** | نسخة Python والمعمارية · **`PYTHONHASHSEED` غير مضبوط حاليًا** ⇒ ترتيب المجموعات يتغير بين العمليات |
| **Locale/TZ** | تثبيتهما |

**«same version/environment» تعني:** نفس نسخ المكتبات + نفس مفسّر Python + نفس المعمارية + نفس الإعدادات المثبتة أعلاه، **وكلها مسجَّلة داخل الدليل المرافق للمخرج**.

---

## 7. OUTPUTS

| المخرج | الفئة |
|---|---|
| DXF · SVG/PDF · GLB/OBJ · مناظير محسوبة | **A** |
| لوحات · PPTX | **B** |
| صور توليدية | **C — غير ملزِم** |
| **Output Manifest خارجي** + البصمات + أدلة التحقق | سجل خارج الـMaster |

### قاعدة الهندسة الواحدة (تصحيح النقطة 7)
```
            MASTER GEOMETRY
                   │
            ┌──────┴──────┐
            ▼             ▼
      Class A        Class B
   (هندسة خام)   (نفس الهندسة + عرض)
```
**ملزم:** لا توجد «هندسة B» مستقلة. B **يستهلك نفس مصدر الهندسة** ولا يعيد اشتقاقها ولا يعدّلها. **وادعاء المولّد بعدم الانحراف لا يكفي** — التحقق من المطابقة **مسؤولية تحقق مستقلة (D)**.

---

## 8. OWNERSHIP MATRIX (One Rule → One Owner)

| السؤال | المالك |
|---|---|
| شكل القيمة ومجالها | **A** |
| وجود المرجع | **B1** |
| الهندسة داخل النموذج | **B2–B5** |
| صحة `[D]` المصرَّح | **B6** |
| الحاجب | **B7** |
| صدق ادعاء المخرج عن مصدره | **B8** |
| الإصدار · الحالات · الزمن | **B9/B10/B11** |
| **حتمية التوليد وإعادة إنتاجه** | **C** |
| **حساب البصمة من المحتوى** | **C** |
| **إنتاج أدلة التحقق** | **C** |
| **الحكم بمطابقة الملف للـMaster** | **D** |
| الكتابة في `outputs[]` | **مفتوح ⇒ E** |
| صدق الاعتماد والهوية | **E** |

**CG-B8-01/02 لا تُغلق بـC** (قرار إغلاق B8 دائم): C يقدّم دليلًا، وD يحكم، **ولا يتحول أي منهما إلى ادعاء ضمني بالمطابقة**.

---

## 9. ATTACK SURFACE

| # | الخطر | الحارس |
|---|---|---|
| AS-01 | **Invention** — قيمة افتراضية لحقل `[U]` | امتناع + تصريح بالعجز |
| AS-02 | **Silent default** — CENTER أو `rotation=0` | فشل صريح |
| AS-03 | **Unauthorized promotion** `[A]/[P]/[U] → [C]/[D]` | لا كتابة في الـMaster · وسم لا ترقية |
| AS-04 | **Contamination** — مخرج B/C مدخلًا للفئة A | منع `derived_from_output` للفئة A |
| AS-05 | **Untrusted input** — `AI_IMAGE`/معيار غير معتمد | رفض حسب §5-A |
| AS-06 | **Circular derivation** | XR-015 + فحص C |
| AS-07 | **Decision disguised as formula** | INV-D2 + مراجعة بشرية |
| AS-08 | **بصمة كاذبة** | الحساب من المحتوى |
| AS-09 | **Stale generation** | مقارنة الإصدار قبل التوليد |
| AS-10 | **Class leakage** — C تُعرض كـA | فصل المسار + الختم |
| AS-11 | **Hidden nondeterminism** | §6 — **مُثبت تجريبيًا** |
| AS-12 | **تجاوز البوابة** | استهلاك B7 |
| **AS-13** | **Self-certification** — C يشهد بمطابقة نفسه | **الفصل: C دليل · D حكم** |
| **AS-14** | **B geometry drift** — هندسة عرض تنحرف عن A | الهندسة الواحدة (§7) + تحقق D |

---

## 10. OPEN GAPS

| # | الثغرة | التصنيف |
|---|---|---|
| GAP-C-01 | لا رندر واقعي محسوب فيزيائيًا | Capability (Phase 00 §2.3) |
| GAP-C-02 🔴 | **`ezdxf`/`trimesh`/`reportlab` غير مثبتة في اللقطة** (ثُبِّتت يدويًا: 1.4.4 / 5.1.0 / OK) · `python-pptx` و`shapely` غير متاحين | **Environment — يُحسم قبل C1** |
| GAP-C-03 | القالب بلا جدران ولا كاميرات | Known Limitation |
| GAP-C-04 🔴 | **لا Canonicalization/Fingerprint Spec معتمدة** | **يُحسم قبل C2** |
| GAP-C-05 | الإضاءة بلا مستهلِك | ⇒ Phase 05 |
| GAP-C-06 🔴 | **ملكية الكتابة في `outputs[]`** | **⇒ 00.5-E (مفتوح)** |
| GAP-C-07 | `meta.track` غير مستهلك | ⇒ 00.5-E |
| **GAP-C-08** 🔴 | **حجية كل نوع جدول غير معرَّفة** (§11) | **يُحسم قبل C4** |
| **GAP-C-09** | **تعريف Presentation Parameters يحتاج اعتمادًا** | يُحسم قبل C5 |

---

## 11. الجداول — تفكيك BOQ (تصحيح النقطة 6)

**لا يُفترض أن كل جدول حتمي = فئة A.** الحجية تحتاج تعريفًا مستقلًا لكل نوع:

| النوع | مصدره | حتمي؟ | الفئة المقترحة | لماذا تحتاج حسمًا |
|---|---|---|---|---|
| **Quantity Schedule** | عدّ/قياس عناصر موجودة بمعرّفاتها | ✅ نعم | **A (مرشّح قوي)** | لا اجتهاد إن كانت العناصر `[C]`/`[D]` |
| **Material Schedule** | ربط عنصر بمادته المعتمدة | ⚠️ جزئيًا | **يحتاج تعريفًا** | مادة غير معتمدة أو `[P]` ⇒ ليست A |
| **Finish Schedule** | تشطيبات لكل سطح | ⚠️ غالبًا لا | **يحتاج تعريفًا** | يتضمن اختيارات تصميمية |
| **BOQ** | كميات **+ بنود + هدر + تسعير** | ❌ **لا** | **ليست A تلقائيًا** | الهدر والتسعير **اجتهاد/مصدر خارجي** — وPhase 00 §3 لا يضمن الأسعار |

**قاعدة R02:** الجدول يكون فئة A **فقط** إذا كان كل بند فيه **مرجوعًا إلى معرّف عنصر موجود** وبلا أي معامل اجتهادي. **BOQ بمعناه الكامل ليس فئة A** ⇒ **GAP-C-08**.

---

## 12. PROPOSED C SUB-STAGES — حدود محدَّثة

| المرحلة | تملك | لا تملك |
|---|---|---|
| **C1 — Generation Preconditions** | استهلاك بوابة B7 · تحديد ما لا يمكن توليده · رفض التوليد عند حاجب | إعادة تنفيذ أي فحص سابق |
| **C2 — Determinism & Fingerprint** | Canonicalization + حساب البصمات + تسجيل البيئة | الحكم بالمطابقة |
| **C3 — Geometry Emitters (A)** | DXF · SVG/PDF · GLB من `[C]`/`[D]` فقط | ترقية الحالات · اختراع أبعاد |
| **C4 — Quantity Schedules** | **Quantity Schedule فقط** بعد حسم GAP-C-08 | BOQ بالتسعير والهدر |
| **C5 — Presentation (B)** | تطبيق presentation parameters على **نفس** الهندسة | أي هندسة بديلة |
| **C6 — Output Manifest (خارجي)** | ملف بيان خارج الـMaster | الكتابة في `outputs[]` |

**ترتيب ملزم:** C1 → C2 → C3 → C4 → C5 → C6. **لا one-shot.**

---

## 13. GATE CRITERIA

**عام لكل مرحلة:** Positive + Negative لكل قاعدة (السلبي يثبت القاعدة المقصودة) · Boundary · **Mutation بلا ناجٍ** · Isolation ضد A/B1–B11 · **صفر كتابة في الـMaster** · **Full Regression: `A 87 · B1 38 · B2 39 · B3 54 · B4 38 · B5 49 · B6 61 · B7 48 · B8 45 · B9 40 · B10 47 · B11 45`** · عرض الملفات الفعلية.

| المرحلة | معيار العبور |
|---|---|
| **C1** | مشروع بحاجب ⇒ **صفر ملفات فئة A** + تصريح صريح بالعجز |
| **C2** | تشغيلان ⇒ **تطابق D0 و D1**؛ تغيير بايت في النموذج ⇒ بصمة مختلفة؛ **البيئة مسجَّلة**؛ **ممنوع ادعاء D2** |
| **C3** | كل إحداثي في المخرج يُرجَع إلى حقل في الـMaster · **صفر ترقية حالة** · حالة `[A]`/`[P]` موسومة لا مرقّاة |
| **C4** | كل بند بمعرّف عنصر · **صفر بنود مخترعة** · لا تسعير ولا هدر داخل فئة A |
| **C5** | تغيير أي presentation parameter **لا يغيّر** الهندسة المستخرَجة (D يتحقق) |
| **C6** | البيان خارجي · **الـMaster غير معدَّل** (مُثبت بالمقارنة) |

---

## 14. قرارات مطلوبة قبل C1

1. **GAP-C-04** — اعتماد Canonicalization + Fingerprint Spec.
2. **GAP-C-06** — ملكية `outputs[]` (⇒ E) **مفتوحة، لم تُغلق**.
3. **GAP-C-02** — سياسة المكتبات غير المحفوظة في اللقطة.
4. **GAP-C-08** — حجية كل نوع جدول.
5. **GAP-C-09** — تعريف Presentation Parameters.

**لم يُكتب كود · لم يُعدَّل أي ملف نظام · لم تُغلق أي ثغرة تخص D أو E. بانتظار مراجعة المشرف.**
