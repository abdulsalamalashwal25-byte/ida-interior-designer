# 00.5-C3 — GEOMETRY EMITTERS (CLASS A) — ARCHITECTURE PROPOSAL
**اقتراح معماري فقط — لا implementation · لا tests · لا schema changes · لا توليد ملفات.**

> **المرجع:** C-STEP-01 R02 §12 (C3 يملك: DXF · SVG/PDF · GLB من `[C]`/`[D]` فقط · لا يملك: ترقية الحالات · اختراع أبعاد) · C1 R05 CLOSED · **C2 R02 + Implementation CLOSED**.
> **الترتيب الملزم:** C1 → C2 → **C3** → C4 → C5 → C6.

---

## 0. INHERITED CONSTRAINTS — تُحمل كما هي

### من شروط إغلاق C2 (CND-C2-01…04)

| الشرط | أثره المباشر على C3 |
|---|---|
| **CND-C2-01** | **8 قرارات مفتوحة**، منها **6 حاجبة** للبصمات الهندسية (01/02/03/05/06/08) و**اثنان مستقلان لا يسقطان**: **DEC-C2-04** (هل تُكتب البصمات في الماستر؟) و**DEC-C2-07** (مالك SEC). ⇒ **C3 لن يستخدم مصطلح `OPEN_DECISIONS` للدلالة على الستة**؛ سيُميّز صراحة `OPEN` مقابل `ENGINEERING_BLOCKING` |
| **CND-C2-02** | **AS-C2-16** (masked mutation عبر imported bindings) و**AS-C2-17** (self-moving assertion) **قاعدتان عامتان** تُطبَّقان على مجموعة C3 القادمة |
| **CND-C2-03** | **ENV-FP ليس Engineering Fingerprint**، ولا يُستخدم بديلًا عنه في أي مخرج من C3 |
| **CND-C2-04** | **ممنوع رفع مستوى الادعاء تلقائيًا**: D0 = `OBSERVED_IN_MEASURED_CASE` · D0-provable = `NOT_ESTABLISHED` · D1 = `NOT_ESTABLISHED` · D2 = `NOT_CLAIMED`. ولا يُستنتج من نجاح اختبارات C3 أن الحتمية العامة ثبتت |

### من المراحل السابقة

| المصدر | القيد |
|---|---|
| **C1 R05** | C3 **لا يُنشئ بوابة جديدة**؛ يستهلك قرار C1 (`GENERATION_ALLOWED`/`ABSTAIN`) كما هو. ولا يعيد فحص الأهلية |
| **B-series** | ملكية القواعد الهندسية تبقى لـB2–B5، والاشتقاق لـB6، والحاجب لـB7. **C3 لا يتحقق من صحة هندسة** |
| **Master SSOT** | قراءة فقط. **لا كتابة** في `project_master.json` ولا في `outputs[]` (GAP-C-06 ⇒ E) |
| **إغلاق B8** | المخرج **لا يصير مصدر حقيقة**؛ والبصمة لا تثبت geometric fidelity |
| **فئات A/B/C** | C3 لفئة **A** فقط. B ملك C5، وC معزول |

---

## 1. مبدأ C3 المؤسِّس

> **C3 مترجم أمين (faithful transcriber)، لا مصمم ولا محرك هندسي.**

| C3 يفعل ✅ | C3 **لا** يفعل ❌ |
|---|---|
| ينقل قيمًا موجودة في الماستر إلى صيغة ملف | يحسب قيمة غير موجودة |
| يستهلك `[C]`/`[D]` فقط | يرقّي `[A]`/`[P]`/`[U]` |
| يمتنع ويصرّح عند النقص | يكمل بالتقدير أو بالافتراضي |
| يُنتج **أدلة** للمقارنة | يحكم بمطابقة الناتج للماستر (**ملك D**) |
| يحترم قرار C1 | ينشئ بوابة موازية |

**معيار القبول الجوهري (من C-STEP-01 §13):** **كل إحداثي في المخرج يُرجَع إلى حقل في الماستر · صفر ترقية حالة.**

---

## 2. 🔴 اكتشاف تنفيذي حرج — GAP-C-02 تحققت فعليًا

**قياس أُجري أثناء إعداد هذا الاقتراح:**

| المكتبة | الساعة 00:52 (أثناء C2) | الآن |
|---|---|---|
| `ezdxf` | **1.4.4 موجودة** | ❌ `MISSING` |
| `trimesh` | **5.1.0 موجودة** | ❌ `MISSING` |
| `reportlab` | **5.0.1 موجودة** | ❌ `MISSING` |
| `matplotlib` · `numpy` · `jsonschema` | موجودة | ✅ باقية |
| `shapely` · `pptx` | مفقودة | ❌ مفقودة |

**وملفات DXF التي ولّدتها أثناء قياس C2 في `/tmp` اختفت أيضًا.**

**الدلالة:** GAP-C-02 لم تعد مخاطرة نظرية — **تحققت داخل الجلسة نفسها**. والمكتبات الثلاث التي يقوم عليها **جوهر C3** (DXF · GLB · PDF) **ليست جزءًا من اللقطة المحفوظة**.

**ثلاث نتائج ملزمة لتصميم C3:**
1. **لا يجوز افتراض وجود مكتبة عند التصميم.** القدرة تُفحص **وقت التشغيل** وتُسجَّل.
2. **اختفاء مكتبة ليس خطأ في C3** — بل **حالة امتناع مشروعة** يجب أن تُعلن (A-BLK-11 في C1).
3. **إعادة إنتاج مخرج سابق قد تتعذّر** لأن أداة توليده لم تعد موجودة ⇒ هذا **دليل حي** على أن D1 تبقى `NOT_ESTABLISHED`.

✅ **ضابط إيجابي:** رغم اختفاء المكتبات، **Full Regression بقيت 707/707 · NONE**، وENV-FP سجّل `NOT_SET` بصدق. أي أن الطبقات المبنية **لا تعتمد** على هذه المكتبات — وهذا يؤكد صحة فصل C1/C2 عن التوليد.

---

## 3. الطبقات الثلاث المقترحة لـC3

**فصل مقصود** لمنع خلط «ما الذي نرسمه» بـ«كيف نكتبه في صيغة».

```
Master ──(C1: هل يجوز؟)──► Geometry Assembly (GA)
                                   │  تمثيل وسيط محايد للصيغة
                                   ▼
                          Emitter Adapters (EA)      DXF | SVG/PDF | GLB
                                   │
                                   ▼
                          Evidence Pack (EP) ──► C6 Manifest ──► D يحكم
```

### 3.1 Geometry Assembly (GA) — تجميع محايد
**يملك:** قراءة `[C]`/`[D]` من الماستر · تحويلها إلى تمثيل هندسي وسيط **محايد تجاه الصيغة** · ترتيب كلي صريح · **تسجيل provenance لكل إحداثي**.
**لا يملك:** حساب هندسي جديد · استنتاج قيمة ناقصة · أي قرار تصميمي.

**قاعدة Provenance (جوهرية):** كل قيمة في GA تحمل **مسار مصدرها في الماستر**. القيمة بلا مسار **لا تدخل** — وهذا ما يجعل معيار «كل إحداثي يُرجَع إلى حقل» **قابلًا للتحقق آليًا** بدل أن يكون وعدًا.

### 3.2 Emitter Adapters (EA) — محوّلات الصيغة
**يملك:** تحويل GA إلى صيغة بعينها · التصريح بما **لا تدعمه** الصيغة.
**لا يملك:** إضافة أو تعديل أي هندسة · «تحسين» المخرج · إخفاء ما تعذّر تمثيله.

**قاعدة عدم الإثراء:** ما ليس في GA **لا يظهر** في الملف. وما في GA وتعذّر تمثيله في الصيغة ⇒ **يُعلَن نقصًا صريحًا، لا يُسقَط بصمت.**

### 3.3 Evidence Pack (EP) — الأدلة
**يملك:** جدول إحداثيات المخرج · خريطة provenance · بطاقة البيئة (من C2) · حالة الحتمية الموروثة · **قائمة ما لم يُمثَّل**.
**لا يملك:** **الحكم بالمطابقة** ⇒ **D**. ولا إصدار بصمة هندسية ⇒ **محجوب بالقرارات الستة**.

---

## 4. مصفوفة المخرجات المقترحة

| المخرج | الصيغة | التبعية التقنية | الحالة المتوقعة اليوم |
|---|---|---|---|
| Floor Plan | **SVG** | **متاح** (كتابة نصية مباشرة أو matplotlib) | ✅ **قابل للتنفيذ** |
| Floor Plan | DXF | `ezdxf` | ⛔ **امتناع — المكتبة مفقودة** |
| Elevation / Section | SVG | متاح | ✅ قابل (يحتاج `ceiling_height` كـ`[C]`/`[D]`) |
| Furniture Plan | SVG | متاح | ✅ قابل |
| RCP | SVG | متاح | ⚠️ الإضاءة بلا مستهلك (GAP-C-05) |
| 3D | GLB/OBJ | `trimesh` | ⛔ **امتناع** |
| Plan | PDF | `reportlab` | ⛔ **امتناع** |
| مناظير محسوبة | SVG | متاح + `cameras[]` | ✅ قابل رياضيًا (لا رندر — GAP-C-01) |

**استنتاج التصميم:** **SVG هو المسار الوحيد القابل للتنفيذ اليوم بلا تبعية مفقودة.** لذلك يُقترح أن يبدأ C3 بـ**محوّل SVG واحد** يُثبت المعمارية كاملة، وتبقى محوّلات DXF/GLB/PDF **معرَّفة كواجهات تمتنع** حتى تتوفر بيئة محفوظة.

**هذا ليس تقليصًا للطموح، بل رفض لبناء طبقة على أداة غير مضمونة الوجود.**

---

## 5. الحالات والامتناع

| الحالة | حكم C3 |
|---|---|
| `[C]` | ✅ يُستهلك |
| `[D]` VERIFIED (بحكم B6) | ✅ يُستهلك |
| `[D]` UNCOMPUTABLE | ⛔ امتناع |
| `[A]` ولو **معتمدًا** | ⛔ **غير مؤهل لفئة A** (C1-FIX-01 · GAP-C-11) |
| `[P]` · `[U]` | ⛔ امتناع |
| `presence = UNKNOWN` | ⛔ امتناع |
| `presence = NOT_PRESENT` | ✅ **حقيقة صالحة** — يُرسم الغياب، لا يُخترع وجود |
| `rotation` غائب | ⛔ **ليس 0°** — يُعلَن نقصًا |
| مصدر غير موثوق | ⛔ حسب نطاق C1 §3.4 |

**قاعدة:** C3 **لا يقرر** أيًا من هذه — **يستهلك قرار C1**. وإن وصل طلب توليد بلا قرار C1 مرافق ⇒ **امتناع fail-closed**.

---

## 6. العلاقة بـC1 وC2 — بلا ازدواج

| السؤال | المالك | دور C3 |
|---|---|---|
| هل يجوز التوليد؟ | **C1** | يستهلك القرار |
| ما نطاق الحاجب؟ | **C1** | لا يعيد حسابه |
| ما الصورة القانونية؟ | **C2** | يستدعي `canonical_bytes`/`stable_sorted` |
| ما البصمة الهندسية؟ | **C2** | **لا يطلبها — محجوبة** |
| ما البيئة؟ | **C2** | يُرفق ENV-FP كما هو |
| هل الناتج مطابق للماستر؟ | **D** | يقدّم الدليل فقط |

**C3 لا يستورد أي validator من B-series** — نفس انضباط C1-ISO-01 وC2-ISO-01.

---

## 7. ATTACK SURFACE المتوقع لـC3

| # | الخطر |
|---|---|
| **AS-C3-01** | **اختراع إحداثي** لسد نقص — الخطر الأول |
| **AS-C3-02** | **ترقية حالة ضمنية** عند الرسم (`[A]` يُرسم كأنه `[C]`) |
| **AS-C3-03** | **افتراضي صامت**: `rotation=0` · CENTER · وحدة مفترضة |
| **AS-C3-04** | **إثراء المخرج** بعناصر ليست في الماستر (شبكة · أبعاد تلقائية · جدار إغلاق) |
| **AS-C3-05** | **إسقاط صامت** لما تعذّر تمثيله في الصيغة |
| **AS-C3-06** | **تجاوز بوابة C1** بالتوليد المباشر |
| **AS-C3-07** | **ادعاء D1/D2** لمخرج C3 (خرق CND-C2-04) |
| **AS-C3-08** | **استعمال ENV-FP كبصمة هندسية** (خرق CND-C2-03) |
| **AS-C3-09** | **حسم قرار مفتوح ضمنيًا** — مثل اختيار عتبة تكميم لرسم الإحداثيات (خرق CND-C2-01 · AS-C2-13) |
| **AS-C3-10** | **مخرج جزئي يبدو مكتملًا** |
| **AS-C3-11** | **الكتابة في الماستر** أو `outputs[]` |
| **AS-C3-12** | **تقديم المخرج كمصدر حقيقة** (خرق إغلاق B8) |

---

## 8. ⚠️ تبعية حرجة: الرسم يحتاج دقة عددية

**مشكلة معمارية يجب إعلانها قبل التنفيذ لا أثناءه:**

كتابة إحداثي في ملف SVG/DXF تتطلب **تمثيلًا نصيًا للعدد** (`6000` أم `6000.0` أم `6000.000`). وهذا **يلامس DEC-C2-02 (سياسة التكميم)** المفتوحة.

**الموقف المقترح — بلا حسم:**
- التمثيل النصي في **ملف العرض** ليس هو **تكميم البصمة**؛ هما استعمالان مختلفان.
- لذلك يُقترح: **C3 ينقل القيمة كما هي في الماستر بلا تقريب**, ويصرّح بذلك؛ فلا يتخذ قرار تكميم أصلًا.
- وإن تبيّن أثناء التنفيذ أن الصيغة **تفرض** تقريبًا ⇒ **توقّف وإبلاغ**, لا اختيار ذاتي.

⇒ **DEC-C3-01 (Decision Pending):** هل يكفي النقل بلا تقريب، أم يحتاج C3 سياسة تمثيل عددي خاصة به منفصلة عن DEC-C2-02؟

---

## 9. OPEN DECISIONS الخاصة بـC3

| # | القرار | لماذا لا أحسمه |
|---|---|---|
| **DEC-C3-01** | التمثيل العددي في ملفات المخرج (§8) | يلامس DEC-C2-02 |
| **DEC-C3-02** | **نطاق المخرج الأول**: هل يبدأ C3 بـSVG وحده؟ | قرار نطاق يحتاج اعتمادك |
| **DEC-C3-03** | ماذا يعني «تمثيل كامل» لمسقط؟ (جدران فقط؟ + فتحات؟ + أثاث؟) | **قرار نطاق تسليم، وليس قرارًا تقنيًا** |
| **DEC-C3-04** | سلوك محوّل تعذّرت مكتبته: امتناع صامت أم خطأ صريح؟ | يمسّ تجربة التشغيل |
| **DEC-C3-05** | هل يحتاج C3 بنية `output → required_paths` رسمية؟ | **مرتبط بـGAP-C-10 المفتوحة** |

**وترثه C3 بلا حسم:** DEC-C2-01…08 · GAP-C-01…14.

---

## 10. خطة الاختبار المقترحة (توصيف — لا تنفيذ)

| المجموعة | ما يجب إثباته |
|---|---|
| **Positive** | ماستر نظيف ⇒ مخرج SVG يحوي **بالضبط** عناصر الماستر |
| **Negative** | لكل حالة في §5 امتناع بالرمز المقصود |
| **Provenance** | **كل إحداثي في المخرج يُرجَع إلى مسار في الماستر** — وإلا فشل |
| **Zero-invention** | مقارنة عدّية: عناصر المخرج == عناصر الماستر، **لا زيادة** (ضد AS-C3-04) |
| **Zero-promotion** | لا `[A]`/`[P]`/`[U]` تظهر كهندسة ملزمة |
| **Boundary** | `NOT_PRESENT` مقابل `UNKNOWN` · `rotation` غائب · عنصر بلا معرّف |
| **Mutation** | تعطيل فحص الحالة ⇒ سقوط · **مع مراعاة AS-C2-16** (النداء عبر الوحدة) و**AS-C2-17** (لا تأكيد متحرك) |
| **Isolation** | لا import لأي validator · لا تكرار لبوابة C1 · **لا حكم بالمطابقة** |
| **Capability** | مكتبة مفقودة ⇒ **امتناع معلن** لا انهيار (سيناريو §2 الواقعي) |
| **Anti-smuggling** | حالة لكل AS-C3-01…12 |
| **No-write** | الماستر/المخطط/القالب بلا تغيير (مقارنة بايتية) |
| **Determinism** | تشغيلان ⇒ **D0-observed فقط**، و**لا ادعاء D1/D2** |
| **Regression** | الأساس الحالي **707/707** بلا انحدار |

---

## 11. GATE CARD — C3 Architecture Proposal

| البند | الحالة |
|---|---|
| Inherited constraints (CND-C2-01…04) | ✅ مثبتة في §0 ومنعكسة في §6/§7 |
| فصل OPEN عن ENGINEERING_BLOCKING | ✅ §0 — وDEC-C2-04/07 **لم تُسقطا** |
| حدود C1 | ✅ C3 يستهلك ولا يكرر |
| ملكية B-series | ✅ لا فحص هندسي في C3 |
| Master SSOT | ✅ قراءة فقط · لا `outputs[]` |
| لا توليد قبل البوابات | ✅ التوليد مشروط بقرار C1 |
| لا حسم قرار مفتوح | ✅ وDEC-C3-01…05 مسجّلة لا محسومة |
| لا implementation / tests / schema | ✅ **هذه الجولة وثيقة فقط** |

### القرار: **HOLD — بانتظار مراجعة Gate المعمارية**

**ثلاث نقاط تحتاج قرارك قبل أي تنفيذ:**

1. **🔴 GAP-C-02 تحققت فعليًا** (§2): `ezdxf`/`trimesh`/`reportlab` **اختفت أثناء الجلسة**. بناء C3 على DXF/GLB/PDF اليوم = بناء على أداة غير مضمونة. **أقترح البدء بـSVG وحده** (DEC-C3-02) — وهذا قرار نطاق يخصّك.
2. **DEC-C3-03** — «ما الذي يحتويه المسقط» قرار تسليم لا تقني.
3. **DEC-C3-01** — التمثيل العددي قد يلامس DEC-C2-02 المحجوبة.

**لم أبدأ أي implementation ولا tests ولا تعديل schema. لم أحسم أي قرار موروث. بانتظار مراجعتك.**
