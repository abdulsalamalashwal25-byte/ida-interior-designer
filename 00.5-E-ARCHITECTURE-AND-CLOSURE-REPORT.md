# 00.5-E — GOVERNANCE / IDENTITY / APPROVAL / OUTPUT OWNERSHIP
## ARCHITECTURE & CLOSURE REPORT

**معمارية فقط — لا كود · لا تعديل Schema/Master/C1–C6/D · لا Implementation · لا F · لا مرحلة لاحقة.**

> **مُثبَّت مسبقًا:** `00.5-D = CLOSED WITH CONDITIONS` · **`GAP-D-01 = OPEN / DEFERRED`** — **لا يُعاد فتح D ولا يُعالَج GAP-D-01 داخل E.**
> **Regression baseline:** 1109/1109 (لم يُمسّ شيء).

---

## 0. قياسات أُجريت قبل التصميم — وأحدها يقلب فرضية كاملة

| # | القياس | النتيجة |
|---|---|---|
| **M-1** | كيانات الحوكمة في المخطط | **موجودة سلفًا**: `decision_entry` · `proposal_entry` · `cr_entry` · `objection_entry` · `assumption_entry` · `unknown_entry` · `changelog_entry` · `output` · `registers` · `meta` |
| **M-2** | **`approved_by`** | **`{"const": "USER"}` — وليس نصًا حرًا** |
| **M-3** | enums دورة الحياة | `proposal.state`: OPEN/APPROVED/REJECTED/**SUPERSEDED** · `cr.state`: DRAFT/PENDING_APPROVAL/APPROVED/REJECTED/DEFERRED/APPLIED · `objection.state`: OPEN/WITHDRAWN/USER_OVERRIDE/ACCEPTED · `assumption.approval_state`: PENDING_APPROVAL/APPROVED/REJECTED |
| **M-4** | القفل/التجميد | `meta.status`: **DRAFT / FROZEN** · `meta.frozen_on` · `meta.track`: **FULL / LITE** |
| **M-5** | `decision_entry.required` | **ثمانية حقول إلزامية** منها `linked_proposal` · `approved_by` · `approved_on` · `approved_in_revision` |
| **M-6** ⭐ | **آليات إثبات الهوية داخل الملف** | **صفر**: لا `signature` · لا `credential` · لا `token` · لا `actor` · لا `user_id` · لا `session` |

### النتيجة الحاسمة من M-2 + M-6

**نصف مشكلة انتحال الاعتماد محلول سلفًا، والنصف الآخر غير قابل للحل داخل الملف:**

- **`approved_by` ليس نصًا حرًا** ⇒ **لا يمكن كتابة اسم منتحل** — الشرط الذي طلبتموه في E2 **مستوفى بنيويًا بالفعل**.
- **لكن أي ملف يستطيع أن يحتوي `"USER"`** ⇒ **لا يوجد ما يميّز اعتمادًا حقيقيًا من اعتماد مصطنع** بالبيانات وحدها.

> ⇒ **GAP-02 لا يُغلق بالبيانات.** إثبات صدق الاعتماد يحتاج **قناة خارج الملف** — وهذا **قرار معماري يخصّكم** ⇒ **CONFLICT-E-01** (§11).

---

## 1. مهمة E ونطاقها

> **E = Governance Layer: يملك الهوية الرسمية، وسلطة الاعتماد، ودورة الحياة، و`outputs[]`.**
> **E لا يُنتج أدلة ولا يحكم هندسيًا — بل يقرر ما الذي يُعتدّ به رسميًا.**

**الفرق الجوهري عن D:** D يسأل *«هل المخرج يطابق مرجعه؟»* (**قياس**). E يسأل *«هل يجوز الاعتداد بهذا رسميًا؟»* (**سلطة**). الأول لا يمنح الثاني، والثاني لا يغني عن الأول.

---

## 2. تصنيف المسؤوليات — **ليست آلية واحدة**

**لا أفترض أن العناصر التسعة تحتاج الآلية نفسها.** تصنيفها إلى **أربع آليات مختلفة**:

| # | الآلية | المسؤوليات | الطبيعة |
|---|---|---|---|
| **G-1** | **Identity Registry** | Project · Master · Revision · Decision · Proposal · Output · Manifest · Evidence | **تسمية ومرجعية** — بنيوية |
| **G-2** | **Authorization Gate** | Approval · من يملك السلطة · ما يُعتمد · anti-impersonation | **سلطة** — تحتاج قناة موثوقة |
| **G-3** | **Lifecycle Machine** | Proposal · Decision · CR · Output · Revision states | **انتقالات** — قواعد صريحة |
| **G-4** | **Ownership Registry** | `outputs[]` · linkage · supersession · lock/freeze/track | **ملكية سجل** |

**لماذا الفصل ضروري:** دمجها يعني أن **تسجيل** مخرج يصير **اعتمادًا** له — وهو أخطر خلط ممكن (AS-E-11).

---

## 3. GOVERNANCE OWNERSHIP MATRIX

| المسؤولية | المالك | دور E |
|---|---|---|
| شكل الحقول وأنماط المعرّفات | **A** | يستهلك |
| وجود المراجع وحلّها (XR-*) | **B1** | يستهلك |
| الهندسة والخلوص والحركة والأبواب | **B2–B5** | **لا شأن له** |
| صحة `[D]` الحسابية | **B6** | يستهلك |
| الحاجب والمجهول | **B7** | يستهلك |
| تمثيل المخرج وادعاءاته | **B8** | يستهلك |
| **تماسك سجل المراجعات** | **B9** | يستهلك |
| **تماسك دورة حياة القرارات** | **B10** | **يستهلك — ولا يعيد تنفيذه** |
| بوابة التوليد | **C1** | يستهلك |
| الحتمية والبصمات والبيئة | **C2** | يستهلك (وهي محجوبة) |
| **`GA ↔ Master` provenance** | **C3** | **لا يملكه** |
| قياس الكميات | **C4** | يستهلك |
| العرض (فئة B) | **C5** | يستهلك |
| تجميع البيان/الأدلة | **C6** | يستهلك |
| **`Output ↔ GA` fidelity** | **D** | **يستهلك — ولا يعيد الحكم** |
| **صدق الاعتماد والهوية (GAP-01/02)** | **E** ✅ | **ملكه** |
| **`outputs[]`** | **E** ✅ | **ملكه** |
| **سلطة الاعتماد ودورة الحياة الرسمية** | **E** ✅ | **ملكه** |
| **Lock / Freeze / Track governance** | **E** ✅ | **ملكه** |

**الحد الحاسم مع B10 (منصوص في كوده):** *«B10 cannot tell whether an approval is GENUINE — that is GAP-02 and belongs to 00.5-E»*, و*«A perfectly consistent but entirely fabricated history passes»*.
⇒ **B10 يفحص تماسك الحالات · E يفحص صدقها.** **لا تداخل** — ويُثبَت بأن E **لا يعيد أي فحص تماسك**.

---

## 4. E1 — ENTITY / IDENTITY MODEL

| الكيان | ما يعرّفه رسميًا | المصدر | حالة اليوم |
|---|---|---|---|
| **Project** | `meta.project_id` بنمط `^PRJ-[0-9]{2,3}$` | A | ✅ موجود |
| **Master** | `project_id` + `meta.revision` + `master_hash` | A/C2 | ⚠️ `master_hash` **يُصرَّح ولا يُحسب** (C2 محجوبة) |
| **Revision** | `meta.revision` + `changelog_entry` | A/B9 | ✅ موجود |
| **Decision** | `decision_id` بنمط `^DEC-[0-9]{3}$` | A | ✅ موجود |
| **Proposal** | `id` بنمط `^P-[0-9]{3}$` + `state` | A | ✅ موجود |
| **Approval** | **لا كيان مستقل** — سمات داخل الكيان المعتمَد | — | 🔴 **DEC-E-02** |
| **Output** | `output_id` | A | ⚠️ **لا طبقة تولّده** (GAP-C6-02) |
| **Manifest** | بيان خارجي | C6 | ⚠️ **بلا معرّف رسمي** |
| **Evidence** | تقارير C2/C4/C6/D | منتِجوها | ⚠️ **بلا معرّف رسمي** |

### العلاقات الرسمية

```
Project ──1:N──► Revision ──1:N──► Decision ──1:1──► Proposal
   │                │                  │
   │                │                  └──► Objection / CR
   │                ▼
   └──1:N──► Output ──N:1──► parent Output (supersession)
                 │
                 ├──► Evidence: D fidelity report
                 ├──► Evidence: C6 manifest
                 └──► Evidence: C2 fingerprint  [NOT_ISSUED]
```

**قاعدة:** كل علاقة **مرجع صريح لكيان موجود**. **لا علاقة ضمنية** بالاسم أو الترتيب أو الزمن.

---

## 5. E2 — APPROVAL CONTRACT

### 5.1 سلطة الاعتماد
**المالك الوحيد: المستخدم البشري** — و`approved_by = {"const": "USER"}` **يفرض ذلك بنيويًا** (M-2). **لا AI ولا طبقة تعتمد شيئًا.**

### 5.2 التمييز الخماسي — **خمسة مفاهيم لا تُخلط**

| # | المستوى | المعنى | المالك | يمنح ما بعده؟ |
|---|---|---|---|---|
| 1 | **REVIEWED** | نُظر فيه | إنساني | ❌ |
| 2 | **VERIFIED** | **قياس تقني نجح ضمن نطاقه** | **D** (fidelity) · B (قواعد) | ❌ **لا يمنح اعتمادًا** |
| 3 | **ACCEPTED** | قُبل تحفظيًا | المستخدم | ❌ |
| 4 | **APPROVED** | **اعتُمد رسميًا** | **المستخدم حصرًا** | ❌ |
| 5 | **FINAL** | **مجمَّد لا يُعدَّل** | E + `meta.status=FROZEN` | — |

**قاعدتان قاطعتان:**
- **`VERIFIED` (من D) لا يُرقَّى تلقائيًا إلى `APPROVED`.** الأول قياس، الثاني سلطة.
- **`APPROVED` لا يعني `VERIFIED`** — قد يُعتمد مخرج لم تُثبت مطابقته.

### 5.3 شروط صحة الاعتماد (مقترحة — مجتمعة)

| # | الشرط |
|---|---|
| **AP-1** | `approved_by = "USER"` (مفروض بنيويًا) |
| **AP-2** | `approved_on` تاريخ صالح **لا يسبق** إنشاء الكيان (**B11 يفحص الزمن؛ E يستهلك**) |
| **AP-3** | `approved_in_revision` **مراجعة موجودة** وليست superseded |
| **AP-4** | الكيان المعتمَد **موجود** ومعرّفه صالح |
| **AP-5** | **`linked_proposal` موجود وحالته ليست REJECTED/SUPERSEDED** (B10 يفحص التماسك؛ E يفحص الأهلية) |
| **AP-6** | **لا اعتماد ضمني** — غياب الاعتماد = **غير معتمد**, لا افتراض |
| **AP-7** | **الاعتماد على كيان مجمَّد محظور** إلا بمسار CR |
| **AP-8** ⭐ | **صدق الاعتماد** — 🔴 **غير قابل للإثبات داخل الملف (M-6)** ⇒ **GAP-02 يبقى OPEN** |

**AP-8 هو الحد الجوهري:** كل ما سبق يفحص **الاتساق والأهلية**، ولا شيء منها يثبت **أن إنسانًا اعتمد فعلًا**.

---

## 6. E3 — `outputs[]` OWNERSHIP CONTRACT

> **E هو المالك الرسمي الوحيد لـ`outputs[]`.** C6 يُجمّع أدلة خارجية، وD يحكم بالمطابقة — **ولا يملك أيٌّ منهما قرار المخرج النهائي.**

| الحقل | المصدر | ملاحظة الحوكمة |
|---|---|---|
| `output_id` | **E يولّده** | 🔴 لا طبقة تولّده اليوم (GAP-C6-02) |
| `file` · `view_type` | المنتِج | يُنقل |
| **`class`** A/B/C | المنتِج | **E يفرض الفصل ولا يحوّل فئة** |
| `guarantee` | مشتق من الفئة | من enum المخطط الثلاثي |
| `binding_geometry` / `binding_visual` | حسب الفئة | **C لا يكون ملزمًا أبدًا** |
| `derived_from_output` | E | **parent linkage رسمي** |
| `supersedes_A` | E | **supersession رسمية** |
| `fingerprint` | **C2** | 🔴 `NOT_ISSUED` — **E لا يخترعه** |
| `generated_on` | المنتِج | 🔴 لا طبقة تُنتجه (GAP-C6-04) |
| **fidelity reference** | **D** | **مرجع لتقرير D — لا حكم من E** |
| **lifecycle state** | **E** | §7 |
| **approval state** | **E** | §5 |

### حقول ثابتة مقابل متغيرة

| Immutable بعد الإصدار | Mutable بحوكمة |
|---|---|
| `output_id` · `class` · `file` · `generated_on` · `fingerprint` · `derived_from_output` | `lifecycle_state` · `approval state` · `supersedes_A` · مراجع الأدلة |

**قاعدة:** تغيير حقل ثابت ⇒ **مخرج جديد بمعرّف جديد**, لا تعديل في مكانه.

---

## 7. E4 — GOVERNANCE STATE MACHINE

**الانتقالات صريحة، وكل ما عداها محظور.**

### Proposal (enum قائم — M-3)
```
OPEN ──► APPROVED          OPEN ──► REJECTED          OPEN ──► SUPERSEDED
APPROVED ──► SUPERSEDED
✗ REJECTED ──► APPROVED        ✗ SUPERSEDED ──► OPEN        ✗ APPROVED ──► OPEN
```

### Change Request (enum قائم)
```
DRAFT ──► PENDING_APPROVAL ──► APPROVED ──► APPLIED
PENDING_APPROVAL ──► REJECTED | DEFERRED       DEFERRED ──► PENDING_APPROVAL
✗ DRAFT ──► APPLIED     ✗ REJECTED ──► APPLIED     ✗ APPLIED ──► DRAFT
```

### Decision
```
(Proposal APPROVED) ──► DECISION_RECORDED ──► SUPERSEDED_BY_CR
✗ قرار بلا proposal        ✗ قرار على proposal REJECTED/SUPERSEDED
```

### Output — **مقترح جديد** (لا enum قائم) ⇒ **DEC-E-03**
```
REGISTERED ──► EVIDENCE_LINKED ──► ACCEPTED ──► APPROVED ──► SUPERSEDED
REGISTERED ──► WITHDRAWN
✗ REGISTERED ──► APPROVED      (لا قفز فوق ربط الأدلة)
✗ SUPERSEDED ──► APPROVED      (لا إحياء لمخرج متجاوَز)
✗ فئة C ──► APPROVED كـA/B     (لا ترقية فئة)
```

### Revision (enum قائم)
```
DRAFT ──► FROZEN
✗ FROZEN ──► DRAFT      (الرجوع يحتاج مراجعة جديدة، لا فكّ تجميد)
```

**قاعدة عامة:** **الانتقال غير المذكور محظور** — وليس «غير معرَّف».

---

## 8. E5 — ANTI-IMPERSONATION CONTRACT

| # | الحالة | الحاجز | قابل للفرض اليوم؟ |
|---|---|---|---|
| 1 | **`approved_by` نص حر** | **`const: "USER"`** | ✅ **مفروض بنيويًا** |
| 2 | **مستخدم مزيف** | — | 🔴 **غير ممكن داخل الملف (M-6)** |
| 3 | **موافقة مزيفة** | — | 🔴 **غير ممكن — GAP-02** |
| 4 | **Decision غير موجودة** | مرجع يُحل (B1/XR) | ✅ |
| 5 | **Approval على كيان غير موجود** | AP-4 | ✅ |
| 6 | **Approval على revision خاطئة** | AP-3 | ✅ |
| 7 | **Output يدّعي parent غير صحيح** | `derived_from_output` يُحل | ✅ |
| 8 | **Output يدّعي fidelity لم يُثبت** | **مرجع تقرير D إلزامي** | ✅ |
| 9 | **Output يدّعي fingerprint لم يصدر** | `NOT_ISSUED` من C2 | ✅ |
| 10 | **استعمال قرار مرفوض/متجاوَز كأنه حي** | §7 + B10 | ✅ |
| 11 | **مصدر مزيف** | `source_type` enum + OR-008 | ✅ (B8) |

**الخلاصة الصادقة:** **تسعة من أحد عشر قابلة للفرض بنيويًا.** والحالتان (2) و(3) — **صدق المستخدم وصدق الاعتماد** — **غير قابلتين للإثبات بالبيانات**, وتحتاجان **قناة خارج الملف** ⇒ **CONFLICT-E-01**.

---

## 9. E6 — LOCK / FREEZE / TRACK CONTRACT

**أربعة مفاهيم مختلفة — لا تُدمج:**

| المفهوم | النطاق | المعنى | الحالة |
|---|---|---|---|
| **LOCKED** | حقل/عنصر | لا يُعدَّل إلا بـ**CR معتمد** | 🔴 **لا آلية قائمة** ⇒ DEC-E-04 |
| **FROZEN** | **مراجعة كاملة** | `meta.status=FROZEN` + `frozen_on` | ✅ **قائم** (B9 يفحص التسجيل) |
| **IMMUTABLE** | حقل بطبيعته | **لا يتغير أبدًا** (`output_id` · `generated_on`) | 🟡 مقترح §6 |
| **TRACK** | مسار المشروع | `FULL` / `LITE` — **عمق الإلزام** | ✅ قائم |

**الفروق الجوهرية:** **LOCKED** قابل للفتح بمسار معتمد · **FROZEN** يخصّ لقطة زمنية كاملة · **IMMUTABLE** لا يُفتح إطلاقًا · **TRACK** لا يقفل شيئًا بل يحدد الصرامة.

**`track` ملاحظة حوكمية:** `LITE` **لا يُسقط الإلزام**؛ يحدد عمق التوثيق فقط ⇒ **DEC-E-05**.

---

## 10. E7 — GOVERNANCE ↔ EVIDENCE BOUNDARY

> **الدليل لا يمنح سلطة. والسلطة لا تصنع دليلًا.**

| الدليل | يثبت | **لا** يثبت |
|---|---|---|
| **تقرير D** `VERIFIED` | مطابقة **ضمن حقول معلنة** | ❌ أن المخرج **معتمد** · ❌ أن التصميم صحيح |
| **بصمة C2** | تطابق مدخلات | ❌ صحة هندسية · ❌ اعتمادًا |
| **بيان C6** | أن مخرجًا سُجِّل بأدلته | ❌ fidelity · ❌ اعتمادًا |
| **provenance C3** | أن القيمة جاءت من الماستر | ❌ **معقولية التصميم** |
| **جدول C4** | كمية محسوبة | ❌ صحة الكمية واقعيًا |

**وبالعكس:** `APPROVED` **لا يثبت** fidelity ولا صحة هندسية ولا معقولية.

**قاعدة ملزمة:** **لا اعتماد ضمني.** ولا يُشتق اعتماد من وجود دليل، مهما كان قويًا.

---

## 11. CONFLICTS / DECISIONS REQUIRED

### 🔴 CONFLICT-E-01 — صدق الاعتماد غير قابل للإثبات داخل الملف

**المقاس (M-6):** لا `signature` ولا `credential` ولا `token` ولا `actor` في المخطط.
**الأثر:** ملف يحوي `approved_by: "USER"` **لا يُميَّز** عن ملف اعتماده مصطنع. **GAP-02 لا يُغلق بالبيانات.**
**الخيارات:** (أ) قبول الحد وتوثيقه · (ب) قناة اعتماد **خارج الملف** · (ج) آلية توقيع **تتطلب تعديل Schema — محظور الآن**.
**اقتراحي:** **(أ) للإصدار الأول مع توثيق صريح**, و(ب) قرار مستقل. **الحسم لكم.**

### جدول القرارات

| # | القرار | الخيارات | اقتراحي | Blocker؟ |
|---|---|---|---|---|
| **DEC-E-01** | نطاق E V1 | (أ) الأربع آليات · (ب) G-1+G-4 أولًا · (ج) G-2 أولًا | **(ب)** — الهوية والملكية أساس الباقي | 🔴 نعم |
| **DEC-E-02** | هل Approval كيان مستقل أم سمات؟ | (أ) سمات (الوضع القائم) · (ب) كيان مستقل | **(أ)** — (ب) يحتاج Schema | 🟡 |
| **DEC-E-03** | enum دورة حياة المخرج | (أ) الستة المقترحة · (ب) أقل · (ج) تأجيل | **(أ)** كعقد **خارجي** بلا تعديل Schema | 🔴 نعم |
| **DEC-E-04** | آلية LOCKED على مستوى الحقل | (أ) تأجيل · (ب) تصميمها الآن | **(أ)** | 🟡 |
| **DEC-E-05** | أثر `track=LITE` | (أ) عمق توثيق فقط · (ب) يخفف الإلزام | **(أ)** — (ب) خطر | 🟡 |
| **DEC-E-06** | **أين يعيش سجل حوكمة E؟** | (أ) **خارجي** · (ب) داخل الماستر | **(أ)** — (ب) يعني كتابة في SSOT | 🔴 نعم |
| **DEC-E-07** | من يولّد `output_id`؟ | (أ) E بنمط معتمد · (ب) المستدعي | **(أ)** | 🔴 نعم |
| **DEC-E-08** | هل يمنع E الاعتماد بلا تقرير D؟ | (أ) نعم · (ب) يُسجَّل تحذيرًا | **(ب)** — (أ) يجعل D شرط سلطة | 🟡 |

---

## 12. TEST CONTRACT (توصيف — لا تنفيذ)

| الفئة | الحالات |
|---|---|
| **Positive** | قرار على proposal APPROVED · مخرج بأدلة كاملة ⇒ ACCEPTED · تجميد مسجَّل · supersession صحيحة |
| **Negative** | **قرار على proposal REJECTED ⇒ رفض** · اعتماد على revision غير موجودة · مخرج بparent وهمي · **ادعاء fidelity بلا تقرير D** · **ادعاء fingerprint وC2 محجوبة** · انتقال محظور (`REJECTED→APPROVED`) |
| **Boundary** | **approval بلا identity** · **identity بلا approval** · مخرج بلا parent · مخرج من revision superseded · decision rejected · proposal superseded · **fingerprint غير موجود** · **fidelity PARTIAL** · **fidelity NOT_VERIFIABLE** · **فئة C لا تُعتمد كـA/B** · revision frozen · **تعديل بعد القفل** |
| **Anti-Smuggling** | **free-text approval** (مستحيل بنيويًا — يُختبر) · free-text identity · **hidden/implicit approval** · fake provenance · fake parent · fake decision reference · **VERIFIED يُقرأ APPROVED** · **APPROVED يُقرأ VERIFIED** |
| **Isolation** | E **لا يعيد** فحوص B9/B10 · لا يحكم fidelity · لا يولّد هندسة · لا يعيد تفسير نتائج validators |
| **No-Write** | سلوكي + تدقيق مسارات (إن كان السجل خارجيًا — DEC-E-06) |
| **Mutation** | السماح بانتقال محظور · قبول اعتماد بلا proposal · ترقية VERIFIED إلى APPROVED · ترقية فئة C · **+ CONTROL** · **مع عزل الدفاعات المكرّرة مسبقًا** |
| **Regression** | **1109/1109** |

**اختباران حاسمان:** **لا اعتماد ضمني** · **لا ترقية من دليل إلى سلطة**.

---

## 13. OPEN GAPS / DEFERRED

| # | الحالة |
|---|---|
| **GAP-D-01** | 🔴 **OPEN / DEFERRED** — **لا يُعالَج في E**؛ ولن يستنتج E `host_wall`/`offset`/`thickness`/`units`/`provenance` من مخرج لا يحويها. **يُسجَّل كـdependency فقط** |
| **GAP-01 / GAP-02** | 🔴 **OPEN** — صدق المصدر والاعتماد (CONFLICT-E-01) |
| **GAP-C6-02** لا هوية مخرج | 🔴 — **يخصّ E** ⇒ DEC-E-07 |
| **GAP-C6-03/04/05** | 🔴 كما هي |
| **GAP-C-02/04/06/08/14** · DEC-C2-01…08 | 🔴 كما هي |
| **GAP-E-01** ⭐ جديدة | **لا آلية إثبات هوية داخل المخطط** (M-6) |

---

## 14. GATE CARD — 00.5-E Architecture

| معيار القبول | الحالة |
|---|---|
| **وضوح الملكية** | ✅ §3 — ومصفوفة تشمل A/B/C/D/E |
| **عدم تضارب المسؤوليات** | ✅ حد B10/E منصوص في كود B10 نفسه |
| **قابلية التحقق** | ✅ 9/11 من حالات الانتحال قابلة للفرض · و2 معلنتان كغير قابلتين |
| **لا free-text كسلطة** | ✅ **`approved_by = const "USER"`** (مقاس) |
| **لا implicit approval** | ✅ AP-6 |
| **لا identity spoofing** | ⚠️ **مضبوط جزئيًا** — CONFLICT-E-01 معلن بصدق |
| **lifecycle واضح** | ✅ §7 — خمس آلات بانتقالات محظورة صريحة |
| **حدود Evidence/Governance** | ✅ §10 |
| **لا تسريب مسؤوليات D/C/B** | ✅ E يستهلك ولا يعيد تنفيذ |
| **عدم فتح GAP-D-01** | ✅ مسجَّل dependency فقط |
| **قابلية التحويل لاختبارات** | ✅ §12 بسبع فئات |
| كود / تعديلات | ✅ **صفر** — 1109/1109 سليم |

### الحالة: 🔴 **HOLD — E Architecture = WAITING FOR SUPERVISOR REVIEW**

**أربعة حواجب — كلها قرارات لا اجتهادات:**

| # | الحاجب |
|---|---|
| **CONFLICT-E-01** | **صدق الاعتماد غير قابل للإثبات داخل الملف** — قرار معماري يخصّكم |
| **DEC-E-01** | نطاق V1 |
| **DEC-E-06** | **أين يعيش سجل E** — داخل الماستر يعني كتابة في SSOT |
| **DEC-E-07** | من يولّد `output_id` |

**ملاحظة أسجّلها بصراحة:** أكثر ما طلبتموه في E2 (**`approved_by` ليس نصًا حرًا**) **مستوفى سلفًا في المخطط** — فلم أخترع له آلية. وما **لا** يمكن تحقيقه (**تمييز اعتماد حقيقي من مصطنع**) أعلنته بدل تغطيته بطبقة توحي بالأمان.

**لم أكتب كودًا ولم أعدّل أي ملف قائم ولم أنشئ Schema، ولم أبدأ E Implementation ولا F.**
