# 00.5-C4 — QUANTITY SCHEDULES / BOQ — ARCHITECTURE PROPOSAL
**اقتراح معماري فقط — لا كود · لا ملفات تنفيذية · لا tests · لا تعديل Schema/Master · لا C5/C6.**

> **المرجع:** C-STEP-01 R02 §11 (تفكيك BOQ) و§12 (C4 يملك **Quantity Schedule فقط بعد حسم GAP-C-08**) · B6 Formula Integrity · C1 R05 · C2 R02 · C3 CLOSED.
> **قاعدة حاكمة:** كل ما في هذه الوثيقة **اقتراح**. لا يتحول أي بند إلى قرار إلا بموافقتك.

---

## 0. INHERITED CONSTRAINTS — تُحمل كما هي

### من شروط إغلاق C3 (الستة)

| # | الشرط | أثره على C4 |
|---|---|---|
| 1 | **761/761 ليست دليل fidelity** | نجاح اختبارات C4 لن يُقدَّم كدليل صحة كمية |
| 2 | **provenance/element-count ليسا دليل صحة** | عدّ 12 عنصرًا لا يثبت أن العدّ **صحيح هندسيًا** |
| 3 | **Capability دليل لكل runtime** | يُعاد فحصها لحظيًا |
| 4 | **No-write يجمع سلوكًا وفحص مسارات** | اختبار سلوكي + فحص `open`/`write`/`json.dump`/`Path.write_*` |
| 5 | **SVG تمثيل مشتق لا مصدر حقيقة** | ⇒ **C4 لا يقيس من مخرجات C3 إطلاقًا** |
| 6 | **`[U]`/الغياب ≠ `NOT_PRESENT`** | الجدول الجزئي يبقى **معلنًا** |

### من C2 (CND-C2-01…04)
8 قرارات مفتوحة (6 حاجبة + **DEC-C2-04/07 مستقلتان لا تسقطان**) · **AS-C2-16/17** قاعدتا اختبار عامتان · ENV-FP ليس بصمة هندسية · **ممنوع رفع مستوى ادعاء الحتمية**.

### من C1 وB-series وMaster
استهلاك بوابة C1 بلا إعادة تنفيذ · **`[A]` المعتمد غير مؤهل لفئة A** · لا import لأي validator · **Master SSOT قراءة فقط** · `outputs[]` ⇒ **E**.

---

## 1. المبدأ المؤسِّس

> **C4 عدّاد وقائس لما هو **مصرَّح** في الماستر — لا مقدِّر تكلفة ولا خبير كميات.**

**الفرق الحاسم عن C3:** C3 ينقل قيمًا موجودة كما هي. **C4 يُنتج أرقامًا جديدة** (مجاميع، أعداد، أطوال) لم تكن في الماستر ⇒ **سطح هجومه أخطر جوهريًا**، لأن الرقم المحسوب يبدو موضوعيًا حتى لو كان مبنيًا على اجتهاد مخفي.

---

## 2. 🔴 اكتشافان بنيويان يحكمان التصميم

### 2.1 B6 يتحقق ولا يشتق — والفجوة حقيقية

نص B6 الصريح: *«B6 never derives a value nobody asked for; it checks what is declared»* و*«an input that is [U]/[P]/[A] makes the result UNCOMPUTABLE, never guessed»*.

⇒ **B6 يملك صحة `[D]` المصرَّح. ولا جهة تملك اشتقاق رقم جديد.** وهنا تحديدًا يقع السؤال المعماري لـC4:

> **هل الكمية المحسوبة قيمة `[D]` تحتاج تصريحًا في الماستر (فيملكها B6)، أم ناتج خارجي في جدول (فيملكه C4)؟**

⇒ **DEC-C4-01 — أخطر قرار في هذه المرحلة.** لا أحسمه.

### 2.2 المخطط لا يدعم الكميات بنيويًا — قياس مُثبت

| الفحص | النتيجة |
|---|---|
| `$defs.quantity` / `schedule` / `boq` | ❌ **غير موجودة إطلاقًا** |
| `fact.unit` enum | `mm · mm2 · deg · count · kelvin · lux · watt · currency · none` |
| **وحدة حجم (`mm3`)** | ❌ **غير موجودة** ⇒ **أي كمية حجمية غير قابلة للتمثيل** |
| وحدات مترية (`m`/`m2`) | ❌ غير موجودة (mm فقط) |
| `$defs.material` | `id · name · color_hex · finish · applied_to` — **لا تغطية · لا سماكة · لا هدر · لا سعر** |
| `derivation_type` enum | `ARITHMETIC · GEOMETRIC · LOGICAL_DEDUCTION` |

**ثلاث نتائج ملزمة:**
1. **أي كمية حجمية (خرسانة، ردم) غير قابلة للتمثيل** — ليست نقصًا في C4 بل في وحدات المخطط ⇒ **GAP-C4-01**.
2. **كل معامل تحويل مادة** (لتر/م²، هدر%) **ليس في الماستر** ⇒ مصدره خارجي ⇒ **لا يدخل فئة A** بحكم قاعدة C-STEP-01 R02.
3. **لا مكان بنيويًا لتخزين الكميات** ⇒ يتسق مع كون الجدول **ناتجًا خارجيًا** (مثل C6 Manifest) لا كتابة في الماستر.

---

## 3. التمييز الثلاثي — Quantity Schedule ≠ BOQ ≠ Cost Estimate

| # | النوع | المحتوى | المصدر | فئة مقترحة |
|---|---|---|---|---|
| **1** | **Quantity Schedule** | عدّ وقياس عناصر موجودة بمعرّفاتها | الماستر وحده | **A (مرشّح — بشروط §5)** |
| **2** | **BOQ** | كميات **+ بنود + هدر + معاملات تنفيذ** | الماستر **+ اجتهاد/معايير خارجية** | ❌ **ليست A** |
| **3** | **Cost Estimate** | BOQ **+ أسعار** | مصادر سوقية خارجية | ❌ **ليست A ولا B** |

**السبب الجوهري للفصل:** الهدر والتسعير **ليسا حسابًا بل اجتهادًا**؛ وPhase 00 §3 لا يضمن الأسعار. ودمجهما مع الكميات يُنتج جدولًا **يبدو كله محسوبًا** بينما نصفه تقدير — وهو **AS-C4-02**.

> ### اقتراح النطاق: **C4 ينفّذ النوع 1 فقط.**
> النوعان 2 و3 **يُعرَّفان معماريًا ويبقيان غير منفَّذين** حتى قرار مستقل. **البند المحسوب لا يصير فئة A لمجرد أنه رقم.**

---

## 4. أنواع الكميات القابلة للاشتقاق الحتمي

| # | الكمية | المدخل | الوحدة | الحتمية | الحالة المقترحة |
|---|---|---|---|---|---|
| Q-01 | **عدّ العناصر** (جدران، فتحات، قطع أثاث) | معرّفات | `count` | ✅ كاملة | **مرشّح A** |
| Q-02 | **طول الجدار** | `start`/`end` | `mm` | ✅ هندسية بحتة | **مرشّح A** |
| Q-03 | **مجموع أطوال الجدران** | Q-02 | `mm` | ✅ | **مرشّح A** |
| Q-04 | **مساحة الفتحة** | `width × height` | `mm2` | ✅ | **مرشّح A** |
| Q-05 | **مساحة أرضية الفراغ** | `space/outline` | `mm2` | ⚠️ **يحتاج قرار** (§6) | **DEC-C4-03** |
| Q-06 | **مساحة سطح الجدار** | الطول × الارتفاع | `mm2` | ⚠️ **هل تُخصم الفتحات؟** | **DEC-C4-04** |
| Q-07 | **كمية المادة** | مساحة × معامل تغطية | — | ❌ **المعامل غير موجود** | **ليست A** |
| Q-08 | **حجم** | — | ❌ **لا `mm3`** | ❌ غير ممثَّلة | **GAP-C4-01** |
| Q-09 | **كميات بالهدر/التسعير** | معاملات خارجية | `currency` | ❌ اجتهاد | **ليست A** |

**Q-01…Q-04 هي النواة القابلة للتنفيذ.** Q-05/Q-06 محجوبتان بقرارَي نطاق **ليسا تقنيَّين**.

---

## 5. شروط أهلية الكمية (مجتمعة — غياب أيٍّ منها ⇒ استبعاد معلن)

| # | الشرط |
|---|---|
| 1 | **كل مدخل `[C]` أو `[D]` VERIFIED** — `[A]` ولو معتمدًا، و`[P]`، و`[U]` **غير مؤهلة** |
| 2 | **المصدر موثوق** — لا `AI_IMAGE`/`EXTERNAL_STANDARD`/`EXTERNAL_LIBRARY`/`AGENT_*`/`NOT_PROVIDED` |
| 3 | **البند مرجوع إلى معرّف عنصر موجود** — صفر بنود مخترعة |
| 4 | **الوحدات متسقة** بلا تحويل ضمني |
| 5 | **صفر معاملات اجتهادية** (هدر، تقريب تجاري، معامل تنفيذ) |
| 6 | **قرار C1 يسمح** بهذا المخرج |
| 7 | **التجميع كامل أو معلن الجزئية** |

**الشرط 7 هو أخطرها عمليًا:** مجموع 8 جدران من أصل 12 (لأن 4 حالتها `[U]`) **ليس «مجموع أطوال الجدران»** — هو مجموع جزئي. تقديمه بلا إعلان = **AS-C4-01**.

---

## 6. UNKNOWN / NOT_PRESENT / UNCOMPUTABLE — التمييز الثلاثي

| الحالة | المعنى | سلوك C4 |
|---|---|---|
| **`[U]` / مفقود** | **لا نعرف** | يُستبعد + **يُعلَن**؛ **والمجموع يصير جزئيًا موسومًا**. ❌ **لا يُعامل صفرًا** |
| **`NOT_PRESENT`** | **نعرف أنه غير موجود** | **حقيقة صالحة**: يساهم بصفر **مشروعًا** في العدّ |
| **UNCOMPUTABLE** | `[D]` تعذّر حسابه (حكم **B7/B6**) | يُستبعد بالحكم **المستهلَك**، بلا إعادة اشتقاق |

**قاعدة حاكمة (شرط إغلاق C3 رقم 6):** **`[U]` ≠ `NOT_PRESENT` ≠ قيمة.** وتحويل الغياب إلى صفر هو **أخطر تزوير كمي ممكن** — لأنه ينتج رقمًا سليم الشكل من معلومة غير موجودة ⇒ **AS-C4-03**.

---

## 7. dimensional / unit safety · rounding

### 7.1 الوحدات
- **لا تحويل ضمني إطلاقًا.** سياسة B6 المعلنة: *«Scale factors serve detection only — nothing is ever converted»*.
- الجبر البُعدي يتبع B6: `mm + mm → mm` · `mm × mm → mm2` · `mm2 ÷ mm → mm`.
- **عدم تطابق الأبعاد ⇒ امتناع**, لا إعادة قياس.
- **العرض بالمتر المربع ممنوع** ما دام `m2` غير موجود في enum ⇒ **DEC-C4-05**.

### 7.2 التقريب
> **C4 لا ينشئ سياسة تقريب/دقة جديدة** — نفس منطق DEC-C3-01.
- المجاميع تُقدَّم **بقيمها الكاملة**.
- إذا فرض العرض تقريبًا قد يغيّر القيمة ⇒ **ABSTAIN + REPORT**.
- **DEC-C2-02 تبقى مفتوحة ولا تُستعمل ذريعة** لسياسة جديدة.
- **التقريب التجاري** (لأعلى لأقرب وحدة شراء) **اجتهاد** ⇒ خارج فئة A.

---

## 8. Ownership Matrix

| المسؤولية | المالك | دور C4 |
|---|---|---|
| صحة `[D]` **المصرَّح** | **B6** | يستهلك، ولا يعيد التحقق |
| وجود `derived_from`/`formula` | **A (F4)** | — |
| قابلية حل المراجع | **B1 (XR-008)** | — |
| طوبولوجيا الجدران/الفتحات | **B2** | **لا يصحح هندسة** |
| الأثاث والخلوص | **B3** | — |
| الحاجب و`[U]` | **B7** | يستهلك |
| **هل يجوز التوليد؟** | **C1** | يستهلك |
| التقنين/البصمات/البيئة | **C2** | يستدعي، **ولا يطلب بصمة هندسية** |
| الهندسة المرسومة | **C3** | **لا يقرأ مخرجات C3** (الشرط 5) |
| **عدّ وقياس عناصر مصرَّحة** | **C4** | ✅ **ملكه** |
| **الحكم بمطابقة الجدول للماستر** | **D** | يقدّم دليلًا فقط |
| `outputs[]` · الحوكمة | **E** | لا يكتب |
| الأسعار والهدر | **لا أحد داخل 00.5** | **يمتنع** |

### منع ازدواج الملكية — ثلاثة حدود صريحة

| مع | الحد |
|---|---|
| **B6** | B6 يحكم على `[D]` **موجودة**؛ C4 يُنتج رقمًا **خارجيًا في جدول**. **إن قُرِّر أن الكمية تُكتب كـ`[D]` في الماستر ⇒ تصير ملك B6 وتخرج من C4** (DEC-C4-01) |
| **D** | C4 **لا يحكم** بصحة الجدول؛ يقدّم الجدول + provenance وD يحكم |
| **E** | C4 **لا يكتب** في الماستر ولا `outputs[]`، ولا ينشئ نظام اعتماد |

---

## 9. Dependency / Data-flow

```
project_master.json  (SSOT — قراءة فقط)
        │
        ├── C1 decision ──────────► يجوز؟ لا ⇒ امتناع
        │
        ▼
   Quantity Assembly (QA4)          ← يقرأ الماستر مباشرة
   · أهلية كل مدخل (§5)
   · provenance لكل بند
   · إعلان المستبعدات
        │
        ▼
   Quantity Computation (QC)
   · Q-01…Q-04 فقط
   · جبر بُعدي (عرف B6)
   · لا تقريب · لا معاملات
        │
        ▼
   Schedule Evidence (SE)  ──────► C6 Manifest ──► D يحكم
   · جدول + مصادر + مستبعدات
   · لا حكم بالصحة

   ✗ C3 outputs ──╳──► C4      (SVG تمثيل مشتق لا مصدر قياس)
   ✗ C4 ──╳──► project_master  (لا كتابة)
```

**سهم مقطوع عمدًا:** C4 **لا يقيس من SVG**. القياس من تمثيل مشتق يجعل خطأ الرسم يتحول إلى «كمية موثقة» — وهو AS-C4-05.

---

## 10. ATTACK SURFACE

| # | الخطر | الضمانة المقترحة |
|---|---|---|
| **AS-C4-01** | **مجموع جزئي يُقدَّم كاملًا** | كل مجموع يحمل `basis: n of N` + قائمة المستبعدين |
| **AS-C4-02** | **خلط المحسوب بالمقدَّر** في جدول واحد | فصل الأنواع الثلاثة؛ Quantity Schedule فقط |
| **AS-C4-03** | **`[U]` يُعامل صفرًا** | تمييز ثلاثي مفروض (§6) |
| **AS-C4-04** | **ترقية حالة** (`[A]` معتمد يدخل الحساب) | أهلية `{C, D}` فقط |
| **AS-C4-05** | **القياس من مخرج C3** | C4 يقرأ الماستر حصرًا |
| **AS-C4-06** | **معامل مخترع** (هدر/تغطية) | لا معاملات؛ غير الموجود ⇒ امتناع |
| **AS-C4-07** | **تحويل وحدة ضمني** | لا تحويل؛ عدم التطابق ⇒ امتناع |
| **AS-C4-08** | **تقريب يغيّر القيمة** | قيم كاملة أو ABSTAIN |
| **AS-C4-09** | **عدّ مزدوج** (عنصر في مجموعتين) | مجموعة معرّفات فريدة + إعلان التداخل |
| **AS-C4-10** | **بند بلا معرّف** | كل بند مرجوع لمعرّف |
| **AS-C4-11** | **ازدواج ملكية مع B6** | DEC-C4-01 قبل التنفيذ |
| **AS-C4-12** | **BOQ يُقدَّم كفئة A** | منصوص: ليس A |
| **AS-C4-13** | **الرقم يُقدَّم كصحة هندسية** | fidelity ملك D |
| **AS-C4-14** | **كمية حجمية مزعومة** | لا `mm3` ⇒ امتناع معلن |
| **AS-C4-15** | **إدخال معيار/سعر من عندي** | محظور نصًا |

---

## 11. CAPABILITY REQUIREMENTS

| المستوى | الحالة |
|---|---|
| **1 — Format** | ✅ الحساب والجداول **بالمكتبة القياسية** — لا تبعية خارجية |
| **2 — Approved output contract** | 🔴 **مفقود** — DEC-C4-02 غير معتمد |
| **3 — Runtime** | ✅ متاح (لا `shapely` ولا `numpy` مطلوبان لـQ-01…Q-04) |

**ملاحظة:** حساب مساحة مضلّع (Q-05) **لا يحتاج `shapely`** (صيغة shoelace رياضية)، لكن **الحاجب قرار نطاق لا قدرة تقنية**. و**GAP-C-02 نشطة**: القدرة تُفحص لحظيًا.

---

## 12. PROPOSED TEST ARCHITECTURE (توصيف فقط)

| المجموعة | ما يجب إثباته |
|---|---|
| **Positive** | ماستر نظيف ⇒ Q-01…Q-04 بقيم صحيحة يدويًا |
| **Negative** | لكل شرط أهلية (§5) حالة استبعاد **بالرمز المقصود** |
| **Triple-distinction** | `[U]` مستبعد ومعلن · `NOT_PRESENT` يساهم بصفر مشروع · قيمة تُحسب — **ثلاث نتائج مختلفة** |
| **Partial-sum** | 8 من 12 ⇒ **الجدول موسوم جزئيًا** ويسمّي المستبعدين |
| **Boundary** | صفر عناصر · عنصر واحد · طول صفري · فتحة أكبر من جدارها (**حكم B2 لا C4**) |
| **Dimensional** | `mm + mm2` ⇒ امتناع · `mm × mm` ⇒ `mm2` · لا تحويل |
| **Mutation** | إدخال `[A]` في الأهلية ⇒ سقوط · معاملة `[U]` صفرًا ⇒ سقوط · إسقاط وسم الجزئية ⇒ سقوط · **+ CONTROL** · **وفق AS-C2-16/17** |
| **Isolation** | لا import لأي validator · **لا قراءة لمخرجات C3** · لا حكم بالصحة |
| **Provenance** | كل بند بمعرّف ومسار؛ **مع نص صريح أن provenance ليس دليل صحة** |
| **Anti-smuggling** | حالة لكل AS-C4-01…15 |
| **No-write** | **سلوكي + فحص مسارات** (`open`/`write`/`json.dump`/`Path.write_*`) — شرط C3 رقم 4 |
| **Capability** | فحص لحظي لا حقيقة مخزَّنة |
| **Regression** | الأساس **761/761** |

---

## 13. OPEN DECISIONS

| # | القرار | لماذا لا أحسمه | حاجب؟ |
|---|---|---|---|
| **DEC-C4-01** ⭐ | **هل الكمية `[D]` في الماستر (ملك B6) أم ناتج خارجي (ملك C4)؟** | يحدد الملكية نفسها؛ حسمه ذاتيًا = ازدواج ملكية | 🔴 **نعم — الأخطر** |
| **DEC-C4-02** | **عقد المخرج**: أي كميات في الإصدار الأول؟ (مقترح Q-01…Q-04) | قرار نطاق تسليم | 🔴 **نعم** |
| **DEC-C4-03** | **مساحة الأرضية**: هل تُحسب من `outline` بـshoelace؟ | اشتقاق هندسي جديد قد يمسّ ملكية B2 | 🔴 نعم لـQ-05 |
| **DEC-C4-04** | **مساحة الجدار**: هل تُخصم الفتحات؟ | **قرار قياس مهني**, لا رياضي | 🔴 نعم لـQ-06 |
| **DEC-C4-05** | وحدة العرض (`mm2` فقط أم يُضاف `m2`؟) | **يمسّ enum المخطط** ⇒ يحتاج قرارًا منفصلًا | 🟡 |
| **DEC-C4-06** | هل يُنفَّذ Material Schedule أصلًا؟ | **GAP-C-08** غير محسومة | 🟡 |
| **GAP-C-08** | **حجية كل نوع جدول** | **C-STEP-01 نصّ: يُحسم قبل C4** | 🔴 **نعم** |
| **GAP-C4-01** ⭐ جديدة | **لا وحدة حجم (`mm3`)** ⇒ الكميات الحجمية غير ممثَّلة | نقص في المخطط، لا في C4 | 🟡 |

**وترثه C4:** DEC-C2-01…08 · DEC-C3-* · GAP-C-01…14 — **بلا حسم**.

---

## 14. GATE CARD

| البند | الحالة |
|---|---|
| Ownership Matrix | ✅ §8 + ثلاثة حدود لمنع الازدواج |
| Data-flow | ✅ §9 — **السهم من C3 مقطوع عمدًا** |
| التمييز الثلاثي للجداول | ✅ §3 — **BOQ ليست A** |
| أنواع الكميات وشروطها | ✅ §4/§5 |
| `[C]` مقابل `[D]` والمشتقات | ✅ §2.1 + §8 |
| منع A/P/U كمصدر مؤكد | ✅ §5 شرط 1 + §6 |
| Unit/dimensional safety | ✅ §7.1 (عرف B6) |
| Rounding | ✅ §7.2 — **لا سياسة جديدة** |
| منع اختراع كميات/أسعار/معايير | ✅ §5 شرط 5 · AS-C4-06/15 |
| Provenance | ✅ §5 شرط 3 + §12 |
| UNKNOWN/NOT_PRESENT/UNCOMPUTABLE | ✅ §6 |
| Attack Surface | ✅ 15 |
| Capability | ✅ §11 — المستوى 2 مفقود |
| Test architecture | ✅ §12 توصيف بلا تنفيذ |
| Open Decisions منفصلة عن القواعد | ✅ §13 مقابل §5–§8 |
| لا كود · لا schema · لا Master | ✅ |

### القرار: **HOLD — بانتظار قراراتك**

---

## 15. التوصية — ما القرار المطلوب منك

**ثلاثة قرارات حاجبة، مرتبة بالأولوية:**

### 🔴 (1) DEC-C4-01 — الأهم على الإطلاق
**هل الكمية المحسوبة:**
- **(أ)** قيمة `[D]` تُصرَّح في الماستر ⇒ **تصير ملك B6** وC4 يصير مجرد مُجمِّع عرض؟ أم
- **(ب)** **ناتج خارجي** في جدول مستقل (مثل C6 Manifest) ⇒ **ملك C4**، والماستر لا يتغير؟

**توصيتي المعمارية (اقتراح لا قرار):** **(ب)** — لأن المخطط **لا يحوي بنية للكميات أصلًا** (مُثبت §2.2)، ولأن الكتابة في الماستر تخالف اتجاه `Master → مشتقات` المعتمد. **لكن القرار قرارك.**

### 🔴 (2) GAP-C-08 — حجية كل نوع جدول
**C-STEP-01 نصّ صراحة: تُحسم قبل C4.** بدونها لا نعرف أي جدول فئة A.

### 🔴 (3) DEC-C4-02 — عقد المخرج الأول
**مقترح:** Q-01 (عدّ) · Q-02 (طول جدار) · Q-03 (مجموع الأطوال) · Q-04 (مساحة فتحة) — وكلها من الماستر مباشرة بلا معاملات.
**ومؤجَّل:** Q-05 (DEC-C4-03) · Q-06 (DEC-C4-04) · Q-07/Q-09 (ليست A) · Q-08 (GAP-C4-01).

**وقرار ثانوي:** DEC-C4-05 (وحدة العرض) — قد يستلزم تعديل enum المخطط، **ولن أقترب منه دون قرار منفصل**.

**لم أكتب كودًا ولم أنشئ ملفات تنفيذية ولم أمسّ Schema/Master. لم أبدأ C5/C6. متوقف بانتظار مراجعتك.**
