# 00.5-C5 — ARCHITECTURE RESOLUTION ADDENDUM
**حسم ثلاث نقاط فقط — لا Implementation · لا Python · لا Schema · لا Master · لا Tests · لا C6/D/E · لا تعديل C1–C4 · لا نطاق جديد.**

> **المرجع:** `00.5-C5-ARCHITECTURE-REPORT.md` (APPROVE WITH CONDITIONS — HOLD).
> **قاعدة حاكمة:** كل ما هنا **عقد معماري**. لا كود، ولا توسيع لـ§5-B، ولا وظيفة جديدة لـC5.

---

## 1. RESOLUTION OF DEC-C5-01 — قناة Geometry Assembly

### 1.1 التمييز المطلوب بين A و B

| | **A — استهلاك GA نفسها المُنتَجة فعلًا** | **B — إعادة استدعاء `assemble()`** |
|---|---|---|
| المصدر | ناتج تشغيل مسار A **الذي جرى فعلًا** | تجميع **جديد ومستقل** |
| العدد | **GA واحدة** تغذّي A وB | **نسختان** من التجميع |
| الارتباط | مرتبطة بالمخرج A بالمعرّف | **غير مرتبطة** — تشغيل منفصل |
| الخطر | — | انحراف صامت لو تغيّر الماستر أو المنطق بين التشغيلين |

**الحكم: (A) هو المعتمد. و(B) مرفوض** — وأسحب صياغتي السابقة «C5 يستدعي `assemble()`» لأنها كانت **وصفًا لـ(B) وهو ما لا نريده**.

**سبب الرفض جوهري:** استدعاء `assemble()` مرة ثانية هو **إعادة تجميع هندسي مستقل**، ولو أعطى النتيجة نفسها اليوم فهو **يخلق مصدرين** — وأي اختلاف مستقبلي يصير انحرافًا غير مكتشَف بين A وB. وهذا يخرق مبدأ **MASTER GEOMETRY → A/B بهندسة واحدة**.

### 1.2 🔎 الاكتشاف الذي يحلّ المشكلة بلا تعديل C3

**قياس مُجرى على `c3_svg_emitter.py` (قراءة فقط):** سجل الإصدار **ينشر GA كاملةً بالفعل** في `record["assembly"]` — أي أن **قناة التسليم موجودة سلفًا ولم أحتج إنشاءها**.

| الفحص | النتيجة |
|---|---|
| GA كاملة الإحداثيات؟ | ✅ `W-01.start = [0,0]` بـ`provenance = walls/0/start` |
| الفتحات محسوبة الموضع؟ | ✅ `OP-01 p0=[1000.0,0.0] p1=[1900.0,0.0]` بأربعة مسارات provenance |
| النطاق والإعلانات؟ | ✅ `tiers = [TIER-0, TIER-1]` + `not_represented` |
| **معزولة عن الماستر؟** | ✅ **عبث المستهلك بها لم يغيّر الماستر ولا إنتاج A لاحقًا** |
| قابلة للتسلسل وثابتة؟ | ✅ JSON-serialisable، وتشغيلان يعطيان **الصورة القانونية نفسها** |

⇒ **`record["assembly"]` هو الـhandoff artifact.** لا تعديل على C3، ولا إعادة تجميع، ولا قناة جديدة.

### 1.3 القرار

> ### **DEC-C5-01 — RESOLVED: الخيار (A)**
> **C5 يستهلك GA artifact المنشور في سجل إصدار مسار A (`record["assembly"]`) كمُدخل، ولا يستدعي `assemble()` ولا يعيد بناء أي هندسة.**

---

## 2. FINAL GA HANDOFF MODEL — **Shared Immutable GA Artifact**

### 2.1 تعريف العقد

> **GA Artifact** = التمثيل الهندسي **المحايد للصيغة** الذي أنتجه مسار A، مُسلَّم كقيمة **ثابتة (immutable) مقروءة فقط**, يحمل معه provenance ونطاقه وإعلاناته، ويغذّي **المسارين A وB معًا**.

```
Master ──► C1 (إذن) ──► Geometry Assembly  ⟵ تُنتَج مرة واحدة
                                │
                    ┌───────────┴───────────┐
                    ▼                       ▼
              A output (C3)         GA Artifact (handoff)
                                            │
                                            ▼
                                    C5 + parameters ──► B output
```

### 2.2 خصائص العقد الملزمة

| # | الخاصية | المعنى |
|---|---|---|
| **H-1** | **مُنتَج مرة واحدة** | GA تُجمَّع في تشغيل مسار A، ولا تُجمَّع ثانية لأجل B |
| **H-2** | **Immutable عند الاستهلاك** | C5 يعامله **قراءة فقط**؛ لا تعديل ولا إثراء ولا حذف |
| **H-3** | **مكتفٍ ذاتيًا** | يحمل الإحداثيات + provenance + النطاق + `not_represented` — فلا يحتاج C5 للرجوع إلى الماستر |
| **H-4** | **معرَّف ومرتبط** | يحمل معرّفًا يربط مخرج B بمخرج A المناظر — **أساس تحقق D** |
| **H-5** | **معزول عن الماستر** | قيمة منفصلة؛ أي عبث به **لا يرتد** على SSOT (مُثبت §1.2) |
| **H-6** | **لا مصدر بديل** | C5 **لا يقبل** هندسة من الماستر مباشرة ولا من ملف SVG ولا من أي مسار آخر |

### 2.3 ما يمنعه هذا النموذج صراحةً
❌ إعادة تجميع مستقل · ❌ نسخة GA ثانية غير مرتبطة · ❌ تعديل C3 · ❌ إعادة حساب إحداثيات داخل C5 · ❌ **القياس من مخرج C3 النهائي** (نفس منع AS-C4-05).

### 2.4 تبعية معلنة
ترتيب التشغيل يصبح: **مسار A يُنتَج أولًا**, ثم يُسلَّم artifactه لـC5. **لا يجوز إنتاج B بلا A مناظر** — وهذا **قيد معماري مقصود**: مخرج عرض بلا أصل دقيق يقابله لا معنى له، ولا يمكن لـD أن يحكم عليه.

---

## 3. FINAL PRESENTATION PARAMETER CONTRACT

> **القاعدة المؤسِّسة: غياب parameter لا يسمح باختراع default صامت.**
> **ولا أوسّع §5-B.** القائمة أدناه **منقولة حرفيًا** منها: *ألوان · سماكة خطوط · ظلال · وسوم · ترتيب اللوحة · مقياس الطباعة*.

### 3.1 حقول العقد لكل معامل

| الحقل | المعنى |
|---|---|
| `name` | المعرّف الصريح |
| `type` | نوع القيمة |
| `value` | القيمة المعلنة |
| `declared_by / source` | **المستدعي أو ملف إعدادات معلن — ولا يُقرأ من الماستر** |
| `eligibility` | نتيجة اختبار §5-B |
| `allowed / rejected` | الحكم |
| `geometry_impact_test` | *لو غُيِّر، هل تتغير الهندسة المستخرَجة؟* |
| `optional / required` | الإلزامية |
| `default_policy` | **دائمًا `NO_SILENT_DEFAULT`** |
| `rejection_behavior` | ما يحدث عند الرفض |

### 3.2 المعاملات الستة المعتمدة في §5-B

| # | `name` | `type` | المصدر | geometry-impact | الحكم | إلزامية | default | عند الرفض |
|---|---|---|---|---|---|---|---|---|
| **P-01** | `color` | لون | المستدعي | ❌ لا يغيّر إحداثيًا | ✅ allowed | optional | **NO_SILENT_DEFAULT** | يُرفض المعامل ويُعلَن؛ العنصر يُرسم **بلا لون مُطبَّق** |
| **P-02** | `line_weight` | عدد | المستدعي | ❌ سماكة عرض لا هندسة | ✅ allowed | optional | **NO_SILENT_DEFAULT** | كما أعلاه |
| **P-03** | `shadow` | منطقي/إعداد | المستدعي | ❌ | ✅ allowed | optional | **NO_SILENT_DEFAULT** | كما أعلاه |
| **P-04** | `label` | نص/وسم | المستدعي | ❌ **بشرط** ألا يُشتق منه رقم هندسي | ✅ allowed **مشروط** | optional | **NO_SILENT_DEFAULT** | يُرفض ويُعلَن |
| **P-05** | `sheet_order` | ترتيب | المستدعي | ❌ ترتيب عرض | ✅ allowed | optional | **NO_SILENT_DEFAULT** | يُرفض ويُعلَن |
| **P-06** | `print_scale` | إطار/حاوية | المستدعي | ⚠️ **مشروط — §4** | ✅ **بحدود §4 فقط** | optional | **NO_SILENT_DEFAULT** | يُرفض ويُعلَن |

**تحفّظ على P-04:** الوسم مؤهل ما دام **نصًا تقديميًا**. أما وسم يحمل **بُعدًا أو رقمًا محسوبًا** فهو **إنتاج معلومة هندسية** (خطر Tier 3 نفسه الذي استُبعد في C3) ⇒ **يُرفض**.

### 3.3 القواعد الملزمة

| # | القاعدة |
|---|---|
| **PP-1** | **غياب معامل ⇒ لا يُطبَّق شيء مكانه.** لا لون افتراضي ولا سماكة افتراضية |
| **PP-2** | **أي معامل خارج القائمة الستة ⇒ مرفوض/Deferred** حتى اعتماد صريح. لا اجتهاد |
| **PP-3** | **معامل غير معروف ⇒ رفض معلن**, لا تجاهل صامت |
| **PP-4** | **الرفض لا يُسقط المخرج بالضرورة**: يُعلَن ويستمر، إلا إن كان جوهريًا للمخرج المطلوب ⇒ **امتناع** |
| **PP-5** | **كل معامل مطبَّق أو مرفوض يُسجَّل** في سجل المخرج بسببه |
| **PP-6** | **لا يُقرأ معامل من الماستر ولا يُكتب فيه** (§5-B: ليست حقائق) |
| **PP-7** | اجتياز اختبار §5-B **شرط لازم لا كافٍ**: القائمة المعتمدة هي الحد الأعلى |

**ملاحظة تنفيذية معلنة:** C3 اليوم يضع قيم ستايل **مثبَّتة في كوده** (`stroke="#000"` · `stroke-width="2"`). هذه **قيم مسار A وليست defaults لـC5**، و**C5 لا يرثها ولا يعتبرها أساسًا**. علاقة هذه القيم بـPP-1 مسألة **مسار A** ولا تُفتح هنا (C3 مغلق) ⇒ تُسجَّل ملاحظة، لا تغييرًا.

---

## 4. RESOLUTION OF DEC-C5-02 — Print Scale Boundary

> ### **DEC-C5-02 — RESOLVED**

### مسموح ✅ — ولا يمسّ GA

| البند | الشرط |
|---|---|
| **page / container size** | حجم الورقة أو الحاوية |
| **viewport** | نافذة العرض/الإطار |
| **presentation framing** | التأطير التقديمي |

**الشرط الحاكم الوحيد:** **ألا يتغير Geometry Assembly** — لا قيمة، ولا وحدة، ولا علاقة.

### ممنوع ❌ — قطعيًا

**تغيير coordinates · scaling للهندسة نفسها · تغيير units · تغيير dimensions · تغيير positions · تغيير topology.**

### الحد الفاصل

> **يُسمح بتغيير ما يُنظر من خلاله. ويُمنع تغيير ما يُنظر إليه.**

**قاعدة منع التحويل المقنّع (SC-1):** `print_scale` **ليس مدخلًا عامًا**. أي استعمال له يؤدي — مباشرة أو بالوسائط — إلى **ضرب الإحداثيات** أو تبديل الوحدة هو **تحويل هندسي مقنّع** ⇒ **محظور ويُرفض المعامل**.

**اختبار القبول (من §5-B):** *لو استُخرجت الهندسة من مخرج B، هل تختلف عن GA؟* **نعم ⇒ المعامل مرفوض.**

**تذكير حدودي:** C5 **يطبّق** هذا الحد، **ولا يشهد** أنه التزمه — **التحقق ملك D**.

---

## 5. UPDATED OWNERSHIP BOUNDARY

**لا تغيير جوهري.** ثلاث إضافات دقيقة:

| الجهة | الإضافة |
|---|---|
| **C3** | يملك **إنتاج GA Artifact** كأثر جانبي طبيعي لمسار A. **C5 مستهلك فقط** — لا يطلب تعديلًا ولا يُنتج GA |
| **C5** | يملك **قبول/رفض المعاملات** و**ربطها بالعرض** فقط. **لا يملك** إنتاج GA ولا التحقق من سلامتها |
| **D** | يملك — إضافة لما سبق — **الحكم بأن مخرج B لم ينحرف عن GA**، مستعينًا بمعرّف الارتباط H-4 |

**Truth Boundary بلا تغيير:** C5 يسجّل **مصدر الهندسة** و**المعاملات المطبَّقة**، ولا يثبت **«B لم تنحرف هندسيًا عن A»**. **Generator ≠ Independent Verifier.**

---

## 6. UPDATED ATTACK SURFACE — ثلاثة أخطار جديدة

AS-C5-01…19 سارية، ويُضاف:

| # | الخطر | الحدّ المانع |
|---|---|---|
| **AS-C5-20** ⭐ | **GA Re-assembly Drift** — إعادة تجميع مستقل تُنتج مصدرًا ثانيًا قد ينحرف صامتًا | **H-1** + رفض الخيار (B) — §1 |
| **AS-C5-21** ⭐ | **Artifact Mutation** — C5 يعدّل GA المسلَّمة (إثراء/حذف) فيُنتج B عن هندسة غير التي أنتجت A | **H-2** immutability + H-6 |
| **AS-C5-22** ⭐ | **Disguised Scale Transform** — `print_scale` يُستعمل لضرب الإحداثيات | **SC-1** + اختبار §5-B — §4 |

**ولم يُلغَ أي خطر سابق.** AS-C5-05 (default injection) يزداد تشددًا بـ**PP-1**.

---

## 7. UPDATED GATE CARD

### ما حُسم في هذا الملحق ✅

| # | البند |
|---|---|
| **DEC-C5-01** | **RESOLVED — الخيار (A)**: استهلاك GA المنشورة فعلًا، **بلا تعديل C3 وبلا إعادة تجميع** |
| **GA Handoff Model** | **Shared Immutable GA Artifact** بستة قيود H-1…H-6 |
| **GAP-C-09** | **عقد المعاملات مصاغ**: ستة معاملات بعشرة حقول + PP-1…PP-7 · **بلا توسيع §5-B** |
| **DEC-C5-02** | **RESOLVED**: إطار العرض مسموح · **التحويل الهندسي محظور** · SC-1 |
| Attack Surface | +3 (AS-C5-20/21/22) |
| Truth Boundary | **بلا تغيير** — D يتحقق |

### 🔴 Blockers المتبقية

| # | البند | لماذا |
|---|---|---|
| **GAP-C-09** | **يحتاج اعتمادكم الصريح** | صغتُ العقد ولم أعتمده. **الصياغة ليست اعتمادًا** |
| **DEC-C5-05** | صيغة أول مخرج B (مقترح SVG) | قرار نطاق |
| **DEC-C5-06** | عقد المحتوى (مقترح وراثة Tier 0+1) | قرار نطاق |

### Open Decisions
DEC-C5-03 (مصدر المعاملات — الماستر **مرفوض**) · DEC-C5-04 (Boards — **مؤجَّل**) · DEC-C5-05 · DEC-C5-06 · DEC-C5-07 (PARTIAL ⇒ نعم موسومًا) · DEC-C5-08 (الدليل لـC5 والحكم لـD).

### Deferred
كما في §19 من التقرير الأصلي، بلا تغيير: standards · engineering/aesthetic judgment · visual fidelity (D) · governance (E) · مكتبات خارجية · PDF/PPTX · Boards · فئة C · أي معامل خارج الستة.

### Dependencies
**GA Artifact من مسار A (تبعية ترتيب: A قبل B)** · C1 · C2 (دليل) · GAP-C-09 · GAP-C-02 (قدرة لحظية) · C6/D/E (تسليم لا انتظار).

### Non-Goals
بلا تغيير: لا هندسة · لا حكم fidelity · لا فئة A أو C · لا Manifest · لا `outputs[]` · لا اعتماد/هوية · لا بصمة · لا D1/D2.

### C5 Architecture Status
## 🟡 **APPROVE WITH CONDITIONS — ما زال HOLD**

**تحسّن جوهري:** الحاجبان التقنيان **ارتفعا** — DEC-C5-01 حُسم بلا تعديل C3 (والحل كان **موجودًا** في المعمارية القائمة)، وDEC-C5-02 حُسم بحد قاطع.

### Implementation Readiness
## ❌ **غير جاهزة — حاجب واحد حوكمي**

**السبب الوحيد المتبقي: GAP-C-09 غير معتمد.** صغتُ عقد المعاملات كما طُلب، **لكن الصياغة ليست اعتمادًا**، ونصّ C-STEP-01 أنه **يُحسم قبل C5**. وبدونه يبقى **المستوى 2 من Capability (عقد مخرج معتمد) مفقودًا** — نفس الحاجب الذي أوقف C3 حتى اعتمدتم Tier 0+1، وC4 حتى اعتمدتم Q-01…Q-04.

**ما يرفع الحجب:** اعتماد صريح لـ**GAP-C-09** (المعاملات الستة) + تثبيت **DEC-C5-05** و**DEC-C5-06**.

**توقفت. لم أكتب كودًا ولا اختبارات، ولم أمسّ Schema/Master/validators/C1–C4، ولم أفتح C6/D/E ولا أي نطاق جديد.**
