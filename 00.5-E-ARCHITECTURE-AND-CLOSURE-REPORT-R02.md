# 00.5-E — GOVERNANCE ARCHITECTURE — **REVISION R02**
**تحديث معماري فقط — لا كود · لا Schema · لا Master · لا تعديل C1–C6/D · لا اختبارات تنفيذية · لا F.**

> **R01 = CONDITIONAL HOLD.** هذه المراجعة تطبّق التصحيحات السبعة ولا تعيد بناء المعمارية.
> **Regression baseline:** 1109/1109 (لم يُمسّ شيء).

---

## 0. سجل التعديلات R01 ⇒ R02

| # | التصحيح | ما تغيّر |
|---|---|---|
| **1** | CONFLICT-E-01 | يبقى **OPEN** + **قاعدة Future-Channel Readiness** (§2) |
| **2** | **DEC-E-01** | **RESOLVED**: المعمارية تغطي **الآليات الأربع**؛ التنفيذ وحده يُجزَّأ (§3) |
| **3** | **DEC-E-06** | **RESOLVED**: السجل **خارجي** + **عقد الارتباط الرسمي** (§4) |
| **4** | **DEC-E-07** | **RESOLVED**: **E يصدر `output_id`** + سلسلة الأدوار (§5) |
| **5** | **Decision Lifecycle** | **صُحِّح**: اعتماد Proposal **لا يُنشئ** Decision — فعل تسجيل صريح (§6) |
| **6** | **DEC-E-08** | **RESOLVED**: D **ليس شرطًا** للاعتماد + **حالة مزدوجة** Approval × Fidelity (§7) |
| **7** | **Output State Machine** | روجعت بستة قيود صريحة (§8) |

---

## 1. قياس جديد في R02

| # | القياس | النتيجة |
|---|---|---|
| **M-7** | أنماط المعرّفات | **`output_id` له نمط معتمد سلفًا: `^OUT-[0-9]{3}$`** · `DEC-###` · `P-###` · `CR-###` · `OBJ-###` |
| **M-8** | ما يتيح ربط سجل خارجي | `project_id` + `revision` + **`master_hash`** + معرّفات الكيانات |

**أثر M-7:** **E يصدر الهوية داخل عرف قائم** — لا يخترع نمطًا جديدًا.
**أثر M-8:** الربط ممكن مرجعيًا، **لكن `master_hash` مُصرَّح لا محسوب** (C2 محجوبة) ⇒ **ربط مرجعي لا إثباتي** (§4).

---

## 2. CONFLICT-E-01 — يبقى OPEN + جاهزية القناة المستقبلية

**المُقر:** `approved_by = "USER"` **يمنع free-text impersonation**, **ولا يثبت أن الاعتماد حدث فعليًا**.

| البند | الحالة |
|---|---|
| **GAP-02** | 🔴 **OPEN** |
| ادعاء أن `USER` يثبت الهوية الفعلية | ❌ **ممنوع** |
| إضافة signature/credential/token الآن | ❌ **لا** |
| تعديل Schema الآن | ❌ **لا** |

### 🆕 قاعدة **FCR-E-01 — Future-Channel Readiness** (ملزمة)

> **يجب أن يكون النظام قابلًا لإضافة قناة اعتماد خارجية مستقبلًا دون إعادة بناء E بالكامل.**

**الشروط المعمارية الثلاثة:**

| # | الشرط | لماذا |
|---|---|---|
| **FCR-1** | **سلطة الاعتماد تُقرأ عبر نقطة واحدة** (Authorization Gate) ولا تُستنتج في مواضع متفرقة | إضافة قناة = استبدال مصدر واحد |
| **FCR-2** | **حالة الاعتماد تُخزَّن كمرجع لا كاستنتاج** — لا حقل يشتق «معتمد» من غيره | القناة الجديدة تملأ المرجع بلا مسّ المستهلكين |
| **FCR-3** | **قوة الإثبات حقل معلن** (`approval_assurance`) قيمته اليوم **`DECLARED_ONLY`** | إضافة القناة ترفعه دون تغيير البنية |

**تصريح صادق:** FCR **لا يجعل النظام آمنًا اليوم** — يجعله **قابلًا للتأمين لاحقًا بلا هدم**.

---

## 3. DEC-E-01 — **RESOLVED** (معدَّل)

> **معمارية E V1 تغطي الآليات الأربع مجتمعة. Architecture كاملة ≠ Implementation واحدة.**

| الآلية | المسؤولية | يجب أن تُصمَّم الآن؟ |
|---|---|---|
| **G-1 Identity Registry** | تعريف الكيانات ومعرّفاتها | ✅ |
| **G-2 Authorization Gate** | سلطة الاعتماد ونقطته الوحيدة | ✅ |
| **G-3 Lifecycle Machine** | الانتقالات المسموحة والمحظورة | ✅ |
| **G-4 Ownership Registry** | `outputs[]` والملكية والـsupersession | ✅ |

**سبب الاعتماد (كما وجّهتم):** تنفيذ G-1/G-4 أولًا **دون تصميم G-2/G-3** يعني اكتشاف لاحق أن **سلطة الاعتماد ودورة الحياة تفرضان إعادة بناء الملكية والعلاقات**.

**التبعيات المتبادلة التي تُحتّم التصميم المشترك:**

| التبعية | الأثر لو صُمِّمت منفردة |
|---|---|
| G-4 يحتاج **حالة اعتماد** لكل مخرج | إعادة بناء سجل الملكية |
| G-3 يحتاج **من يملك سلطة الانتقال** (G-2) | انتقالات بلا سلطة = تغييرات ضمنية |
| G-2 يحتاج **هوية الكيان** (G-1) | اعتماد على كيان بلا هوية |
| G-1 يحتاج **معرفة أي الحقول ثابتة** (G-4) | هوية قابلة للتبديل |

**التجزئة المسموحة:** **تنفيذية فقط** — على أن يبقى **عقد الآليات الأربع مغلقًا** قبل أول سطر كود.

---

## 4. DEC-E-06 — **RESOLVED**: سجل حوكمة خارجي + عقد ارتباط

> **Governance Record = EXTERNAL.** ولا يُضاف إلى الماستر.

### عقد الارتباط الرسمي (إلزامي)

كل سجل حوكمة خارجي يحمل:

| الحقل | المصدر | الغرض |
|---|---|---|
| `project_id` | `meta.project_id` | ربط المشروع |
| `master_revision` | `meta.revision` | **ربط النسخة** |
| `master_hash_declared` | `meta.master_hash` | ربط اللقطة — **مُصرَّح لا محسوب** |
| `geometry_version` | `meta.geometry_version` | ربط الهندسة |
| `entity_refs[]` | `DEC-###` · `P-###` · `CR-###` · `OUT-###` · `OBJ-###` | ربط الكيانات |
| `binding_assurance` | **`REFERENTIAL_ONLY`** | **قوة الارتباط المعلنة** |

### ثلاثة تصريحات صادقة

| # | التصريح |
|---|---|
| 1 | **السجل الخارجي ليس tamper-proof** — ولا يُدّعى ذلك |
| 2 | **`master_hash` مُصرَّح لا محسوب** (C2 محجوبة) ⇒ الارتباط **مرجعي لا إثباتي** |
| 3 | **لا آلية تشفيرية الآن** — والبنية **تسمح بإضافتها** دون تغيير العقد (FCR-E-01) |

**ما يحققه العقد فعلًا:** يجعل **انفصال السجل عن نسخته قابلًا للكشف** عند تغيّر `revision`/`master_hash` المُصرَّحين. **ولا يمنع التزوير المتعمد.**

---

## 5. DEC-E-07 — **RESOLVED**: E يملك ويصدر `output_id`

> **E owns and issues `output_id`. المنتِج لا يملك سلطة إصدار الهوية الرسمية.**

### سلسلة الأدوار — بلا تداخل

```
Producer (C3/C4/C5) ──► artifact + evidence        [لا هوية رسمية]
C6                  ──► Evidence / Manifest assembly   [لا هوية رسمية]
D                   ──► Fidelity judgment              [لا هوية رسمية]
E                   ──► يمنح output_id ويسجّل في outputs[]   ✅
```

**النمط المعتمد سلفًا (M-7):** **`^OUT-[0-9]{3}$`** — فـE **يصدر داخل عرف قائم** لا نمطًا مخترعًا.

### تصحيح تفسير GAP-C6-02

> **GAP-C6-02 لا يعني أن C6 سيصبح مالكًا لهوية المخرج.**
> يعني فقط أن **C6 لا يجد هوية ليحملها** ⇒ يسجّل `NOT_SET`. **والملكية كانت ولا تزال لـE.**

**قاعدة:** أي طبقة تولّد `output_id` من عندها = **انتحال سلطة هوية** (AS-E-12).

---

## 6. تصحيح Decision Lifecycle — **اعتماد Proposal ≠ إنشاء Decision**

**المُلغى:** `Proposal APPROVED → DECISION_RECORDED` كنتيجة ضمنية.

### النموذج المصحَّح — ثلاث خطوات منفصلة

```
[1] Proposal.state = APPROVED
        │   ← سلطة المستخدم على المقترح فقط
        │   ✗ لا يُنشئ قرارًا
        ▼
[2] Decision Recording Action           ← فعل صريح ومنفصل
        │   بسلطة المستخدم
        ▼
[3] Decision exists with explicit references
        decision_id · linked_proposal · rationale ·
        alternatives_considered · approved_by · approved_on ·
        approved_in_revision          [ثمانية حقول إلزامية — M-5]
```

| القاعدة | النص |
|---|---|
| **DL-1** | **اعتماد المقترح لا يُنشئ قرارًا** — ولا يُستنتج وجوده |
| **DL-2** | **Decision كيان قائم بذاته** قابل للتتبع والتحقق من علاقاته |
| **DL-3** | مقترح `APPROVED` **بلا قرار مسجَّل** حالة **مشروعة** — لا نقص |
| **DL-4** | قرار بلا `linked_proposal` صالح ⇒ **مرفوض** |
| **DL-5** | **فعل التسجيل يحتاج سلطة صريحة** (G-2) — لا تلقائية |

**لماذا هذا مهم:** الاشتقاق الضمني يعني أن **الموافقة على فكرة** تصير **قرارًا ملزمًا** بلا فعل بشري ثانٍ — وهو **اعتماد ضمني** محظور.

---

## 7. DEC-E-08 — **RESOLVED**: D ليس شرطًا للاعتماد + الحالة المزدوجة

> **Evidence ≠ Authority.** فلا `D VERIFIED` شرطًا مسبقًا لـ`APPROVED`.

### الحالة المزدوجة (Dual-State) — منع الالتباس

**كل مخرج يحمل حالتين مستقلتين تُقرآن معًا ولا تُشتق إحداهما من الأخرى:**

```
output_state = { approval_state , fidelity_state }

approval_state ∈ REGISTERED · EVIDENCE_LINKED · ACCEPTED · APPROVED · SUPERSEDED · WITHDRAWN   [ملك E]
fidelity_state ∈ VERIFIED · PARTIAL · NOT_VERIFIABLE · FAILED · ABSTAIN · NOT_ASSESSED          [ملك D]
```

### التوليفات المشروعة — كلها صالحة

| التوليفة | المعنى |
|---|---|
| `APPROVED + VERIFIED` | معتمد ومطابقته مُثبتة ضمن نطاقها |
| **`APPROVED + PARTIAL`** | **معتمد رغم أن جزءًا غير قابل للتحقق** — مشروع |
| **`APPROVED + NOT_VERIFIABLE`** | **معتمد بلا تحقق ممكن** — مشروع وموثَّق |
| `APPROVED + FAILED` | ⚠️ **مشروع لكن يجب إبرازه** — اعتماد رغم عدم مطابقة مُثبتة |
| `ACCEPTED + VERIFIED` | مطابق لكن **غير معتمد بعد** |
| `REGISTERED + NOT_ASSESSED` | مسجَّل فقط |

### أربع قواعد قاطعة

| # | القاعدة |
|---|---|
| **DS-1** | **لا ترقية تلقائية**: `VERIFIED` لا يرفع `approval_state` أبدًا |
| **DS-2** | **لا اشتقاق عكسي**: `APPROVED` لا يغيّر `fidelity_state` |
| **DS-3** | **قراءة الحالة تُظهر الحقلين معًا** — وعرض أحدهما منفردًا **مضلِّل** |
| **DS-4** | **`APPROVED + FAILED` يُعرض بتنبيه صريح** — لا يُخفى ولا يُمنع |

**DS-4 مقصود:** منعه يعني أن **D يملك فيتو على سلطة المستخدم** — وهذا نقل ملكية محظور. الصواب: **اعتماد مكشوف** لا اعتماد ممنوع.

---

## 8. مراجعة Output State Machine

```
REGISTERED ──► EVIDENCE_LINKED ──► ACCEPTED ──► APPROVED ──► SUPERSEDED
     │                │                │            │
     └──► WITHDRAWN   └──► WITHDRAWN   └──► WITHDRAWN└──► WITHDRAWN
```

### القيود الستة — مُراجَعة كما طُلب

| # | القيد | الحالة |
|---|---|---|
| 1 | **`EVIDENCE_LINKED` لا تعني صحة الدليل** | ✅ تعني **وجود ربط** فقط |
| 2 | **`ACCEPTED` لا تعني `VERIFIED`** | ✅ حقلان مستقلان (§7) |
| 3 | **`APPROVED` لا تعني `VERIFIED`** | ✅ DS-1/DS-2 |
| 4 | **`SUPERSEDED` لا يُحيا** | ✅ **انتقال محظور قطعيًا** |
| 5 | **فئة C لا تُرقّى إلى A/B** | ✅ `class` **immutable** (§9) |
| 6 | **لا انتقالات ضمنية** | ✅ **كل انتقال فعل صريح** |

### الانتقالات التي تحتاج **قرار مستخدم صريح**

| الانتقال | السلطة |
|---|---|
| `EVIDENCE_LINKED → ACCEPTED` | **المستخدم** |
| `ACCEPTED → APPROVED` | **المستخدم حصرًا** |
| `أي حالة → WITHDRAWN` | **المستخدم** |
| `APPROVED → SUPERSEDED` | **المستخدم** (عبر مخرج بديل) |

### الانتقالات الآلية المسموحة

| الانتقال | الفاعل |
|---|---|
| `(إصدار) → REGISTERED` | E عند منح الهوية |
| `REGISTERED → EVIDENCE_LINKED` | E عند اكتمال مراجع الأدلة — **ربط لا حكم** |

**كل ما عدا ذلك محظور** — وليس «غير معرَّف».

---

## 9. OUTPUT OWNERSHIP CONTRACT — محدَّث

| الحقل | المالك | Immutable؟ | ملاحظة |
|---|---|---|---|
| **`output_id`** | **E يصدره** | ✅ | نمط `^OUT-[0-9]{3}$` (M-7) |
| `file` · `view_type` | المنتِج | ✅ | يُنقل |
| **`class`** | المنتِج | ✅ **immutable** | **لا ترقية فئة** |
| `guarantee` · `binding_geometry` · `binding_visual` | مشتق من الفئة | ✅ | C غير ملزم أبدًا |
| `generated_on` | المنتِج | ✅ | 🔴 لا طبقة تُنتجه (GAP-C6-04) |
| `fingerprint` | **C2** | ✅ | 🔴 `NOT_ISSUED` — **E لا يخترعه** |
| `derived_from_output` | **E** | ✅ | parent رسمي |
| **`approval_state`** | **E** | ❌ متغير بحوكمة | §8 |
| **`fidelity_state`** | **D** | ❌ يُحدَّث بتقرير D | **E ينقل ولا يحكم** |
| **`approval_assurance`** | **E** | ❌ | **`DECLARED_ONLY` اليوم** (FCR-3) |
| `supersedes_A` | **E** | ❌ | supersession رسمية |

**قاعدة:** تغيير حقل ثابت ⇒ **مخرج جديد بهوية جديدة**.

---

## 10. DECISION REGISTER — محدَّث

| # | القرار | الحالة | الخلاصة |
|---|---|---|---|
| **DEC-E-01** | نطاق معمارية V1 | ✅ **RESOLVED** | **الآليات الأربع مجتمعة** · التجزئة تنفيذية فقط |
| **DEC-E-02** | Approval كيان أم سمات | 🟡 **OPEN** | سمات (الوضع القائم) — كيان مستقل يحتاج Schema |
| **DEC-E-03** | enum دورة حياة المخرج | 🟡 **OPEN** | الست المقترحة **كعقد خارجي** بلا تعديل Schema |
| **DEC-E-04** | LOCKED على مستوى الحقل | 🟡 **OPEN** | مؤجَّل |
| **DEC-E-05** | أثر `track=LITE` | 🟡 **OPEN** | عمق توثيق لا تخفيف إلزام |
| **DEC-E-06** | موضع سجل الحوكمة | ✅ **RESOLVED** | **خارجي** + عقد ارتباط + `REFERENTIAL_ONLY` |
| **DEC-E-07** | مُصدِر `output_id` | ✅ **RESOLVED** | **E** — وGAP-C6-02 لا ينقل الملكية لـC6 |
| **DEC-E-08** | D شرط للاعتماد؟ | ✅ **RESOLVED** | **لا** — حالة مزدوجة + DS-1…DS-4 |
| **CONFLICT-E-01** | صدق الاعتماد | 🔴 **OPEN** | + **FCR-E-01** |

---

## 11. OPEN GAPS

| # | الحالة |
|---|---|
| **CONFLICT-E-01** | 🔴 **OPEN** |
| **GAP-02** صدق الاعتماد | 🔴 **OPEN** |
| **GAP-01** صدق المصدر | 🔴 **OPEN** |
| **GAP-D-01** | 🔴 **OPEN / DEFERRED** — **لم يُفتح في E**؛ ولا يستنتج E `host_wall`/`offset`/`thickness`/`units`/`provenance`. **dependency فقط** |
| **GAP-E-01** لا آلية هوية في المخطط | 🔴 OPEN |
| **GAP-C6-02** هوية المخرج | 🟢 **مُعالَج ملكيًا بـDEC-E-07** — التنفيذ مؤجَّل |
| GAP-C6-03/04/05 · GAP-C-02/04/06/08/14 · DEC-C2-01…08 | 🔴 كما هي |

**لا تعارض جديد ظهر في R02.**

---

## 12. UPDATED GATE CARD — 00.5-E Architecture R02

| معيار القبول | الحالة |
|---|---|
| وضوح الملكية | ✅ E يستهلك B10/D/C6 ولا يعيد تنفيذها |
| عدم تضارب المسؤوليات | ✅ C3/C6/D/E محفوظة |
| قابلية التحقق | ✅ 9/11 من الانتحال قابلة للفرض · واثنتان معلنتان |
| **لا free-text كسلطة** | ✅ `const "USER"` (مقاس) |
| **لا implicit approval** | ✅ **DL-1** + **DS-1** |
| lifecycle واضح | ✅ خمس آلات + انتقالات محظورة + **سلطة كل انتقال** |
| Evidence ≠ Governance | ✅ §7 — حالة مزدوجة |
| لا تسريب مسؤوليات | ✅ **GAP-C6-02 لا ينقل الهوية لـC6** |
| **عدم فتح GAP-D-01** | ✅ dependency فقط |
| قابلية التحويل لاختبارات | ✅ Test Contract (R01 §12) ساري + حالات الحالة المزدوجة |
| **التصحيحات السبعة** | ✅ كلها مطبَّقة |
| كود / تعديلات | ✅ **صفر** — 1109/1109 سليم |

### الحالة

# 🟡 **E Architecture = WAITING FOR SUPERVISOR REVIEW**

**ما حُسم في R02:** DEC-E-01 · DEC-E-06 · DEC-E-07 · DEC-E-08 + تصحيح دورة حياة القرار + مراجعة آلة حالة المخرج + **FCR-E-01**.

**ما يبقى مفتوحًا:** **CONFLICT-E-01** · **GAP-02** · **GAP-01** · **GAP-D-01 (DEFERRED)** · DEC-E-02/03/04/05.

**تصريح ختامي:** R02 **لا يجعل الاعتماد قابلًا للإثبات** — يجعل النظام **قابلًا لإضافة قناة إثبات لاحقًا بلا هدم**، ويمنع **الاعتماد الضمني** و**ترقية الدليل إلى سلطة**. وهذان ما يمكن تحقيقه اليوم بصدق.

**لم أكتب كودًا ولم أعدّل أي ملف قائم ولم أبدأ Implementation ولا F.**
