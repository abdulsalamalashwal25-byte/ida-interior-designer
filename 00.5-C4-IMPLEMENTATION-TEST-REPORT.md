# 00.5-C4 — IMPLEMENTATION & TEST REPORT
**المرجع:** C4 Architecture + **Decision Resolution Addendum (CLOSED)** · OWN-C4-01 · A-Q1…A-Q8
**النطاق المنفَّذ:** Q-01 · Q-02 · Q-03 · Q-04 **فقط**
**النتيجة:** `C4 = 82/82` · **Base Regression = 761/761 PASS** · **الإجمالي 843/843 · REGRESSIONS: NONE**

---

## 1. الملفات ومسؤولياتها

| الملف | الحالة | المسؤولية |
|---|---|---|
| `scripts/c4_quantities.py` | **جديد** | بوابة الأهلية · Q-01…Q-04 · عقد الجزئية · تجميع الجدول الخارجي |
| `tests/test_00_5_C4.py` | **جديد** | 82 اختبارًا |
| `tests/run_all.py` | **معدَّل** | سطر واحد لتسجيل C4 |

**لم يُمسّ أي ملف محظور** — md5 بعد التنفيذ:

| الملف | md5 |
|---|---|
| `project_master.schema.json` | `1b911e017ffce17a3b5da2da6c5a1bdb` |
| `project_master.template.json` | `a6b13613b719f54fbc1a07895c8a124b` |
| `validate_formulas.py` (B6) | `c202ca84535bb0d5a6444ee9990a8096` |
| `schema_gate.py` (A) | `57552bfb22038b64c6cbba5a1c599555` |
| `c1_preconditions.py` | `b63fff2f0e327539b6521e62bbc6c7d8` |
| `c2_fingerprint.py` · `c2_environment.py` | `983ecf52…` · `d129d19a…` |
| `c3_geometry_assembly.py` · `c3_svg_emitter.py` | `729f3528…` · `da2fb83f…` |
| مجموعات C1/C2/C3 | `be24fb02…` · `65f4c92a…` · `f7998c59…` |

**لا C5 · لا C6 · لا Q-05/Q-06 · لا Material Schedule · لا BOQ · لا تسعير · لا `m2`/`mm3`.**

---

## 2. Q-01…Q-04 — حالة التنفيذ

| # | الكمية | الحالة | العملية | الوحدة | تحقق عددي |
|---|---|---|---|---|---|
| **Q-01** | Count | ✅ | عدّ معرّفات **فريدة** | `count` | walls=3 · openings=1 · materials=2 |
| **Q-02** | Individual Wall Length | ✅ | `sqrt(dx²+dy²)` | `mm` | مثلث 3-4-5 ⇒ **4000 · 3000 · 5000** |
| **Q-03** | Total Wall Length | ✅ | مجموع المؤهلة | `mm` | **12000**, `basis = 3 of 3` |
| **Q-04** | Opening Area | ✅ | `width × height` | `mm2` | 900×2100 = **1890000** |

**قيود النطاق المنفَّذة حرفيًا:** Q-02 **خط محوري فقط** — نصّ العملية يعلن `excludes thickness and joints` (C4-PV-06) · Q-03 **لا يصدر بلا `basis`** · Q-04 **`height` غير مؤهل ⇒ استبعاد لا افتراض** (C4-N-08) · Q-01 **لا يعدّ `outputs[]`**.

---

## 3. Eligibility — منفَّذة بالرموز الستة المعتمدة

| الرمز | الحالة | الاختبار |
|---|---|---|
| `EX-MISSING` | الحقل غائب | C4-N-01 |
| `EX-NOT-FACT` | عقدة بلا `status` | C4-N-02 |
| `EX-STATUS` | **`[A]` معتمد · `[P]` · `[U]`** | C4-N-03 |
| `EX-UNCOMPUTABLE` | `[D]` بلا حكم VERIFIED | C4-N-04 |
| `EX-SOURCE` | مصدر غير موثوق | C4-N-05 |
| `EX-UNIT` | وحدة خاطئة **أو غير معلنة** | C4-N-06/07 |

**كل استبعاد يحمل الحقول الأربعة** (code · element_id · path · reason) — مُثبت بـ**C4-N-09**. **الاستبعاد الصامت غير ممكن بنيويًا.**

---

## 4. Provenance — منفَّذة

كل بند يحمل الستة: `element_id` · `source_paths[]` · `inputs[]` · `operation` · `unit` · `basis` (C4-PV-01). المسارات حقيقية (`walls/0/start`, `walls/0/end` — C4-PV-02)، وQ-03 يحتفظ بمصادر **كل** مدخل جُمِع (C4-PV-03).

**والجدول يحمل تحفّظه معه:** *«Provenance يثبت أن القيمة جاءت من الماستر. لا يثبت أنها صحيحة ولا أن الحقل المصدر صحيح»* (C4-PV-05).

---

## 5. Partial Schedule — سلوك مُثبت بمثال حقيقي

ثلاثة جدران، أحدها `[U]`:

```
overall      : PARTIAL
Q-03 value   : 7000.0 mm          ← وليس 12000
basis        : 2 of 3
class_a Q-03 : False              ← المجموع الجزئي ليس فئة A
partial note : PARTIAL — one or more declared elements were excluded…
exclusions   : [EX-STATUS] W-03 @ walls/2/end
```

| القاعدة | التنفيذ |
|---|---|
| P-1 `basis: n of N` | ✅ C4-PT-01 |
| P-2 وسم `PARTIAL` | ✅ C4-PT-02 |
| P-3 قائمة المستبعدين بأسبابهم | ✅ C4-PT-03 |
| P-4 لا مجموع جزئي بلا وسم | ✅ **ولا يكون فئة A** (C4-PT-04) |
| P-5 لا بند مؤهل ⇒ **ABSTAIN** لا جدول صفري | ✅ C4-PT-05 |
| رفض C1 ⇒ امتناع كامل | ✅ C4-PT-06 |

---

## 6. التمييز الثلاثي — ثلاث نتائج مختلفة فعلًا

| الحالة | السلوك | الاختبار |
|---|---|---|
| **`[U]`** | استبعاد معلن · **المجموع 8000 لا 12000 ولا صفر** · الجدول PARTIAL | C4-TD-01/02 |
| **الغياب المعلن** | **صفر مشروع** والجدول يبقى COMPLETE | C4-TD-03 |
| **UNCOMPUTABLE** | يُستهلك من B6/B7 · **السبب ينسب لمالكه** بلا إعادة اشتقاق | C4-TD-04 |

**C4-TD-05** يثبت اختلاف النتائج الثلاث في تشغيل واحد.

---

## 7. الوحدات والأبعاد

`sqrt(mm2) → mm` (C4-DM-01) · `mm × mm → mm2` (C4-DM-02) · `count` (C4-DM-03) · **عدم التطابق ⇒ استبعاد بلا إعادة قياس** (C4-DM-04) · **لا `m2` ولا `mm3` في أي قيمة مُنتَجة** (C4-DM-05) · **لا معاملات تحويل** `*1000`/`/1000` (C4-AS-07).

**الجبر مستعار من B6 لا مُعاد اختراعه** — تحققت أن B6 نفسه يطبّق `sqrt` ويفحص أُسّ البُعد.

---

## 8. الدقة

**لا `round(` في المحرك إطلاقًا** (C4-AS-08). القيم كاملة: `0.1+0.2` أعطت `3999.7000000000003` لا `4000` (C4-BD-05). **لا تقريب تجاري · لا سياسة جديدة · DEC-C2-02 تبقى مفتوحة.**

---

## 9. AS-C4-16 — No-write (سلوكي + فحص مسارات)

**لم أعتمد على `open()` وحده**، كما اشترط شرط إغلاق C3 رقم 4:

| الاختبار | النوع | النتيجة |
|---|---|---|
| C4-NW-01 | **سلوكي** — الماستر في الذاكرة | غير معدَّل |
| C4-NW-02 | **سلوكي** — بايتات القالب والمخطط | ثابتة |
| C4-NW-03 | **سلوكي** — جرد الملفات قبل/بعد | **لا ملف جديد** |
| C4-NW-04 | **سلوكي** — `project_master.json` | غير موجود |
| **C4-NW-05** | **فحص مسارات** — 10 آليات | **صفر**: `open(` · `.write(` · `json.dump` · `write_text` · `write_bytes` · `os.replace` · `shutil.copy` · `pathlib` · `tempfile` · `pickle` |
| C4-NW-06 | لا إنتاج Manifest (ملك C6) | مؤكد |

---

## 10. AS-C4-17 — عزل `outputs[]`

| الاختبار | النتيجة |
|---|---|
| C4-OUT-01 | `outputs[]` **لا يُعدّ** ولو امتلأ بثلاثة عناصر |
| C4-OUT-02 | مستبعد **بالملكية** صراحةً في الكود |
| C4-OUT-03 | ملء `outputs[]` **لا يغيّر أي كمية** |
| C4-OUT-04 | لا مسار كتابة يستهدفه |

---

## 11. Isolation

لا import لأي validator (C4-ISO-01) · لا import لـC1/C2/C3 (C4-ISO-02) · **يستهلك حكم B6 ولا يعيد اشتقاقه** (C4-ISO-03) · **لا مفتاح حكم** `correct`/`matches_master`/`approved`/`fidelity_ok` (C4-ISO-04) · **fidelity منسوبة لـD** (C4-ISO-05) · **لا ذكر لـsvg** (C4-AS-05).

---

## 12. Mutation — 10/10

| ID | الطفرة | النتيجة |
|---|---|---|
| C4-M-01 | إدخال `[A]` في الأهلية | ✅ تموت N-03 |
| C4-M-02 | معاملة `[U]` كقيمة | ✅ تموت TD-01 |
| C4-M-03 | دمج PARTIAL في COMPLETE | ✅ تموت PT-02/04 |
| C4-M-04 | تجريد provenance | ✅ تموت PV-01 |
| **C4-M-05a** | **خط أساس:** الحارسان سليمان | ✅ لا عدّ |
| **C4-M-05** | **تعطيل الحارسَين معًا** | ✅ **الخرق يظهر** |
| **C4-M-05b** | حارس الملكية وحده | ✅ **ما زال يمنع** |
| C4-M-06 | إفراغ المصادر غير الموثوقة | ✅ تموت N-05 |
| C4-M-07 | **CONTROL** | ✅ لا ينقلب شيء |

**طفرات خارجية:** حذف المحرك ⇒ `exit=1` · تعطيل حارس الوحدات ⇒ **سقطت N-06 وDM-04 حصرًا (80/82)** · تعطيل استهلاك حكم B6 ⇒ **انهيار صريح** (قيمة UNCOMPUTABLE كانت ستدخل المجموع صامتة).

---

## 13. Positive / Negative

**Positive 7/7** · **Negative 9/9** (رمز مقصود لكل حالة) · **Boundary 6/6** (صفر عناصر · جدار واحد · طول صفري · معرّف مكرر · ضوضاء float · NaN) · **Anti-smuggling 14/14** · **Triple distinction 5/5** · **Partial 6/6** · **Provenance 6/6** · **Dimensional 5/5** · **Isolation 5/5** · **outputs[] 4/4** · **No-write 6/6** · **Mutation 10/10**.

---

## 14. العيوب المكتشفة — خمسة، **كلها عيوب اختبار**

| # | العيب | التشخيص | الإصلاح |
|---|---|---|---|
| **DEF-C4-01** | DM-05/AS-14 رصدتا `mm3` | **إيجابية كاذبة:** الكلمة في نص **يعلن الكميات الحجمية خارج النطاق** | الفحص على **الوحدات المُنتَجة فعلًا** |
| **DEF-C4-02** | AS-06 رصدت `rate` | **مطابقة جزئية:** `rate` داخل `enume**rate**` | مطابقة بحدود الكلمة `\b` |
| **DEF-C4-03** | M-03 لم تنقلب | المسبار فحص وجود الإشعار، والإشعار يظهر بأي قيمة | استهدف **تمييز الحالة** بقيمة حرفية |
| **DEF-C4-04** | M-04 أسقطت المجموعة | الطفرة جرّدت `quantity_id` فكسرت منطقًا غير مقصود | طفرة **جراحية** تزيل provenance فقط |
| **DEF-C4-05** ⭐ | M-05 لم تنقلب | **دفاع مزدوج:** `outputs` محمي بحارسَين مستقلَّين، فتعطيل أحدهما لا يثبت شيئًا (درس C1/M-01 نفسه) | عزل: **خط أساس + تعطيل الاثنين + إثبات الحارس الثاني منفردًا** |

### تحسين واحد في المحرك (وليس مجاملة للاختبار)

**اكتُشف أن `q01_count(master, collections=COUNTABLE_COLLECTIONS)` يجمّد الثابت وقت الاستيراد** ⇒ الطفرة لا تصله. أُثبت ذلك تجريبيًا (`__defaults__` لم تتغير بعد تعديل الوحدة). عُولج بـ**late binding** داخل الدالة.

⇒ **AS-C4-18 (جديد):** *default-argument binding* صورة أخرى من AS-C2-16 — تُخفي التهيئة اللاحقة **و**تُبطل اختبار الطفرات. **أوصي باعتمادها قاعدة انضباط عامة.**

---

## 15. Full Regression

```
A 87 · B1 38 · B2 39 · B3 54 · B4 38 · B5 49 · B6 61 · B7 48
B8 45 · B9 40 · B10 47 · B11 45 · C1 58 · C2 58 · C3 54   =  761  ← الأساس، مطابق
C4 82
REGRESSIONS: NONE            (22.3s)
```
**Base Regression: 761/761 PASS** · **الإجمالي: 843/843.**
**ولا يُقرأ العدد كدليل fidelity** — شرط إغلاق C3 رقم 1، ساري.

---

## 16. Capability Evidence (runtime الحالي)

| البند | القيمة |
|---|---|
| runtime | CPython 3.13.14 |
| os/arch | Linux x86_64 |
| تبعيات C4 | **`math` من المكتبة القياسية فقط** |
| `shapely` · `ezdxf` | `NOT_SET` — **غير مطلوبة** |
| `numpy` | موجودة — **غير مستخدمة** |

**Capability مقيدة بـ(حساب × Q-01…Q-04) في هذا الـruntime، ولا تُعمَّم على مراحل أخرى.** GAP-C-02 نشطة (ثبت تقلب المكتبات داخل الجلسة).

---

## 17. Blockers / Deviations

**لا blockers. لا انحرافات عن العقد. لم أُعد فتح أي قرار معماري مغلق.**

| البند | الحالة |
|---|---|
| DEC-C4-03/04/05/06 · GAP-C4-01 | **DEFERRED — لم تُمسّ** |
| BOQ · Cost Estimate · Material Schedule | **غير منفَّذة** |
| DEC-C2-01…08 · GAP-C-01…14 | **مفتوحة كما هي** |
| تعديل Schema/Master/B6/C1/C2/C3 | **صفر** |
| جديد مسجَّل | **AS-C4-18** (default-argument binding) |

---

## 18. C4 IMPLEMENTATION GATE CARD

| البند | الحالة |
|---|---|
| النطاق Q-01…Q-04 فقط | ✅ بلا أي توسّع |
| **OWN-C4-01** ناتج خارجي | ✅ `EXTERNAL_DERIVED_RESULT` · `written_to_master=False` |
| Class A بالشروط الثمانية | ✅ والمجموع الجزئي **ليس A** |
| Eligibility بالرموز الستة | ✅ بلا استبعاد صامت |
| Provenance بالحقول الستة | ✅ + تحفّظ صريح |
| Partial Schedule | ✅ basis · وسم · مستبعدون · ABSTAIN |
| التمييز الثلاثي | ✅ ثلاث نتائج مختلفة |
| الوحدات والأبعاد | ✅ عرف B6 · لا تحويل |
| الدقة | ✅ لا تقريب · لا سياسة جديدة |
| **AS-C4-16 No-write** | ✅ **سلوكي + 10 مسارات كتابة** |
| **AS-C4-17 outputs[]** | ✅ حارسان مستقلان، كلاهما مُختبَر |
| Isolation | ✅ لا validators · لا C3 · لا حكم |
| Mutation (AS-C2-16/17) | ✅ 10/10 + CONTROL + 3 خارجية |
| Base Regression 761/761 | ✅ **PASS** |
| Capability | ✅ evidence للـruntime الحالي |

### القرار: **PASS WITH CONDITIONS**

**الشروط — تبعيات خارج ملكية C4:**
1. **الأرقام صحيحة حسابيًا ومطابقة للعقد، وهذا ليس إثبات صحة هندسية.** طول جدار مُدخل خطأً في الماستر سيُحسب بأمانة ويبقى خطأً — **الحكم لـD**.
2. **843/843 ليست دليل fidelity** ولا دليل صحة الماستر.
3. **AS-C4-18 جديد** — أوصي باعتماده قاعدة انضباط عامة.
4. **Capability دليل لحظي** لا حقيقة دائمة (GAP-C-02 نشطة).
5. **الجدول لا يُحفظ ولا يُدرَج في Manifest** — إدراجه ملك **C6**، و`outputs[]` ملك **E**.

**توقفت عند 00.5-C4 — IMPLEMENTATION GATE. لم أبدأ C5. C4 ليس مغلقًا حتى مراجعتكم المستقلة.**
