# 00.5-C5 — IMPLEMENTATION & TEST REPORT
**المرجع:** C5 Architecture CLOSED · Gate 1 APPROVED WITH CONDITIONS (CONDITION-01/02/03) · القواعد الإلزامية 1–21.
**النتيجة:** `C5 = 81/81` · **Base Regression = 850/850 PASS** · **الإجمالي 931/931 · REGRESSIONS: NONE**

---

## 1. الملفات التي تغيّرت

| الملف | الحالة | المسؤولية |
|---|---|---|
| `scripts/c5_presentation.py` | **جديد** | قبول/رفض المعاملات (PP-1…PP-8) · ربط الستايل · إنتاج SVG فئة B · أدلة |
| `tests/test_00_5_C5.py` | **جديد** | 81 اختبارًا |
| `tests/run_all.py` | **معدَّل — سطر واحد** | تسجيل C5 |

**لم يُمسّ أي ملف محظور** — md5 بعد التنفيذ:

| الملف | md5 |
|---|---|
| `project_master.schema.json` | `1b911e017ffce17a3b5da2da6c5a1bdb` |
| `project_master.template.json` | `a6b13613b719f54fbc1a07895c8a124b` |
| `c1_preconditions.py` | `b63fff2f0e327539b6521e62bbc6c7d8` |
| `c2_fingerprint.py` · `c2_environment.py` | `983ecf52…` · `d129d19a…` |
| **`c3_geometry_assembly.py`** · **`c3_svg_emitter.py`** | **`729f3528…`** · **`da2fb83f…`** |
| `c4_quantities.py` | `e4b4097b75369cf83324641ef98aafa8` |
| مجموعات C1/C3/C4 | `be24fb02…` · `f7998c59…` · `430a67a9…` |

**لا Schema · لا Template · لا Master · لا C1–C4 · لا validators · لا C6/D/E.**

---

## 2. تنفيذ القواعد الإلزامية 1–21

| # | القاعدة | التنفيذ | الإثبات |
|---|---|---|---|
| 1 | `record["assembly"]` المصدر الوحيد | المدخل `ga_artifact` حصرًا | C5-AS-02 |
| 2 | ممنوع `assemble()` | **صفر إشارة** في الكود المنفَّذ | C5-N-09 · AS-20 |
| 3 | ممنوع قراءة Master | لا `project_master` ولا `json.load` | C5-N-10 · NW-06 |
| 4 | ممنوع قراءة SVG C3 | لا `findall`/`parse(`/`ElementTree` | C5-ISO-03 |
| 5 | لا مصدر هندسي بديل | — | C5-N-10 · AS-02 |
| 6 | GA read-only | `copy.deepcopy` عند الدخول | **C5-IMM-01/02** |
| 7 | لا إعادة حساب إحداثيات | نسخ حرفي | C5-IMM-06 |
| 8 | لا rounding/quantization | **لا `round(`** في الكود | C5-IMM-07 |
| 9 | لا تغيير topology/IDs/dimensions | — | C5-IMM-04/09 |
| 10 | PP-1…PP-8 | منفَّذة كلها | §4 |
| 11 | لا silent defaults | لا ثوابت ستايل في الكود | C5-N-01 · AS-05 |
| 12 | لا invented semantics/units | رفض بـRJ-03/RJ-04 | C5-N-04/05 |
| 13 | SVG فقط | — | C5-P-01 |
| 14 | Class B فقط | ختم B · لا ختم A | C5-P-02 · AS-09 |
| 15 | Tier 0+1 فقط | موروث من الـartifact | C5-P-09 |
| 16 | PARTIAL من NR-01/02/03/04/06 فقط | ثابت معلن | **C5-PT-06** |
| 17 | لا حكم fidelity | `NOT_ASSESSED` | C5-N-14 · AS-19 |
| 18 | لا بصمة هندسية | `NOT_ISSUED` | C5-N-15 |
| 19 | لا كتابة Master | سلوكي + تدقيق مسارات | §8 |
| 20 | لا كتابة `outputs[]` | **لا ذكر لها إطلاقًا** | C5-ISO-04 |
| 21 | لا حفظ ملف | يُعيد نصًا | C5-NW-05 |

---

## 3. تنفيذ شروط Gate 1

### CONDITION-01 — GA Identity
الأدلة تحمل `project_id` · `master_revision` · `contract_tiers` · `provisional_digest` — **وسمها `PROVISIONAL` و`usable_as_evidence: False`**, ونصها يعلن صراحةً أنها **ليست `artifact_id`، وليست بصمة هندسية، ولا تُنشئ هوية GA فريدة ولا ادعاء ربط A↔B**. **ولا مفتاح باسم `artifact_id` في المخرجات** (C5-AS-23).

### CONDITION-02 — SVG Reverse Extraction
**الإثبات الأساسي** لعدم تعديل GA هو **مقارنة before/after على الـartifact نفسه** (C5-IMM-01/02، موسومة `PRIMARY`). والاستخراج العكسي **ثانوي** (C5-IMM-04/05/06، موسومة `SECONDARY`) ولا يُقدَّم كإثبات fidelity ولا صحة ماستر، **ولا ينشئ قواعد هندسية جديدة**.

### CONDITION-03 — No-Write
**لم أستخدم «Master غير موجود» كإثبات وحيد.** الإثبات: **سلوكي** (الماستر في الذاكرة · بصمات القالب/Schema/**مصدري C3** · **جرد ملفات قبل/بعد**) **+ تدقيق 12 مسار كتابة** ⇒ صفر.

---

## 4. عقد المعاملات — منفَّذ

**الستة حصرًا** تحت PP-1…PP-8، مع أحكام خاصة:

| المعامل | الحكم المنفَّذ |
|---|---|
| `color` | نص معلن ⇒ يُطبَّق |
| `line_weight` | يتطلب `{value, unit}` — **قيمة عارية بلا وحدة ⇒ RJ-04، ولا تُفترض وحدة** |
| `shadow` | منطقي/نص معلن |
| `label` | نص تقديمي — **يحمل رقمًا ⇒ RJ-06** (خطر Tier 3) |
| `sheet_order` | عدد صحيح — يُسجَّل بلا اختراع سلوك (Boards مؤجَّل) |
| `print_scale` | **إطار فقط** (`page_size`/`viewport`/`framing`) — أي مفتاح آخر ⇒ **RJ-05 (SC-1)** |

---

## 5. نتائج الاختبارات — 81/81

| المجموعة | العدد | أبرز ما تثبته |
|---|---|---|
| **Positive** | 9 | مخرج B مختوم · الستة تُطبَّق · الهندسة مطابقة حرفيًا |
| **Negative** | 15 | **كل بند طلبتموه مغطّى** (جدول §6) |
| **State semantics** | 5 | `[A]` معتمد · `[P]` · `[U]` · NOT_PRESENT · UNCOMPUTABLE |
| **Geometry Immutability** | 9 | **PRIMARY** قبل/بعد + **SECONDARY** استخراج عكسي |
| **PARTIAL** | 7 | وسم · قائمة · لا ترقية · **NR-05 لا تُنتج جزئية** |
| **Boundary** | 6 | صفر معاملات · الستة مجتمعة · خليط صالح/فاسد |
| **Mutation** | 7 | كل طفرة تقتل اختبارها + CONTROL |
| **Isolation** | 6 | لا validators · لا C3/C4 · لا `outputs[]` |
| **Anti-Smuggling** | 11 | AS-C5-01/02/05/07/09/10/19/20/21/22 + CONDITION-01 |
| **No-Write** | 6 | سلوكي + تدقيق مسارات |

### 5.1 الاختبارات السلبية المطلوبة — كلها منفَّذة

| الحالة المطلوبة | الاختبار | النتيجة |
|---|---|---|
| parameter absent | C5-N-01 | لا تطبيق ولا default |
| parameter unknown | C5-N-02 | RJ-02 |
| parameter خارج الستة | C5-N-03 | RJ-01 ×2 |
| parameter بلا semantic | C5-N-04 | RJ-03 |
| **parameter بلا unit والوحدة لازمة** | C5-N-05 | **RJ-04 — لا افتراض** |
| silent default injection | C5-N-06 · AS-05 | مستحيل بنيويًا |
| **disguised geometry scaling** | C5-N-07/08 | **RJ-05 + الإحداثيات ثابتة** |
| second GA assembly | C5-N-09 · AS-20 | صفر إشارة |
| alternate geometry source | C5-N-10 | لا مصدر آخر |
| **GA mutation** | C5-IMM-01/02 · AS-21 | **تطابق تام** |
| A/P/U كمدخل هندسي | C5-ST-01/02/03 | لا يُرقّى ولا يُرسم |
| NOT_PRESENT | C5-ST-04 | لا يُرسم وجود · COMPLETE |
| UNKNOWN | C5-ST-03 | مستبعد معلن |
| UNCOMPUTABLE | C5-ST-05 | مستهلَك لا مُعاد اشتقاقه |
| PARTIAL | C5-PT-01…07 | موسوم لا مكتمل |
| **Fidelity self-claim** | C5-N-14 · AS-19 | **لا مفتاح حكم** |

### 5.2 Mutation — **5 killed · 1 CONTROL · 1 NON-ACTIVE (غير محتسبة)**

> **لا يُدّعى أن Mutation Testing = 7/7 killed.** المحاسبة الصادقة:
> **`MUTATIONS: 5 killed (M-01..M-05) · 1 CONTROL (M-07) · 1 NON-ACTIVE defensive guard (M-06, not counted as killed)`**
> — وهذا النص يُطبع في مخرج المجموعة نفسها، لا في التقرير فقط.

| ID | الطفرة | التصنيف |
|---|---|---|
| C5-M-01 | توسيع قائمة المعاملات | ✅ **KILLED** — تموت N-03 |
| C5-M-02 | إفراغ رموز الجزئية | ✅ **KILLED** — تموت PARTIAL |
| C5-M-03 | **عدّ NR-05 كجزئية** | ✅ **KILLED** — تموت PT-06 |
| C5-M-04 | توسيع مفاتيح الإطار | ✅ **KILLED** — تموت N-07 |
| C5-M-05 | افتراض وحدة لـ`line_weight` | ✅ **KILLED** — تموت N-05 |
| **C5-M-06** | إزالة حارس `deepcopy` | ⚠️ **`DEFENSIVE-GUARD / NON-ACTIVE MUTATION`** — **ليست kill** (§7.1) |
| C5-M-07 | **CONTROL** | ✅ ضابط — لا ينقلب شيء |

**خارجية:** حذف المحرك ⇒ `exit=1` · تعطيل حارس PP-2 ⇒ **سقطت N-03 وBD-06 حصرًا (79/81)** · تعطيل حارس SC-1 ⇒ **سقطت N-07 وAS-22 حصرًا (79/81)** · الاستعادة ⇒ 81/81.

---

## 6. مخرج حقيقي (معاينة فعلية)

```xml
<svg viewBox="0 0 6000 4000">
  <!-- CLASS B — PRESENTATION VISUAL. Geometry is taken unchanged from the class A geometry assembly. -->
  <!-- Geometrically binding, aesthetically non-binding (Phase 00 section 10). -->
  <!-- engineering fingerprint: NOT_ISSUED -->
  <!-- fidelity: NOT_ASSESSED — owned by D -->
  <!-- ===== PARTIAL ===== … It is NOT a complete design. -->
  <g id="walls">
    <line x1="0" y1="0" x2="6000" y2="0" stroke="#334155" stroke-width="2" data-id="W-01" …/>
  </g>
  <!-- OMITTED ELEMENTS (declared, not silently dropped):
       [NR-01] walls/2/end: status [U] … not eligible
       [NR-03] walls/2 (W-03): wall omitted -->
  <!-- REJECTED PARAMETERS (declared, not applied):
       [RJ-01] opacity: not among the six … section 5-B is not widened here. -->
</svg>
```

الستايل مُطبَّق، **والإحداثيات حرفية من الـartifact**، والجزئية والمرفوضات **معلنة داخل الملف**.

---

## 7. العيوب والملاحظات

### عيوب مكتشفة — أربعة، **ثلاثة منها عيوب اختبار**

| # | العيب | التشخيص | الإصلاح |
|---|---|---|---|
| **DEF-C5-01** | `_style_for` انهار بـ`AttributeError` أثناء طفرة M-05 | **عيب محرك حقيقي**: الدالة وثقت بشكل المعامل المقبول | جُعلت دفاعية: **الشكل غير المعرَّف في العقد يُتجاهَل لا يُفسَّر** (PP-8) |
| **DEF-C5-02** | C5-N-09 وAS-20 رصدتا `assemble` | **إيجابية كاذبة**: الكلمة في نص **ينفي** السلوك («C5 does not assemble») | الفحص على كود **مجرَّد من السلاسل والتعليقات** |
| **DEF-C5-03** | C5-AS-23 رصدت `artifact_id` | **إيجابية كاذبة**: في نص التنصّل نفسه | الفحص على **مفاتيح المخرجات** لا النص |

### 7.1 C5-M-06 — تصنيف رسمي: `DEFENSIVE-GUARD / NON-ACTIVE MUTATION`

طفرة إزالة `copy.deepcopy` **لم تُنتج تلويثًا** للـartifact، لأن المسار الحالي **لا يكتب في `ga` أصلًا**.

**التصنيف النافذ وأربع نتائج ملزمة:**

| # | النتيجة |
|---|---|
| 1 | **الاختبار لا يثبت حاليًا أن `deepcopy` مطلوب سلوكيًا** |
| 2 | **M-06 لا تُحتسب mutation killed** |
| 3 | **لا يُدّعى أن Mutation Testing = 7/7 killed** — المحاسبة: **5 killed · 1 CONTROL · 1 NON-ACTIVE** |
| 4 | **لم يُغيَّر التنفيذ** لإجبار الطفرة على الفشل ولا لتحسين رقم الاختبار |

الحارس يبقى في الكود **دفاع عمق** أمام أي تعديل مستقبلي يكتب في `ga`، لكنه **ليس قاعدة نشطة اليوم** ولا يُقدَّم كذلك.

---

## 8. Regression

```
A 87 · B1 38 · B2 39 · B3 54 · B4 38 · B5 49 · B6 61 · B7 48 · B8 45
B9 40 · B10 47 · B11 45 · C1 58 · C2 58 · C3 54 · C4 89   =  850  ← الأساس مطابق
C5 81
REGRESSIONS: NONE            (21.5s)
```
**Base 850/850 PASS · الإجمالي 931/931.**
**ولا يُقرأ العدد كدليل fidelity.**

---

## 9. Limitations المتبقية

| # | الحد |
|---|---|
| 1 | **C5 لا يثبت أن B لم تنحرف عن A** — دليل فقط، **والحكم لـD** |
| 2 | **OBS-C5-01 قائمة**: لا هوية GA فريدة؛ الربط **قرينة lineage** لا معرّف رسمي |
| 3 | **C5-M-06**: حارس deepcopy دفاعي غير نشط اليوم |
| 4 | `sheet_order` مقبول عقديًا **بلا تطبيق فعلي** (Boards مؤجَّل) — يُسجَّل ولا يُخترع له سلوك |
| 5 | **GAP-C-02 نشطة** — القدرة فحص لحظي (SVG بلا تبعية خارجية) |
| 6 | DEC-C2-01…08 · GAP-C-01…14 · DEC-C5-04 **مفتوحة** — لم تُغلق أي منها |
| 7 | لا حفظ ولا Manifest — **C6** يدرج، و**E** يملك `outputs[]` |

**لا BLOCKERS.** لم أحتج إلى تعديل C3 ولا Schema ولا اختراع semantics أو default أو هندسة، ولم أوسّع نطاق C5.

---

## 10. C5 GATE CARD

| البند | الحالة |
|---|---|
| GA Handoff | ✅ `record["assembly"]` حصرًا · **لا `assemble()`** |
| القواعد 1–21 | ✅ كلها منفَّذة ومُثبتة |
| CONDITION-01 | ✅ lineage evidence بلا هوية · `PROVISIONAL` |
| CONDITION-02 | ✅ **PRIMARY** قبل/بعد · **SECONDARY** استخراج عكسي |
| CONDITION-03 | ✅ سلوكي + **12 مسار كتابة** |
| PP-1…PP-8 | ✅ بالستة حصرًا |
| Geometry Immutability | ✅ سبعة محاور |
| PARTIAL | ✅ NR-01/02/03/04/06 فقط · **NR-05 لا** |
| Class B فقط | ✅ |
| لا fidelity · لا بصمة | ✅ |
| No-write | ✅ صفر مسار |
| Mutation | ✅ **5 killed · 1 CONTROL · 1 NON-ACTIVE (M-06 غير محتسبة)** + 3 خارجية مستهدفة |
| Base Regression | ✅ **850/850** |
| الإجمالي | ✅ **931/931 · NONE** |

---

## 11. الحدود المثبَّتة رسميًا

| # | الحد النافذ |
|---|---|
| **L-1** | **`931/931` ليست دليل Fidelity** — ولا دليل صحة تصميم أو هندسة |
| **L-2** | **`provisional_digest` ليس `artifact_id`، وليس engineering fingerprint، ولا يُنشئ هوية GA فريدة** — وسمه `PROVISIONAL / usable_as_evidence: False` |
| **L-3** | **Fidelity بين B وA وMaster ملك D وحده** |
| **L-4** | **C5 لا يوسّع نطاقه إلى C6 أو D أو E** |
| **L-5** | **لا تعديلات على Schema أو Master أو C1–C4 أو validators** — مُثبت بالبصمات (§1) |
| **L-6** | **C5-M-06 = `DEFENSIVE-GUARD / NON-ACTIVE MUTATION`** — لا تُحتسب kill، ولا ادعاء 7/7 |

---

### القرار: **C5 IMPLEMENTATION — CLOSED WITH CONDITIONS**

**الشروط السارية (تبعيات خارج ملكية C5):**
1. **مطابقة B لـA غير مُثبتة هنا ولن تُثبت** — ملك **D** (L-3).
2. **931/931 ليست دليل fidelity** (L-1).
3. **C5-M-06 مصنَّف `DEFENSIVE-GUARD / NON-ACTIVE MUTATION`** (L-6).
4. **OBS-C5-01** قائمة: الربط **قرينة lineage لا هوية** (L-2).
5. الحفظ والإدراج لـ**C6/E** (L-4).

**لم أبدأ C6. الخطوة التالية — عند أمركم — هي C6 Architecture فقط، بلا كود.**
