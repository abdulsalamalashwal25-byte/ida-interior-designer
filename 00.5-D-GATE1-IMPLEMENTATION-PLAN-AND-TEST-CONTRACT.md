# 00.5-D — GATE 1: IMPLEMENTATION PLAN & TEST CONTRACT
**تخطيط وعقد اختبار فقط — لا كود · لا ملفات تنفيذية · لا Schema · لا Master · لا تعديل C1–C6 · لا `outputs[]` · لا fingerprint · لا identity · لا approval.**

> **المرجع:** D Architecture **APPROVED WITH CONDITIONS** · DEC-D-01…07 **معتمدة**.
> **المبدأ:** **D = Independent Fidelity Judge, not a Repair Engine.**
> **Regression baseline: 1028/1028** — baseline فقط، وليس دليلًا على صحة D.

---

## 0. قياسات أُجريت لبناء هذا العقد

| # | القياس | النتيجة |
|---|---|---|
| **M-1** | البدائيات المُصدَّرة في A وB | `svg` · `g` · `polygon` · `line` — **لا غير** |
| **M-2** | التمثيل العددي | جدران: `"6000"` (من int) · فتحات: `"1000.0"` (من float) |
| **M-3** | **دقة float عبر SVG** | `0.30000000000000004` **تعود كما هي**؛ `float(نص) == قيمة GA` ⇒ **round-trip دلالي سليم بلا أي tolerance** |
| **M-4** | سمات B | هندسية: `x1 y1 x2 y2 points` · **عرضية تُستبعد:** `stroke` · `stroke-width` · `fill` · `data-*` |
| **M-5** | غير قابل للاستخراج (من D Architecture §0) | `host_wall` · `offset` · `thickness` · الوحدات · provenance |

**نتيجة M-2 + M-3 معًا:** المقارنة **النصية** تفشل زورًا (`"6000"` ≠ `"6000.0"`)، بينما **الدلالية** تنجح بلا تسامح هندسي — وهذا بالضبط ما اعتمدتموه في DEC-D-03.

---

## 1. SEMANTIC EXTRACTION CONTRACT (SEC) — DEC-D-01 Option A

### 1.1 Input
| المدخل | الوصف | إلزامي |
|---|---|---|
| **GA Artifact** | `record["assembly"]` — **مرجع المقارنة الوحيد** | ✅ |
| **Output artefact** | نص SVG لمخرج A أو B | ✅ |
| **Output class** | `A` أو `B` كما صرّح به المنتِج | ✅ |
| **C6 manifest** | سياق وحدود (اختياري) | ⬜ |

### 1.2 Output Extraction Schema
```
extracted = {
  elements: [ {id, kind, coordinates[], primitive, source_group} ],
  outline:  { points[] } | NOT_SET,
  unsupported_primitives: [...],
  extraction_failures: [...],
  normalizations_applied: [...]
}
```

### 1.3 Element identity
**المعرّف يُقرأ من `data-id` حصرًا.** لا اشتقاق من الترتيب ولا من الموضع ولا من اسم المجموعة. عنصر بلا `data-id` ⇒ **`NOT_VERIFIABLE` لذلك العنصر** (لا FAILED).

### 1.4 Coordinate extraction
| البدائية | الحقول | التمثيل |
|---|---|---|
| `line` | `x1 y1 x2 y2` | نقطتان |
| `polygon` | `points="x,y x,y …"` | قائمة نقاط |

### 1.5 Supported primitives
**`line` · `polygon` فقط** (مقاس M-1). و`svg`/`g` حاويات لا هندسة.

### 1.6 Unsupported primitives
`path` · `circle` · `rect` · `ellipse` · `polyline` · `text` · `use` · `transform` — **أيٌّ منها ⇒ `NOT_VERIFIABLE` معلن لذلك العنصر، ولا محاولة تفسير**.

> **قاعدة حاسمة:** وجود `transform` يعني أن الإحداثي المكتوب **ليس** الإحداثي الفعلي ⇒ **`NOT_VERIFIABLE` إجباري**، لأن حسابه هو **إعادة بناء هندسي** محظور.

### 1.7 Numeric representation — **جوهر DEC-D-03**
> **Exact semantic numeric equality — NOT exact textual/byte equality.**

| القاعدة | التطبيق |
|---|---|
| **N-1** | المقارنة على **القيمة** بعد `float()`، لا على النص |
| **N-2** | `6000` == `6000.0` ✅ (سياق ووحدة متوافقان) |
| **N-3** | `6000` مقابل `5999.9` ⇒ **FAILED** — **لا تسامح مخفي** |
| **N-4** | **لا engineering tolerance · لا hidden rounding · لا silent normalization** |
| **N-5** | نص غير قابل للتحويل ⇒ **malformed** ⇒ `NOT_VERIFIABLE` |
| **N-6** | `NaN`/`inf` ⇒ **لا تُعدّ متساوية أبدًا** ⇒ malformed |
| **M-3 مؤكد** | دقة float تعود كاملة ⇒ **التطابق التام ممكن فعليًا** |

### 1.8 Semantic normalization — المسموح فقط
| مسموح ✅ (representation) | محظور ❌ |
|---|---|
| `"6000"` → `6000.0` للمقارنة | تقريب أو تدوير |
| تجاهل المسافات حول الفواصل | مواءمة قيم متقاربة |
| ترتيب العناصر بالمعرّف | إعادة ترتيب النقاط داخل الشكل |
| تجاهل التعليقات والسمات العرضية | حذف عنصر «شاذ» |

**وكل تطبيع مطبَّق يُدرَج في `normalizations_applied`.** التطبيع غير المعلن = **إخفاء فرق** (AS-D-09).

### 1.9 Declared omissions
تُقرأ من الـGA (`not_represented`) ومن بيان C6 — **والغياب المطابق لإغفال معلن ليس فشلًا**. غياب **غير** معلن ⇒ **FAILED (missing element)**.

### 1.10 Extraction failure behavior
| الحالة | النتيجة |
|---|---|
| SVG غير قابل للقراءة | **ABSTAIN** |
| بدائية غير مدعومة | `NOT_VERIFIABLE` للعنصر |
| قيمة malformed | `NOT_VERIFIABLE` للحقل |
| لا معرّف | `NOT_VERIFIABLE` للعنصر |
| صفر عناصر مستخرَجة مع GA غير فارغة | **FAILED** |

**بوابة كفاية قبل أي حكم** — وغياب الدليل **ليس** دليل نجاح ولا فشل.

---

## 2. المقارنة

### 2.1 GA ↔ A و GA ↔ B — **كلاهما مباشرةً مع GA** (DEC-D-05)
**لا يُعتمد `B ↔ A` كمرجع.** ويجوز تسجيل تطابق A↔B **كملاحظة إضافية** لا كبديل.

### 2.2 مصفوفة المقارنة

| # | الفحص | الطريقة | نتيجة عدم التطابق |
|---|---|---|---|
| 1 | **ID matching** | مجموعة معرّفات GA ↔ المستخرَجة | حسب 3/4 |
| 2 | **Coordinate matching** | قيمة بقيمة (N-1…N-6) | **FAILED** |
| 3 | **Element missing** | في GA وغير مستخرَج | **FAILED** إن لم يكن إغفالًا معلنًا |
| 4 | **Element extra** | مستخرَج وغير موجود في GA | **FAILED** (اختراع) |
| 5 | **Duplicate IDs** | معرّف مكرر في المخرج | **FAILED** — الهوية لم تعد فريدة |
| 6 | **Malformed values** | غير قابل للتحويل | **`NOT_VERIFIABLE`** |
| 7 | **Unsupported representation** | بدائية/`transform` | **`NOT_VERIFIABLE`** |

**B إضافة:** السمات العرضية (`stroke` · `stroke-width` · `fill` · `data-*`) **مستبعدة من المقارنة** (M-4) — واختلافها **ليس فرقًا هندسيًا**.

---

## 3–5. نموذج الحالات وشروط الدخول

| الحالة | متى **بالضبط** | لا يجوز حين |
|---|---|---|
| **VERIFIED** | بوابة الكفاية مجتازة · **صفر** فروق · **صفر** حقول `NOT_VERIFIABLE` ضمن النطاق | وجود أي حقل غير مفحوص |
| **FAILED** | **فرق مُثبت** في قيمة مستخرَجة · أو عنصر مفقود غير معلن · أو مخترع · أو معرّف مكرر | عجز الأداة |
| **PARTIAL** | جزء تحقق **و** جزء `NOT_VERIFIABLE` · **بلا أي FAILED** | وجود فشل ⇒ الحكم FAILED |
| **NOT_VERIFIABLE** | لا شيء ضمن النطاق أمكن التحقق منه | توفر ما يكفي للحكم |
| **ABSTAIN** | **لم يُفحص أصلًا**: لا GA · لا مخرج · مخرج ممتنع · خارج النطاق | بعد بدء الفحص |

**أسبقية ملزمة:** `FAILED` > `PARTIAL` > `VERIFIED`. فشل واحد **لا يُبتلع** في حكم جزئي.
**والافتراضي تحفظي:** `PARTIAL` هو المتوقع عمليًا (M-5).

### بنية كل حكم (إلزامية — DEC-D-06/07)
```
verdict, scope, fields_checked[], fields_not_verifiable[<field/element> + reason],
differences[{element_id, field, ga_value, extracted_value}],
evidence{ga_ref, output_ref, normalizations_applied[]}, reason
```
**`VERIFIED` بلا `fields_checked` ممنوع** — حكم بلا نطاق = **AS-D-13**.

---

## 6. سياسة المساواة العددية
`6000 == 6000.0` ✅ · **لا engineering tolerance** · **لا hidden rounding** · **لا silent normalization** · والتطبيع **تمثيلي معلن فقط**.

---

## 7. اختلافات التمثيل

| الفرق | الحكم |
|---|---|
| ترتيب العناصر | **ليس فرقًا** — المطابقة بالمعرّف |
| المسافات البيضاء | ليس فرقًا |
| `6000` مقابل `6000.0` | **ليس فرقًا** (N-2) |
| الألوان وسماكة الخط في B | **ليست هندسة** — مستبعدة |
| حقول العرض `data-*` | مستبعدة |
| التعليقات والميتاداتا | ليست بيانات مقارنة |
| **ترتيب النقاط داخل polygon** | ⚠️ **فرق دلالي محتمل** ⇒ يُقارَن **بالترتيب** ولا يُعاد ترتيبه |

---

## 8. NO-REPAIR INVARIANT
**D لا يغيّر:** Master · GA · A · B · C6 · أي دليل مصدري.
**عند اكتشاف خلل:** يصف الفرق (`ga_value` مقابل `extracted_value`) **ولا يقترح تصحيحًا ولا قيمة صحيحة**.

## 9. NO-GENERATION INVARIANT
**لا استدعاء لـC3/C5** · **لا إعادة بناء GA** · **لا حساب هندسة جديدة**.
**مسموح:** حساب طول من إحداثيات مستخرَجة **للمقارنة فقط** — لا يُخزَّن ولا يصير مرجعًا.

## 10. DEPENDENCY ISOLATION

| الملف (مقترح) | المسؤولية | Dependencies مسموحة | ممنوعة |
|---|---|---|---|
| `scripts/d_sec.py` | عقد الاستخراج · تحليل SVG · التطبيع المعلن | stdlib (`re`) | أي طبقة |
| `scripts/d_compare.py` | المقارنة الدلالية · الفروق | `d_sec` · stdlib | أي طبقة |
| `scripts/d_verdict.py` | الحالات الخمس · بنية الحكم | `d_compare` · stdlib | أي طبقة |
| `scripts/d_report.py` | التقرير المُهيكل | الثلاثة أعلاه | أي طبقة |
| `tests/test_00_5_D.py` | الاختبارات | وحدات D + C3/C5 **كمولِّدي fixtures فقط** | — |
| `tests/run_all.py` | **سطر واحد** | — | — |

**وحدات D لا تستورد C1–C6 ولا أي validator.** الاستيراد في الاختبارات فقط، لتوليد **مخرجات حقيقية**، ويُثبت العزل باختبار صريح.

## 11. NO-WRITE TESTING — ثلاثة مستويات
**سلوكي** (GA/A/B غير معدَّلة · بصمات · جرد ملفات) · **تدقيق 12 مسار كتابة** · **عزل** (لا `outputs`).

---

## 12. TEST MATRIX

| # | الفئة | الهدف | المدخل | النتيجة | شرط الفشل |
|---|---|---|---|---|---|
| 1 | **Positive** | تطابق حقيقي | GA + A سليم | `PARTIAL` بنطاق مُعلن · صفر فروق | ادعاء `VERIFIED` رغم حقول غير مفحوصة |
| 2 | **Positive-B** | B ضد GA مباشرة | GA + B | نفس الهندسة · العرض مستبعد | اعتبار اللون فرقًا |
| 3 | **Negative** | إحداثي مُعدَّل | A معبوث | **FAILED** بتحديد العنصر والحقل | مرور الفرق |
| 4 | **Negative** | عنصر محذوف غير معلن | A ناقص | **FAILED (missing)** | اعتباره إغفالًا |
| 5 | **Negative** | عنصر مخترع | A زائد | **FAILED (extra)** | تجاهله |
| 6 | **Negative** | معرّف مكرر | A بمعرّفين | **FAILED (duplicate)** | قبول أولهما |
| 7 | **NOT_VERIFIABLE** | حقل غير مُصدَّر | `thickness` في GA | `NOT_VERIFIABLE` **لذلك الحقل** | PASS أو FAIL |
| 8 | **NOT_VERIFIABLE** | بدائية غير مدعومة | `path`/`transform` | `NOT_VERIFIABLE` للعنصر | تفسير الشكل |
| 9 | **NOT_VERIFIABLE** | malformed | `x1="abc"` | `NOT_VERIFIABLE` | تحويل قسري |
| 10 | **Boundary** | `6000` vs `6000.0` | تمثيلان | **متساويان** | FAILED زائف |
| 11 | **Boundary** | `6000` vs `5999.9` | فرق ضئيل | **FAILED** | ابتلاعه |
| 12 | **Boundary** | ضوضاء float | `0.1+0.2` | **متساوية** (M-3) | FAILED زائف |
| 13 | **Boundary** | صفر عناصر · عنصر واحد · إغفال معلن | — | حكم صحيح | اختراع حالة |
| 14 | **ABSTAIN** | لا GA · لا مخرج · مخرج ممتنع | — | **ABSTAIN** لا FAILED | خلطه بالفشل |
| 15 | **Mutation** | §13 | — | حسب التصنيف | طفرة ناجية غير معلنة |
| 16 | **Isolation** | لا توليد ولا استيراد | كود D | صفر | أي استدعاء مولّد |
| 17 | **Anti-Smuggling** | AS-D-01…15 | حالات مصمَّمة | لا false PASS/FAIL | أي تسريب |
| 18 | **No-Write** | لا كتابة | تشغيل كامل | صفر | أي تعديل |
| 19 | **Regression** | لا انحدار | `run_all.py` | **1028/1028** | أي انحدار |

**اختباران حاسمان:** **false PASS** (حقل غير مفحوص لا يُحتسب نجاحًا) · **false FAIL** (عجز الأداة أو فرق تمثيلي لا يُعدّ فشلًا).

---

## 13. MUTATION PLAN

| # | الطفرة | التصنيف المتوقع |
|---|---|---|
| **M-01** | تعطيل مقارنة الإحداثيات | KILLED |
| **M-02** | إسقاط كشف العنصر المفقود | KILLED |
| **M-03** | **اعتبار `NOT_VERIFIABLE` = `VERIFIED`** | KILLED — **يمنع false PASS** |
| **M-04** | **اعتبار `NOT_VERIFIABLE` = `FAILED`** | KILLED — **يمنع false FAIL** |
| **M-05** | السماح بفرق رقمي مخفي (tolerance) | KILLED |
| **M-06** | السماح بإعادة تجميع GA | KILLED |
| **M-07** | السماح بتعديل المخرج | KILLED |
| **M-08** | السماح لـD بإصدار حكم على الماستر | KILLED |
| **M-09** | إسقاط كشف العنصر المخترع | KILLED |
| **M-10** | إسقاط `fields_checked` من الحكم | KILLED |
| **CONTROL** | تغيير تجميلي | **CONTROL** |

**عزل الدفاعات المكرّرة مسبقًا:** قبل كل طفرة يُتحقق أن **الحارس المستهدف وحده** هو الفاصل — تفاديًا لتكرار درس C1-M-01 وC6-M-01.

## 14. MUTATION CLASSIFICATION
**KILLED / SURVIVED / CONTROL / NON-ACTIVE** — **تصنيف صادق**، والناجية **تُعلَن بسببها**، **ولا يُعدَّل الكود لإجبار طفرة على الفشل** (سابقة C5-M-06 وC6 المعتمدة).

## 15. REGRESSION
`run_all.py` بعد التنفيذ · **1028/1028 بلا انحدار** في A · B1–B11 · C1–C6. والعدد **baseline لا دليل صحة**.

---

## 16. الحدود الإلزامية

**D لا يملك:** تعديل الماستر · توليد هندسة · إصلاح · قرارات تصميم · هوية · بصمة · اعتماد · `outputs[]` · حكم بمعايير هندسية · سلطة لصور AI.

**ولا يُقبل دليلًا على fidelity:** provenance · element-count · visual similarity.

**GAP-D-01 يبقى مفتوحًا:** حدود C3 تعني أن D **قد يُنتج `PARTIAL`** لأن `host/offset/thickness/units` **لا تصل إلى المخرج** — و**D لا يحاول حلها بإعادة الاستنتاج**.

---

## 17. GATE CARD

| البند | الحالة |
|---|---|
| SEC بعشرة بنود | ✅ §1 — مبني على قياس M-1…M-5 |
| المقارنة السبعة | ✅ §2 — A وB **كلاهما ضد GA** |
| الحالات الخمس بشروط دخول | ✅ §3–5 + أسبقية FAILED |
| بنية الحكم | ✅ scope · fields_checked · not_verifiable · differences · evidence · reason |
| المساواة الدلالية | ✅ §6 — **بلا tolerance**, ومُثبت أنها ممكنة (M-3) |
| اختلافات التمثيل | ✅ §7 — **وترتيب نقاط polygon محفوظ** |
| No-Repair · No-Generation | ✅ §8/§9 |
| Dependency isolation · No-write | ✅ §10/§11 |
| Test Matrix | ✅ 19 فئة بهدف/مدخل/نتيجة/شرط فشل |
| Mutation Plan | ✅ 10 + CONTROL · **مع عزل مسبق للدفاعات المكرّرة** |
| Regression | ✅ 1028/1028 |
| كود / ملفات | ✅ **صفر** |

### الحالة

# **D Gate 1 = READY / AWAITING SUPERVISOR REVIEW**

**لم أكتب كودًا ولم أنشئ ملفات ولم أمسّ Schema/Master/C1–C6. لا أدّعي تنفيذًا ولا نتائج — لم يُنفَّذ شيء بعد.**
