# IDA — PHASE 01 — ARCHITECTURE SPECIFICATION **R02 — DELTA**

**DELTA ONLY — لا كود · لا Schema · لا Master · لا Implementation · لا اختبارات · لا تعديل A–F · لا إغلاق GAP/DEC سابق · لا تحديد نطاق Phase 02.**

> **R01 معتمد مشروطًا.** R02 **دلتا محصورة في A-01…A-06**, وكل ما عداها في R01 **سارٍ بلا تغيير**.
> **Regression:** 1297/1297 (لم يُمسّ شيء).

---

## 0. قياسان يسندان التعديلين الأولين

| # | القياس | النتيجة |
|---|---|---|
| **M-6** | **`unknown_entry.blocking`** | **حقل منطقي مُصرَّح** في السجل · وB7 **يقرأه** (`if entry.get("blocking")`) **ولا يستنتجه** |
| **M-7** | **`assumption_entry`** | `approval_state` enum = `PENDING_APPROVAL` · `APPROVED` · `REJECTED` — **و`approval_state` حقل إلزامي** |

**الأثر:** **A-01** و**A-02** ليسا قاعدتين جديدتين — بل **التزام بما تفرضه البنية أصلًا**.

---

## A-01 — علاقة G1 بـ`[U]` (تعديل §5 · §8)

### القاعدة النافذة

> **G1 يمنع البناء فقط عند غياب حقل مصنَّف مسبقًا كـBLOCKING.**

| # | القاعدة |
|---|---|
| **G1-1** | **غياب حقل NON-BLOCKING لا يمنع بناء الماستر** |
| **G1-2** | الحقول **غير المكتملة وغير الحاجبة** تدخل الماستر **`[U]`** وتُسجَّل في **`registers.unknowns`** |
| **G1-3** | **`[U]` ليست خطأً ولا فشلًا** — حالة معرفية مشروعة |
| **G1-4** | **G1 لا يستنتج أن حقلًا حاجب من تلقاء نفسه** — التصنيف **مُصرَّح في السجل** (M-6) ويُقرأ كما هو |
| **G1-5** | **آلية B7 لا تتغير** — Phase 01 **يستهلك تصنيفها** ولا يعيد تنفيذه |

### التمييز الدقيق

| الحالة | سلوك G1 | النتيجة |
|---|---|---|
| حقل **BLOCKING مُصرَّح** غائب | **ABSTAIN** — لا بناء | `INTAKE_INCOMPLETE` |
| حقل **NON-BLOCKING** غائب | **البناء يستمر** | `[U]` + قيد في `unknowns` |
| حقل غير مصنَّف | **لا يُفترض حاجبًا** | يُعامل كغير حاجب ويُسجَّل |

**سُحبت صياغة R01** التي أوحت بأن أي نقص يوقف G1. **الإيقاف للحاجب المُصرَّح وحده.**

---

## A-02 — إعادة صياغة DEC-P1-03 بشأن `[A]`

### الصياغة المسحوبة
~~«(ب) نعم بإعلان واعتماد E» — كانت توحي بأن Phase 01 **يستطيع اعتماد** `[A]`.~~

### القاعدة النافذة

| # | القاعدة |
|---|---|
| **AS-1** | **Phase 01 لا يستطيع اعتماد `[A]`** — إطلاقًا |
| **AS-2** | إن احتاجت العملية افتراضًا، **لا يتحول إلى حقيقة ولا إلى مدخل معتمد** |
| **AS-3** | **أي `[A]` يحتاج اعتمادًا يبقى `PENDING_APPROVAL` حتى E** (M-7) |
| **AS-4** | **Phase 01 لا يمنح `[A]` أهلية للتصميم ولا لفئة A** |
| **AS-5** | **لا implicit approval بأي صورة** |

**واتساق مع C1:** `[A]` **ولو معتمدًا** غير مؤهل لفئة A (GAP-C-11) — فأهلية التصميم **ليست ملك Phase 01 أصلًا**.

### DEC-P1-03 بعد الإصلاح

| الخيارات | الموصى به |
|---|---|
| (أ) Phase 01 **لا ينشئ `[A]` إطلاقًا** · (ب) **ينشئه معلنًا بحالة `PENDING_APPROVAL` فقط، بلا اعتماد وبلا أهلية** | **(ب) بالقيود AS-1…AS-5** — والاعتماد **ملك E وحده** |

---

## A-03 — تعريف `VALIDATED` بدقة (تعديل §7)

> **`VALIDATED` = اجتياز الفحوص المطلوبة **ضمن نطاق Phase 01** (A + B1–B11 بلا ERROR).**

### ما **لا** تعنيه — نصًا

| ❌ | السبب |
|---|---|
| **Design Ready** | لا تصميم في Phase 01 |
| **Generation Ready** | بوابة التوليد ملك **C1** |
| **User Approved** | الاعتماد ملك **E** |
| **Engineering Correct** | القواعد تفحص التناسق لا الصواب الهندسي المطلق |
| **Fidelity Verified** | fidelity ملك **D** — ولا مخرج بعد أصلًا |

**و`READY_DECLARED` مقابل `BLOCKED_DECLARED` يبقيان نتيجتين مختلفتين لبطاقة الجاهزية** — وكلتاهما تليان `VALIDATED`.

---

## A-04 — ضبط معنى G5 (تعديل §5)

> **`ready=True` يعني فقط أن `final_approval_readiness` لم يجد Blocking condition ضمن نطاقه.**

### ما **لا** يعنيه

❌ User Approval · ❌ E Approval · ❌ Final Design Approval · ❌ Engineering Certification · ❌ Fidelity Verification.

### `ready=False`

> **نتيجة صحيحة ومشروعة — وليست فشلًا في Phase 01.**
> والفشل الحقيقي هو **إخفاء الحاجب أو تجاوزه**, لا وجوده.

**وقاعدة R01 باقية:** الحاجب **لا يمنع إعلان البطاقة**.

---

## A-05 — ملكية O-3 (تعديل §2.2)

> **O-3 = Consumed / Assembled Validation Result Reference** من **A + B1–B11**.
> **وليس إعادة تنفيذ ولا نسخة جديدة من ملكية المدقّقات.**

| # | القاعدة |
|---|---|
| **O3-1** | Phase 01 **يستدعي المدقّقات ويجمّع نتائجها كمراجع** |
| **O3-2** | **لا يعيد تفسير `severity`** |
| **O3-3** | **لا يخفف ERROR** ولا يحوّله إلى WARNING |
| **O3-4** | **لا يحوّل نتيجة validator** بأي صورة |
| **O3-5** | **ملكية الحكم تبقى للمدقّق المُصدِر** — وPhase 01 ناقل |

**اتساق:** نفس نمط C6 (مُجمِّع لا مُقيِّم) — **ما لم تقله الطبقة، لا يقوله Phase 01**.

---

## A-06 — إعادة تصنيف GAP-P1-01

### التصنيف النافذ

| البند | القيمة |
|---|---|
| **المعرّف** | **NOTE-P1-01** (كان GAP-P1-01) |
| **النوع** | **Architectural Note** |
| **النص** | القالب فارغ وكل المصفوفات فارغة · و`ceiling_height = [U]` |
| **الحالة** | ✅ **ليس dependency failure ولا blocker** |
| **السبب** | **بناء الماستر من القالب هو الوظيفة الأساسية لـPhase 01** — فوجوده فارغًا **نقطة البداية المتوقعة لا نقص** |

**سُحب من سجل الثغرات** وأُبقي **ملاحظة معمارية** للسياق.

---

## 1. UPDATED OPEN GAP / DEC REGISTER

### ثغرات Phase 01

| # | الحالة |
|---|---|
| ~~GAP-P1-01~~ | ✅ **أُعيد تصنيفه `NOTE-P1-01`** — ملاحظة لا ثغرة (A-06) |
| **GAP-P1-02** | 🔴 **OPEN** — Phase 00 لا يُعدِّد محتوى 01–11 ⇒ **نطاق Phase 02 غير محدد** · **ولا أحدده** |

### قرارات Phase 01

| # | الحالة |
|---|---|
| **DEC-P1-01** نطاق Phase 01 | 🔴 OPEN — الموصى به: Intake → Validated Master |
| DEC-P1-02 من يملأ الماستر | 🟡 OPEN |
| **DEC-P1-03** `[A]` | 🟡 **OPEN — أُعيدت صياغته بـAS-1…AS-5** |
| **DEC-P1-04** FULL/LITE | 🔴 OPEN — النواة غير قابلة للحذف |
| DEC-P1-05 موضع الحفظ | 🟡 OPEN |
| **DEC-P1-06** استدعاء المدقّقات | 🔴 OPEN — الموصى به: **استهلاك لا إعادة تنفيذ** (A-05) |
| DEC-P1-07 تناقض المدخلات | 🟡 OPEN — إعلان بلا تصحيح |

### ثغرات الطبقات السابقة — **لم يُغلق أي منها**

**GAP-01 · GAP-02 · CONFLICT-E-01 · GAP-D-01 · GAP-F-01 · GAP-F-02 · GAP-F-03 · CLG-F-01 · GAP-C-02 · GAP-C6-02…05 · GAP-E-01 · DEC-C2-01…08 · DEC-E-02…05 · DEC-F-02…05** — **كلها OPEN كما هي، كـdependencies فقط.**

---

## 2. CONFIRMATIONS

### A–F remain unchanged
**مؤكَّد بالبصمات:**
`project_master.schema.json 1b911e01…` · `project_master.template.json a6b13613…` · `intake_form.md acf9df38…` · `RULES.md fb5b5723…` · `f_reference.py 523d7c63…` · `e_ownership.py e3e0cf9b…`
**ولم يُنشأ ولا عُدِّل أي validator ولا أي وحدة في A–F.**

### Implementation has NOT started
**مؤكَّد:** لا `scripts/p1_*.py` · لا `tests/test_01*` · لا Schema · لا Master · لا UI · لا API.
**والانحدار 1297/1297 · REGRESSIONS: NONE.**

### Cross-Layer Conflict
**لا شيء.** التعديلات الستة **تضبط سلوك Phase 01 داخل الواجهات القائمة** — ولم يستدعِ أيٌّ منها تعديل طبقة سابقة.

---

## 3. UPDATED GATE CARD — Phase 01 Architecture R02

| البند | الحالة |
|---|---|
| **A-01** G1 ↔ `[U]` | ✅ **الإيقاف للحاجب المُصرَّح وحده** · `[U]` ليست خطأً · **لا استنتاج للحجب** |
| **A-02** `[A]` | ✅ **لا اعتماد من Phase 01** · `PENDING_APPROVAL` حتى E · **لا أهلية تصميم** |
| **A-03** `VALIDATED` | ✅ **نطاق Phase 01 فقط** · وخمسة نفي صريح |
| **A-04** G5 | ✅ **`ready=True` نطاقي** · و**`ready=False` نتيجة صحيحة** |
| **A-05** O-3 | ✅ **مرجع مُجمَّع لا إعادة تنفيذ** · لا إعادة تفسير severity |
| **A-06** GAP-P1-01 | ✅ **أُعيد تصنيفه ملاحظة معمارية** |
| بقية R01 | ✅ **سارية بلا تغيير** |
| **A–F unchanged** | ✅ البصمات ثابتة |
| **لم يُغلق GAP/DEC سابق** | ✅ |
| **نطاق Phase 02** | ✅ **لم أحدده** (GAP-P1-02) |
| كود / Schema / Master / Tests | ✅ **صفر** |

### الحالة

# **PHASE 01 ARCHITECTURE R02 = READY FOR SUPERVISOR REVIEW**

**لم أبدأ التنفيذ، ولم أعدّل A–F، ولم أغلق أي GAP أو DEC، ولم أفترض نطاق Phase 02.**
