# 00.5-C6 — ARCHITECTURE CLOSURE REVISION **R03**
**مراجعة مركّزة على أربع نقاط — لا كود · لا tests · لا Schema · لا Master · لا تعديل C1–C5 · لا D/E · لا Manifest implementation · لا إعادة تصميم · لا نطاق جديد.**

> **الهدف الثابت:** **C6 = Manifest / Evidence Assembler بأقل دلالات ممكنة.**
> ما لم تصرّح به الطبقة **لا يخترعه C6**، وأي ملخص أو مقارنة **بيانات وصفية ميكانيكية** لا حكم.

---

## 1. CHANGES FROM R02

| # | ما كان في R02 | ما صار في R03 |
|---|---|---|
| **1** | `source_status = PARTIAL` مع `source_status_field = completeness` | **عيب أُصلح.** ثلاثة حقول **مستقلة دلاليًا**: `source_status` · `source_coverage` · `source_completeness` — **لا نقل معنى حقل إلى اسم آخر** (§2) |
| **2** | `by_source_status` عدّاد واحد | **ثلاثة عدّادات منفصلة**، فلا يُحتسب `completeness` كـ`status` (§3) |
| **3** | `producer_channel` يُستنتج من `contract + capability.output` | **Option B معتمد:** لا سجل عقود قائم ⇒ **`producer_channel = NOT_SET`** ولا استنتاج (§4) |
| **4** | `comparable` / `divergence` بلا تصنيف | **مصنَّفان صراحةً `mechanical comparison metadata`** بمعانٍ محصورة (§5) |
| **5** | `approval_managed_by: "E"` | **مصنَّف `architectural ownership metadata`** — ليس حالة ولا حكمًا (§6) |
| **6** | — | **اكتشاف جديد:** `completeness` في C4 **قاموس لكل كمية** لا قيمة مفردة ⇒ **ممنوع تسطيحه** (§2.3) |

---

## 2. STATUS / COVERAGE / COMPLETENESS SEMANTICS

### 2.1 العيب المُقرّ به

R02 اقترح `source_status = PARTIAL` مع `source_status_field = "completeness"`. **هذا إعادة تفسير**: يُدخل قيمة اكتمال في خانة اسمها «حالة»، فيبدو أن C4 يملك `status` وهو **لا يملكه** (مقاس: `'status' in Q ⇒ False`).

### 2.2 التمثيل المعتمد — ثلاثة حقول مستقلة

> **القاعدة: C6 ينقل معنى الحقل الأصلي، ولا يغيّر اسمه الدلالي إلى معنى آخر.**

| المنتِج | `source_status` | `source_coverage` | `source_completeness` |
|---|---|---|---|
| **C3** | `EMITTED` \| `ABSTAIN` | **`NOT_SET`** | **`NOT_SET`** |
| **C5** | `EMITTED` \| `ABSTAIN` | `COMPLETE` \| `PARTIAL` | **`NOT_SET`** |
| **C4** | **`NOT_SET`** | **`NOT_SET`** | `COMPLETE` \| `PARTIAL` \| `ABSTAIN` |

**أربعة محظورات:** ❌ نقل `completeness` إلى `source_status` · ❌ نقل `coverage` إلى `source_completeness` · ❌ توحيدها في حقل واحد · ❌ اشتقاق حقل غائب من آخر حاضر.

**و`NOT_SET` هنا تعني حصرًا:** *«هذا المنتِج لا يُصرّح بهذا الحقل»* — **لا COMPLETE ولا PARTIAL ولا نقص**.

### 2.3 اكتشاف جديد — `completeness` في C4 ليس قيمة مفردة

**القياس:** `Q.completeness = {"Q-02": "PARTIAL", "Q-03": "PARTIAL", "Q-04": "COMPLETE", "overall": "PARTIAL"}`.

⇒ **تسطيحه إلى قيمة واحدة يفقد تفصيلًا صرّح به C4.** التمثيل المعتمد:

```
source_completeness         : <overall كما صرّح به C4>
source_completeness_detail  : <القاموس كما هو، بلا إعادة تشكيل>
```

**ولا يُعاد حساب `overall` من التفاصيل** — يُنقل كما صرّح به مالكه.

---

## 3. AGGREGATE SUMMARY SEMANTICS

**ثلاثة عدّادات منفصلة، ولا خلط بينها:**

```
aggregate_summary:                      ← mechanical counts ONLY
  members_total               : <عدد>
  by_source_status            : { "EMITTED": n, "ABSTAIN": n, "NOT_SET": n }
  by_source_coverage          : { "COMPLETE": n, "PARTIAL": n, "NOT_SET": n }
  by_source_completeness      : { "COMPLETE": n, "PARTIAL": n, "ABSTAIN": n, "NOT_SET": n }
```

**خصائص ملزمة:** عدّ حرفي فقط · **لا جمع بين العدّادات** · **لا حالة موحّدة جديدة** · لا ترجيح · لا حكم · وكل عضو يحتفظ بحقوله كما هي.

**ملاحظة دقيقة:** `NOT_SET` في كل عدّاد تعني **«منتِجون لا يصرّحون بهذا الحقل»** — فـC4 يظهر في `by_source_status` تحت `NOT_SET` **لأنه لا يملك الحقل**, لا لأنه فشل.

**النص الملزم:** *«هذه عدادات ميكانيكية لحقول الأعضاء. لا تمثّل حالة للبيان، ولا تُقرأ كحكم على اكتمال التسليم.»*

---

## 4. PRODUCER CHANNEL CONTRACT — **Option B معتمد**

### 4.1 الفحص المقاس

| الفحص | النتيجة |
|---|---|
| هل يرسل C3/C5 حقل `producer_channel`؟ | ❌ **لا** |
| هل يوجد **Contract Registry** معتمد في أي وثيقة مغلقة؟ | ❌ **لا شيء** |
| هل يوجد mapping معماري صريح `contract → channel`؟ | ❌ **غير موجود** |

### 4.2 القرار

> ### **Option B — لا يوجد عقد ربط صريح ⇒ `producer_channel = NOT_SET`.**

**وأسحب ما ورد في R02** من اشتقاق القناة عبر `contract + capability.output` — كان **استنتاجًا دلاليًا** لا تطبيق mapping قائم.

**مصادر محظورة قطعيًا لإثبات القناة:** اسم `contract` وحده · اسم `format` · **تعليق SVG** · **غياب حقل `class`** · **معرفة C6 الداخلية**.

**ما يُنقل بدل ذلك:** `contract` و`format` **كقيم خام موصوفة**, بلا ترجمة إلى قناة. ومن يقرأ البيان يرى القيمة الحرفية ويحكم بنفسه إن كان يملك الصلاحية.

**رفع الحالة مستقبلًا** يحتاج **Contract Registry معتمدًا** ⇒ **DEC-C6-11 (جديد · OPEN)** — وليس اجتهادًا من C6.

---

## 5. REVISION COMPARISON METADATA

**التصنيف الرسمي:** `revision_view` كلها **`mechanical comparison metadata`**.

| الحقل | يعني **حصرًا** | لا يعني |
|---|---|---|
| `divergence: OBSERVED` | **القيم المصرَّح بها للمراجعات مختلفة** | تعارض هندسي · خطأ · بطلان |
| `divergence: NONE_OBSERVED` | القيم المصرَّح بها متطابقة | صحة · اتساق هندسي |
| `divergence: NOT_DETERMINABLE` | بعض الأعضاء بلا مراجعة مصرَّحة | فشل · نقص في المخرج |
| `comparable: false` | **لا يمكن إجراء مقارنة كاملة وفق بيانات المراجعة المتاحة** | **أن المخرجات غير صالحة** |
| `comparable: true` | المقارنة أُجريت على القيم المصرَّحة | أن النتيجة مقبولة |

**ليست:** engineering verdict · approval verdict · fidelity judgment · قرار قبول/رفض التجميع.

**القواعد الأربع باقية:** ❌ اختيار الأحدث · ❌ إخفاء التعارض · ❌ خلط الأدلة · ❌ رفض تلقائي للتجميع.

**والحد المقاس باقٍ:** **A لا يحمل `master_revision` بنيويًا** (GAP-C6-03) ⇒ `comparable: false` هو الوضع الغالب اليوم، **وهذا وصف حالة لا عيب في المخرج**.

---

## 6. APPROVAL OWNERSHIP SEMANTICS

| الحقل | التصنيف | يعني **حصرًا** | لا يعني |
|---|---|---|---|
| `approval_managed_by: "E"` | **`architectural ownership metadata`** | **من يملك الاعتماد معماريًا** | حالة اعتماد · حكمًا على المشروع · استنتاجًا من C6 · بديلًا عن verdict |
| `approval_value: NOT_SET` | إعلان غياب مُدخل | **لم تصل قيمة اعتماد إلى C6** | غير معتمد · مرفوض · غير قابل للاعتماد · غير منطبق |

**الاعتماد الفعلي يبقى بالكامل لدى E**, ولا يظهر في البيان إلا إن **مرّره E نفسه** — وحينها يُنقل **كما هو** بلا إعادة تفسير.

---

## 7. UPDATED OUTPUT CONTRACT — LEVEL 2

**التعديلات على §11 من R02 فقط؛ باقي البنود بلا تغيير.**

### 7.1 حقول الحالة في سجل العضو

| الحقل | الإلزام | الملاحظة |
|---|---|---|
| `source_owner` | **إلزامي** | `C3` · `C4` · `C5` |
| `source_status` | **إلزامي** | قيمة حرفية أو `NOT_SET` |
| `source_coverage` | **إلزامي** | قيمة حرفية أو `NOT_SET` |
| `source_completeness` | **إلزامي** | قيمة حرفية أو `NOT_SET` |
| `source_completeness_detail` | اختياري | قاموس C4 **كما هو** (§2.3) |
| `producer_channel` | **إلزامي** | **`NOT_SET`** حتى اعتماد DEC-C6-11 |
| `declared_class` | **إلزامي** | حقل `class` المصرَّح أو `NOT_SET` |
| `contract` · `format` | **إلزامي** | **قيم خام بلا ترجمة** |
| `approval_managed_by` · `approval_value` | **إلزامي** | §6 |
| `parent_ref` · `parent_evidence` | **إلزامي** | بلا حكم «orphan» |
| `fidelity` | **إلزامي** | دائمًا `NOT_ASSESSED` |
| `engineering_fingerprint` · `determinism` | **إلزامي** | `NOT_ISSUED` · D1 `NOT_ESTABLISHED` · D2 `NOT_CLAIMED` |
| الطوابع الثلاثة | اختياري | `production_timestamp` = `NOT_SET` اليوم |

**وأُزيل نهائيًا:** `source_status_field` — كان الآلية التي سمحت بإعادة التفسير.

### 7.2 Notices الإلزامية (محدَّثة)
`provenance ≠ fidelity` · `lineage ≠ engineering identity` · `وجود البيان ليس دليل fidelity` · **`العدّادات الثلاثة ميكانيكية ومنفصلة، ولا تُجمع`** · **`NOT_SET إعلان غياب لا حكم`** · **`revision_view بيانات مقارنة ميكانيكية لا حكم هندسي`** · **`approval_managed_by ملكية معمارية لا حالة اعتماد`**.

---

## 8. ATTACK SURFACE — تحديثات

**AS-C6-01…22 سارية**, مع إضافتين:

| # | الخطر | الحدّ |
|---|---|---|
| **AS-C6-23** ⭐ | **Field-semantics migration** — نقل قيمة حقل إلى خانة حقل آخر (`completeness` ⇒ `status`) فيبدو المنتِج مالكًا لحقل لا يملكه | §2 — ثلاثة حقول مستقلة · حذف `source_status_field` |
| **AS-C6-24** ⭐ | **Channel inference** — استنتاج القناة من اسم عقد أو صيغة أو غياب حقل | §4 — Option B · `NOT_SET` |

**وتشديد على AS-C6-21:** جمع العدّادات الثلاثة في رقم واحد = اختراع حالة مجمّعة.

---

## 9. OPEN GAPS

**لم تُغلق أي ثغرة.**

| # | الحالة |
|---|---|
| **GAP-C6-01** حالة الاكتمال غير موحّدة | 🔴 قائمة — **عولجت عرضًا لا مصدرًا** |
| **GAP-C6-02** لا هوية مخرج رسمية | 🔴 قائمة |
| **GAP-C6-03** A بلا `master_revision` بنيوي | 🔴 قائمة |
| **GAP-C6-04** لا طبقة تُنتج طابعًا زمنيًا | 🔴 قائمة |
| **GAP-C6-05** ⭐ جديدة | **لا Contract Registry معتمد** ⇒ `producer_channel` غير قابل للإثبات (§4) |
| GAP-C-01…14 · DEC-C2-01…08 · OBS-C5-01 | 🔴 كما هي |

**GAP-C6-01/03/04/05 لا تُعالَج داخل C6** — معالجتها تعني تعديل C3/C4/C5 المغلقة أو إنشاء سجل عقود، وكلاهما خارج هذه المرحلة.

---

## 10. CAPABILITY STATUS

| المستوى | الحالة |
|---|---|
| **1 — Format** | ✅ JSON · المكتبة القياسية · لا تبعية خارجية |
| **2 — Approved output contract** | 🟡 **مكتمل معماريًا (§7) — غير معتمد** |
| **3 — Runtime** | ✅ متاح — فحص لحظي (GAP-C-02 نشطة) |

---

## 11. GATE CARD — R03

| النقطة المطلوبة | الحالة |
|---|---|
| **1 — فصل status/coverage/completeness** | ✅ **ثلاثة حقول مستقلة · `source_status_field` أُزيل · +تفصيل C4** |
| **2 — producer_channel** | ✅ **Option B** — `NOT_SET` · الاستنتاج سُحب |
| **3 — revision comparison** | ✅ **mechanical comparison metadata** بمعانٍ محصورة |
| **4 — approval ownership** | ✅ **architectural ownership metadata** |
| Aggregate summary | ✅ ثلاثة عدّادات منفصلة · لا جمع |
| Output Contract L2 | ✅ محدَّث |
| Attack Surface | ✅ 24 (+2) |
| Open Gaps | ✅ بلا إغلاق · **+1 جديدة** |
| كود / tests / تعديلات | ✅ **صفر** — البصمات ثابتة · الانحدار **931/931** |

### C6 Architecture Status: 🔴 **HOLD — R03 ليست مغلقة ذاتيًا**

| # | الحاجب / المفتوح | الحالة |
|---|---|---|
| **DEC-C6-01** | هوية المخرج — لا طبقة تنتجها · الملكية **E** | 🔴 **BLOCKER** |
| **DEC-C6-09** | اعتماد Output Contract Level 2 — **صغتُه ولم أعتمده** | 🔴 **BLOCKER** |
| **DEC-C6-11** ⭐ | **Contract Registry** لإثبات `producer_channel` | 🟡 **OPEN** — لا يحجب (البديل `NOT_SET` آمن) |
| DEC-C6-05 · 06 · 07 · 08 · 10 | كما هي | 🟡 OPEN |
| DEC-C6-02 | مخفَّض — العقد صار صريحًا | 🟡 |
| DEC-C6-03 · 04 | محسومان معماريًا في R02 | 🟢 |

**اتجاه R03 — تقليص لا توسيع:** حُذف حقل (`source_status_field`) · سُحب استنتاج (القناة) · فُصلت ثلاثة عدّادات بدل واحد · وصُنِّف حقلان كبيانات وصفية لا أحكام. **ولم يُضف أي نطاق.**

**لم أكتب كودًا ولا اختبارات، ولم أمسّ Schema/Master/C1–C5، ولم أبدأ D/E ولا أي Manifest implementation.**
