# 00.5-C1 — GENERATION PRECONDITIONS — **R05**
**خريطة فقط. لا كود · لا تعديل schema · لا تعديل validator · لا تشغيل اختبارات · لا C2–C6 · لا كتابة في `project_master.json` · لا قرارات تصميمية.**

> **موضع C1:** `Pre-generation gates (A · B1…B11)` → **C1: ما الذي يجوز توليده؟** → `C2…C6 التوليد` → `B8 التحقق` → `D: QA + fidelity`
>
> **الجملة الحاكمة:** *C1 لا يولّد. يستهلك أحكام غيره ويقرر — لكل مخرج على حدة — **ما يجوز، وما يُمتنع عنه، ولماذا**.*

---

## 0. سجل التعديلات — R01 ⇒ R02

| الإصلاح | ما كان في R01 | ما صار في R02 |
|---|---|---|
| **C1-FIX-01** | `[A]` معتمد ⇒ «مسموح-بقيد» في Class A | **`[A]` معتمد = مدخل غير مؤهل لـClass A**. لا ترقية · لا وسم داخل مخرج ملزم. سُجِّل كقرار معماري مؤجل ⇒ **GAP-C-11** |
| **C1-FIX-02** | «A ممنوع لأي سبب ⇒ B ممنوع» | **أُلغيت القاعدة العامة.** فُصلت الأهلية إلى ثلاث طبقات (§4): Geometry · A-specific · B-specific. B يُمنع **فقط** إذا مسّ السبب الهندسة أو بيانات مخرج B المحدد |
| **C1-FIX-03** | A-BLK-02/03 = منع شامل | **ضُيِّقا** إلى `blocker → affected scope → affected outputs` (§3.2). المنع الشامل صار **استثناءً** مقصورًا على ما لا يملك موضعًا هندسيًا |
| **C1-FIX-04** ⭐ R03 | A-BLK-01 = «أي ERROR ⇒ شامل لأن النموذج غير سليم» | **تصحيح من blanket ERROR blocking إلى scope-aware ERROR consumption** (§3.3). ثلاث فئات: Model-wide · Output/Element-scoped · **ERROR بلا Scope موثوق ⇒ fail-closed**. C1 **لا يعيد تفسير** قاعدة المالك ولا يعيد حساب سبب الـERROR. القصور البنيوي في سلسلة `verdict → severity → scope → paths → outputs` سُجِّل كثغرة معمارية ⇒ **GAP-C-12** |

| **C1-FIX-05** ⭐ R04 | A-BLK-13 = «مدخل غير موثوق ⇒ **شامل** — تلوث مصدر» | **جعل A-BLK-13 scope-aware بدل blanket blocking** (§3.4). يُطبَّق مبدأ §3.3 نفسه: Model-wide (بحكم المالك) · Element/Path-scoped · Unproven scope ⇒ fail-closed بإعلان `UNPROVEN_SCOPE`. **C1 لا يقرر بنفسه أن `AI_IMAGE` أو معيارًا غير معتمد Model-wide** |

| **C1-FIX-06** ⭐ R05 | A-BLK-12 = «عدم تطابق `revision`/`geometry_version` ⇒ **شامل** (AS-09)» · وA-BLK-09 = «شامل للهندسة» | **جعل A-BLK-12 scope-aware بدل blanket blocking، مع مراجعة صياغة A-BLK-09 دون توسيع النطاق** (§3.5 و§3.6). **تصحيح إسناد:** مالك حكم الـstale output هو **B1/XR-017** لا B9؛ وحكمه **مُعلَّق بمخرج بعينه** ⇒ الحالة الطبيعية **②**. ما يخص التغيير المشترك (INV-TF2) ملك **A**. ⇒ **GAP-C-13** |

**لم يُعدَّل قرار C-STEP-01 R02. لم تُعَد قراءة قواعد A/B-series. لم تُغلق أي ثغرة.**

---

## 1. المبدأ المؤسِّس

C1 **بوابة امتناع**, لا محرك فحص.

| C1 **يفعل** | C1 **لا يفعل** |
|---|---|
| يستهلك أحكامًا جاهزة من A/B1–B11 | يعيد تنفيذ أي قاعدة منها |
| يترجم الحكم إلى **قرار توليد لكل مخرج** | يصدر حكمًا هندسيًا أو تصميميًا |
| يحدّد **نطاق** أثر الحاجب | **يعيد حساب سبب** الحاجب |
| يعلن العجز صراحة | يخترع قيمة أو نطاقًا |

**قاعدة عدم التكرار:** ما أجابت عنه A أو B1–B11 **يُستدعى ولا يُعاد حسابه** — وإلا خرق One Rule → One Owner.

---

## 2. ما يستهلكه C1 — واجهات موجودة فعلًا (مُتحقَّق منها تنفيذيًا)

| الواجهة | المصدر | ما يعطيه C1 |
|---|---|---|
| `final_approval_readiness(data)` → `(ready, blockers)` | **A** | قائمة حواجب **كنصوص حرة** |
| `readiness_report(data)` | **B7** | readiness + حواجب A + أخطاء B7 + `unknown_impact` |
| `build_uncertainty_map(data)` → `{path: (سبب, شرح)}` | **B7** | **مسار صريح** لكل لايقين: `UNKNOWN`/`PROPOSED`/`UNAPPROVED_ASSUMPTION`/`UNCOMPUTABLE` + انتشار عبر `derived_from` |
| `classify_unknowns(data)` | **B7** | `BLOCKS_PROJECT` · `AFFECTS_OUTPUT` · `DECLARED_ONLY` · `OUT_OF_SCOPE` |
| `presence_register` | A (INV-S1) | `PRESENT`/`NOT_PRESENT`/`UNKNOWN` لكل عنصر |
| verdicts `[D]` | **B6** | `VERIFIED`/`UNCOMPUTABLE` — **لا يعيد C1 الحساب** |
| `validate_*` ×12 | A/B1–B11 | `ok` لكل طبقة |

**الأداة الحاسمة لـFIX-03:** `build_uncertainty_map` و`presence_register` **يعطيان مسارًا**, بخلاف قائمة حواجب A النصية. هذا ما يجعل التضييق ممكنًا **بلا اختراع**.

---

## 3. الشروط المانعة و**نطاق** كل منها

### 3.1 جدول الشروط

| # | الشرط | المصدر | **النطاق** |
|---|---|---|---|
| **A-BLK-01** | ERROR من A أو B1–B11 | الطبقات | **حسب §3.3** — لا يُفترض أنه شامل (**FIX-04**) |
| **A-BLK-02** | حاجب معلن من A | A | **حسب §3.2** (لم يعد شاملًا) |
| **A-BLK-03** | مجهول `BLOCKS_PROJECT` | B7 | **حسب §3.2** (لم يعد شاملًا) |
| **A-BLK-04** | `presence = UNKNOWN` | A/INV-S1 | المخرجات التي يدخل فيها ذلك العنصر |
| **A-BLK-05** | حقل لازم `[U]` | B7 | المخرجات المستهلِكة للحقل |
| **A-BLK-06** | حقل لازم `[P]` أو `[A]` | B7 | المخرجات المستهلِكة — **بلا استثناء لـ`[A]` المعتمد (FIX-01)** |
| **A-BLK-07** | `[D]` لازم `UNCOMPUTABLE` | B6 | المخرجات المستهلِكة |
| **A-BLK-08** | تبعية غير محلولة عبر `derived_from` | B7 | المخرجات المستهلِكة للمشتق |
| **A-BLK-09** | هندسة لازمة غائبة | البيانات | **حسب §3.6** — بقدر اشتراك الهندسة الناقصة، لا بنوعها (**FIX-06**) |
| **A-BLK-10** | لا كاميرا | `cameras[]` | **المناظير فقط** |
| **A-BLK-11** | قدرة تقنية غائبة | البيئة | المخرجات بذلك الصيغة فقط |
| **A-BLK-12** | عدم تطابق `revision`/`geometry_version` | **B1/XR-017 · A/INV-TF2** | **حسب §3.5** — لا يُفترض أنه شامل (**FIX-06**) |
| **A-BLK-13** | مدخل غير موثوق (`AI_IMAGE`/معيار غير معتمد) | A/B6/B8 | **حسب §3.4** — لا يُفترض أنه شامل (**FIX-05**) |

### 3.2 Scope Resolution — `blocker → scope → outputs` (**FIX-03**)

C1 **لا يعيد حساب** سبب الحاجب؛ يحدد **قابلية انطباقه** فقط. وقابلية الانطباق تُشتق من موضع مثبت:

| نوع الحاجب | هل له موضع؟ | كيف يُشتق **بلا اختراع** | الأثر |
|---|---|---|---|
| `BLOCKING UNKNOWN U-xxx` | ✅ | الحقيقة التي تستشهد بـ`unknown_id` تعطي المسار | يمنع مستهلكي **ذلك المسار** |
| `PRESENCE UNKNOWN <key>` | ✅ | المفتاح **هو** العنصر | يمنع مستهلكي **ذلك العنصر** |
| `KNOWN CONFLICT at <path>` | ✅ | المسار مُصرَّح به | يمنع مستهلكي المسار |
| `UNAPPROVED_ASSUMPTION` / `PROPOSED` | ✅ | مسار من `build_uncertainty_map` | يمنع مستهلكي المسار |
| `ASSUMPTION PENDING` | ⚠️ جزئي | إن استشهدت به حقيقة ⇒ مسار؛ وإلا **لا موضع** | مسار ⇒ جزئي · بلا مسار ⇒ **شامل** |
| `CHANGE REQUEST DRAFT/PENDING` | ❌ لـC1 | نطاق CR **ملك B1** | **شامل** حتى يصرّح مالكه |
| `OBJECTION OPEN` | ❌ | اعتراض حوكمي بلا موضع هندسي | **شامل** |
| `GAP-02` (اعتمادات بلا سجل) | ❌ | حوكمي ⇒ 00.5-E | **شامل على المخرجات الملزِمة** |

**قاعدة الأمان (fail-closed):** إذا **تعذّر** إثبات النطاق من بيانات موجودة ⇒ **يبقى الحاجب شاملًا**. C1 **ممنوع** أن يخمّن نطاقًا ليضيّق منعًا. التضييق يحتاج دليلًا؛ الغموض يعني منعًا.

**دليل تنفيذي (القالب):** 12/12 حقيقة غير يقينية ترتبط بمجهولها بمسار صريح (`U-001 → space/outline` … `U-012 → presence_register/circulation`) ⇒ **التضييق قابل للتنفيذ فعليًا** لهذه الفئة، لا نظريًا.

**الحد المتبقي:** لا توجد بنية رسمية `output → required_paths` معتمدة. §6 يقترحها، وهي **بحاجة اعتماد** ⇒ **GAP-C-10**.

---

### 3.3 استهلاك الـERROR وفق النطاق — **FIX-04**

**أُلغي الافتراض:** «أي ERROR من أي طبقة ⇒ النموذج غير سليم ⇒ منع شامل». هذا الافتراض كان يعيد إنتاج Over-blocking الذي عالجه FIX-03، ويجعل خطأً في عنصر أثاث واحد يمنع كل مخرجات المشروع.

#### الفئات الثلاث

| الفئة | التعريف | الشرط | الأثر |
|---|---|---|---|
| **① Model-wide ERROR** | حكم الطبقة **نفسه** يثبت أن النموذج أو حقيقة مشتركة **غير صالحة للاستهلاك** | أن يكون الاتساع من **حكم المالك**, لا من استنتاج C1 | ⛔ **شامل** |
| **② Output/Element-scoped ERROR** | الحكم يخص **عنصرًا أو مسارًا أو مخرجًا محددًا** | وجود موضع صريح في الحكم | ⛔ **المخرجات المتأثرة فقط** |
| **③ ERROR بلا Scope موثوق** | لا يمكن إثبات الاتساع ولا الضيق من الحكم | — | ⛔ **fail-closed: يبقى شاملًا** · **ممنوع اختراع نطاق لتضييقه** |

#### حدود دور C1 (صريحة)

- C1 **لا يعيد تفسير** قواعد B1–B11، و**لا يعيد حساب** سبب الـERROR، و**لا يستنتج** خطورته.
- **المالك السابق للقاعدة** هو من يحدد `severity` + `scope` + `affected path/element` — **إن كانت واجهته توفرها**.
- C1 يستهلك الحكم كما هو ويترجمه إلى **أهلية مخرج** فحسب.
- **الفئة ① لا يقررها C1.** إن لم يكن الاتساع صادرًا عن المالك، فالحالة **③ لا ①** — والنتيجة عمليًا منع شامل، لكن **بتكييف صادق**: «نطاق غير مثبت»، لا «النموذج غير سليم».

**تمييز جوهري:** ① و③ يؤديان إلى المنع الشامل نفسه، لكن **سببهما مختلف** ويجب ألا يُخلطا في إعلان الامتناع (§7). ① حكم مُثبت؛ ③ **نقص معلومات** — وهو مؤشر على ثغرة GAP-C-12 لا على فساد النموذج.

#### العلاقة المفاهيمية المطلوبة ومدى توفرها فعليًا

`validator verdict → severity → scope → affected paths/elements → affected outputs`

**ما تم التحقق منه بقراءة ساكنة للمحركات (بلا تنفيذ):**

| الحلقة | الحالة | الدليل |
|---|---|---|
| `verdict` | ✅ متوفرة | كل محرك يعيد findings |
| `severity` | ✅ متوفرة | حقل صريح في `Finding`؛ القيم المستعملة: `ERROR` · `WARN` · `INFO` · `UNKNOWN` |
| **`scope`** | ❌ **غير موجود** | **لا يوجد أي حقل `scope` في أي محرك** — الكلمة لا ترد إلا في تعليقات توثيقية |
| `affected paths` | ⚠️ **جزئية وغير مضمونة** | `Finding.location` موجود، لكنه **أحيانًا مسار دقيق** (`meta/frozen_on`) و**أحيانًا نطاق عريض** (`meta`, `registers`) و**أحيانًا نص وصفي مركّب** (`registers/proposals/{i} (ID)`) ⇒ **غير قابل للاعتماد آليًا كمفتاح نطاق** |
| `affected outputs` | ❌ غير موجودة | لا محرك يربط حكمًا بمخرج (وهو GAP-C-10) |

**الخلاصة البنيوية:** بنية `Finding` الحالية — `rule` · `location` · `message` · `severity` — **لا توفر العلاقة رسميًا**. فالتصنيف بين ① و② **غير محسوم آليًا اليوم** لمعظم الأحكام.

**القرار المترتب (بلا اختراع):** حتى تُعتمد هذه العلاقة، **الحالة الافتراضية لكل ERROR هي ③ fail-closed** ما لم يُصرّح مالك القاعدة بنطاق. C1 **لا يسدّ** هذا النقص بتخمين، ولا يضيف حقولًا للمحركات (خارج ملكيته) ⇒ سُجِّل في **GAP-C-12**.

---

### 3.4 المصادر غير الموثوقة وفق النطاق — **FIX-05**

**أُلغي الافتراض:** «وجود `AI_IMAGE` أو معيار غير معتمد في أي موضع ⇒ تلوث المشروع كله ⇒ منع كل Class A». هذا يعيد Over-blocking: صورة مزاجية مرتبطة بلوحة واحدة كانت تمنع مسقطًا لا صلة له بها.

#### تطبيق مبدأ §3.3 على المصدر

| الفئة | التعريف | الأثر |
|---|---|---|
| **① Model-wide** | **مالك القاعدة يصرّح** بأن المصدر غير الموثوق **يلوث حقيقة أو نموذجًا مشتركًا** | ⛔ **شامل** |
| **② Element/Path-scoped** | المصدر مرتبط بعنصر أو مسار محدد | ⛔ **فقط المخرجات المستهلِكة لذلك المصدر** |
| **③ Unproven scope** | تعذّر إثبات نطاق المصدر | ⛔ **fail-closed** + إعلان **`UNPROVEN_SCOPE`** · **بلا اختراع نطاق** |

#### لماذا الفئة ② هي الغالبة هنا — علاقة نطاق موثوقة

بخلاف حالة ERROR العامة (§3.3) التي يعوقها غياب `scope`، **المصدر غير الموثوق يملك موضعًا بنيويًا**:

- `source_type` **حقل على مستوى الحقيقة (fact) نفسها**, ضمن enum معتمد يشمل `AI_IMAGE` و`EXTERNAL_STANDARD` و`EXTERNAL_LIBRARY` و`AGENT_PROPOSAL` و`AGENT_ASSUMPTION` و`NOT_PROVIDED`.
- ومن ثمّ فكل مصدر غير موثوق **مُعلَّق بمسار الحقيقة التي يحمله**، وهي **علاقة نطاق موثوقة** لا تخمين فيها.
- الحكم المالك المعني (تلوث عكسي) **يُصدر الموضع مع حكمه** — أي أن الموضع **يأتي من المالك**، ولا يستنبطه C1.

⇒ الحالة الطبيعية لمصدر غير موثوق هي **② Path-scoped**، والمنع الشامل **استثناء يحتاج تصريح مالك**.

#### حدود دور C1 (صريحة)

- **C1 لا يقرر أن `AI_IMAGE` أو معيارًا غير معتمد «Model-wide».** الاتساع يأتي **من المالك/الحكم** أو من **علاقة نطاق موثوقة**، وإلا فالحالة **③ لا ①**.
- C1 **لا يعيد تفسير** قواعد A أو B-series، ولا يعيد تصنيف `source_type`، ولا يقرر أي المصادر «موثوق» — هذا التصنيف **ملك مالكيه**.
- C1 **لا يرقّي** ولا يخفّف: مصدر غير موثوق لا يصبح مقبولًا لأن المخرج مهم.
- **الانتشار ليس اجتهادًا:** إن غذّى مصدر غير موثوق قيمة مشتقة، فالامتداد يُؤخذ من **انتشار `derived_from`** المملوك لـB7 — **لا** من تقدير C1.

**ملاحظة تكامل:** غياب حقل `scope` (GAP-C-12) يظل قائمًا هنا أيضًا؛ ما يخفف أثره في هذه الحالة تحديدًا هو **ارتباط `source_type` بالمسار بنيويًا**. هذا **لا يغلق GAP-C-12 ولا يعدّله**.

#### حدّ صريح متبقٍ

ربط «المسار الملوث» بـ«المخرجات المستهلِكة له» يظل محكومًا بغياب بنية `output → required_paths` ⇒ **GAP-C-10** (مفتوحة). فحيث يتعذّر إثبات أي المخرجات تستهلك المسار، **يبقى المنع أوسع احتياطًا وفق ③**، لا أضيق.

---

### 3.5 عدم تطابق الإصدار/الهندسة وفق النطاق — **FIX-06**

**أُلغي الافتراض:** «أي عدم تطابق `revision`/`geometry_version` ⇒ منع شامل». المنع الشامل يجب أن يصدر عن **حكم مالك** أو **إثبات بنيوي**، وإلا فهو `UNPROVEN_SCOPE`.

#### تصحيح إسناد الملكية (نتيجة فحص هذه الجولة)

نسبت الإصدارات السابقة A-BLK-12 إلى **B9**. الفحص أظهر أن ذلك **غير دقيق**:

| الحكم | المالك الفعلي | الموضع الذي يصدره |
|---|---|---|
| بصمة مخرج تخالف `geometry_version` الماستر («View is from a stale model») | **B1 / XR-017** | `outputs/{i} (output_id)` — **مخرج بعينه** |
| بصمة تستشهد بـ`revision` غير موجودة · `project_id` مخالف | **B1 / XR-017** | `outputs/{i} (output_id)` |
| `TECHNICAL_FIX` غيّر `geometry_version` | **A / INV-TF2** | `registers/changelog/{i}` |
| تناسق changelog مقابل `meta` | **B9** | حسب حكمه |

**الأثر المعماري:** حكم الـstale output **مُعلَّق ببصمة مخرج واحد**، لا بالنموذج. ⇒ **الحالة الطبيعية لـA-BLK-12 هي ② Output-scoped**، والشمول استثناء.

#### الفئات الثلاث

| الفئة | الشرط | الأثر |
|---|---|---|
| **① Model-wide mismatch** | **مالك الحكم نفسه يثبت** أن الـmismatch يمس Master/Geometry المشترك أو حقيقة مشتركة لازمة لكل المخرجات — مثل حكم A بأن تغييرًا غير مشروع مسّ `geometry_version` المشترك | ⛔ **شامل** |
| **② Element/Path/Output-scoped** | الـmismatch مرتبط بعنصر أو مسار أو **نسخة مخرج محددة** — وهو النمط الذي يصدره XR-017 | ⛔ **المخرجات المستهلِكة لذلك المسار فقط** · مخرج قديم البصمة **لا يمنع** توليد غيره |
| **③ بلا Scope موثوق** | تعذّر إثبات الاتساع أو الضيق | ⛔ **fail-closed** + **`UNPROVEN_SCOPE`** · **ممنوع** اختراع النطاق أو تحويله تلقائيًا إلى «النموذج غير صالح» |

#### حدود دور C1

- C1 **لا يعيد تفسير B9** ولا يعيد حساب revision integrity، و**لا يقرر بنفسه معنى mismatch**.
- يستهلك `verdict → severity → scope/path` فقط.
- **تنبيه ترتيبي:** حكم XR-017 يخص **مخرجًا موجودًا سلفًا**؛ وC1 بوابة **ما قبل** التوليد. فبصمة قديمة على مخرج سابق **دليل على قدم ذلك المخرج**, وليست بذاتها منعًا لإعادة توليده — بل غالبًا **سبب لإعادة التوليد**. تحويلها إلى منع شامل يخلط بين *فساد النموذج* و*قِدَم أثر*.

**الحد البنيوي:** بنية `Finding` في B1/B9 لا تحمل حقل `scope` (كما في GAP-C-12)؛ ما يسمح بالتضييق هنا هو أن `location` في XR-017 **مُعلَّق بمخرج مُعرَّف بنيويًا**. أما ما لا يصرّح بموضعه فيبقى ③. وتمييز «أي المخرجات تستهلك المسار» يظل محكومًا بـ**GAP-C-10** ⇒ سُجِّل اللبس في الإسناد والنطاق تحت **GAP-C-13**.

---

### 3.6 مراجعة صياغة A-BLK-09 — **بلا توسيع نطاق**

**الصياغة السابقة** («شامل للهندسة إن كان `outline`/الجدران») كانت توحي بأن **أي** مشكلة في **أي** جدار تمنع كل المخرجات. **التصحيح صياغي لا توسيعي:** المعيار هو **قدر اشتراك الهندسة الناقصة**، لا نوعها.

| الحالة | الأثر |
|---|---|
| هندسة **مشتركة فعلًا** يحتاجها كل مخرج (مثل غياب `outline` كليًا بحيث لا يقوم أي مخرج) | ⛔ **A وB المتأثران** — وفق §4-① |
| **عنصر هندسي محدد** (جدار بعينه · فتحة بعينها · قطعة أثاث) | ⛔ **مخرجاته المستهلِكة فقط** |
| النطاق **غير مثبت** | ⛔ **`UNPROVEN_SCOPE` / fail-closed** |

**تمييز لازم:** «الهندسة الأم» وصف **لدور** المسار في المخرج المعني، لا تصنيف ثابت لاسم الحقل. وC1 **لا يقرر** أي عنصر «مشترك» بالاجتهاد؛ يستند إلى ما يثبته المالك أو إلى علاقة نطاق موثوقة، وإلا فـ③.

---

## 4. طبقات الأهلية الثلاث (**FIX-02**)

**أُلغيت** قاعدة «A ممنوع ⇒ B ممنوع».

| الطبقة | السؤال | الأثر على B |
|---|---|---|
| **① Geometry Eligibility** | هل الهندسة الأساسية المشتركة صالحة؟ | ⛔ **فشلها يمنع A وB معًا** — لأن المصدر واحد |
| **② A Output-Specific** | هل يملك **هذا المخرج A** ما يلزمه؟ | ⟶ **لا أثر على B** ما لم يشترك في النقص |
| **③ B Presentation-Specific** | هل تتوفر معاملات العرض والقدرة؟ | ⛔ يمنع **مخرج B المعني** وحده |

**القاعدة المعتمدة:**
> **يُمنع Class B فقط إذا كان سبب المنع يؤثر فعليًا على الهندسة أو البيانات التي يحتاجها مخرج B المحدد.**

| المثال | A | B |
|---|---|---|
| لا `cameras[]` | ⛔ Perspective A | ✅ **Floor Plan B جائز** |
| `ceiling_height` = `[U]` | ⛔ Section/Elevation A | ✅ **Floor Plan B جائز** |
| `space/outline` = `[U]` كليًا | ⛔ الكل | ⛔ **الكل** — هندسة مشتركة يحتاجها كل مخرج (§3.6) |
| خلل في **جدار بعينه** | ⛔ مستهلكوه | ⛔ مستهلكوه — **لا منع شامل** (§3.6) |
| بصمة مخرج سابق قديمة (XR-017) | ⛔ **ذلك المخرج** — ولا يمنع توليد غيره | ⛔ نفسه (§3.5) |
| نقص عنصر لا يدخل المخرج | لا أثر | لا أثر |

**ما يبقى ساريًا بلا تخفيف:**
- **`MASTER GEOMETRY → A/B` من مصدر واحد.** B **لا** ينشئ هندسة بديلة ولا يعدّل إحداثيات أو أبعادًا أو IDs.
- **اختبار أهلية المعامل:** لو غُيِّر المعامل، هل تتغير هندسة مستخرَجة من الملف؟ **نعم ⇒ محظور.**
- **لا استبدال صامت:** لا يُقدَّم مخرج B **مكان** مخرج A ممنوع كأنه يفي بالطلب. جواز B مستقل ≠ تعويض عن A. (AS-C1-02، أُعيدت صياغته)

**Class C (AI Mood):** معزول. لا يخضع لـ§3 لأنه لا يدّعي هندسة، و**ممنوع** أن يغذّي A/B أو يُشتق منه رقم.

**تفريق لازم بعد FIX-05:** **وجود** مخرج Class C في المشروع **ليس** بذاته تلويثًا ولا يمنع شيئًا — العزل هو الوضع الطبيعي. المنع يبدأ فقط حين **يُستهلك** مصدر غير موثوق داخل حقيقة تغذّي A/B، وحينها يكون النطاق **مسار تلك الحقيقة** (§3.4-②).

---

## 5. مصفوفة الحالات

| الحالة | Class A | Class B | القاعدة |
|---|---|---|---|
| **`[C]`** | ✅ | ✅ | — |
| **`[D]` `VERIFIED`** | ✅ | ✅ | بحكم B6 وحده |
| **`[D]` `UNCOMPUTABLE`** | ⛔ | ⛔ | A-BLK-07 |
| **`[U]`** | ⛔ مستهلكوه | ⛔ مستهلكوه | يبقى `[U]` — لا تقدير |
| **`[A]` غير معتمد** | ⛔ | ⛔ | A-BLK-06 |
| **`[A]` معتمد** | ⛔ **غير مؤهل (FIX-01)** | ⛔ لمستهلكيه | **اعتماد المستخدم لا يحوّل الافتراض إلى حقيقة هندسية** ⇒ **GAP-C-11** |
| **`[P]`** | ⛔ | ⛔ | Proposal ≠ Decision |
| **blocker** | حسب §3.2 | حسب §4 | لا منع شامل تلقائي |
| **dependency غير محلولة** | ⛔ | ⛔ | انتشار B7 |
| **missing geometry** | ⛔ + تصريح | ⛔ إن كانت مشتركة | لا اختلاق |
| **missing cameras** | ⛔ مناظير | ✅ لغير المعتمِد | FIX-02 |
| **missing capability** | ⛔ بالصيغة | ⛔ بالصيغة | GAP-C-02 |
| **مصدر غير موثوق** (`AI_IMAGE`/معيار غير معتمد) | ⛔ **مستهلكو المسار الملوث فقط** — لا منع شامل تلقائي | ⛔ نفسه | **§3.4 (FIX-05)** · الشمول يحتاج تصريح مالك · تعذّر الإثبات ⇒ `UNPROVEN_SCOPE` |

**أربع قواعد لا تُخترق:**
1. **لا ترقية:** `[U]`/`[P]`/`[A]` — **ولو معتمدًا** — لا تصبح `[C]`/`[D]`.
2. **لا افتراضي صامت:** الغياب ⇒ امتناع، لا `0` ولا CENTER.
3. **التمييز الثلاثي:** `NOT_PRESENT` **حقيقة صالحة**؛ `UNKNOWN` **مانع**.
4. **التضييق يحتاج دليلًا؛** الغموض يعني منعًا (§3.2).

---

## 6. OUTPUT CAPABILITY MATRIX

**القدرة والبيانات شرطان مستقلان.** عمود «يحتاج» هو **أساس Scope Resolution** — ومقترح بانتظار اعتماد (**GAP-C-10**).

| المخرج | الفئة | يحتاج | القدرة |
|---|---|---|---|
| **Floor Plan (DXF)** | A | outline · walls · openings | ⚠️ `ezdxf` غير محفوظة (GAP-C-02) |
| **Floor Plan (SVG/PDF)** | A | نفسه | ✅ matplotlib |
| **RCP** | A | + `ceiling_height` + lighting | ⚠️ GAP-C-05 |
| **Elevations / Sections** | A | + `ceiling_height` + ارتفاعات | ✅ |
| **Furniture Plan** | A | + furniture (`position_reference` · `rotation`) | ✅ |
| **Circulation Plan** | A | + circulation | ✅ |
| **GLB / OBJ** | A | هندسة ثلاثية | ⚠️ `trimesh` غير محفوظة |
| **مناظير محسوبة** | A | + `cameras[]` (**فارغة في القالب**) | ✅ حسابيًا |
| **Quantity Schedule** | A (مرشّح) | عناصر بمعرّفات | ✅ — بانتظار GAP-C-08 |
| **Material/Finish Schedule** | **غير محسوم** | مواد معتمدة | GAP-C-08 |
| **BOQ كامل** | **ليست A** | + هدر + تسعير | اجتهاد ⇒ خارج A |
| **لوحات عرض** | B | هندسة A **المشتركة** + معاملات عرض | ⚠️ `python-pptx` غير متاحة |
| **AI Mood** | C | — | معزول · غير ملزِم |
| **رندر فيزيائي** | — | — | ❌ مستحيل (GAP-C-01) |

---

## 7. FAILURE / ABSTENTION CONTRACT

**الامتناع نتيجة صحيحة. الفشل هو التوليد رغم عدم الجواز — أو المنع بلا سبب منطبق.**

| الحقل | المحتوى |
|---|---|
| المخرج | مُعرَّف بدقة |
| القرار | `GENERATED` · `ABSTAINED` |
| السبب | رمز A-BLK-xx |
| **النطاق** | الحاجب + **لماذا ينطبق على هذا المخرج تحديدًا** |
| **أساس النطاق** | `OWNER_DECLARED` (حكم المالك) · `PATH_DERIVED` (مسار مثبت) · **`UNPROVEN_SCOPE`** (fail-closed §3.3-③) |
| المصدر | الطبقة المُصدِرة (لا رأي C1) |
| المسار المتأثر | تحديد دقيق |
| لرفع المنع | سؤال محدد قابل للإجابة |

**محظورات:**
- ⛔ ملف «جزئي» يبدو مكتملًا.
- ⛔ استبدال A ممنوع بـB **بصمت**.
- ⛔ «لا توجد أخطاء» لإخفاء امتناع.
- ⛔ تحويل الامتناع إلى اقتراح قيمة.
- ⛔ **منع مخرج بحاجب لا ينطبق عليه** (FIX-02/03).
- ⛔ **تخمين نطاق** لتبرير تضييق.
- ⛔ **تقديم منع من نوع ③ على أنه ①** — أي الإيحاء بأن «النموذج غير سليم» بينما الحقيقة أن **النطاق غير مثبت** (§3.3).
- ✅ التمييز: منع بيانات · منع قدرة · منع حوكمة · **منع لنقص معلومات النطاق**.

**حالة `GENERATED_WITH_DECLARED_LIMITATIONS`:** أُزيلت من مسار Class A تبعًا لـFIX-01 — لا مخرج ملزم بقيود معلنة على مدخل غير مؤهل. بقاؤها لفئات أخرى **قرار مؤجل** ضمن GAP-C-11.

---

## 8. ATTACK SURFACE — C1

| # | الخطر |
|---|---|
| **AS-C1-01** | **Gate bypass** — توليد رغم حاجب منطبق |
| **AS-C1-02** | **Silent substitution** — تقديم B مكان A ممنوع كأنه يفي بالطلب |
| **AS-C1-03** | **Partial-output illusion** |
| **AS-C1-04** | **Rule re-implementation** — C1 يعيد الفحص |
| **AS-C1-05** | **Stale judgement** (AS-09) |
| **AS-C1-06** | **Over-blocking** — حاجب غير منطبق يشلّ مخرجات سليمة |
| **AS-C1-07** | **Promotion at the gate** — `[A]` معتمد يُعامل كـ`[C]` |
| **AS-C1-08** | **Capability bluffing** |
| **AS-C1-09** | **Scope creep** — C1 يقرر ما **يجب** توليده |
| **AS-C1-10** | **Presence confusion** — `UNKNOWN` كـ`NOT_PRESENT` |
| **AS-C1-11** | **Scope invention** ⭐ — تخمين نطاق حاجب لتبرير تضييق (خطر **مستحدث بـFIX-03**) |
| **AS-C1-12** | **Under-blocking by narrowing** ⭐ — تضييق حاجب حوكمي بلا موضع فيمرّ مخرج ملزم |
| **AS-C1-13** | **Severity re-interpretation** ⭐ — C1 يعيد تقدير خطورة ERROR أو يخفضه لتمرير مخرج (خرق ملكية · **FIX-04**) |
| **AS-C1-14** | **False model-wide claim** ⭐ — تصنيف ERROR كـ①Model-wide بلا حكم مالك، فيتحول نقص معلومات إلى اتهام للنموذج ويخفي GAP-C-12 |
| **AS-C1-15** | **Contamination over-reach** ⭐ — مصدر غير موثوق في مسار واحد يُستعمل لمنع مخرجات لا تستهلكه (**FIX-05**) |
| **AS-C1-16** | **Contamination under-reach** ⭐ — تضييق التلوث إلى مسار واحد بينما انتشر عبر `derived_from`, فيمرّ مخرج ملزم مبني على قيمة ملوثة |
| **AS-C1-17** | **Staleness over-reach** ⭐ — بصمة قديمة على مخرج واحد تُستعمل لمنع توليد كل المخرجات (**FIX-06**) |
| **AS-C1-18** | **Owner misattribution** ⭐ — نسبة حكم إلى المالك الخطأ (كنسبة XR-017 إلى B9)، فيُبنى نطاق على مالك لا يملك الحكم أصلًا |

**التوتر المقصود:** AS-C1-01/11/12 (تشدد) مقابل AS-C1-06 (تساهل). **الحكَم الوحيد: هل يدخل الحقل فعلًا في هذا المخرج، وهل النطاق مُثبت؟**

---

## 9. قواعد الاختبار المطلوبة لاحقًا (توصيف — **لا تنفيذ**)

| النوع | ما يجب إثباته |
|---|---|
| **Positive** | نموذج كامل ⇒ يُسمح · بلا كاميرات ⇒ يُسمح بالمسقط ويُمنع المنظور |
| **Negative** | لكل A-BLK-01…13 حالة تثبت أن **الشرط المقصود بعينه** أطلق المنع |
| **Scope (FIX-03)** | حاجب في `cameras` **لا** يمنع المسقط · حاجب في `outline` **يمنع** الكل · حاجب بلا موضع (OBJECTION/GAP-02) **يبقى شاملًا** |
| **B-independence (FIX-02)** | منع Perspective A **لا** يمنع Floor Plan B · ونقص الهندسة المشتركة **يمنع** الاثنين |
| **`[A]` (FIX-01)** | `[A]` معتمد ⇒ **منع Class A** — واختبار سلبي يثبت أن الرفض سببه عدم الأهلية لا شيء آخر |
| **ERROR scope (FIX-04)** | ERROR مصرَّح باتساعه من المالك ⇒ منع شامل (①) · ERROR بموضع عنصري ⇒ يمنع مستهلكيه فقط ولا يمنع مخرجًا غير متصل (②) · **ERROR بلا نطاق موثوق ⇒ منع شامل مع تكييف `UNPROVEN_SCOPE` لا «نموذج غير سليم»** (③) · واختبار يثبت أن C1 **لم يغيّر** `severity` الوارد |
| **Untrusted source scope (FIX-05)** | `AI_IMAGE` في مسار واحد ⇒ يمنع مستهلكيه **ولا** يمنع مخرجًا غير متصل (ضد AS-C1-15) · مصدر غير موثوق منتشر عبر `derived_from` ⇒ **يمنع المشتقات أيضًا** (ضد AS-C1-16) · وجود مخرج Class C معزول ⇒ **لا يمنع شيئًا** · تعذّر إثبات النطاق ⇒ منع مع `UNPROVEN_SCOPE` لا مع ادعاء تلوث شامل |
| **Version mismatch scope (FIX-06)** | بصمة قديمة على مخرج واحد ⇒ تمنع **ذلك المخرج** ولا تمنع توليد غيره (ضد AS-C1-17) · حكم مالك يثبت مساس الهندسة المشتركة ⇒ منع شامل (①) · mismatch بلا موضع ⇒ `UNPROVEN_SCOPE` لا «نموذج غير صالح» · واختبار يثبت أن C1 **لم يعد حساب** revision integrity |
| **Geometry scope (A-BLK-09)** | غياب `outline` كليًا ⇒ يمنع الكل · خلل في **جدار بعينه** ⇒ يمنع مستهلكيه فقط ولا يمنع مخرجًا غير متصل · نطاق غير مثبت ⇒ fail-closed |
| **Boundary** | `NOT_PRESENT` يمرّ · `UNKNOWN` يمنع · `OUT_OF_SCOPE` لا يمنع |
| **Isolation** | C1 لا يصدر رمز قاعدة من A/B1–B11 · حذف محرك سابق **يُعطِّل** قرار C1 لا يلتف عليه |
| **Mutation** | تعطيل استهلاك B7 ⇒ سقوط · قلب شرط ⇒ سقوط · **تحويل منع إلى تحذير ⇒ سقوط** · **توسيع نطاق حاجب ضيق ⇒ سقوط** |
| **Anti-smuggling** | لا تهريب `[U]` عبر `derived_from` · لا ترقية · لا B بديلًا صامتًا · لا افتراضي |
| **No-write** | `project_master.json` **غير متغير** بعد C1 (مقارنة بايتية) |

**معيار العبور المستقبلي:** + Full Regression `A 87 · B1 38 · B2 39 · B3 54 · B4 38 · B5 49 · B6 61 · B7 48 · B8 45 · B9 40 · B10 47 · B11 45` بلا انحدار.

---

## 10. الثغرات — **مفتوحة**

| # | الحالة |
|---|---|
| **GAP-C-02** مكتبات غير محفوظة | 🔴 مفتوحة |
| **GAP-C-04** Canonicalization/Fingerprint | 🔴 مفتوحة (C2) |
| **GAP-C-06** ملكية `outputs[]` | 🔴 مفتوحة ⇒ 00.5-E |
| **GAP-C-08** حجية الجداول | 🔴 مفتوحة (C4) |
| **GAP-C-09** Presentation Parameters | 🔴 مفتوحة (C5) |
| **GAP-C-10** — لا بنية معتمدة `output → required_paths`؛ بدونها يبقى Scope Resolution جزئيًا وبعض الحواجب شاملة احتياطًا. **(R03: نطاقها كما هو — تخص الطرف الأخير `paths → outputs`؛ الطرف الأول `verdict → scope` صار GAP-C-12)** | 🔴 مفتوحة |
| **GAP-C-11** ⭐ **جديدة** — **Decision Pending:** هل يصلح `[A]` **معتمد** مدخلًا لأي فئة مخرجات، وبأي وسم؟ الحكم الحالي: **غير مؤهل لـA** | 🔴 مفتوحة — قرار معماري |
| **GAP-C-12** ⭐ **جديدة (R03)** — **ثغرة معمارية:** العلاقة `validator verdict → severity → scope → affected paths/elements → affected outputs` **غير موفَّرة رسميًا**. بنية `Finding` = `rule`/`location`/`message`/`severity` فقط · **لا حقل `scope` في أي محرك** · `location` غير موحّد (مسار دقيق تارة ونطاق عريض أو نص مركّب تارة) ⇒ التمييز الآلي بين ①/② متعذر اليوم، والافتراضي fail-closed. **المعالجة خارج ملكية C1** (تمسّ المحركات/الحوكمة) | 🔴 مفتوحة |
| **GAP-C-13** ⭐ **جديدة (R05)** — **ثغرة معمارية في نطاق الـmismatch:** حكم «المخرج من نموذج قديم» يملكه **B1/XR-017** (موضعه مخرج بعينه) بينما `geometry_version` المشترك تحكمه **A/INV-TF2**، و**B9** يعنى بتناسق الـchangelog — ولا توجد جهة تصرّح بأن mismatch معيّن **Model-wide**. البنية لا توفر `scope` رسميًا (كـGAP-C-12) ⇒ التصنيف ①/② متعذر آليًا والافتراضي fail-closed. **المعالجة خارج ملكية C1** | 🔴 مفتوحة |
| GAP-C-01 · C-03 · C-05 · C-07 | كما هي |

**لم تُغلق أي ثغرة. GAP-C-10/11/12/13 مسجّلة كثغرات مفتوحة لا كقرارات.**

---

**لم يُكتب كود · لم يُعدَّل ملف نظام · لم تُنفَّذ اختبارات · لم تُفتح B-series · لم يبدأ C2 · لم يُمسّ `project_master.json`. بانتظار مراجعة المشرف.**
