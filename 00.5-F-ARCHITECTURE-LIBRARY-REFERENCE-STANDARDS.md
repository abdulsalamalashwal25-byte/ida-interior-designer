# 00.5-F — LIBRARY / REFERENCE DATA / STANDARDS GOVERNANCE
## ARCHITECTURE SPECIFICATION

**معمارية فقط — لا كود · لا Schema · لا Master · لا تعديل A/B/C1–C6/D/E · لا Implementation · لا قاعدة بيانات · لا API.**

> **المبادئ غير القابلة للتفاوض:**
> **Reference ≠ Fact** · **Reference ≠ Decision** · **Reference ≠ Approval** · **Reference ≠ Engineering Truth** · **Standard ≠ Automatic Compliance**

---

## 0. قياسات أُجريت قبل التصميم — والنتيجة تُعيد تعريف دور F

| # | القياس | النتيجة |
|---|---|---|
| **M-1** | `fact.source_type` enum | يحوي **`EXTERNAL_STANDARD`** و**`EXTERNAL_LIBRARY`** و**`AI_IMAGE`** سلفًا |
| **M-2** | **`$defs.external_ref`** | **موجود سلفًا**: `ref` (إلزامي) · `url` · `retrieved_on` (إلزامي) · **`applicability_note`** |
| **M-3** | موضعه | داخل `$defs.fact` كحقل `external` |
| **M-4** | **RULES.md §9** | **المسار الإلزامي منصوص:** `External Source → Reference → [P] أو [D] → Approval → [C]` · ولا تُقبل بلا `external.ref` + `retrieved_on` + `applicability_note` |
| **M-5** | B6 | يصنّف `EXTERNAL_STANDARD`/`EXTERNAL_LIBRARY` ضمن **`NON_FACTUAL_SOURCE_TYPES`** |
| **M-6** | C1 | يضعهما ضمن **`UNTRUSTED_SOURCES`** — غير مؤهلة لفئة A |
| **M-7** ⭐ | **حدود A المعلنة** | **«Whether an external citation is real or fabricated (URL is not fetched at validation time)»** و**«external.ref / url are not fetched or verified at validation time»** |

### الأثر المعماري الحاسم

**البنية والقاعدة موجودتان سلفًا. الفجوة في مكان آخر تمامًا:**

> **لا جهة تتحقق من أن الاستشهاد حقيقي، ولا تصف سلطة المصدر، ولا تدير نسخه وتعارضاته.**

⇒ **F ليس Catalog Schema** — البنية موجودة. **F هو طبقة حوكمة المصادر** التي تملأ ما أعلنت A عجزها عنه (M-7)، **دون أن تعد بما لا تستطيع** (§10).

---

## A — F ARCHITECTURE SPECIFICATION

### A.1 المهمة

> **F = Reference Governance Layer.**
> يصف **مصادر خارجية**, و**سلطتها المحدودة**, و**صلاحيتها**, و**تعارضاتها**, و**نسخها** — **ويقدّم Reference Evidence فقط.**

### A.2 النطاق

| داخل F ✅ | خارج F ❌ |
|---|---|
| وصف المصدر وهويته ونسخته | **الماستر والحقائق** |
| **Authority Scope المجزّأ** | القرارات والاعتمادات |
| حالة التحقق والصلاحية | الهندسة والتحقق الهندسي |
| **الملاءمة (Applicability)** | fidelity ودورة حياة المخرج |
| **رصد التعارض ووصفه** | **حسمه كقرار تصميمي** |
| اللقطات والنسخ والتجاوز | **شهادة المطابقة** |
| Reference Evidence | **ترقية أي قيمة إلى `[C]`** |

### A.3 القاعدة المؤسِّسة
**F يصف ولا يقرر.** وكل ما يخرج منه **دليل مرجعي موسوم**, لا حقيقة ولا التزام.

---

## B — SOURCE MODEL — الكيانات الثمانية

**لا أبدأ من «قاعدة بيانات منتجات».** الكيانات مفصولة لأن **خلطها هو مصدر الترقية الصامتة**:

| # | الكيان | يجيب عن | مثال توضيحي **(EXAMPLE ONLY)** |
|---|---|---|---|
| **1** | **Source** | **من أين جاءت المعلومة؟** | كتالوج مصنّع · وثيقة معيار · إدخال مستخدم |
| **2** | **Reference Item** | **ما الشيء المرجعي؟** | طاولة طراز X · خامة Y |
| **3** | **Reference Attribute** | **ما الخاصية المأخوذة؟** | العمق · اللون · رقم الموديل |
| **4** | **Evidence** | **ما الدليل عليها؟** | صفحة الكتالوج · لقطة · نص |
| **5** | **Applicability** | **على أي حالة تنطبق؟** | البلد · السوق · نوع المشروع |
| **6** | **Authority** | **ما سلطة المصدر — على هذه الخاصية تحديدًا؟** | §C |
| **7** | **Validity** | **هل ما زالت صالحة؟** | نافذة · منتهية · مسحوبة |
| **8** | **Usage** | **كيف يجوز استخدامها؟** | §F.2 |

**قاعدة الفصل:** خاصية واحدة قد تكون **سلطوية**, وأخرى من **المصدر نفسه** غير سلطوية. الكيان `Attribute` هو ما يجعل ذلك ممكنًا.

---

## C — AUTHORITY MODEL

### C.1 رفض `trusted_source = true`

> **لا سلطة شاملة لمصدر.** الحقل المنطقي الشامل **محظور معماريًا** لأنه يُخفي أن السلطة **مجالية**.

### C.2 الحقول الثمانية — دراسة العلاقة كما طُلب

| الحقل | يجيب عن | ملاحظة |
|---|---|---|
| `source_type` | **نوع** المصدر | تصنيف عام — **لا يمنح سلطة بذاته** |
| `source_identity` | **من** هو المصدر بالضبط | اسم/جهة — **مُعلن لا مُثبت** (M-7) |
| `source_version` | أي نسخة | أساس التجاوز (§H) |
| `retrieved_on` | متى استُرجع | **موجود سلفًا في `external_ref`** |
| `document_reference` | الموضع الدقيق | صفحة/قسم |
| `citation` | الاستشهاد الكامل | **`ref` إلزامي سلفًا** |
| `verification_state` | **هل تُحقق منه؟** | §C.4 |
| **`authority_scope`** | **سلطوي على ماذا بالضبط؟** | **المحور — §C.5** |

**العلاقة المقترحة:** `source_type` **يقترح** سقف سلطة · `source_identity` + `source_version` + `citation` **يحددان الكيان** · `verification_state` **يصف ما فُعل** · **`authority_scope` وحده يقرر الاستعمال المسموح**.

⇒ **السلطة لا تُشتق من النوع.** مصنّف مشهور ببيان خاطئ **لا يصير سلطويًا بنوعه**.

### C.3 مستويات المصدر — **مقترحة لا نهائية**

`Official Manufacturer` · `Official Standard / Authority` · `Verified Technical Document` · `Trusted Reference Database` · `Secondary Reference` · `User-provided Reference` · `Agent Research` · **`AI-generated Reference`** · `Unknown / Unverified`.

> ⚠️ **لا أعتمدها كـenum نهائي** (كما وجّهتم) ⇒ **DEC-F-01**. والسبب: تسع درجات قد توحي بتدرج ثقة **بينما السلطة مجالية لا تدرجية**.

### C.4 Verification State — **ما فُعل فعلًا، لا ما نظنه**

| الحالة | المعنى |
|---|---|
| `UNVERIFIED` | لم يُتحقق — **الافتراضي** |
| `CITATION_RECORDED` | الاستشهاد مسجَّل ولم يُفتح |
| `RETRIEVED` | المحتوى استُرجع فعلًا |
| `CONTENT_CHECKED` | قُرئ وطوبق مع الادعاء |
| `UNREACHABLE` | تعذّر الوصول |
| `CONTRADICTED` | مصدر آخر يخالفه |
| **`NOT_VERIFIABLE`** | **لا وسيلة للتحقق بالأدوات المتاحة** |

**الافتراضي `UNVERIFIED`**, ولا يُرقّى إلا بفعل موصوف.

### C.5 Authority Scope — مجزّأ

```
authority_scope = [
  { attribute: "<الخاصية>", authoritative: true|false, basis: "<لماذا>" }
]
```

**مثال توضيحي (EXAMPLE ONLY):** كتالوج مصنّع **قد يكون** سلطويًا على `product_dimensions` · `manufacturer_material_spec` · `model_number` — و**ليس** سلطويًا على `suitability_for_this_project` · `local_code_compliance` · `final_placement` · `circulation_clearance` · `design_approval`.

**قاعدة:** خاصية **خارج** النطاق المعلن ⇒ **استعمالها انتهاك** (TM-14) · وغياب النطاق ⇒ **`NOT_AUTHORITATIVE` افتراضًا**.

---

## D — REFERENCE LIFECYCLE

```
CAPTURED ──► CITED ──► RETRIEVED ──► CHECKED ──► IN_USE
   │           │           │            │           │
   │           │           │            │           ├──► SUPERSEDED  (نسخة أحدث)
   │           │           │            │           ├──► EXPIRED     (انتهت صلاحيته)
   │           │           │            │           └──► WITHDRAWN   (سُحب من مصدره)
   └───────────┴───────────┴────────────┴──────────────► UNREACHABLE
```

**قواعد ملزمة:**

| # | القاعدة |
|---|---|
| **L-1** | الانتقال **فعل موصوف**, لا مرور زمن |
| **L-2** | **`IN_USE` لا يعني صحة** — يعني أن المشروع استعمله |
| **L-3** | **`SUPERSEDED`/`EXPIRED` لا يغيّران ما استُعمل سابقًا** (§H) |
| **L-4** | `UNREACHABLE` **ليس فشلًا للمشروع** — وصف حالة |
| **L-5** | **لا ترقية تلقائية** بين الحالات |

---

## E — STANDARDS GOVERNANCE — التمييز الخماسي

| # | المفهوم | المعنى | المالك |
|---|---|---|---|
| **1** | **Standard Reference** | نسخة من معيار | **F** ✅ |
| **2** | **Requirement** | **متطلب مشروع فعلي** | **المستخدم/E** — قرار |
| **3** | **Applicability** | **هل ينطبق أصلًا؟** | **F يصف · المستخدم يقرر** |
| **4** | **Compliance Claim** | **ادعاء المطابقة** | **خارج F** |
| **5** | **Compliance Verification** | **من يتحقق؟** | **خارج 00.5 بالكامل** |

> ### القاعدة القاطعة
> **F يوفّر المرجع. ولا يمنح Compliance Certification إطلاقًا.**
> وممنوع أن يقول النظام *«المقاس صحيح لأنه مطابق للمعيار»* — فوجود معيار في المكتبة **لا يثبت**: أنه ينطبق · أن المشروع يحققه · أن جهة مختصة تحققت.

**تصريح:** **Compliance Verification خارج نطاق F وخارج 00.5** ⇒ **يُسجَّل ولا يُخترع له آلية** (**GAP-F-01**).

---

## F — PROVENANCE MODEL & USAGE

### F.1 حقول النسب
`source_id` · `source_identity` (مُعلن) · `source_version` · `retrieved_on` · `retrieved_by` · `reference_id` · `attribute` · `evidence_ref` · `citation` · `authority_scope` · `verification_state` · `usage_context` · `applicability`.

**ثلاثة تصريحات (كما طُلب):** ❌ **لا cryptographic authenticity** · ❌ **لا tamper-proof provenance** · ❌ **لا verified publisher identity**. النسب **إفصاح عن الأصل**, لا إثبات له.

### F.2 مسار الاستخدام — **المنصوص في RULES.md §9**

```
External Source ──► Reference Item ──► Reference Attribute ──► Evidence
                                                                  │
                                                                  ▼
                                                         Project Usage
                                                                  │
                    ┌─────────────────────────────────────────────┤
                    ▼                                             ▼
          يبقى Reference فقط                         [P] Proposal أو [D] Derived
                                                                  │
                                                          Approval (USER — E)
                                                                  ▼
                                                              [C] Confirmed
```

**قاعدة (F.2-1):** **المرجع لا يدخل الماستر كـ`[C]` أبدًا.** يدخل كـ`[P]`/`[D]` بفعل صريح، ثم **اعتماد المستخدم** يرقّيه.
**قاعدة (F.2-2):** **لا ترقية صامتة** — وجود القيمة في مكتبة **لا يُنتج** `[A]` ولا `[P]` ولا `[C]`.
**قاعدة (F.2-3):** **الترقية ليست ملك F** — الحالات ملك A/B7، والاعتماد ملك E. **F يقدّم الدليل فقط.**

---

## G — CONFLICT RESOLUTION MODEL

**مثال توضيحي (EXAMPLE ONLY):** `Source A: depth = 800mm` مقابل `Source B: depth = 820mm`.

**ما يُنتجه F:**
```
CONFLICT:
  attribute        : depth
  source_a         : {identity, version, citation, authority_scope, verification_state}
  source_b         : {identity, version, citation, authority_scope, verification_state}
  scope_overlap    : هل كلاهما سلطوي على هذه الخاصية؟
  required_resolution : USER_DECISION
  resolved         : false
```

| # | القاعدة |
|---|---|
| **CF-1** | **ممنوع الاختيار التلقائي** لأحدهما |
| **CF-2** | **ممنوع المتوسط أو المدى أو «الأقرب»** |
| **CF-3** | يُعرض **الاثنان كاملين** بنسبهما |
| **CF-4** | **التعارض حالة مشروعة** لا خطأ نظام |
| **CF-5** | **F لا يحسمه كقرار تصميمي** إلا بقاعدة **معتمدة مسبقًا** — ولا قاعدة كهذه اليوم ⇒ **DEC-F-04** |
| **CF-6** | مصدر خارج نطاق سلطته **لا يُرجَّح** |

---

## H — VERSION / SNAPSHOT MODEL

### H.1 القاعدة الأهم
> **تحديث المكتبة لا يعدّل المشروع أبدًا.**
> استعمل المشروع `Reference v1`? يبقى **v1** حتى بعد ظهور **v2**. والاستبدال **فعل مستخدم صريح**, وتاريخ المشروع **يبقى قابلًا لإعادة البناء**.

| # | القاعدة |
|---|---|
| **V-1** | الاستعمال يثبّت **`source_version` + `retrieved_on`** |
| **V-2** | **v2 يُسجَّل كـ`SUPERSEDES` ولا يُطبَّق** |
| **V-3** | **لا مزامنة تلقائية** بين المكتبة والمشروع |
| **V-4** | منتج `discontinued` ⇒ **الاستعمال السابق يبقى موصوفًا** |
| **V-5** | مواصفة تغيّرت ⇒ **تعارض نسختين** لا استبدال |
| **V-6** | رابط ميت ⇒ **`UNREACHABLE`**, والاستشهاد يبقى |

### H.2 Reference Snapshot

`snapshot_id` · `source_version` · `retrieval_timestamp` · `citation` · `availability_status` · `content_fingerprint: NOT_ISSUED`.

**ثلاثة تصريحات:** ❌ **لا ادعاء أن اللقطة tamper-proof** · ❌ **لا ادعاء حفظ كل شيء** · ❌ **لا عقد بصمة جديد** — `content_fingerprint` يبقى **`NOT_ISSUED`** احترامًا لحدود **C2** (DEC-C2-01/02/03 مفتوحة).

---

## I — APPLICABILITY MODEL

> **مصدر عالمي لا ينطبق تلقائيًا على مشروع محلي.**

**أبعاد الملاءمة:** البلد · المنطقة · السوق · المورّد · العملة · **الوحدات** · القوانين · المعايير · نوع المشروع.

| الحالة | المعنى |
|---|---|
| `APPLICABLE_DECLARED` | **مُعلن انطباقه — بقرار المستخدم لا باستنتاج F** |
| `NOT_APPLICABLE` | مُعلن عدم انطباقه |
| **`APPLICABILITY_UNKNOWN`** | **الافتراضي** |
| `PARTIALLY_APPLICABLE` | جزئي بوصف صريح |

**قاعدة:** **الافتراضي `APPLICABILITY_UNKNOWN`**, و**F لا يستنتج الانطباق** — يصف الأبعاد ويترك الحكم. وهذا يتسق مع `applicability_note` الموجود سلفًا (M-2) الذي ينص أن **المعيار العام ليس دليلًا على ملاءمته لهذا المشروع**.

---

## J — SECURITY / TRUST MODEL

| ما يُقدَّم ✅ | ما **لا** يُدّعى ❌ |
|---|---|
| تسجيل المصدر والنسخة والزمن | **صدق الناشر** |
| الاستشهاد والموضع | **أصالة تشفيرية** |
| حالة التحقق الموصوفة | **نسب مقاوم للعبث** |
| وصف السلطة المجالية | **أن المصدر يقول ما نُسب إليه** |

**الحد المقاس (M-7):** A تعلن أنها **لا تتحقق مما إذا كان الاستشهاد حقيقيًا أم ملفقًا**. **وF لا يغلق هذه الفجوة** — يجعلها **موصوفة** عبر `verification_state`، لا مغلقة ⇒ **GAP-F-02**.

**المستخدم قدّم مواد:** **الصورة لا تثبت الأبعاد** · **رسم المنتج لا يثبت ظروف المشروع** · PDF/رابط/لقطة ⇒ **`CAPTURED` + `UNVERIFIED`**, ورقم مكتوب في صورة **ليس قياسًا**.

**AI-generated Reference:** **مادة اقتراح فقط** · **ليست مصدرًا موثوقًا** · **ولا تُستعمل كدليل هندسي أو مواصفة حقيقية** · ومتسقة مع `AI_IMAGE` في `UNTRUSTED_SOURCES` (M-6).

**External Retrieval — capability-aware:** لا افتراض بتوفر أي API أو خدمة. غير المتاح ⇒ **`UNREACHABLE`/`NOT_VERIFIABLE`/`UNKNOWN`** بحسب الحالة. **لا اختراع.**

---

## K — A–E INTEGRATION / OWNERSHIP MATRIX

| الطبقة | العلاقة بـF | الحد |
|---|---|---|
| **A** | يملك `source_type` و`external_ref` و`status` | **F لا يعدّل الشكل**؛ الدليل يدخل عبر البنية القائمة (M-2) |
| **B6** | يصنّف الخارجي **غير واقعي** | **F لا يعترض** |
| **B7** | يملك انتقالات اليقين | **F لا يرقّي حالة** |
| **B10** | تماسك دورة حياة القرارات | لا تكرار |
| **C1** | يمنع الخارجي عن **فئة A** | **F لا يخفف** |
| **C2** | البصمات | **`NOT_ISSUED`** — لا عقد جديد |
| **C3–C5** | يستهلك `[C]`/`[D]` فقط | **F لا يغذّيها مباشرة** |
| **D** | fidelity | **لا يستعمل Reference Evidence كمرجع مطابقة** |
| **E** | الاعتماد والحوكمة | **اختيار المستخدم لمرجع = فعل حوكمي يسجّله E** |

### إجابات §19 صراحةً

| السؤال | الجواب |
|---|---|
| **A:** كيف يدخل الدليل بلا خلطه بحقيقة؟ | عبر **`external_ref` + `source_type` الخارجي + `status` غير `[C]`** — البنية **قائمة** |
| **B:** كيف يُمنع تجاوز الثوابت؟ | B6 يصنّفه غير واقعي · B7 يملك الحالات · **F لا يملك ترقية** |
| **C:** هل يقرأ C من F؟ | **لا مباشرة.** C1 يمنع المصادر الخارجية عن فئة A؛ والدخول **فقط** بعد `[P]/[D] → Approval → [C]` |
| **D:** هل يستعمل D الدليل المرجعي؟ | **لا كمرجع مطابقة** — مرجع D هو **GA حصرًا**. ويجوز عرضه **سياقًا** لا حكمًا |
| **E:** كيف تُسجَّل الحوكمة؟ | **اختيار مرجع أو الاعتماد عليه = فعل يملكه E** بسلطة USER |
| **C2:** البصمات مستقبلًا | `content_fingerprint = NOT_ISSUED` · **لا عقد جديد** |

---

## L — THREAT MODEL

| # | التهديد | Detection | Classification | Allowed | Forbidden | Owner | Escalation |
|---|---|---|---|---|---|---|---|
| TM-01 | **مصدر مزيف** | ❌ **غير قابل للكشف (M-7)** | `UNVERIFIED` | التسجيل والوسم | ادعاء التحقق | **F يصف · المستخدم يقرر** | GAP-F-02 |
| TM-02 | مصدر مجهول | غياب الهوية | `Unknown` | استعمال مرجعي | سلطة | F | المستخدم |
| TM-03 | مصدر قديم | `source_version` | `SUPERSEDED` | إبقاء المستعمل | استبدال تلقائي | F | المستخدم |
| TM-04 | **متعارض** | مقارنة | `CONTRADICTED` | عرض الاثنين | **الاختيار التلقائي** | F يرصد | **المستخدم** |
| TM-05 | ناقص | حقول غائبة | `INCOMPLETE` | إعلان النقص | إكماله | F | — |
| TM-06 | **URL ميت** | تعذّر الوصول | `UNREACHABLE` | إبقاء الاستشهاد | اعتباره تحققًا | F | — |
| TM-07 | PDF غير مقروء | فشل القراءة | `NOT_VERIFIABLE` | الإعلان | تخمين المحتوى | F | — |
| TM-08 | **OCR خاطئ** | ❌ **غير قابل للكشف** | `UNVERIFIED` | وسم المصدر كـOCR | معاملته كنص موثوق | F | المستخدم |
| TM-09 | **وحدة مجهولة** | غياب الوحدة | `UNIT_UNKNOWN` | الرفض المعلن | **افتراض وحدة** | F | المستخدم |
| TM-10 | **قيمة بلا وحدة** | — | `UNIT_MISSING` | الرفض | الافتراض | F | المستخدم |
| TM-11 | مواصفة تغيّرت | فرق النسخ | `SUPERSEDED` | تعارض نسختين | استبدال صامت | F | المستخدم |
| TM-12 | **AI hallucinated product** | ❌ **غير قابل للكشف** | `AI-generated` | **مادة اقتراح فقط** | **دليل هندسي** | F يوسم | المستخدم |
| TM-13 | **AI hallucinated standard** | ❌ **غير قابل للكشف** | `AI-generated` | وسم صريح | **ادعاء معيار** | F | **المستخدم** |
| TM-14 | **استعمال خارج نطاق السلطة** | مقارنة بـ`authority_scope` | `SCOPE_VIOLATION` | الرفض المعلن | التمرير | **F** ✅ | المستخدم |
| TM-15 | **ترقية صامتة Reference→Fact** | حالة الحقيقة ومصدرها | `PROMOTION_VIOLATION` | الرفض | الترقية | **A/B7/C1** | E |
| TM-16 | **تحديث مكتبة يغيّر المشروع** | مقارنة النسخ | `AUTO_UPDATE_VIOLATION` | الإعلان | **التطبيق** | **F** ✅ | المستخدم |
| TM-17 | معايير متعارضة | مقارنة | `STANDARD_CONFLICT` | عرض الاثنين | ترجيح تلقائي | F يرصد | **المستخدم** |
| TM-18 | **معلومة مستخدم خاطئة** | ❌ **غير قابل للكشف** | `User-provided` | التسجيل | **معاملتها كمُتحقَّقة** | المستخدم | — |

**الحصيلة الصادقة:** **ستة تهديدات قابلة للكشف بنيويًا** (TM-02/03/04/05/14/16 + جزئيًا 06/07/09/10)، و**ستة غير قابلة للكشف** (TM-01/08/12/13/18 وجزء من 04) — **تُوصف ولا يُدّعى ضبطها**.

---

## M — DECISION REGISTER

| # | السؤال | الخيارات | الموصى به | السبب | الأثر | التبعيات | الحالة | يحتاج اعتمادًا؟ |
|---|---|---|---|---|---|---|---|---|
| **DEC-F-01** | enum مستويات المصدر | (أ) التسع · (ب) أقل · (ج) بلا enum — `authority_scope` وحده | **(ج) أو (ب)** | التدرج يوحي بثقة **بينما السلطة مجالية** | يحدد النموذج كله | — | 🔴 OPEN | ✅ **نعم** |
| **DEC-F-02** | أين تعيش مكتبة F؟ | (أ) **خارجية** · (ب) داخل الماستر | **(أ)** | (ب) كتابة في SSOT | بنيوي | DEC-E-06 | 🔴 OPEN | ✅ نعم |
| **DEC-F-03** | هل يحتاج `external_ref` توسيعًا؟ | (أ) الاكتفاء بالقائم · (ب) إضافة `version`/`authority_scope` | **(أ) لـV1** | **(ب) تعديل Schema — محظور** | يحدد العمق | **Schema** | 🔴 OPEN | ✅ **نعم** |
| **DEC-F-04** | قاعدة حسم التعارض | (أ) **بلا قاعدة — المستخدم** · (ب) قاعدة أولوية | **(أ)** | (ب) قرار تصميمي | حاسم | — | 🔴 OPEN | ✅ نعم |
| **DEC-F-05** | نطاق V1 | (أ) الوصف والنسب والتعارض · (ب) + استرجاع | **(أ)** | لا API مضمون | نطاق | GAP-C-02 | 🔴 OPEN | ✅ نعم |
| **DEC-F-06** | حالة `verification_state` الافتراضية | (أ) `UNVERIFIED` · (ب) `CITATION_RECORDED` | **(أ)** | الأكثر تحفظًا | سلوكي | — | 🟡 OPEN | — |
| **DEC-F-07** | هل يقرأ D من F؟ | (أ) **لا** · (ب) سياقًا فقط | **(أ)** | مرجع D هو GA حصرًا | حدود | D CLOSED | 🟡 OPEN | — |

---

## N — OPEN GAPS / CROSS-LAYER CONFLICTS

| # | الحالة |
|---|---|
| **GAP-F-01** | **Compliance Verification خارج F وخارج 00.5** — لم يُخترع له آلية |
| **GAP-F-02** | **صدق الاستشهاد غير قابل للتحقق** (M-7) — F يصفه ولا يغلقه |
| **GAP-F-03** ⭐ | **لا حقل `authority_scope` ولا `source_version` في `external_ref`** ⇒ النموذج المقترح **أغنى من البنية القائمة** |
| **GAP-C-02** | 🔴 القدرة لحظية · لا API مضمون |
| **DEC-C2-01…08** · GAP-D-01 · CONFLICT-E-01 · GAP-01/02 | 🔴 **كما هي — لم تُفتح** |

### ⚠️ CROSS-LAYER GAP — **لا أصلحه**

> **CLG-F-01 — النموذج المقترح يتجاوز بنية `external_ref` القائمة.**
> **السبب:** `authority_scope` · `source_version` · `verification_state` **غير موجودة** في `$defs.external_ref` (الحقول: `ref` · `url` · `retrieved_on` · `applicability_note`).
> **الطبقة المتأثرة:** **A (Schema)**.
> **الخيارات:** (أ) **سجل F خارجي** يرتبط مرجعيًا بـ`external.ref` — **بلا تعديل Schema** · (ب) توسيع `$defs.external_ref` — **يمسّ A المغلقة**.
> **الأثر:** (أ) يحافظ على إغلاق A لكن يوزّع البيانات · (ب) أنظف بنيويًا لكنه **يفتح طبقة مغلقة**.
> **يحتاج قرارًا معماريًا:** ✅ **نعم** ⇒ مرتبط بـ**DEC-F-02** و**DEC-F-03**.
> **توقفت هنا ولم أعدّل شيئًا.**

---

## O — GATE CARD — 00.5-F Architecture

| البند | الحالة |
|---|---|
| Reference ≠ Fact/Decision/Approval/Truth | ✅ §F.2 — المسار المنصوص في RULES.md §9 |
| Standard ≠ Automatic Compliance | ✅ §E — **لا Compliance Certification** |
| ليست Catalog Schema | ✅ §B — ثمانية كيانات مفصولة |
| Source Trust Model | ✅ §C — **والـenum غير محسوم (DEC-F-01)** |
| **Authority Scope مجزّأ** | ✅ **`trusted_source=true` محظور** |
| Reference → Master | ✅ **بلا ترقية صامتة** · والترقية **ليست ملك F** |
| لا تجاوز A–E | ✅ §K |
| Versioning / Supersession | ✅ **تحديث المكتبة لا يعدّل المشروع** |
| Conflicting Sources | ✅ CF-1…CF-6 — **لا اختيار تلقائي** |
| Reference/Assumption/Proposal | ✅ **الانتقال ملك A/B7/E لا F** |
| مواد المستخدم | ✅ **الصورة لا تثبت الأبعاد** |
| AI references | ✅ **مادة اقتراح فقط** |
| External Retrieval | ✅ **capability-aware · لا افتراض API** |
| Snapshot | ✅ **`NOT_ISSUED` · لا ادعاء tamper-proof** |
| Expiration / Validity | ✅ **exists ≠ current ≠ applies** |
| Applicability | ✅ **الافتراضي UNKNOWN** |
| Security / Provenance | ✅ **ثلاثة نفي صريحة** |
| Threat Model | ✅ 18 — **وستة معلنة كغير قابلة للكشف** |
| Decision Register | ✅ 7 — **بلا حسم ذاتي** |
| **CROSS-LAYER GAP** | ✅ **CLG-F-01 مسجَّل ولم يُصلَح** |
| كود / Schema / تعديلات | ✅ **صفر** — 1208/1208 سليم |
| أمثلة موسومة | ✅ **EXAMPLE ONLY** حيثما وردت |

### الحالة

# **F Architecture = READY FOR SUPERVISOR REVIEW**

**أبرز ما وجدته:** البنية والقاعدة **موجودتان سلفًا** (`external_ref` + RULES.md §9)، والفجوة الحقيقية أن **A تعلن صراحةً أنها لا تتحقق من صدق الاستشهاد** (M-7). فدور F **وصف سلطة المصادر وصلاحيتها وتعارضها** — **لا إغلاق فجوة الصدق**, وهي تبقى **GAP-F-02 مفتوحة**.

**لم أكتب كودًا ولم أعدّل Schema/Master/A–E، ولم أبدأ Implementation، ولم أحوّل أي مثال إلى قاعدة.**
