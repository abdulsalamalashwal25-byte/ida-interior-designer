# 00.5-C3 — IMPLEMENTATION & TEST REPORT
**المرجع:** C3 Architecture (CLOSED) + Decision Resolution Addendum (CLOSED)
**العقد المعتمد:** DEC-C3-03 = **Tier 0 + Tier 1** · DEC-C3-02 = **SVG-first** · DEC-C3-01 = **faithful serialization أو ABSTAIN**
**النتيجة:** `C3 = 54/54` · **Full Regression = 761/761 · REGRESSIONS: NONE**

---

## 1. IMPLEMENTATION SUMMARY

| الملف | الحالة | الطبقة | الدور |
|---|---|---|---|
| `scripts/c3_geometry_assembly.py` | **جديد** | **GA** | نسخ أمين لـ`[C]`/`[D]` إلى تمثيل محايد + provenance لكل قيمة + إعلان كل ما لم يُمثَّل |
| `scripts/c3_svg_emitter.py` | **جديد** | **EA + EP** | تحويل GA إلى SVG · Capability ثلاثي · Evidence Pack |
| `tests/test_00_5_C3.py` | **جديد** | — | 54 اختبارًا |
| `tests/run_all.py` | **معدَّل** | — | سطر واحد |

### ما نُفِّذ فعليًا

- **Tier 0:** `space/outline` · `walls` (id · start · end · thickness إن كانت مؤهلة) · بطاقة هوية · **إعلان صريح لكل ما لم يُمثَّل**.
- **Tier 1:** `openings` (door/window) موضوعة بعُرف **`host_wall` + `offset` من بداية الجدار**.
- **خارج النطاق (مُعلن داخل الملف):** الأثاث · الأبعاد المكتوبة · أسماء الغرف/الشمال/المقياس.

### قرار ملكية جوهري

عُرف وضع الفتحة (`offset` من `start` على متجه الجدار) **مملوك ومفروض أصلًا في B2/B5** (تحقّقت من `validate_topology.py` و`aperture_geometry` في `validate_doors.py`). **C3 يستهلكه ولا يعرّفه ولا يعيد التحقق منه** — لو اخترعت عرفًا لكان ذلك ازدواج ملكية.

### مخرج حقيقي مُنتَج (معاينة فعلية)

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 6000 4000">
  <!-- CLASS A OUTPUT — SCOPE: Tier 0 + Tier 1 (outline, walls, openings).
       NOT a complete construction drawing. -->
  <!-- project_id: PRJ-01 | master_revision: R01 -->
  <!-- engineering fingerprint: NOT_ISSUED (DEC-C2-01/02/03/05/06/08 open) -->
  <!-- fidelity: NOT_ASSESSED — owned by D -->
  <g id="space-outline">
    <polygon points="0,0 6000,0 6000,4000 0,4000" … data-provenance="space/outline"/>
  </g>
  <g id="walls">
    <line x1="0" y1="0" x2="6000" y2="0" … data-id="W-01" data-tier="TIER-0"/>
    …
  </g>
  <g id="openings">
    <line x1="1000.0" y1="0.0" x2="1900.0" y2="0.0" … data-id="OP-01" data-kind="door"/>
    <line x1="6000.0" y1="1200.0" x2="6000.0" y2="2700.0" … data-id="OP-02" data-kind="window"/>
  </g>
  <!-- NOT REPRESENTED (declared):
       [NR-05] dimensions: … could introduce a number absent from the master.
       [NR-05] room names / scale: Tier 4 outside the approved contract. -->
</svg>
```

**كل إحداثي يُرجَع إلى حقل في الماستر:** `OP-01` عند `1000→1900` هو حرفيًا `offset=1000` + `width=900` على `W-01` من `[0,0]`.

---

## 2. TEST REPORT — 54/54

| المجموعة | العدد |
|---|---|
| Positive | 8 |
| Negative | 8 |
| Boundary | 6 |
| Zero-Invention / Provenance | 8 |
| Mutation | 5 |
| Isolation | 5 |
| Anti-Smuggling | 10 |
| No-Write | 4 |

### 2.1 Positive — 8/8
`C3-P-01` EMITTED · `P-02` XML سليم · `P-03` الجداران منسوخان · `P-04` الفتحة منسوخة · **`P-05` الموضع `p0=[1000,0] p1=[1900,0]` مطابق لعُرف B2/B5** · `P-06` المحيط بـprovenance · `P-07` وسم النطاق · `P-08` جدول إحداثيات بـ6 مدخلات.

### 2.2 Negative — 8/8 (كل سلبي بالرمز المقصود)

| ID | الحالة | المتوقع | الفعلي |
|---|---|---|---|
| N-01 | `[U]` outline | `C3-ABS-01` | مطابق |
| N-02 | `[P]` جدران | `C3-ABS-02` | مطابق |
| **N-03** | **`[A]` معتمد** | **NR-01 · غير مؤهل** | مطابق |
| N-04 | `AI_IMAGE` | NR-02 | مطابق |
| N-05 | host مجهول | NR-06 | مطابق |
| N-06 | `[U]` offset | الفتحة تُسقط والمسقط يصدر | مطابق |
| **N-07** | **NaN** | **`C3-ABS-03` بلا تقريب** | مطابق |
| N-08 | حقل مفقود | NR-03 بلا افتراضي | مطابق |

### 2.3 Boundary — 6/6
`BD-01` جدار منحل ⇒ الفتحة تمتنع **والحكم على الجدار يبقى لـB2** · `BD-02` `[U]` سماكة ⇒ الجدار يُرسم والسماكة **لا تُخترع** · `BD-03` محيط بنقطتين ⇒ امتناع · `BD-04` `offset=0` **قيمة لا غياب** · **`BD-05` `0.1+0.2` تُكتب `0.30000000000000004` حرفيًا** · `BD-06` بلا فتحات ⇒ مسقط Tier 0 صالح.

### 2.4 Zero-Invention / Provenance — 8/8
`ZI-01` لا عنصر زائد · `ZI-02` كل إحداثي بمسار · `ZI-03` الفتحة بأربعة مسارات · `ZI-04` **3 عناصر مرسومة = 3 عناصر منسوخة** · `ZI-05`/`ZI-06` **Zero-invention لا يُقدَّم كـfidelity** · `ZI-07` الطبقات خارج النطاق مُعلنة · `ZI-08` الأثاث الموجود في الماستر **يُعلن ولا يُرسم**.

---

## 3. MUTATION — 5/5 + طفرتان خارجيتان

| ID | الطفرة | النتيجة |
|---|---|---|
| M-01 | إدخال `A` في `ELIGIBLE_STATUSES` | ✅ تموت N-03 |
| M-02 | إفراغ `UNTRUSTED_SOURCES` | ✅ تموت N-04 |
| M-03 | إدخال تقريب في `_fmt` | ✅ تموت BD-05 |
| M-04 | **تعطيل `Assembly.omit` سلوكيًا** | ✅ تموت ZI-07 |
| M-05 | **CONTROL** | ✅ لا ينقلب شيء |

**خارجية:** حذف `c3_geometry_assembly.py` ⇒ `exit=1`. وتعطيل فحص `host_wall` ⇒ **`KeyError: 'W-01'`** — أي أن الفحص حامل فعلًا، وغيابه **يفشل بصوت عالٍ** بدل وضع فتحة على جدار غير موجود.

---

## 4. ISOLATION — 5/5
`ISO-01` صفر import لأي validator · `ISO-02` لا تكرار لبوابة C1 · `ISO-03` **لا طلب بصمة هندسية** · `ISO-04` **لا حكم على الجدار المنحل (B2 يملكه)** · `ISO-05` لا مفتاح حكم fidelity/approval.

---

## 5. ATTACK-SURFACE RESULTS — 10/10

| # | الخطر | النتيجة |
|---|---|---|
| AS-C3-01 | اختراع إحداثي | ✅ NR-03 بدل قيمة |
| AS-C3-02 | ترقية حالة | ✅ `[A]`/`[P]`/`[U]` لا تصير هندسة |
| AS-C3-03 | افتراضي صامت | ✅ لا `rotation`/`CENTER` في الكود المنفَّذ · **AS-03b:** حقل اختياري غائب ⇒ `None` لا بديل |
| AS-C3-04 | إثراء المخرج | ✅ ثلاث مجموعات فقط، بلا شبكة/إطار/عنوان |
| AS-C3-05 | إسقاط صامت | ✅ كل إسقاط مُعلن |
| AS-C3-07 | ادعاء D1/D2 | ✅ `NOT_ESTABLISHED`/`NOT_CLAIMED` |
| AS-C3-08 | ENV-FP كبصمة هندسية | ✅ لم يُستعمل بديلًا |
| AS-C3-10 | مخرج جزئي يبدو كاملًا | ✅ **وسم النطاق داخل الملف** |
| AS-C3-11 | كتابة في الماستر/`outputs[]` | ✅ صفر |
| AS-C3-12 | المخرج كمصدر حقيقة | ✅ fidelity غير مُقيَّمة |

---

## 6. NO-WRITE EVIDENCE — 4/4

`NW-01` الماستر في الذاكرة غير معدَّل · `NW-02` القالب والمخطط بـsha256 ثابت · `NW-03` `project_master.json` غير موجود · `NW-04` **لا ذكر لـ`outputs` إطلاقًا** في كود C3.

**تأكيد خارجي — md5 ثابتة:**
`schema 1b911e01…` · `template a6b13613…` · `schema_gate 57552bfb…` · `validate_blocking c499f8e6…` · `validate_topology 4e68ff0d…` · `c1_preconditions b63fff2f…` · `c2_fingerprint 983ecf52…` · `c2_environment d129d19a…` · `test_00_5_C1 be24fb02…`

**ولا ملف مخرج محفوظ:** المُصدِر **يُعيد نصًا ولا يكتب** (AS-09: لا `open(` في الوحدتين). صفر `.svg/.dxf/.glb` في مساحة العمل.

---

## 7. CAPABILITY EVIDENCE

**فحص لحظي (لا يُخزَّن كحقيقة دائمة):**

| المكتبة | الحالة الآن |
|---|---|
| `ezdxf` · `trimesh` · `reportlab` · `shapely` · `pptx` | **`NOT_SET`** |
| `matplotlib 3.10.9` · `numpy 2.3.5` · `jsonschema 4.26.0` | موجودة |

**Capability لـ(SVG × floor-plan/tier-0+1):**

| المستوى | الحالة |
|---|---|
| 1 — Format | ✅ نص، stdlib فقط |
| 2 — **Approved output contract** | ✅ **DEC-C3-03 معتمد** |
| 3 — Runtime | ✅ بلا تبعية خارجية |
| **capable** | ✅ **True** |

**DXF/GLB/PDF:** لم تُنفَّذ — **عقود غير مفعّلة**، والمستوى 3 غير متوفر. لم أفترض وجود أي منها.

---

## 8. العيوب المكتشفة

إخفاقان في أول تشغيل (`51/53`) — **كلاهما عيب اختبار**:

| # | العيب | التشخيص | الإصلاح |
|---|---|---|---|
| **DEF-C3-01** | `AS-03` رصدت `rotation` | **إيجابية كاذبة:** الكلمة في **docstring يمنع الافتراضي** («a missing rotation is NOT 0») لا في كود. تحقق: بعد تجريد docstrings والتعليقات ⇒ `rotation=False`, `center=False` | الفحص على **الكود المنفَّذ** فقط + أُضيف **AS-03b** إثباتًا سلوكيًا موجبًا |
| **DEF-C3-02** | `M-04` لم تنقلب | **طفرة تسمية لا سلوك:** تصفير `NR_OUT_OF_CONTRACT` غيّر **رمز** البند فقط والبنود بقيت مُعلنة (2 بندًا) ⇒ لم تختبر شيئًا | الطفرة صارت **سلوكية**: تعطيل `Assembly.omit` نفسه |

**لم يُكتشف عيب في المحرك، ولم يُخفَّف أي اختبار.**

---

## 9. FULL REGRESSION

```
A 87 · B1 38 · B2 39 · B3 54 · B4 38 · B5 49 · B6 61 · B7 48
B8 45 · B9 40 · B10 47 · B11 45 · C1 58 · C2 58 · C3 54
REGRESSIONS: NONE            (21.1s)
```
**707 سابقة (مطابقة للأساس) + 54 = 761/761.**

---

## 10. GATE CARD

| البند | الحالة |
|---|---|
| العقد المعتمد Tier 0 + Tier 1 | ✅ منفَّذ حرفيًا، ولا Tier إضافي |
| SVG-first · لا adapter بلا capability | ✅ DXF/GLB/PDF غير مفعّلة |
| DEC-C3-01 | ✅ نقل أمين · **NaN ⇒ ABSTAIN** · لا تقريب · **لا سياسة تكميم جديدة** |
| C1 يُستهلك ولا يُعاد تنفيذه | ✅ ISO-02 |
| لا import لأي B-series | ✅ ISO-01 |
| لا اختراع ولا ترقية حالات | ✅ ZI + AS |
| لا defaults | ✅ AS-03 + AS-03b |
| Provenance لكل قيمة هندسية | ✅ ZI-02/03 |
| **Zero-invention ليس دليل fidelity** | ✅ ZI-05/06 منصوص في المخرج والدليل |
| **Fidelity ملك D** | ✅ `NOT_ASSESSED` |
| لا Engineering Fingerprint · لا D1/D2 | ✅ ISO-03 · AS-06 |
| لا schema/Master/`outputs[]` | ✅ md5 + NW-04 |
| AS-C2-16 / AS-C2-17 مطبَّقتان | ✅ الطفرات عبر الوحدة · التوقعات حرفية |
| Full Regression | ✅ **761/761 · NONE** |

### القرار: **PASS WITH CONDITIONS**

**الشروط — تبعيات خارج ملكية C3:**
1. **Fidelity غير مُثبتة ولن تُثبت هنا.** المخرج مطابق للعقد ومنسوخ بأمانة، **لكن صحة التمثيل الهندسي حكم D**. عدّ العناصر وprovenance **لا يثبتان** صحة الموضع أو الطوبولوجيا.
2. **GAP-C-02 نشطة:** المكتبات تقلبت داخل الجلسة ⇒ Capability **فحص لحظي دائمًا**.
3. **DXF/GLB/PDF غير منفَّذة** — عقود تنتظر capability موثوقة.
4. **DEC-C2-01…08 مفتوحة** (ومعها DEC-C2-04/07) · **GAP-C-01…14 مفتوحة** — لم تُغلق أي منها.
5. **لا ملف محفوظ:** المُصدِر يُعيد نصًا؛ **قرار الحفظ وملكية `outputs[]` تبقى لـE** (GAP-C-06).

**لم أبدأ C4/C5/C6. بانتظار مراجعتك وإغلاق C3.**
