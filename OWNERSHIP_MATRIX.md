# OWNERSHIP MATRIX
**Interior Designer Agent · Validation Rule Ownership**
**قاعدة الحوكمة: لكل عطل مالك واحد فقط. ممنوع أن تفرض طبقتان نفس الاختصاص.**

---

## مبدأ الفصل

| الطبقة | تحكم على | لا تحكم على |
|---|---|---|
| **A — Schema Gate** | **شكل** البيانات و**موثوقية** مصدرها | وجود المرجع · الهندسة |
| **B1 — Cross-Reference** | **وجود** المرجع وقابليته للحل ونوعه | موثوقية المصدر · الهندسة |
| **B2 — Topology** | **صحة** العلاقة الهندسية بين الجدران والفتحات | التداخل الأثاثي · الحركة · الحسابات |

**اختبار الملكية:** إذا سُئل «هل هذا المرجع موجود؟» ⇒ B1. «هل مصدره جدير بالثقة؟» ⇒ A. «هل موضعه هندسيًا سليم؟» ⇒ B2.

---

## القواعد المنقولة (سجل تاريخي — لا يُعاد فتحه)

| القاعدة | المالك السابق | المالك الحالي | سبب النقل |
|---|---|---|---|
| **INV-D1** (جزء الوجود) | A | **B1 / XR-008** | «هل `derived_from` يُحل؟» سؤال وجود لا موثوقية |
| **INV-D1** (جزء الثقة) | A | **A — باقية** | «هل المدخل `[C]`/`[D]`/`[A]` معتمد؟» سؤال موثوقية خالص |
| **INV-APPR1** | A | **B1 / XR-006** | «هل `decision_id` له سجل؟» وجود مرجع |
| **INV-APPR3** | A | **B1 / XR-010** | «هل `linked_proposal` موجود؟» وجود مرجع |

> بعد النقل: الطبقة A **تمر نظيفة** على هذه الحالات، وB1 **يرفضها**. مُثبَت في `test_00_5_A.py` (N-D-01c · N-APPROVAL-01 · N-APPROVAL-02) — كل اختبار يؤكد الطرفين معًا.

---

## قواعد الطبقة A — Schema Gate

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| F1 | AI ليس مصدرًا | موثوقية المصدر |
| F2 | `[C]` من العميل أو الاعتماد فقط | موثوقية المصدر |
| F3 | `[C]` بالاعتماد يستلزم Decision Record **كامل الحقول** | شكل البيانات |
| F4 | `[D]` يعلن مدخلاته ومعادلته ونوع اشتقاقه | شكل البيانات |
| F5–F7 | قيود `[A]` و`[U]` و`[P]` | شكل + موثوقية |
| F8 | المعايير الخارجية لا تكون `[C]` | موثوقية المصدر |
| F9–F10 | قيود القفل | شكل البيانات |
| T1–T4 | الحالات الثلاث PRESENT/NOT_PRESENT/UNKNOWN | شكل + موثوقية |
| M1–M2, O1–O3, CR1, TF1, P1 | البيانات الوصفية والمخرجات والحوكمة | شكل البيانات |
| **INV-D1** | ثقة مدخلات الاشتقاق **فقط** | موثوقية — **لا وجود** |
| INV-D2 | منع تهريب قرار تصميمي عبر `[D]` | دلالة (GAP-05: تخفيف لا إغلاق) |
| INV-TF1/TF2 | تساوي البصمة والنسخة في Technical Fix | مقارنة حقول متجاورة |
| INV-S1 | اتساق سجل الوجود مع التجميعات | تناسق داخلي |
| INV-OPEN1 | منع اختراع فتحات عند `presence: UNKNOWN` | موثوقية |
| **WG1/WG2** (DEF-CROSS-01a/b) | مجال قيمة سماكة/ارتفاع الجدار: موجب حصرًا عند KNOWN | **شكل البيانات** — استحالة رياضية لا معيار |
| **SG1/SG2** (DEF-CROSS-01c/d) | مجال ارتفاع السقف (موجب) وحد أدنى 3 نقاط للمخطط | **شكل البيانات** — استحالة رياضية لا معيار |

---

## DEF-CROSS-01 — Targeted Remediation (بعد B-Series Closure Audit)

**القرار:** المجال العددي للقيمة المعروفة **ملك A**، بسابقة **RULE FG2** المعتمدة (`rotation` مقيَّد بـ−360…360 داخل المخطط نفسه). أُضيفت أربع قواعد بنفس النمط تمامًا: WG1 · WG2 · SG1 · SG2.

**لماذا ليست B2؟** B2 **لا يقرأ** `thickness` ولا `height` ولا `space` إطلاقًا (صفر إشارة في الملف)، ونطاقه المعلن هو **العلاقة** الهندسية بين الجدران والفتحات لا **مجال** القيمة المفردة. مستهلكا `thickness` فعليًا هما **B3** و**B4** عبر `th/2.0`.

**الضرر المُثبت:** بسماكة `-200` يتقلّص جسم الجدار حسابيًا، فتقارير B3 **نجاحًا نظيفًا** على أثاث مغروس فعليًا في الجدار (مُثبت عند y=320 و350) — سلبية كاذبة في فحص تصادم.

**لماذا ليست B12 ولا محركًا أفقيًا؟** ليست اختصاصًا جديدًا بل **قيد شكل** على القيمة، ومكانه حيث تُفرض بقية قيود الشكل.

**الحد الصارم:** استحالة رياضية فقط. **`thickness = 50mm` و`ceiling = 50mm` يمرّان** — الحكم عليهما يحتاج معيارًا خارجيًا وهو محظور. الطفرة M7 (تهريب حد أدنى 100mm) تحرس هذا الحد وتُقتَل.

**`[U]` معفى:** القاعدة مشروطة بـ`status ∈ {C,D,A,P}`، فالتمييز الثلاثي `[U]` ≠ NONE ≠ قيمة سليم (VD-B01/02/03).

---

## قواعد B1 — Cross-Reference Integrity

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| XR-000 | تكرار معرّف | وحدانية الهوية |
| **XR-001** | **وجود** الجدار المضيف للفتحة | وجود مرجع — **ليست B2** |
| XR-002 | وجود المنطقة | وجود مرجع |
| XR-003 | وجود مادة الأثاث | وجود مرجع |
| XR-004 | وجود مادة تشطيب الجدار | وجود مرجع |
| XR-005 | وجود هدف `applied_to` | وجود مرجع |
| XR-006 | وجود Decision Record | وجود مرجع *(منقولة من A)* |
| XR-007 | تسجيل الافتراض/الاقتراح/المجهول | وجود مرجع |
| XR-008 | قابلية حل `derived_from` | وجود مرجع *(منقولة من A)* |
| XR-009 | وجود CR المذكور في التعارض | وجود مرجع |
| XR-010 | قرار → اقتراح/اعتراض/عناصر | وجود مرجع *(منقولة من A)* |
| XR-011 | سلامة الرابط العكسي اقتراح ↔ قرار | تناسق ثنائي الاتجاه |
| XR-012 | وجود CR في السجل | وجود مرجع |
| XR-013 | وجود هدف CR | وجود مرجع |
| XR-014 | وجود المخرج الأب ومنع المرجع الذاتي | أبوة/بنوة |
| XR-015 | فئة B لا ترث إلا من A | أبوة/بنوة دلالية |
| XR-016 | دورات الاشتقاق *(المرجع الذاتي مستثنى — يملكه XR-014)* | أبوة/بنوة |
| XR-017 | تماسك البصمة (مشروع/إصدار/نسخة) | وجود مرجع |
| XR-018 | وجود الكاميرا ⇒ قابلية إعادة الإنتاج | وجود مرجع |
| XR-019 | الخلط النوعي في الحقول الحرة | نوع المرجع |

---

## قواعد B2 — Wall & Opening Topology Integrity

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| WT-001 | جدار منعدم الطول | صحة هندسية |
| WT-002 | هندسة جدار غير قابلة للحل (`[U]` أو `null`) | صحة هندسية |
| WT-003 | إغلاق المضلع عند طلبه | طوبولوجيا |
| WT-004 | فجوة بين أطراف الجدران | طوبولوجيا |
| WT-005 | تقاطع ذاتي بين جدارين غير متجاورين | طوبولوجيا |
| WT-006 | تكرار أو تراكب خطي بين جدارين | طوبولوجيا |
| WT-007 | جدار يتيم غير متصل بالسلسلة | طوبولوجيا |
| WT-008 | الفتحة تتجاوز امتداد جدارها | صحة هندسية |
| WT-009 | إزاحة سالبة للفتحة | صحة هندسية |
| WT-010 | تراكب فتحتين على نفس الجدار | طوبولوجيا |
| WT-011 | فتحة على جدار هندسته غير قابلة للحل | صحة هندسية |

**تفويض معلن:** «كل فتحة مرتبطة بجدار موجود» ضمن نطاق B2 المطلوب، لكنها **مملوكة لـ XR-001 في B1**. لم أُعِد تنفيذها — B2 يتخطى الفتحة اليتيمة ويترك رفضها لـ B1، منعًا لازدواج الاختصاص.

---

## قواعد B3 — Furniture Geometry & Clearance Integrity

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| FC-001 | هندسة أثاث غير قابلة للحل (`[U]`/null/مشوّهة) | صحة هندسية — **لا تُخترع أبعاد** |
| FC-002 | بصمة أرضية غير موجبة | صحة هندسية |
| FC-003 | تداخل أثاث مع أثاث | تعارض هندسي |
| FC-004 | تداخل أثاث مع جسم جدار | تعارض هندسي |
| FC-005 | تداخل أثاث مع عمود ثابت | تعارض هندسي |
| FC-006 | تكرار غير مشروع (نفس الموضع والبصمة) | وحدانية العنصر |
| FC-007 | خرق خلوص **معتمد ومصدره موثوق** | إنفاذ قاعدة معلنة |
| FC-008 | خلوص معلن لكنه **غير قابل للإنفاذ** — INFO/GAP | حوكمة — **لا تحويله إلى قاعدة** |
| **FC-009** | مرجع الموضع مفقود أو غير صالح أو غير معتمد | صحة هندسية — **لا يُفترض CENTER** |
| **FC-010** | الدوران مفقود أو غير صالح أو غير معتمد | صحة هندسية — **لا يُفترض 0°**؛ يحجب (ERROR) **فقط عند تعارض مُثبَت في كل الاتجاهات**؛ التعارض الممكن غير المُثبَت والتعارض المستحيل يُسجَّلان INFO. **«داخل الدائرة» ≠ «تصادم مُثبَت»** |

**تفويضات معلنة (بلا إعادة تنفيذ):** وجود `material_ref`/`zone_ref` ⇒ XR-003/XR-002 · عيوب الجدران ⇒ WT-* · المصدر والحالة ⇒ الطبقة A.

**سياسة الأرقام:** FC-007 لا ينفّذ إلا قيمة `[C]` أو `[D]` من مصدر موثوق. الاقتراح والافتراض غير المعتمد والمعيار الخارجي ⇒ FC-008 فقط. **لا رقم من عند المحرّك** (مُثبت: CL-07).

---

## قواعد B4 — Movement & Circulation Integrity

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| MV-001 | هندسة المسار غير قابلة للحل (`[U]`/null/مشوّهة/أقل من نقطتين) | صحة هندسية — **لا تُخترع مسارات** |
| MV-002 | قطعة منعدمة الطول داخل المسار | صحة هندسية |
| **MV-003** | انقطاع داخلي بين قطعتين متتاليتين — **أُعيدت بقرار المستخدم** بعد توسيع التمثيل إلى `segments` صراحةً، فصارت قابلة للإطلاق فعلًا | اتصال المسار — **الفجوة لا تُغلق بالافتراض** |
| MV-004 | `start_ref` / `end_ref` **لا يُحَل** | اتصال المسار بمرجعه — **ليس وجود المرجع (B1) بل قابلية استعماله هنا** |
| MV-005 | طرف المسار **بعيد هندسيًا** عن العنصر الذي يدّعي الاتصال به | اتصال هندسي فعلي |
| MV-006 | عائق **معلوم الهندسة** يقطع المسار ⇒ BLOCKED | كشف الإعاقة على المسار |
| MV-007 | عائق **مجهول الهندسة** قد يقطع المسار ⇒ UNKNOWN | **لا يُحوَّل المجهول إلى سالك ولا إلى محجوب** |
| MV-008 | خرق `required_width` **المعتمد ومصدره موثوق** | إنفاذ قاعدة معلنة |
| MV-009 | عرض مطلوب معلن لكنه **غير قابل للإنفاذ** — INFO/GAP | حوكمة — **لا تحويله إلى قاعدة** |
| MV-010 | تصنيف نهائي لكل مسار: `PASSABLE` / `BLOCKED` / `UNKNOWN` | إعلان الحالة صراحةً |
| **MV-011** | **إلزام إعلان الحركة**: الغياب الصامت خطأ؛ `NOT_PRESENT` مقبول، `UNKNOWN` يُسجَّل ولا يُعدّ خلوًّا، و`PRESENT` بلا مسارات = نقص لا فراغ | حوكمة البيانات — **يستعمل `presence_register` المعتمد في A ولا ينشئ آلية موازية** |

**تفويضات معلنة (B4 يستهلك ولا يملك):**
- **وجود** أي معرّف مُشار إليه ⇒ **B1 (XR-\*)**. MV-004 لا يفحص الوجود العام، بل قابلية حل الطرف **لغرض المسار**.
- تداخل أثاث/أثاث وأثاث/جدار وأثاث/عمود ⇒ **B3 (FC-003/004/005)**. B4 **يستهلك بصمات B3 كمدخل هندسي** ولا يُصدر أي رمز تصادم أثاث.
- صلاحية المصدر والحالة ⇒ **الطبقة A**. سلامة الجدران ⇒ **B2 (WT-\*)**.
- **دوائر فتح الأبواب ⇒ B5.** B4 يعامل الفتحة كعنصر مرجعي فقط ولا يحسب أي قوس فتح.

**سياسة الأرقام:** MV-008 لا ينفّذ إلا `required_width` بحالة `[C]`/`[D]` من مصدر موثوق. غير ذلك ⇒ MV-009 فقط. **لا عرض ممر ولا رقم إرغونومي من عند المحرّك** (مُثبت: MW-05).

**تمثيل المسار (CP1):** هندسة المسار تُعلن بطريقة واحدة فقط — `centerline` أو `segments` — لا كلتاهما ولا أيّهما. **سياسة المجهول:** `UNKNOWN` حالة ثالثة مستقلة. لا تُرقّى إلى `PASSABLE` ولا تُخفّض إلى `BLOCKED` بلا دليل هندسي.

---

## قواعد B5 — Door Swing & Doorway Integrity

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| DS-001 | `kind` = door لكن الجدار المضيف **لا يُحَل هندسيًا** ⇒ لا حكم | صحة هندسية — **ليس وجود المرجع (B1) بل صلاحيته لحساب الفتحة** |
| DS-002 | أبعاد الفتحة (`width`) غير معروفة أو غير موثوقة ⇒ لا هندسة فتح | **لا تُخترع أبعاد أبواب** |
| DS-003 | **فجوة تمثيل**: `swing` غير معلن ⇒ GAP مسجَّل، **لا Swept Volume ولا اتجاه مفترض** | حوكمة — **الصمت ليس اتجاهًا** |
| DS-004 | `swing` معلن لكن **بنيته غير كافية** لحساب القوس ⇒ GAP | **لا تخمين لمحور المفصلة ولا لجهة الفتح** |
| DS-005 | `swing` معلن **وكامل البنية** لكن قيمه غير صالحة ⇒ ERROR | صحة البيانات المعلنة |
| DS-006 | القوس المحسوب **يتعارض مع باب آخر** — ERROR عند الإثبات على الغلاف الداخلي، وINFO داخل نطاق التقريب | تعارض خاص بالأبواب — **لا يملكه أحد غير B5** |
| DS-007 | القوس **يتعارض مع أثاث** (بصمات B3 مستهلَكة) — ERROR **فقط** بالإثبات على الغلاف الداخلي؛ وما بين الغلافين **UNKNOWN لا PASS** | تعارض الفتح — **B3 يملك التداخل الساكن لا القوس** |
| DS-008 | **عتبة الباب مسدودة** — ERROR حصرًا من: فتحة قابلة للحل **+** عائق قابل للحل **+ تقاطع هندسي مُثبَت**. **القرب وحده لا يكفي إطلاقًا** | فجوة حقيقية لا يغطيها FC-004 (جسم الجدار) ولا B4 (مسار معلن) |
| DS-009 | التصنيف الصريح لكل باب: `PASS` / `ERROR` / `UNKNOWN` / `INFO` | إعلان الحالة صراحةً |

**تفويضات معلنة (B5 يستهلك ولا يملك):**
- وجود المعرّفات ⇒ **B1 (XR-001)** · الفتحة تتجاوز الجدار/إزاحة سالبة/تراكب فتحتين ⇒ **B2 (WT-008/009/010)** · **B5 لا يعيد أيًّا منها**.
- **تداخل الأثاث الساكن ⇒ B3 (FC-003/004/005)**؛ B5 يستورد `footprint()` ويستهلك البصمة فقط.
- **سلوك المسار المعلن ⇒ B4 (MV-\*)**؛ B5 لا يصدر أي حكم على مسار.
- المصدر والحالة ⇒ **الطبقة A** · صحة المعادلات ⇒ **B6** · بوابة المجهول ⇒ **B7**.

**سياسة تقريب القوس (R02):** القوس يُمثَّل بغلافين — **داخلي محاط** (التقاطع معه يُثبت التصادم) و**خارجي حاوٍ** (عدم التقاطع معه يُثبت الخلوّ). ما بينهما نطاق غير حاسم ⇒ **UNKNOWN/INFO ولا يُرقّى إلى PASS**. زيادة عدد القطع تضيّق النطاق ولا تلغيه.

**سياسة الأرقام والاتجاه:** **لا عرض باب، ولا زاوية فتح، ولا جهة مفصلة، ولا معيار خارجي** من عند المحرّك. `swing` غير المعلن ⇒ **DS-003 GAP** لا افتراض. المجهول **لا يتحول تلقائيًا إلى ERROR**، وERROR **حصرًا** عند تعارض مُثبَت من البيانات.

---

## قواعد B6 — Formula / Derived-Value Integrity

**مسؤولية B6 بدقة:** التحقق من أن كل قيمة `[D]` **صحيحة حسابيًا وقابلة لإعادة الإنتاج** من مدخلاتها المعلنة. الطبقة A تفرض **شكل** الـ`[D]` (F4: وجود `derived_from` و`formula` و`derivation_type`)، وB1/XR-008 يفرض **قابلية حل المراجع**، و**لا أحد يفحص الحساب نفسه** — هذه فجوة مُثبَتة عمليًا: قيمة تدّعي `2800 - 100 = 9999` تمرّ من A وB1 معًا.

| القاعدة | الاختصاص | سبب الملكية |
|---|---|---|
| FM-001 | الصيغة **غير قابلة للتحليل** أو تستعمل بنية غير مسموحة | حتمية الحساب — **لا تنفيذ لكود حر** |
| FM-002 | الصيغة تشير إلى **مُدخل غير معلن** في `derived_from` | شفافية الاشتقاق — **لا مدخلات خفية** |
| FM-003 | مُدخل معلن في `derived_from` **غير مستعمَل** في الصيغة | صدق النسب — الإعلان يطابق الواقع |
| FM-004 | **(مُشدَّدة في R02)** المُدخل غير مقبول للاشتقاق: حالة غير صالحة (`[U]`/`[P]`/`[A]`) · غير عددي · **`source_type` غير واقعي** (`EXTERNAL_STANDARD`/`AGENT_PROPOSAL`/`AGENT_ASSUMPTION`/`AI_IMAGE`/`NOT_PROVIDED`) أي **قرار أو معيار غير معتمد متنكر كرقم** · **`[D]` بلا `formula`/`derived_from`** (سلسلة نسب مكسورة) | **المجهول لا يُخمَّن**؛ كون القيمة رقمًا لا يجعلها حقيقة |
| FM-005 | **(مُوسَّعة في R02) جبر أبعاد كامل**: الجمع/الطرح يتطلب **نفس البعد ونفس الوحدة**؛ الضرب يركّب الأبعاد (`mm × mm → mm²`)؛ القسمة تقسمها (`mm² ÷ mm → mm` · `mm ÷ s → mm/s`). **رقم صحيح ببعد خاطئ لا يُصنَّف VERIFIED أبدًا** | **لا تحويل ضمني**؛ معاملات المقياس **للكشف فقط**، ولا تُعاد كتابة أي قيمة |
| FM-006 | **دورة اشتقاق** (`A→B→A`) | منع الاستنتاج الدائري |
| FM-007 | **القيمة المخزَّنة تخالف الناتج المحسوب** | صحة الحساب — **جوهر B6** |
| FM-008 | الصيغة **غير حتمية** (عشوائية/زمن/حالة خارجية) | حتمية الاشتقاق |
| FM-009 | لغة **اختيار أو تفضيل تصميمي** داخل `[D]` ⇒ يجب أن تكون `[P]` | **القرار ليس اشتقاقًا** |
| FM-010 | تصنيف صريح لكل `[D]`: `VERIFIED` / `MISMATCH` / `UNCOMPUTABLE` | إعلان الحالة صراحةً |

**حدود B6 — ما لا يملكه (يستهلك ولا يعيد تنفيذ):**
- **وجود** `derived_from` و`formula` و`derivation_type` ⇒ **A (F4)**. B6 لا يعيد فرض الشكل.
- **قابلية حل** مراجع `derived_from` ⇒ **B1 (XR-008)**.
- الطوبولوجيا ⇒ **B2** · تداخل الأثاث ⇒ **B3** · الحركة ⇒ **B4** · الأبواب ⇒ **B5**. **B6 لا يفحص أي هندسة.**
- حجب المجهول ⇒ **B7** · انتحال المصدر ⇒ **00.5-E**.

**ما يبقى UNKNOWN إلزامًا:** مُدخل `[U]` أو `[P]` أو `[A]` · مُدخل بلا قيمة عددية · وحدة مفقودة عند لزومها · صيغة غير قابلة للتحليل. **في كل هذه الحالات النتيجة `UNCOMPUTABLE` ولا تُستبدل بقيمة مخمَّنة ولا تتحول تلقائيًا إلى ERROR للمخطط.**

**خارج نطاق B6 نهائيًا:** المعايير الخارجية · الإرغونوميا · التفضيلات التصميمية · التحسين · الإصلاح التلقائي · اقتراح صيغ · **اشتقاق قيمة لم يطلبها أحد** — B6 **لا يفترض أن كل رقم قابل للاشتقاق**؛ يفحص المُعلن فقط.

---

## 00.5-B7 — Unknown / Blocking Integrity

**المسؤولية:** ضمان أن `UNKNOWN` / `UNRESOLVED` / `UNCOMPUTABLE` / `INCOMPLETE` **لا تتسلل** إلى حالة أو قرار أو مخرج يبدو مؤكدًا. B7 **لا يحسب، لا يحل مراجع، لا يملأ مجهولًا، لا يخمّن، ولا يصلح**.

### الحالات المعتمدة (موجودة فعليًا في النموذج — لم تُخترع أي حالة)

| المحور | القيم | المصدر |
|---|---|---|
| `fact.status` | `C` `D` `A` `U` `P` | مخطط A |
| `fact.presence` | `PRESENT` `NOT_PRESENT` `UNKNOWN` | مخطط A (ثلاثي B2/B4) |
| `fact.approval_state` | `PENDING_APPROVAL` `APPROVED` `REJECTED` | مخطط A |
| `meta.status` | `DRAFT` `FROZEN` | مخطط A |
| `proposal.state` | `OPEN` `APPROVED` `REJECTED` `SUPERSEDED` | مخطط A |
| أحكام B6 | `VERIFIED` `MISMATCH` `UNCOMPUTABLE` | B6 (تُستهلك، لا تُعاد) |
| `unknown_entry` | `blocking` + `resolved_on` + `affects` | مخطط A |

### قواعد B7

| القاعدة | ما تفحصه | لماذا B7 وليس غيره |
|---|---|---|
| UB-001 | `[U]` صار `[C]`/`[D]` بينما مدخلة السجل **ما تزال مفتوحة** (`resolved_on` غائب) | انتقال حالة — لا يملكه أحد |
| UB-002 | مدخلة سجل عليها `resolved_on` بينما الحقيقة المرتبطة **ما تزال `[U]`** | تزامن السجل/الحقيقة |
| UB-003 | `presence: UNKNOWN` انقلب إلى `NOT_PRESENT` بلا تأكيد | **`[]` ≠ `NOT_PRESENT` ≠ `UNKNOWN`** |
| UB-004 | **انتشار**: مخرج **مُلزِم** يعتمد على سلسلة تحوي `[U]` أو `UNCOMPUTABLE` | الانتشار — لا يملكه أحد |
| UB-005 | `UNCOMPUTABLE` (حكم B6) يصل إلى مخرج/قرار دون حجب | يستهلك حكم B6 ولا يعيد حسابه |
| UB-006 | **حجب واعٍ بالاعتماد**: تصنيف كل مجهول BLOCKING/AFFECTING/DECLARED_OUT_OF_SCOPE | `UNKNOWN = ERROR` دائمًا **مرفوض** |
| UB-007 | `meta.status = FROZEN` مع وجود حاجب مفتوح | جاهزية |
| UB-008 | قرار (`decision_entry`) مبني على عنصر مجهول/غير قابل للحساب | القرار لا يلد يقينًا |
| UB-009 | تجميع الجاهزية: **يستدعي** `final_approval_readiness` الخاص بـA ولا يكرره | منع ازدواج الملكية |

### ما لا يملكه B7 (حدود صريحة)

- **صحة الحساب** ⇒ B6 (B7 يقرأ الحكم فقط) · **حل المراجع** ⇒ B1/XR-008 · **شكل `[D]` وINV-D1** ⇒ A · **الهندسة** ⇒ B2–B5.
- **قائمة حاجبات الاعتماد المعلنة** (`final_approval_readiness`) ⇒ **A**؛ B7 **يستدعيها ولا يعيد كتابتها**.
- لا معايير · لا إرغونوميا · لا قرارات تصميمية · **لا ملء مجهول** · **لا auto-fix** · لا اقتراح قيم.
- **انتحال `approved_by`** ⇒ **00.5-E** (GAP-01/02).

---

## 00.5-B8 — Output / Representation Integrity

**المسؤولية:** سلامة **تمثيل** الحقيقة الموجودة في SSOT عند خروجها. B8 **لا يولّد مخرجًا ولا يرسم ولا يقيس**؛ يفحص **ادعاءات** المخرج مقابل مصدره.

### تعريف Output Integrity — الطبقات الخمس (**بلا حالات مخترعة**)

| الطبقة | التمثيل الفعلي في النموذج | السلطة |
|---|---|---|
| **DATA / MASTER** | `space` · `walls` · `registers` … | **المصدر الوحيد للحقيقة** |
| **DERIVED OUTPUT** | `class: A` + `DETERMINISTIC_GEOMETRIC_MATCH` | مُلزِم هندسيًا |
| **PRESENTATION OUTPUT** | `class: B` + `GEOMETRY_DERIVED_VISUAL_TREATMENT_MAY_DIFFER` | هندسة مُلزِمة/بصريات غير مُلزِمة |
| **NON-AUTHORITATIVE VISUAL** | `class: C` + `NOT_GEOMETRICALLY_GUARANTEED_NON_BINDING` | **لا سلطة** |
| **FINAL / APPROVAL-BOUND** | `meta.status = FROZEN` + جاهزية A | نهائي |

**اتجاه السلطة أحادي:** `MASTER → A → B → C`. و**`C → MASTER` ممنوع دائمًا**.

### قواعد B8 — Rule → Owner → Why

| القاعدة | ما تفحصه | لماذا B8 |
|---|---|---|
| OR-001 | المخرج يدّعي يقينًا أعلى من مصدره (`class A/B` بينما عناصره `[U]`/`[P]`/`[A]`) | ادعاء التمثيل — لا يملكه أحد |
| OR-002 | **`UNCOMPUTABLE`/`UNKNOWN` يظهر كحقيقة** في مخرج مُلزِم بلا إفصاح | إخفاء عدم اليقين عند الإخراج |
| OR-003 | **فقدان provenance**: بصمة صفرية/عديمة الارتباط بمحتوى الماستر | «لا يفقد provenance أثناء التحويل» |
| OR-004 | **بصمة مكرّرة** لملفين مختلفين ⇒ أحدهما يكذب | تفرّد الأثر |
| OR-005 | **مفارقة زمنية**: `generated_on` أقدم من الماستر الذي يدّعيه | التسلسل الزمني للاشتقاق |
| OR-006 | **لغة سلطة زائفة** في `caption` (final/approved/as-built/for construction) بينما الماستر `DRAFT` أو غير جاهز | «لا يبدو نهائيًا» |
| OR-007 | مشروع اختباري يُخرج بلا وسم `TEST PROJECT` | منع تسرب مخرج اختباري |
| OR-008 | **التلوث العكسي**: حقيقة `[C]` مصدرها `AI_IMAGE` أو مخرج | **`AI IMAGE → CONFIRMED FACT` ممنوع** |
| OR-009 | تصنيف وعي المخرج: `AUTHORITATIVE` / `DERIVED` / `INDICATIVE` / `NON_BINDING` | **لا `UNKNOWN = ERROR`** |

### ما لا يملكه B8 (حدود مُثبتة بالفحص)

- **class ↔ guarantee ↔ binding** (O1/O2/O3) ⇒ **A/المخطط**. B8 لا يعيد فرضها.
- **ربط `derived_from_output` ودورات المخرجات وتماسك البصمة** ⇒ **B1 (XR-014/015/016/017/018)**.
- الحساب ⇒ B6 · انتقالات عدم اليقين وحجبها ⇒ **B7** (B8 يستهلك ولا يكرر) · الهندسة ⇒ B2–B5.
- **`lint_caption` للفئة C (لغة 100%)** ⇒ **A**. B8 يفحص **لغة السلطة** لا لغة المطابقة.
- **انتحال الاعتماد والهوية** ⇒ **00.5-E**.
- **B8 لا يثبت التطابق الهندسي للملف** (لا يقرأ DXF/GLB) ⇒ **Capability Gap معلن**.

---

## 00.5-B9 — Revision / Changelog Traceability Integrity

### كيف استُخرجت المسؤولية (لا من الاسم)

**Phase 00 لا تذكر «B9» إطلاقًا** (grep = صفر). سلسلة B تفكيك هندسي اخترناه، لا قائمة تفرضها المواصفة، وخارطة المواصفة هي: 00.5 ⇒ **C/D/E/F**. لذلك **لم أشتق المسؤولية من الاسم**، بل بحثت عن **اختصاص متبقٍ فعلًا** وأثبته بالفحص.

**النتيجة المُثبتة:** المخطط يخزّن حقول تتبّع (`master_hash_before/after` · `geometry_version_before/after` · `tests_rerun` · `regressions_found` · `output_diff_explained`)، لكن **A تفرضها على `TECHNICAL_FIX` فقط (TF1)** و`cr_id` على `CR_APPLIED` (CR2). أما **`INITIAL` و`FREEZE` و`REGENERATION` فبلا التزامات إثبات**، **ولا أحد يطابق السجل مع `meta`**.

**سبر الحدود — خمس حالات مرّت من A وB1 وB6 وB7 وB8 جميعًا:**

| الحالة | النتيجة |
|---|---|
| `meta.revision=R07` لا يظهر في السجل | **ALL GREEN** |
| `INITIAL` يقول R99 و`meta` يقول R01 | **ALL GREEN** |
| `FREEZE` بـ`master_hash_after` ≠ `meta.master_hash` | **ALL GREEN** |
| `FROZEN` بلا أي قيد `FREEZE` في السجل | **ALL GREEN** |
| `REGENERATION` بلا `output_diff_explained` | **ALL GREEN** |
| ترتيب زمني عكسي في السجل | **ALL GREEN** (B1 التقطت `cr_id` فقط لا الزمن) |

⇒ **B9 اختصاص مستقل مُثبت، وليس إعادة تسمية لقاعدة قائمة.**

### B9 Owns — Rule → Why

| القاعدة | ما تفحصه | لماذا B9 |
|---|---|---|
| RT-001 | `meta.revision` غير مذكور في السجل ⇒ إصدار بلا أثر | لا مالك سابق |
| RT-002 | قيد سجل يذكر إصدارًا لا يعرفه الماستر | تناسق السجل/الماستر |
| RT-003 | `FROZEN` بلا قيد `FREEZE` مقابل | التجميد حدث يجب أن يُوثَّق |
| RT-004 | `FREEZE` بهاش يخالف `meta.master_hash` | البصمة المجمَّدة يجب أن تطابق |
| RT-005 | `REGENERATION` بلا `output_diff_explained` | إعادة التوليد تغيّر المخرجات ⇒ تفسير |
| RT-006 | ترتيب زمني عكسي بين القيود | سببية السجل |
| RT-007 | `INITIAL` مكرر أو غائب مع وجود تاريخ | نقطة بدء واحدة |
| RT-008 | قيد يعترف `regressions_found=true` خارج TECHNICAL_FIX | الاعتراف بانحدار لا يُبتلع |

### B9 Consumes (حسب الحاجة الفعلية فقط)

- **A:** `meta` وTF1/CR2 و`final_approval_readiness` — **يُستهلك ولا يُعاد**.
- **B1:** وجود `cr_id` (XR-012) — B9 **يصمت** عن الوجود ويفحص **التماسك** فقط.
- **B7/B8:** لا استدعاء في R01 (لا حاجة مُثبتة) — **عدم الاستهلاك قرار معلن لا سهو**.

### B9 Must Not Own

TF1/CR2 و`meta` ⇒ **A** · وجود المراجع ⇒ **B1** · الطوبولوجيا/الأثاث/الحركة/الأبواب ⇒ **B2–B5** · الحساب ⇒ **B6** · انتقالات المجهول ⇒ **B7** · **provenance المخرجات ⇒ B8** · **انتحال الاعتماد والهوية ⇒ 00.5-E**.

**خارج القدرة (معلن):** B9 **لا يحسب الهاش من المحتوى** ⇒ لا يثبت أن `master_hash` صادق؛ يفحص **التماسك الداخلي** فقط.

---

## 00.5-B10 — Register Lifecycle / Decision-State Integrity

### كيف استُخرجت المسؤولية (لا من الاسم)

**Phase 00 لا تذكر «B10»** — كما لم تذكر B9. لذلك بحثتُ عن **اختصاص متبقٍ** بمسح أكواد القواعد المملوكة فعليًا (INV/XR/WT/FC/MV/DS/FM/UB/OR/RT) ثم بفحص **آلات الحالة** في السجلات.

**الاكتشاف الحاسم:** القيم `REJECTED` · `SUPERSEDED` · `DEFERRED` · `APPLIED` · `USER_OVERRIDE` **لا تظهر في أي محرك على الإطلاق**. B1 يفحص أن الروابط **موجودة** (XR-010/011/013)، وA يفحص **شكل** القيد، **ولا أحد يفحص أن الحالات متماسكة منطقيًا**.

**سبر الحدود — ثماني حالات مرّت من A وB1 وB6 وB7 وB8 وB9 معًا:**

| الحالة | النتيجة |
|---|---|
| اقتراح `REJECTED` ومع ذلك له قرار معتمد | **ALL GREEN** |
| اقتراح `OPEN` وله قرار بالفعل | **ALL GREEN** |
| اقتراح `SUPERSEDED` ما يزال يقود قرارًا | **ALL GREEN** |
| قرار قائم بينما اعتراض عليه `OPEN` | **ALL GREEN** |
| CR `REJECTED` يظهر كـ`CR_APPLIED` في السجل | **ALL GREEN** |
| CR `DRAFT` طُبِّق بالفعل | **ALL GREEN** |
| اعتراض `USER_OVERRIDE` بلا `user_override_of` مقابل | **ALL GREEN** |
| حقيقة `approval_state=APPROVED` وسجلها يقول `REJECTED` | **ALL GREEN** |

⇒ **اختصاص مستقل مُثبت:** تماسك **دورة حياة القرار** — لا وجود الروابط (B1) ولا شكلها (A).

### B10 Owns — Rule → Why

| القاعدة | ما تفحصه | لماذا B10 |
|---|---|---|
| RL-001 | قرار مبني على اقتراح `REJECTED`/`SUPERSEDED` | **Proposal ≠ Decision** — لا مالك سابق |
| RL-002 | اقتراح `APPROVED`/`REJECTED` بلا قرار مقابل (والعكس) | تماسك ثنائي الاتجاه في **الحالة** لا في الرابط |
| RL-003 | قرار قائم واعتراض عليه `OPEN` بلا `USER_OVERRIDE` | **واجب الاعتراض** |
| RL-004 | `USER_OVERRIDE` بلا `user_override_of` مقابل | التجاوز حدث يجب أن يُوثَّق |
| RL-005 | CR في السجل بحالة تناقض ما طُبِّق فعليًا | دورة حياة CR |
| RL-006 | CR `APPLIED`/`REJECTED` ثم تغيّر لاحقًا | لا رجوع بعد الحسم |
| RL-007 | `approval_state` في الحقيقة يخالف سجلها | **انجراف الحقيقة عن السجل** |
| RL-008 | افتراض `REJECTED` ما يزال يغذّي بيانات حيّة | المرفوض لا يبقى عاملًا |

### B10 Consumes
**A:** شكل القيود و`final_approval_readiness` · **B1:** وجود الروابط (XR-010/011/013) — **يُستهلك ولا يُعاد**. **B7/B8/B9 لا تُستهلك في R01** (لا حاجة مُثبتة) — **قرار معلن لا سهو**.

### B10 Must Not Own
شكل القيد ⇒ **A** · وجود الروابط ⇒ **B1** · B2–B5 · الحساب ⇒ **B6** · المجهول ⇒ **B7** · المخرجات ⇒ **B8** · **السجل والإصدارات ⇒ B9** · **انتحال الاعتماد والهوية ⇒ 00.5-E**.

**خارج القدرة (معلن):** B10 **لا يتحقق من صدق الاعتماد** (GAP-02) ولا من **من** اتخذ القرار؛ يفحص **التماسك المنطقي بين الحالات** فقط.

---

## 00.5-B11 — Temporal Integrity / Chronological Coherence

### كيف استُخرجت المسؤولية (لا من الاسم ولا من الترقيم)
Phase 00 **لا تذكر B11** (كما لم تذكر B9 وB10). لم تُفترض المسؤولية من تسلسل الترقيم. المسار:

1. **مسح استهلاك المفاتيح** لكل قسم من النموذج عبر المحركات الأحد عشر.
2. **اكتشاف حاسم:** المخطط **يفرض** حقول تاريخ إلزامية في عشرة تعريفات (`recorded_on` · `raised_on` · `approved_on` · `created_on` · `generated_on` · `retrieved_on` · `resolved_on` · `frozen_on`)، لكن `grep` أثبت أن **`raised_on` و`approved_on` و`recorded_on` لا يقرأها أي محرك إطلاقًا**. القارئان الوحيدان للتاريخ: **B8** (`generated_on` مقابل `created_on`، OR-فقط للمخرجات) و**B9** (`date` **داخل** الـchangelog حصرًا، RT-006).
3. **سبر تجريبي** لاثنتي عشرة حالة ضد A+B1…B10 مجتمعة ⇒ **UNOWNED بالكامل**.
4. **ضوابط سلبية:** حالة متماسكة زمنيًا تبقى نظيفة، ما يثبت أن الاختصاص ليس رفضًا شاملًا.

**المبدأ المنتهَك:** المخطط يفرض تخزين التاريخ، والسابقة المعتمدة تقول **«ممنوع تخزين حقل بينما المحرك يتجاهله»**. الزمن مخزَّن ومُهمَل.

### جدول السبر (اثنتا عشرة حالة مرّت من A+B1…B10 معًا)
| # | الحالة | المالك قبل B11 |
|---|---|---|
| 1 | قرار اعتُمد **قبل** أن يُرفع اقتراحه | UNOWNED |
| 2 | اعتراض رُفع **قبل** القرار الذي يعترض عليه | UNOWNED |
| 3 | مجهول حُلّ **قبل** أن يُرفع | UNOWNED |
| 4 | CR اعتُمد **قبل** أن يُرفع | UNOWNED |
| 5 | قيد سجل مؤرَّخ **قبل** إنشاء المشروع | UNOWNED |
| 6 | حقيقة `recorded_on` **قبل** إنشاء المشروع | UNOWNED |
| 7 | حقيقة `recorded_on` في **مستقبل بعيد** | UNOWNED |
| 8 | قرار اعتُمد **بعد** تجميد R01 وما يزال يدّعي R01 | UNOWNED |
| 9 | `frozen_on` موجود بينما `status=DRAFT` | UNOWNED |
| 10 | مرجع خارجي `retrieved_on` في المستقبل | UNOWNED |
| 11 | حقيقة اعتُمدت **قبل** أن تُسجَّل | UNOWNED |
| 12 | محتوى سُجّل **بعد** التجميد بلا CR | UNOWNED |

### B11 Owns — Rule → ما تفحصه → لماذا B11
| القاعدة | ما تفحصه | لماذا |
|---|---|---|
| **TI-001** | `approved_on` للقرار يسبق `raised_on` لاقتراحه | لا يُعتمد ما لم يُقترح بعد |
| **TI-002** | `raised_on` للاعتراض يسبق `approved_on` لقراره | لا يُعترض على ما لم يوجد |
| **TI-003** | `resolved_on` يسبق `raised_on` (مجهول/CR) | لا يُحَل سؤال قبل طرحه |
| **TI-004** | أي تاريخ في السجلات يسبق `meta.created_on` | لا يسبق المحتوى وعاءه |
| **TI-005** | `approved_on` للحقيقة يسبق `recorded_on` | لا يُعتمد ما لم يُسجَّل |
| **TI-006** | أي تاريخ في **مستقبل** بالنسبة لأحدث تاريخ معلن في الملف | تاريخ مستقبلي = ادعاء غير قابل للتحقق |
| **TI-007** | محتوى مؤرَّخ **بعد** `frozen_on` بلا CR مرتبط | التجميد يعني توقف الزمن على المعتمد |
| **TI-008** | `frozen_on` موجود مع `status=DRAFT` أو غائب مع `FROZEN` | علامة التجميد وتاريخه يتناقضان |

### B11 Consumes
`iter_facts` من `schema_gate` (قراءة فقط) · حقول التاريخ الموجودة أصلًا في المخطط. **لا يستورد أي محرك تحقق.**

### B11 Must Not Own
شكل التاريخ ونمطه ⇒ **A** · وجود الروابط ⇒ **B1** · B2–B5 · الحساب ⇒ **B6** · المجهول ⇒ **B7** · `generated_on` للمخرجات ⇒ **B8** · **ترتيب قيود الـchangelog داخليًا (RT-006) ووجود قيد FREEZE (RT-003) ⇒ B9** · **حالات السجلات ⇒ B10** · **صدق التاريخ وهوية من كتبه ⇒ 00.5-E**.

**خارج القدرة (معلن سلفًا):** B11 يفحص **الاتساق الداخلي للتواريخ المصرَّح بها فقط**. لا ساعة خارجية ولا تحقق من الواقع: **تاريخ مفبرك لكنه متسق داخليًا يمرّ**. الدقة يوم واحد (لا وقت)، فأحداث اليوم نفسه لا تُرتَّب.


---

## خارج نطاق الطبقات الحالية

| الاختصاص | المالك المستقبلي |
|---|---|
| ~~الحركة وعرض الممرات~~ | ✅ مملوكة الآن لـB4 |
| ~~دوائر فتح الأبواب~~ | ✅ مملوكة الآن لـB5 |
| ~~صحة المعادلات حسابيًا~~ | ✅ مملوكة الآن لـB6 |
| ~~بوابة المجهول الحاجب~~ | ✅ مملوكة الآن لـB7 |
| ~~سلامة تمثيل المخرجات~~ | ✅ مملوكة الآن لـB8 |
| ~~تتبّع الإصدارات والسجل~~ | ✅ مملوكة الآن لـB9 |
| ~~دورة حياة القرارات والسجلات~~ | ✅ مملوكة الآن لـB10 |
| ~~الاتساق الزمني للتواريخ~~ | ✅ مملوكة الآن لـB11 |
| انتحال المصدر والاعتماد (GAP-01/02) | 00.5-E |
