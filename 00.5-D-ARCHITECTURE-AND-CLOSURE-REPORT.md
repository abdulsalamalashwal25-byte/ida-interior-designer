# 00.5-D — ARCHITECTURE & CLOSURE REPORT
**معمارية فقط — لا كود · لا Schema · لا Master · لا تعديل C1–C6 · لا validators منفَّذة · لا `outputs[]` · لا fingerprint · لا identity · لا approval · لا تصميم هندسي.**

> **المبدأ المؤسِّس:** **D = Independent Fidelity Judge, not a Repair Engine.**
> عند اكتشاف خلل: **D يثبت ويصف عدم المطابقة.** لا يُصلح · لا يُعيد التوليد · لا يُعدّل الماستر · لا يقترح تصميمًا بديلًا.

---

## 0. قياسات أُجريت قبل التصميم (لا افتراض)

فحصت ما يمكن استخراجه فعليًا من مخرج C3 الحقيقي مقابل ما تحمله GA:

| العنصر | في GA | في SVG المُنتَج | قابل للتحقق من SVG وحده؟ |
|---|---|---|---|
| معرّفات العناصر | ✅ | ✅ `data-id` | **نعم** |
| إحداثيات النقاط | ✅ | ✅ `x1/y1/x2/y2` | **نعم** |
| الأطوال | مشتقة | مشتقة | **نعم (حسابيًا)** |
| **الوحدات** | ✅ `unit: mm` | ❌ **ضمنية** | **لا** |
| **`host_wall`** | ✅ | ❌ **غير موجود** | **لا** |
| **`offset` الأصلي** | ✅ | ❌ (يظهر كإحداثي محسوب فقط) | **لا** |
| **`thickness`** | ✅ `200 mm` | ❌ **لا يصل إطلاقًا** | **لا** |
| **provenance** | ✅ | ❌ | **لا** |
| `rotation` | — | — | **خارج النطاق** (Tier 0+1 بلا أثاث) |

**وقياس ثانٍ:** مخرج B يحمل **المعرّفات والإحداثيات نفسها** حرفيًا (مطابقة تامة مع A).

⇒ **هذه الجدولة هي التي تحدد نطاق D كله.** أي ادعاء تحقق خارجها **غير قابل للإثبات بالأدوات القائمة**.

---

## 1. تعريف Fidelity في نظامنا

> **Fidelity = مطابقة المخرج لمصدر محدد ومُعلن، ضمن الحقول القابلة للاستخراج من ذلك المخرج، وفق عقد استخراج معتمد.**

| Fidelity تعني ✅ | Fidelity **لا** تعني ❌ |
|---|---|
| ما في المخرج يطابق ما في المصدر | أن المصدر نفسه صحيح |
| لا عنصر مخترع ولا مفقود ضمن النطاق | أن التصميم جيد أو معقول |
| الإحداثيات المستخرَجة تطابق المُصرَّحة | مطابقة الواقع المبني |
| العلاقات المُمثَّلة تطابق المُصرَّحة | جودة بصرية أو جمالية |
| **حكم ضمن نطاق مُعلن** | **حكمًا مطلقًا على المخرج** |

**قاعدة قاطعة:** **D لا يحكم على جودة التصميم.** المخرج المطابق لماستر يحوي خطأ تصميمي **يجتاز D** — لأن D يقيس المطابقة لا الصواب. وهذا **مقصود**: خلطهما يجعل D مصدر حقيقة جديدًا.

---

## 2. التمييز الخماسي — خمسة مفاهيم لا تُخلط

| # | المفهوم | يثبت | **لا يثبت** | القوة |
|---|---|---|---|---|
| **1** | **Provenance** | أن القيمة **جاءت من** حقل في الماستر | أنها صحيحة، ولا أنها **وُضعت في الموضع الصحيح** | ضعيف جدًا |
| **2** | **Element-count agreement** | لا إضافة ولا حذف **عددًا** | أن العناصر **نفسها** أو في **مواضعها** | ضعيف |
| **3** | **Structural consistency** | تماسك البنية داخليًا (معرّفات فريدة، مراجع محلولة) | مطابقة المصدر | متوسط |
| **4** | **Geometric fidelity** | أن **القيم الهندسية المستخرَجة** تطابق المصرَّحة | ما لا يمكن استخراجه (§0) | **الأقوى المتاح** |
| **5** | **Visual fidelity** | تشابه بصري | **لا شيء هندسيًا** | **لا حجية** |

**تسلسل ملزم:** `1 ⊂ 2 ⊂ 3 ⊂ 4`. واجتياز الأضعف **لا يدل** على الأقوى.
**و(5) مستبعدة كليًا** من حجية D — تشابه بصري ليس دليلًا، وصورة AI ليست مصدر حقيقة أبدًا.

---

## 3. ما تستطيع D إثباته — وما لا تستطيع

### ✅ قابل للإثبات (ضمن الأدوات القائمة)
- **تطابق مجموعة المعرّفات** بين GA والمخرج.
- **تطابق الإحداثيات** المستخرَجة مع المُصرَّحة (قيمة بقيمة).
- **عدم وجود عنصر مخترع** أو مفقود ضمن نطاق المخرج.
- **تطابق الأطوال المحسوبة** من الإحداثيات.
- **تطابق A مع B** في الهندسة (قياس §0: متطابقان حرفيًا).
- **اتساق الإغفالات المُعلنة** مع ما هو غائب فعلًا.

### ❌ **غير قابل للإثبات من المخرج** (مقاس §0)
| البند | السبب |
|---|---|
| **الوحدات** | ضمنية في SVG — لا تظهر |
| **`host_wall`** | غير مُصدَّر |
| **`offset` الأصلي** | غير مُصدَّر (يذوب في إحداثي) |
| **`thickness`** | **لا يصل إلى SVG إطلاقًا** رغم وجوده في GA |
| **provenance** | غير مُصدَّر |
| **topology الكاملة** | لا تُستخرج من خطوط منفصلة |
| **rotation** | خارج نطاق Tier 0+1 |

### ❌ خارج صلاحية D أصلًا
صحة الماستر · جودة التصميم · المعقولية الهندسية · مطابقة الواقع · الاعتماد · الهوية · البصمات.

---

## 4. الموقع في السلسلة

```
Master ──► C1 (إذن) ──► C3 ──┬──► GA Artifact ──► A output
                              └──► C5 ──► B output
                    C4 ──► Quantity Schedule
                              │
                              ▼
                         C6 Manifest (أدلة مجمَّعة)
                              │
                              ▼
                    ┌──────────────────┐
                    │   D — FIDELITY   │  حكم مستقل
                    └────────┬─────────┘
                             ▼
                        E (حوكمة · outputs[] · اعتماد)

  ✗ D ──╳──► Master     ✗ D ──╳──► إصلاح     ✗ D ──╳──► إعادة توليد
```

**D يستهلك:** GA Artifact (المصدر) · المخرجات (A/B) · بيان C6 (السياق والحدود).
**D لا يستدعي** C3/C4/C5 لإعادة الإنتاج، **ولا يبني هندسة**.

---

## 5. مصدر الحقيقة الذي تقارن به D

> ### **GA Artifact هو مرجع المقارنة — لا الماستر مباشرة، ولا SVG.**

| المرشح | الحكم |
|---|---|
| **GA Artifact** | ✅ **المرجع** — هو ما غذّى A وB فعلًا، ويحمل الإحداثيات وprovenance، ومُثبت أنه معزول وثابت |
| الماستر | ⚠️ **المصدر الجذري** لكن المقارنة معه مباشرة تعني **إعادة تجميع** — أي أن D تصير مولّدًا |
| SVG | ❌ **تمثيل مشتق** — ليس مصدر حقيقة |

**تبعية:** تطابق GA مع الماستر **ملك C3** (provenance)، وD **يستهلك** ذلك ولا يعيد التحقق منه. **ويُسجَّل هذا كحد صريح:** D يثبت `output ↔ GA` **ولا يثبت** `GA ↔ Master` ⇒ **DEC-D-02**.

---

## 6. معالجة الحالات

| الحالة | سلوك D |
|---|---|
| **Tier 0** (محيط · جدران) | يُتحقق منه — قابل للاستخراج |
| **Tier 1** (فتحات) | يُتحقق من **موضعها المرسوم** فقط؛ **`host_wall` و`offset` غير قابلين للتحقق** (§0) ⇒ **`NOT_VERIFIABLE` جزئي معلن** |
| **PARTIAL** | يُتحقق من **الموجود**؛ والغائب **لا يُعدّ فشلًا** — يُقارَن بقائمة الإغفالات المُعلنة |
| **ABSTAIN** | **لا مخرج ⇒ لا حكم fidelity** ⇒ `NOT_VERIFIABLE` (وليس FAILED) |
| **UNKNOWN / `[U]`** | مستبعد في C3 ⇒ **غيابه من المخرج هو السلوك الصحيح**؛ ظهوره = **FAILED** |
| **NOT_PRESENT** | غياب مشروع ⇒ **لا يُتوقع ظهوره**؛ ظهوره = FAILED |
| **UNCOMPUTABLE** | حكم B6/B7 مستهلَك ⇒ غيابه صحيح |
| **unsupported representation** | صيغة لا يملك D عقد استخراج لها ⇒ **`NOT_VERIFIABLE` معلن، لا تخمين** |

**قاعدة حاكمة:** **لا حالة من هذه تتحول إلى PASS أو FAIL بالافتراض.**

---

## 7. ما تتحقق منه D — بحدود صريحة

| # | البند | الحالة | الطريقة |
|---|---|---|---|
| 1 | **وجود العناصر** | ✅ | مجموعة معرّفات GA == مجموعة معرّفات المخرج |
| 2 | **الهوية** | ✅ | تطابق المعرّفات نصًا |
| 3 | **الإحداثيات** | ✅ | قيمة بقيمة، بلا تقريب |
| 4 | **الأطوال** | ✅ | محسوبة من الإحداثيات المستخرَجة |
| 5 | **الجدران** | ✅ | نقطتان لكل معرّف |
| 6 | **الفتحات** | ⚠️ **جزئي** | الموضع المرسوم فقط |
| 7 | **علاقة host/opening** | ❌ **NOT_VERIFIABLE** | **`host_wall` غير مُصدَّر** (مقاس) |
| 8 | **Topology** | ❌ **NOT_VERIFIABLE** | لا تُستخرج من خطوط منفصلة |
| 9 | **Rotation** | ⚪ **خارج النطاق** | Tier 0+1 بلا أثاث |
| 10 | **الوحدات** | ❌ **NOT_VERIFIABLE** | **ضمنية في SVG** (مقاس) |
| 11 | **Omissions** | ✅ | قائمة الإغفالات ↔ الغياب الفعلي |
| 12 | **Partial outputs** | ✅ | الوسم ↔ التغطية الفعلية |

**سبعة من اثني عشر قابلة للتحقق فعليًا. والباقي `NOT_VERIFIABLE` — ويجب أن يُعلَن لا أن يُفترض نجاحه.**

---

## 8. منع الحكم عند نقص البيانات

> **بوابة الكفاية (Sufficiency Gate) قبل أي حكم.**

| الشرط | إن غاب |
|---|---|
| GA Artifact متاح | `NOT_VERIFIABLE` |
| المخرج متاح ونصه مقروء | `NOT_VERIFIABLE` |
| **عقد استخراج معتمد للصيغة** | `NOT_VERIFIABLE` |
| الحقل المطلوب **قابل للاستخراج** | `NOT_VERIFIABLE` **لذلك الحقل تحديدًا** |
| ارتباط المخرج بالـGA معلن | `NOT_VERIFIABLE` |

**قاعدة fail-closed:** **غياب الدليل ليس دليل نجاح، ولا دليل فشل.** وهذا يمنع **false PASS** و**false FAIL** معًا.

---

## 9. نموذج الحالات — خمسة، وكلها لازمة

| الحالة | المعنى | متى |
|---|---|---|
| **VERIFIED** | تطابق مُثبت **ضمن النطاق المُعلن** | كل الحقول القابلة للتحقق تطابقت |
| **NOT_VERIFIABLE** | **لا يمكن الحكم** | البيانات/الأدوات غير كافية (§8) |
| **FAILED** | **عدم مطابقة مُثبت** | قيمة مستخرَجة تخالف المصرَّحة، أو عنصر مخترع/مفقود |
| **PARTIAL** | جزء تُحقق منه وجزء `NOT_VERIFIABLE` | الحالة **الغالبة عمليًا** (§7) |
| **ABSTAIN** | D امتنع عن الفحص أصلًا | لا مخرج · لا مرجع · خارج النطاق |

**هل نحتاجها كلها؟ نعم — وأبرر:**
- دمج `NOT_VERIFIABLE` مع `FAILED` ⇒ **false FAIL** (نعاقب مخرجًا سليمًا لعجز أداتنا).
- دمجه مع `VERIFIED` ⇒ **false PASS** — وهو **الخطر الأكبر**.
- بلا `PARTIAL` سيُجبر كل مخرج على حكم أحادي رغم أن **7/12 فقط قابلة للتحقق**.
- `ABSTAIN` ≠ `NOT_VERIFIABLE`: الأول **لم يُفحص**, والثاني **فُحص وتعذّر الحسم**.

**وكل حكم يحمل نطاقه:** `VERIFIED` بلا قائمة الحقول المتحقَّق منها **ممنوع**.

---

## 10. التعامل مع SVG الحالي

| القاعدة | التطبيق |
|---|---|
| **SVG ليس مصدر حقيقة** | المرجع GA؛ وSVG **موضوع الفحص** لا مقياسه |
| **provenance ليس fidelity** | لا يُقبل كدليل مطابقة |
| **element count ليس fidelity** | شرط لازم غير كافٍ |
| **reverse extraction وحده لا يكفي** | يثبت **ما استُخرج فقط**, ويبقى `host/offset/thickness/units` خارج المدى |

**الطريقة المقترحة:** استخراج **بعقد صريح** (ما يُستخرج وكيف يُقارَن)، ثم مقارنة بـGA، ثم **إعلان كل حقل لم يُتحقق منه**. والنتيجة الواقعية المتوقعة: **`PARTIAL` مع قائمة `NOT_VERIFIABLE` صريحة** — لا `VERIFIED` مطلق.

---

## 11. كيف تتحقق D بلا أن تصير مولّدًا

| ممنوع على D | البديل |
|---|---|
| إعادة تجميع GA | استقبالها جاهزة |
| حساب هندسة جديدة | **مقارنة قيم فقط** |
| استنتاج قيمة غائبة | إعلانها `NOT_VERIFIABLE` |
| اقتراح تصحيح | وصف الفرق |
| إعادة توليد المخرج | لا شيء |

**الفرق الجوهري:** D يسأل *«هل ما في المخرج = ما في المرجع؟»* ولا يسأل *«ماذا ينبغي أن يكون؟»*. السؤال الثاني تصميم، والأول قياس.

**وحساب الطول من إحداثيات مستخرَجة ليس توليدًا** — هو **قياس للمقارنة** ولا يُكتب في أي مكان ولا يصير قيمة مرجعية.

---

## 12. ما يبقى خارج D

| البند | المالك |
|---|---|
| الاعتماد والهوية و`outputs[]` والحوكمة | **E** |
| صحة الماستر · جودة التصميم · المعقولية | خارج 00.5 |
| البصمات والتقنين والحتمية | **C2** (محجوبة) |
| **إثبات `GA ↔ Master`** | **C3 provenance** ⇒ DEC-D-02 |
| تصدير `host/thickness/units` إلى المخرج | **C3** (مغلق) ⇒ GAP-D-01 |
| **الإصلاح وإعادة التوليد** | **لا أحد داخل D** |

---

## 13. ATTACK SURFACE

| # | الخطر | الحدّ |
|---|---|---|
| **AS-D-01** | **false PASS** — `VERIFIED` رغم حقول لم تُفحص | §9: كل حكم بنطاقه · `PARTIAL` افتراضيًا |
| **AS-D-02** | **false FAIL** — عجز الأداة يُقرأ خطأً | §8: `NOT_VERIFIABLE` ≠ `FAILED` |
| **AS-D-03** | **D تصير مولّدًا** | §11 |
| **AS-D-04** | **D تصلح المخرج** | Judge not Repair — وصف فقط |
| **AS-D-05** | **D تعدّل الماستر أو GA** | read-only + no-write |
| **AS-D-06** | **SVG كمصدر حقيقة** | §10 |
| **AS-D-07** | **provenance/count كدليل fidelity** | §2 |
| **AS-D-08** | **tolerance يتحول إلى تصحيح** | §20 |
| **AS-D-09** | **normalization تخفي فرقًا حقيقيًا** | §20 |
| **AS-D-10** | **حكم على جودة التصميم** | §1 — لا standards |
| **AS-D-11** | **صورة AI كدليل** | مستبعدة كليًا |
| **AS-D-12** | **`NOT_VERIFIABLE` يُقرأ نجاحًا** | نص صريح في كل تقرير |
| **AS-D-13** | **تعميم نطاق الحكم** | الحكم مقيَّد بالحقول المفحوصة |
| **AS-D-14** | **D تنتحل ملكية C2/E** | لا بصمة · لا هوية · لا اعتماد |
| **AS-D-15** | **إعادة تفسير حالات C6** | تُستهلك كما هي |

---

## 14. INVARIANTS غير قابلة للتفاوض

1. **D لا يصلح ولا يولّد ولا يعدّل** — Judge only.
2. **غياب الدليل ليس دليلًا** — لا PASS ولا FAIL.
3. **كل حكم يحمل نطاقه** صراحةً.
4. **SVG ليس مصدر حقيقة.**
5. **provenance ≠ fidelity · count ≠ fidelity · visual ≠ geometric.**
6. **D لا يحكم على جودة التصميم.**
7. **tolerance للمقارنة لا للتصحيح.**
8. **D لا يملك هوية ولا بصمة ولا اعتمادًا.**
9. **لا لغة «100%»** إلا بنطاق حتمي مُثبت.
10. **نجاح الاختبار يثبت نطاقه فقط.**

---

## 15. القرارات المفتوحة — تحتاج Supervisor

| # | القرار | الخيارات | التوصية | Blocker؟ |
|---|---|---|---|---|
| **DEC-D-01** | **عقد الاستخراج الدلالي (SEC)** لـSVG: ما يُستخرج وكيف يُقارَن | (أ) عقد صريح معتمد · (ب) استخراج مخصص | **(أ)** | 🔴 **نعم** |
| **DEC-D-02** | هل يشمل D إثبات `GA ↔ Master`؟ | (أ) لا — ملك C3 · (ب) نعم | **(أ)** — (ب) تعني إعادة تجميع | 🔴 **نعم** |
| **DEC-D-03** | **سياسة tolerance** | (أ) تطابق تام بلا تسامح · (ب) عتبة معتمدة | **(أ) للإصدار الأول** | 🔴 **نعم** |
| **DEC-D-04** | نطاق الإصدار الأول | (أ) A فقط · (ب) A+B · (ج) +C4 | **(ب)** — A↔B متطابقان (مقاس) | 🟡 |
| **DEC-D-05** | هل تُفحص A↔B مباشرة؟ | (أ) كلاهما ضد GA · (ب) B ضد A | **(أ)** | 🟡 |
| **DEC-D-06** | صيغة تقرير D | (أ) بنية مُعادة · (ب) ملف | **(أ)** | 🟡 |
| **DEC-D-07** | هل يُعلَن `NOT_VERIFIABLE` لكل حقل أم إجماليًا؟ | (أ) لكل حقل · (ب) إجمالي | **(أ)** | 🟡 |

**وتبعية معلنة:** **DEC-C2-07 (مالك SEC)** ما زال مفتوحًا في C2 — وDEC-D-01 يلامسه ⇒ **يُسجَّل التداخل ولا يُحسم هنا**.

---

## 16. الحد الأدنى القابل للتنفيذ (الإصدار الأول)

### ✅ مقترح للتنفيذ
1. **بوابة الكفاية** (§8).
2. **مقارنة المعرّفات** GA ↔ A.
3. **مقارنة الإحداثيات** قيمة بقيمة.
4. **كشف العنصر المخترع/المفقود**.
5. **مقارنة A ↔ B** هندسيًا.
6. **إعلان `NOT_VERIFIABLE` لكل حقل غير قابل للاستخراج** (§7).
7. **تقرير بنطاق صريح** بحالة من الخمس.

### ⏸️ مؤجَّل
`host/offset` · `thickness` · الوحدات · topology الكاملة · rotation · C4 · فئة C · tolerance · `GA ↔ Master` · أي صيغة غير SVG.

---

## 17. خطة الاختبار (توصيف — قبل أي كود)

| الفئة | ما يجب إثباته |
|---|---|
| **Positive** | GA ومخرج متطابقان ⇒ `VERIFIED` **بنطاق مُعلن** · وA↔B متطابقان |
| **Negative** | إحداثي مُعدَّل ⇒ **`FAILED`** بتحديد العنصر · عنصر محذوف ⇒ FAILED · **عنصر مخترع ⇒ FAILED** |
| **NOT_VERIFIABLE** | غياب عقد استخراج · حقل غير مُصدَّر (`thickness`) ⇒ **`NOT_VERIFIABLE` لا PASS ولا FAIL** |
| **Boundary** | مخرج جزئي · صفر عناصر · عنصر واحد · مخرج ممتنع |
| **Mutation** | تعطيل مقارنة الإحداثيات ⇒ سقوط · جعل `NOT_VERIFIABLE` يُحتسب PASS ⇒ سقوط · قبول فرق ضمن tolerance ⇒ سقوط · **+ CONTROL** · وفق **AS-C2-16/17** و**AS-C4-18** |
| **Isolation** | لا إعادة تجميع · لا استيراد مولّد · **لا كتابة** · لا حكم على الماستر |
| **Anti-Smuggling** | حالة لكل AS-D-01…15 |
| **No-write** | سلوكي + تدقيق مسارات (شرط C3 رقم 4) |
| **Regression** | الأساس **1028/1028** |

**اختباران حاسمان:** **false PASS** (حقل غير مفحوص لا يُحتسب نجاحًا) · **false FAIL** (عجز الأداة لا يُعدّ فشلًا).

---

## 18. مخاطر false PASS / false FAIL

| الخطر | السبب | التصميم المضاد |
|---|---|---|
| **false PASS** | اعتبار `NOT_VERIFIABLE` نجاحًا · حكم بلا نطاق · قبول تشابه بصري | **الافتراضي `PARTIAL`** · `VERIFIED` يتطلب **تعداد الحقول** · لا حجية بصرية |
| **false FAIL** | عجز أداة يُقرأ فشلًا · فرق تمثيلي لا هندسي · tolerance غائب حيث يلزم | **`NOT_VERIFIABLE` منفصلة** · §19 · `FAILED` يتطلب **فرقًا مُثبتًا في قيمة مستخرَجة** |

**عدم التماثل المقصود:** **false PASS أخطر** — لأنه يمنح ثقة زائفة، بينما false FAIL يوقف العمل ويُكتشف. لذلك **الافتراضي تحفظي**.

---

## 19. اختلافات التمثيل التي ليست اختلافًا هندسيًا

| الفرق | الحكم المقترح |
|---|---|
| ترتيب العناصر في الملف | **ليس فرقًا هندسيًا** — المقارنة بالمعرّف |
| المسافات البيضاء والتنسيق | ليس فرقًا |
| `6000` مقابل `6000.0` | **⚠️ يحتاج قرارًا** — `1000.0` تظهر للفتحات و`6000` للجدران (مقاس) ⇒ **DEC-D-03** |
| الألوان والسماكة (فئة B) | **ليست هندسة** — مستبعدة من المقارنة |
| تعليقات الملف | ليست بيانات |

**قاعدة:** التطبيع **للمقارنة فقط**, ويُعلَن **كل تطبيع مطبَّق**. وما لا يُحسم ⇒ **`NOT_VERIFIABLE` لا PASS**.

---

## 20. منع tolerance/normalization من أن يصيرا تصحيحًا

| # | القاعدة |
|---|---|
| **T-1** | التطبيع يُطبَّق على **نسخة للمقارنة**؛ **المخرج لا يُمسّ** |
| **T-2** | **كل تطبيع يُعلَن** في التقرير |
| **T-3** | **لا تطبيع غير معلن** — وإلا صار إخفاء فرق |
| **T-4** | tolerance **بلا قيمة معتمدة ⇒ تطابق تام** (DEC-D-03) |
| **T-5** | D **لا يكتب قيمة مطبَّعة** في أي مكان |
| **T-6** | عند الشك: **`NOT_VERIFIABLE`**, لا تسوية |

**الفرق الحاسم:** التطبيع يجعل القيمتين **قابلتين للمقارنة**. أما تعديل قيمة لتتطابق فهو **تصحيح** ⇒ **محظور قطعيًا** (AS-D-08/09).

---

## 21. GATE CARD

| البند | الحالة |
|---|---|
| تعريف Fidelity وحدوده | ✅ §1 |
| التمييز الخماسي | ✅ §2 بتسلسل ملزم |
| ما تثبته D وما لا تثبته | ✅ §3 **مبني على قياس** |
| الموقع والمصدر المرجعي | ✅ §4/§5 — **GA لا SVG ولا الماستر** |
| الحالات الثمانية | ✅ §6 |
| البنود الاثنا عشر | ✅ §7 — **7 قابلة · 5 لا** |
| بوابة الكفاية | ✅ §8 fail-closed |
| نموذج الحالات الخمس | ✅ §9 مع تبرير كل حالة |
| SVG · عدم التوليد · ما يبقى خارج D | ✅ §10/§11/§12 |
| Attack Surface | ✅ 15 |
| Invariants | ✅ 10 |
| القرارات المفتوحة | ✅ 7 (**3 حاجبة**) |
| الحد الأدنى والمؤجَّل | ✅ §16 |
| خطة الاختبار | ✅ §17 |
| false PASS/FAIL | ✅ §18 |
| التمثيل وtolerance | ✅ §19/§20 |
| كود / تعديلات | ✅ **صفر** — الانحدار **1028/1028 · NONE** |

### الحالة: 🔴 **HOLD**

**ثلاثة حواجب — كلها قرارات لا اجتهادات:**

| # | الحاجب | لماذا |
|---|---|---|
| **DEC-D-01** | **عقد الاستخراج الدلالي غير معتمد** | بلا عقد يحدد ما يُستخرج وكيف يُقارَن، تصير «fidelity» رأيًا لا قياسًا — **وهو نفس المستوى 2 من Capability الذي أوقف C3/C4/C5/C6** |
| **DEC-D-02** | **حدود `GA ↔ Master`** | بلا حسم قد تنزلق D إلى إعادة تجميع |
| **DEC-D-03** | **سياسة tolerance** | تلامس DEC-C2-02 المفتوحة؛ واختيارها ذاتيًا = قرار مخفي |

**ملاحظة جوهرية أسجّلها:** القياس في §0 يعني أن أي تنفيذ لـD **سيُنتج `PARTIAL` في الغالب لا `VERIFIED`** — لأن `host/offset/thickness/units` **لا تصل إلى المخرج أصلًا**. **هذه ليست عيبًا في D، بل حدًّا في ما يُصدِّره C3** ⇒ **GAP-D-01** (لا تُعالَج هنا: C3 مغلق).

**لم أكتب كودًا ولم أمسّ Schema/Master/C1–C6، ولم أبدأ E. بانتظار مراجعتكم.**
