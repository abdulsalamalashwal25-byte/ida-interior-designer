# 00.5-C6 — ARCHITECTURE CLOSURE REVISION **R02**
**مراجعة معمارية فقط — لا كود · لا tests · لا Schema · لا Master · لا تعديل C1–C5 · لا D/E · لا Manifest implementation.**

> **الهدف المعلن:** جعل C6 **Manifest/Evidence Assembler بأقل دلالات ممكنة**, ومنع انزلاقه إلى Validator أو Governance Layer.
> **R01 = 🔴 HOLD.** هذه المراجعة تعالج ثماني نقاط، ولا توسّع النطاق.

---

## 1. التعديلات — ملخص R01 ⇒ R02

| # | ما كان في R01 | ما صار في R02 |
|---|---|---|
| **1** | **تناقض:** «نقل الحالات كما صرّحت» ثم تعريف `GENERATED` كتحويل من `EMITTED` | **أُلغي `GENERATED` نهائيًا.** `source_status` يُنقل **حرفيًا** مع `source_owner`. **لا حالة على مستوى البيان** (§3) |
| **2** | بيان يضم جزئيًا **يُوسم جزئيًا** (DEC-C6-04) | **سُحب.** لا «Manifest Status». **ملخص عددي ميكانيكي فقط** بلا دلالة جديدة (§4) |
| **3** | `approval: NOT_APPLICABLE — owned by E` | **سُحب** — كان حكمًا مقنّعًا. ثلاثة تمثيلات منفصلة (§9) |
| **4** | `class: NOT_SET` عند غياب الحقل | + **تمييز `producer_channel` (بنيوي) عن `declared_class` (مُصرَّح)** (§5) |
| **5** | التعارض يُعلَن بلا حكم | **عقد تمثيل صريح للمراجعات المختلطة** + اكتشاف مقاس: **A لا يحمل `master_revision` أصلًا** (§6) |
| **6** | `orphan output` كخطر/حكم | **سُحب الحكم.** `parent_ref: NOT_SET` + `parent_evidence: MISSING` فقط (§7) |
| **7** | `stale evidence` بلا تفصيل | **ثلاثة طوابع مفصولة** + اكتشاف مقاس: **لا طبقة تُنتج أي طابع زمني** (§8) |
| **8** | JSON كصيغة | **Output Contract Level 2 كامل** بـ14 بندًا (§11) |

---

## 2. 🔎 قياسات هذه المراجعة (لا افتراض)

| # | القياس | النتيجة |
|---|---|---|
| **M-1** | مفردات الحالة الفعلية | **C3:** `EMITTED`/`ABSTAIN` **فقط، بلا أي حقل اكتمال** · **C5:** `EMITTED`/`ABSTAIN` + `coverage: COMPLETE/PARTIAL` · **C4:** `completeness: COMPLETE/PARTIAL/ABSTAIN` |
| **M-2** | **الطوابع الزمنية** | **لا طبقة تُنتج أي طابع** — A وB وQ: **صفر حقول زمنية** |
| **M-3** | **مراجعة الماستر** | **B** يحملها بنيويًا في `lineage_evidence` · **A لا يحملها إطلاقًا** (داخل تعليق SVG فقط) · **Q لا يحملها** |
| **M-4** | تمييز القناة | **A:** `contract="floor-plan/tier-0+1"` + `capability.output` · **B:** `contract="presentation/class-b/tier-0+1"` + `class="B"` |

**M-3 هو الأخطر:** التحقق من تعارض المراجعات **غير ممكن بنيويًا لـA اليوم**.

---

## 3. STATUS SEMANTICS — نقل حرفي، بلا طبع

> ### **القاعدة: C6 ينقل، ولا يترجم.**

**أُلغيت `GENERATED`** — كانت إعادة تسمية لـ`EMITTED`، وهي **إعادة تفسير محظورة**.

### 3.1 التمثيل المعتمد

لكل عضو، **حقلان منفصلان لا ثالث لهما**:

| الحقل | المحتوى |
|---|---|
| `source_status` | **القيمة الحرفية كما صرّحت بها الطبقة** (`"EMITTED"` · `"ABSTAIN"`) |
| `source_owner` | الطبقة المالكة (`C3` · `C4` · `C5`) |
| `source_status_field` | **اسم الحقل الأصلي** (`status` · `coverage` · `completeness`) |

**مثال تمثيلي:** `{source_owner: "C5", source_status_field: "status", source_status: "EMITTED", source_coverage_field: "coverage", source_coverage: "PARTIAL"}`.

### 3.2 محظورات

❌ تحويل `EMITTED` إلى `GENERATED` · ❌ توحيد مفردات C3/C4/C5 · ❌ اشتقاق اكتمال لـA من `not_represented` · ❌ **أي «manifest-level status» تشغيلي أو هندسي**.

### 3.3 الحالة الوحيدة التي يُنشئها C6
**`NOT_SET`** — وهي **إعلان غياب مُدخل**, ليست حكمًا ولا قيمة. تُستعمل حصرًا حين **لا تُمرَّر** المعلومة.

> **إن احتاجت D أو E حالة على مستوى البيان مستقبلًا، فهي عقد مستقل يُعتمد صراحةً — ولا تُخلط بحالة المصدر** ⇒ **DEC-C6-10 (جديد، مفتوح)**.

---

## 4. MANIFEST AGGREGATION SEMANTICS

**DEC-C6-04 سُحب بصيغته السابقة.** وجود عضو جزئي **لا يجعل البيان جزئيًا** — ذلك **اشتقاق دلالي جديد** يخرق مبدأ «أقل الدلالات».

**البديل المعتمد — ملخص ميكانيكي بحت:**

```
aggregate_summary:
  members_total            : <عدد>
  by_source_status         : { "EMITTED": n, "ABSTAIN": n }        ← عدّ حرفي
  by_source_coverage       : { "COMPLETE": n, "PARTIAL": n, "NOT_SET": n }
  members_without_coverage : <عدد>        ← A ليس لديه حقل اكتمال (M-1)
```

**خصائصه الملزمة:** **عدّ فقط** · **بلا حالة مشتقة** · **بلا ترجيح** · **بلا حكم** · وكل عضو **يحتفظ بحالته كما هي**.

**القاعدة النصية:** *«هذا الملخص عدّ ميكانيكي لحالات الأعضاء. لا يمثّل حالة للبيان، ولا يُقرأ كحكم على اكتمال التسليم.»*

---

## 5. IDENTITY MODEL — القناة مقابل الفئة

### 5.1 التمييز الجوهري (معالجة DEC-C6-03)

| المفهوم | المعنى | المصدر | إن غاب |
|---|---|---|---|
| **`producer_channel`** | **دور معماري**: أي مكوّن أنتج السجل | **بنيوي ومقاس**: `contract` + `capability.output` (M-4) | `NOT_SET` |
| **`declared_class`** | **حقل `class` المصرَّح داخل السجل** | حقل صريح فقط (B لديه `"B"`؛ **A ليس لديه**) | **`NOT_SET`** |

**كيف يثبت C6 أن سجلًا من قناة A؟** بحقل **`contract`** البنيوي (`"floor-plan/tier-0+1"`) — وهو **جزء من عقد C3 ومُصرَّح في السجل**، لا استنتاج.

**ثلاثة محظورات:** ❌ استنتاج `class="A"` من **غياب** الحقل · ❌ **قراءة تعليق SVG كمصدر بيانات** · ❌ الخلط بين القناة والفئة.

> **`producer_channel = A-channel` لا يساوي `declared_class = "A"`.** الأول دور إنتاج، والثاني تصنيف حجية. **وC6 لا يشتق الثاني من الأول.**

### 5.2 هوية المخرج
**بلا تغيير عن R01:** لا طبقة تُنتج `output_id` ⇒ `NOT_SET` ما لم يصرّح المستدعي · **لا توليد** · `provisional_digest` **ليس** artifact_id ولا بصمة هندسية · **`outputs[]` و`output_id` الرسمي ملك E**.

---

## 6. REVISION MODEL — المراجعات المختلطة

### 6.1 الاكتشاف المقاس (M-3)

| السجل | `master_revision` بنيويًا؟ |
|---|---|
| **B (C5)** | ✅ في `lineage_evidence` |
| **A (C3)** | ❌ **غير موجود** — في تعليق SVG فقط |
| **Q (C4)** | ❌ غير موجود |

⇒ **اكتشاف المراجعات المختلطة غير ممكن بنيويًا اليوم** إلا لما يُمرِّره المستدعي.

### 6.2 عقد التمثيل المعتمد

```
revision_view:
  declared_revisions : { "A": "NOT_SET", "B": "R01", "Q": "NOT_SET" }
  comparable         : false          ← لأن بعض الأعضاء بلا مراجعة مصرَّحة
  divergence         : NOT_DETERMINABLE
```

**ثلاث حالات وثلاثة سلوكيات فقط:**

| الحالة | التمثيل |
|---|---|
| كل الأعضاء بمراجعة مصرَّحة **متطابقة** | `comparable: true` · `divergence: NONE_OBSERVED` |
| كل الأعضاء بمراجعة مصرَّحة **مختلفة** (A=X · B=Y · Q=X) | `comparable: true` · `divergence: OBSERVED` + **سرد المراجعات كما هي** |
| عضو أو أكثر **بلا مراجعة** | `comparable: false` · `divergence: NOT_DETERMINABLE` |

**أربعة محظورات:** ❌ اختيار الأحدث · ❌ إخفاء التعارض · ❌ خلط الأدلة · ❌ **رفض التجميع من تلقاء C6** (الرفض حكم).

> **`OBSERVED` رصد لا حكم.** ما إذا كان الخلط مقبولًا **قرار D/E**.

---

## 7. PROVENANCE MODEL — الأب واليتيم

**`orphan` سُحب كحكم** (معالجة AS-C6-16). C6 لا يعرّف يتيمًا ولا يحكم به.

**التمثيل المعتمد:**

| الحقل | القيم |
|---|---|
| `parent_ref` | المرجع المصرَّح · أو **`NOT_SET`** |
| `parent_evidence` | `PRESENT` · **`MISSING`** |

**مثال:** مخرج B بلا A مناظر مُمرَّر ⇒ `{parent_ref: NOT_SET, parent_evidence: MISSING}` — **وقف. بلا كلمة «orphan» وبلا تحذير تقييمي.**

**والحكم لمن؟** للطبقة المالكة أو **D/E** بحسب الاختصاص. **C6 يسجّل الغياب فقط.**

**وقاعدتا R01 باقيتان:** **provenance ≠ fidelity** · **lineage ≠ engineering identity** · **ووجود البيان ليس دليل صحة**.

---

## 8. الطوابع الزمنية — ثلاثة مفاهيم مفصولة

### 8.1 الاكتشاف المقاس (M-2)
**لا طبقة من A/B/Q تُنتج أي طابع زمني.** فأي طابع في البيان **سيكون من صنع C6**, وتقديمه كوقت إنتاج **تزوير**.

### 8.2 التمثيل المعتمد

| الطابع | المعنى | المصدر | إن غاب |
|---|---|---|---|
| `production_timestamp` | متى أُنتج المخرج | **الطبقة المنتِجة** | **`NOT_SET`** (الحالة اليوم) |
| `evidence_timestamp` | متى التُقط الدليل | مُنتِج الدليل (مثل C2) | `NOT_SET` |
| `manifest_assembly_timestamp` | متى جُمِّع البيان | **C6** | يجوز أن يُسجَّل **موسومًا بوضوح** |

**ثلاثة محظورات:** ❌ استعمال وقت التجميع كوقت إنتاج · ❌ اختراع طابع لمخرج سابق · ❌ دمج الثلاثة في حقل واحد.

> **وقت التجميع لا يُثبت متى أُنتج المخرج، ولا يمنح الدليل حداثة.** وB11 تملك سلامة الزمن، وC6 **لا يحكم على تقادم الدليل** — يعرض الطوابع أو `NOT_SET`.

---

## 9. APPROVAL SEMANTICS — ثلاثة تمثيلات منفصلة

**`approval: NOT_APPLICABLE` سُحب** — كان **حكمًا مقنّعًا** بأن الاعتماد لا ينطبق، وC6 لا يملك ذلك.

| التمثيل | المعنى | من يقوله |
|---|---|---|
| `approval_managed_by: "E"` | **C6 لا يدير الاعتماد** — بيان ملكية | حقيقة معمارية |
| `approval_value: NOT_SET` | **لم تصل قيمة اعتماد إلى C6** — بيان غياب مُدخل | C6 |
| *(غائب عن البيان)* | **الاعتماد الفعلي** | **E وحده** |

**القاعدة:** C6 يقول **«لم يصلني»** ولا يقول **«لا ينطبق»** ولا **«غير معتمد»**. والثلاثة مختلفة، وخلطها **اختراع verdict**.

---

## 10. EVIDENCE vs VERDICT — بلا تغيير جوهري

| السؤال | المالك |
|---|---|
| هل المخرج يطابق الماستر؟ | **D** |
| هل هو معتمد؟ | **E** |
| هل الهندسة صحيحة؟ | **B2–B5** |
| هل المراجعات المختلطة مقبولة؟ | **D/E** (§6) |
| هل الدليل متقادم؟ | **خارج C6** (§8) |
| **ماذا وصلني وبأي حدود؟** | **C6** ✅ |

**C6 لا يعيد تفسير `severity` ولا يغيّر نتيجة طبقة.**

---

## 11. OUTPUT CONTRACT — LEVEL 2 (معماري فقط)

**الصيغة:** JSON خارجي. **والصيغة وحدها ليست عقدًا** — وهذه بنوده الأربعة عشر:

### 11.1 Structure
```
manifest_kind            : "EXTERNAL_OUTPUT_MANIFEST"
manifest_contract_version: <معرّف نسخة العقد>
written_to_master        : false
assembled_by             : "C6"
project_id · master_revision : مُمرَّران أو NOT_SET
manifest_assembly_timestamp  : موسوم (§8)
revision_view            : (§6)
aggregate_summary        : عدّ ميكانيكي (§4)
members[]                : سجل لكل مخرج
notices[]                : القواعد النصية الملزمة
```

### 11.2 حقول العضو — إلزامي / اختياري

| الحقل | الإلزام |
|---|---|
| `producer_channel` · `source_owner` · `source_status` · `source_status_field` | **إلزامي** |
| `declared_class` · `contract` · `format` | **إلزامي** (يقبل `NOT_SET`) |
| `parent_ref` · `parent_evidence` | **إلزامي** |
| `engineering_fingerprint` · `determinism` | **إلزامي** |
| `fidelity` | **إلزامي** — دائمًا `NOT_ASSESSED` |
| `approval_managed_by` · `approval_value` | **إلزامي** |
| `source_coverage` · `omissions` · `exclusions` · `parameters_*` · `lineage_evidence` · `environment_evidence` · `capability` · `limitations` | اختياري — يُدرَج **إن مُرِّر** |
| `production_timestamp` · `evidence_timestamp` | اختياري — `NOT_SET` اليوم |

### 11.3 البنود الاثنا عشر الباقية

| البند | الحكم |
|---|---|
| **source-status preservation** | نقل حرفي + `source_owner` + اسم الحقل الأصلي (§3) |
| **NOT_SET semantics** | **إعلان غياب مُدخل فقط** — ليس `NOT_PRESENT` ولا `UNKNOWN` ولا حكمًا |
| **provenance** | نقل بلا إعادة بناء + قاعدتا §7 |
| **revision representation** | `revision_view` بثلاث حالات (§6) |
| **partial representation** | حالة العضو كما هي + عدّ ميكانيكي · **لا ترقية ولا تحويل إلى NOT_PRESENT** |
| **identity boundary** | لا توليد معرّف · القناة ≠ الفئة (§5) |
| **fingerprint boundary** | `NOT_ISSUED` · `provisional_digest` بوسمه · **لا اختراع عقد غير مغلق** |
| **evidence vs verdict** | §10 |
| **A/B/C separation** | فئة C **قسم منفصل موسوم غير موثوق** · لا تحويل فئة · لا مجاورة بلا فاصل |
| **no outputs[] ownership** | **لا قراءة ولا كتابة** — ملك E |
| **no Master write** | لا كتابة · ولا قراءة هندسية |
| **no invented semantics** | لا قيمة ولا حالة ولا طابع ولا معرّف ولا حكم — **الغياب يُعلَن** |

### 11.4 Notices الإلزامية داخل البيان
`provenance ≠ fidelity` · `lineage ≠ engineering identity` · `وجود البيان ليس دليل fidelity` · `الملخص التجميعي عدّ ميكانيكي لا حالة` · `NOT_SET إعلان غياب لا حكم`.

---

## 12. DEC-C6-01…10 بعد المراجعة

| # | القرار | الحالة بعد R02 | Blocker؟ |
|---|---|---|---|
| **DEC-C6-01** | هوية المخرج | **`NOT_SET` ما لم يصرّح المستدعي · لا توليد** — الملكية **E** | 🔴 نعم |
| **DEC-C6-02** | حالة الاكتمال غير الموحّدة | **نُقل كل مصدر بحقله واسم حقله · لا توحيد · لا اشتقاق لـA** (§3) | 🟡 **خُفِّض** — العقد صار صريحًا |
| **DEC-C6-03** | تصنيف A | **`producer_channel` بنيوي ≠ `declared_class` مُصرَّح · غياب ⇒ `NOT_SET`** (§5) | 🟢 **محسوم معماريًا** |
| **DEC-C6-04** | حالة جزئية للبيان | **سُحب.** ملخص ميكانيكي فقط · **لا Manifest Status** (§4) | 🟢 **محسوم بالسحب** |
| **DEC-C6-05** | تقنين البيان | بلا تقنين معتمد · الحد مسجَّل (GAP-C-04) | 🟡 |
| **DEC-C6-06** | حفظ الملف | **يُعيد بنية؛ المستدعي يحفظ** | 🟡 |
| **DEC-C6-07** | المراجعات المختلطة | **عقد `revision_view` بثلاث حالات · رصد لا حكم · لا رفض ذاتي** (§6) | 🟡 **خُفِّض** |
| **DEC-C6-08** | إدراج Quantity Schedule | **نعم بنوعه المصرَّح** · **ليس BOQ ولا Cost Estimate** | 🟡 |
| **DEC-C6-09** | عقد المخرج | **Level 2 مكتمل معماريًا (§11)** — **بانتظار اعتمادكم** | 🔴 نعم |
| **DEC-C6-10** ⭐ جديد | هل تحتاج D/E حالة على مستوى البيان؟ | **مفتوح** — عقد مستقل إن لزم، **لا يُخلط بحالة المصدر** | 🟡 |

---

## 13. ATTACK SURFACE — تحديثات

**AS-C6-01…19 سارية**, مع تعديلين وثلاث إضافات:

| # | التغيير |
|---|---|
| **AS-C6-14** | أُعيدت صياغته: الخطر هو **اختراع طابع** أو تقديم وقت التجميع كوقت إنتاج — **لا الحكم بالتقادم** |
| **AS-C6-16** | أُعيدت صياغته: الخطر هو **إصدار حكم «orphan»**؛ المطلوب `parent_evidence: MISSING` فقط |
| **AS-C6-20** ⭐ | **Status translation** — طبع حالات المصدر أو إعادة تسميتها (`EMITTED → GENERATED`) ⇒ §3 |
| **AS-C6-21** ⭐ | **Aggregate-status invention** — اشتقاق حالة للبيان من حالات الأعضاء ⇒ §4 |
| **AS-C6-22** ⭐ | **Approval verdict smuggling** — `NOT_APPLICABLE`/`NOT_APPROVED` بدل «لم يصلني» ⇒ §9 |

---

## 14. OPEN GAPS

**لم تُغلق أي ثغرة.**

| # | الحالة |
|---|---|
| **GAP-C6-01** حالة الاكتمال غير موحّدة | 🔴 **قائمة** — عولجت **عرضًا** لا مصدرًا |
| **GAP-C6-02** لا هوية مخرج رسمية | 🔴 قائمة |
| **GAP-C6-03** ⭐ جديدة | **A لا يحمل `master_revision` بنيويًا** (M-3) ⇒ كشف تعارض المراجعات **غير ممكن لـA** |
| **GAP-C6-04** ⭐ جديدة | **لا طبقة تُنتج طابعًا زمنيًا** (M-2) ⇒ `production_timestamp` = `NOT_SET` دائمًا |
| GAP-C-02 · 04 · 06 · 08 · 14 · 01 · 03 · 05 · 07 · 10 · 11 · 12 · 13 | 🔴 كما هي |
| DEC-C2-01…08 · OBS-C5-01 | 🔴 مفتوحة |

**تنبيه:** GAP-C6-03/04 **لا تُعالَج داخل C6** — معالجتهما تعني تعديل C3/C4/C5 المغلقة، وهو محظور. تُسجَّلان كحدود.

---

## 15. CAPABILITY STATUS

| المستوى | الحالة |
|---|---|
| **1 — Format** | ✅ JSON بالمكتبة القياسية · لا تبعية خارجية |
| **2 — Approved output contract** | 🟡 **مكتمل معماريًا (§11) — غير معتمد** |
| **3 — Runtime** | ✅ متاح (فحص لحظي · GAP-C-02 نشطة) |

---

## 16. GATE CARD — R02

| البند | الحالة |
|---|---|
| **1 Status preservation** | ✅ `GENERATED` أُلغيت · نقل حرفي · لا حالة للبيان |
| **2 Manifest-level PARTIAL** | ✅ **سُحب** · ملخص ميكانيكي فقط |
| **3 Approval semantics** | ✅ ثلاثة تمثيلات · `NOT_APPLICABLE` سُحب |
| **4 A classification** | ✅ القناة ≠ الفئة · لا قراءة تعليق SVG |
| **5 Cross-revision** | ✅ عقد بثلاث حالات · **+GAP-C6-03** |
| **6 Parent/orphan** | ✅ الحكم سُحب · `parent_evidence` فقط |
| **7 Stale evidence** | ✅ ثلاثة طوابع · **+GAP-C6-04** |
| **8 Output Contract L2** | ✅ 14 بندًا معماريًا |
| Attack Surface | ✅ 22 (+3 · تعديل 2) |
| Open Gaps | ✅ بلا إغلاق · **+2 جديدتان** |
| كود / tests / تعديلات | ✅ **صفر** — البصمات ثابتة · الانحدار **931/931** |

### C6 Architecture Status: 🔴 **HOLD — لا تُعتبر R02 مغلقة ذاتيًا**

**الحاجبان المتبقيان (قراران خارجيان):**

| # | الحاجب | لماذا يبقى |
|---|---|---|
| **DEC-C6-01** | **هوية المخرج** | لا طبقة تنتجها؛ توليدها اختراع؛ **الملكية E** |
| **DEC-C6-09** | **اعتماد Output Contract Level 2** | صغتُه ولم أعتمده — **الصياغة ليست اعتمادًا**، وهو نفس ما أوقف C3/C4/C5 |

**وقرارات مفتوحة لا تحجب:** DEC-C6-05 · 06 · 07 · 08 · 10 · وDEC-C6-02 (مخفَّض).

**ما رُفع في R02:** DEC-C6-03 و DEC-C6-04 **محسومان معماريًا** — الأول بالتمييز البنيوي، والثاني **بالسحب** لا بالإضافة.

**وأسجّل صراحةً:** اتجاه هذه المراجعة كان **تقليص الدلالات لا توسيعها** — أُلغيت حالة، وسُحب وسم، وسُحب حكمان (`NOT_APPLICABLE` و`orphan`).

**لم أكتب كودًا ولا اختبارات، ولم أمسّ Schema/Master/C1–C5، ولم أبدأ D/E ولا أي Manifest implementation.**
