# 00.5-D — GATE 1 AMENDMENT **R02**
**تصحيحات محصورة في أربع نقاط — لا كود · لا ملفات تنفيذية · لا Schema/Master/C1–C6 · لا توسيع نطاق D.**

> **غير مُعدَّل:** DEC-D-01…07 · No-Repair · No-Generation · Dependency Isolation · حد D↔GA · GAP-D-01.
> **Regression baseline:** 1028/1028.

---

## 1. النسخة النهائية من القواعد المعدَّلة

### CORRECTION-01 — صفر عناصر مستخرَجة

**القاعدة السابقة (مُلغاة):** «صفر عناصر مستخرَجة مع GA غير فارغة ⇒ FAILED».
**السبب:** تتعارض مع الإغفالات المعلنة، وتخلط عجز الاستخراج بالفقد.

#### القاعدة النافذة — **ترتيب تقييم إلزامي**

```
لكل عنصر في GA لم يظهر في المخرج، بهذا الترتيب:

  [1] هل الاستخراج فشل لسبب representation مثبت في المخرج؟
        (unsupported primitive · transform · malformed)
        نعم ⇒ NOT_VERIFIABLE   ← لا يُعاد تصنيفه missing أبدًا
  [2] هل يغطيه إغفال معلن صالح (not_represented / C6)؟
        نعم ⇒ DECLARED_OMISSION — لا FAILED
  [3] غير ذلك ⇒ FAILED (missing element)

النتيجة الإجمالية:
  صفر عناصر مستخرَجة + كل الغياب مغطى بـ[1]        ⇒ NOT_VERIFIABLE
  صفر عناصر مستخرَجة + كل الغياب مغطى بـ[2]        ⇒ VERIFIED/PARTIAL حسب النطاق (لا FAILED)
  صفر عناصر مستخرَجة + عنصر واحد يقع في [3]        ⇒ FAILED
  خليط [1] و[2] بلا [3]                            ⇒ PARTIAL
```

**ملاحظة نطاق مقاسة:** مرجع المقارنة **`GA.walls/openings/outline`** لا الماستر. فعنصر استُبعد في C3 (مثل جدار `[U]`) **ليس في GA أصلًا** ⇒ لا ينشأ «missing» منه. القاعدة تغطي الحالة **بنيويًا** لصيغ ومخرجات مستقبلية.

---

### CORRECTION-02 — unsupported representation ≠ missing

> **أولوية الاستخراج تسبق كشف الفقد.** (الخطوة [1] أعلاه.)

| الحالة | الحكم |
|---|---|
| عنصر GA له counterpart في SVG داخل `path` | **`NOT_VERIFIABLE`** |
| counterpart تحت `transform` | **`NOT_VERIFIABLE`** |
| counterpart ببدائية غير مدعومة (`rect`/`circle`/`polyline`/`use`) | **`NOT_VERIFIABLE`** |
| counterpart بقيمة malformed | **`NOT_VERIFIABLE`** للحقل |
| **لا counterpart إطلاقًا وبلا إغفال معلن** | **`FAILED — missing`** |

**قاعدتان ملزمتان:**
1. **فشل الاستخراج الجزئي لا يعيد تصنيف العنصر كمفقود.** العنصر **موجود** في المخرج؛ ما تعذّر هو **قراءته**.
2. **«لم أستطع القراءة» ≠ «غير موجود».** خلطهما هو **AS-D-02 (false FAIL)** بعينه.

**تمييز التنفيذ:** يُسجَّل `counterpart_present: true/false` **قبل** أي حكم، فيُفصل عجز الأداة عن الفقد الحقيقي.

---

### CORRECTION-03 — ترتيب نقاط polygon (قيد تعاقدي لا ادعاء هندسي)

| # | القاعدة |
|---|---|
| **PG-1** | **V1 يقارن نقاط polygon بالترتيب المعلن في SEC** |
| **PG-2** | **لا cyclic rotation** |
| **PG-3** | **لا reverse-order normalization** |
| **PG-4** | **لا إعادة ترتيب للنقاط** إطلاقًا |
| **PG-5** | اختلاف الترتيب **يُسجَّل وفق العقد الحالي كاختلاف في مقارنة التمثيل/الهندسة** |
| **PG-6** | **لا يُدّعى** أن اختلاف نقطة البداية يعني بالضرورة اختلافًا هندسيًا في كل السياقات |
| **PG-7** | **canonical polygon equivalence قرار جديد مستقل خارج V1** |

**سحب صياغة سابقة:** R01 وصف ترتيب النقاط بأنه «فرق دلالي محتمل». **الصياغة النافذة:** V1 **يقارن بالترتيب المعلن ولا يطبّع**, ويسجّل الاختلاف **بلا تأويل هندسي عام**.

---

### CORRECTION-04 — صياغة دلالات float

#### الصياغة النافذة (تحل محل M-3 وتضيّق N-1…N-6)

> **V1 يحلّل القيم العددية في SVG باستخدام تمثيل النقطة العائمة، ثم يقارن القيم المحلَّلة (parsed-value equality) مقارنة تامة. وهذا ليس ادعاءً بأن النقطة العائمة تمثّل كل قيمة عشرية ممكنة بدقة غير محدودة.**

**قياس يبرر التضييق:** `float('0.1') + float('0.2') != float('0.3')` ⇒ **float لا يمثّل كل عشري بدقة**. فالادعاء الصحيح **parsed-value equality** لا «حفظ الدقة الكاملة».

**وما يبقى ساريًا بلا تغيير:**

| # | القاعدة |
|---|---|
| N-1 | المقارنة على القيمة المحلَّلة لا على النص |
| N-2 | `6000 == 6000.0` ✅ |
| N-3 | `6000 != 5999.9` ⇒ **FAILED** |
| N-4 | **لا tolerance · لا rounding · لا silent normalization** |
| N-5 | نص غير قابل للتحويل ⇒ **malformed ⇒ `NOT_VERIFIABLE`** |
| N-6 | **`NaN` · `inf` · `-inf` ⇒ malformed** — ولا تُعدّ متساوية أبدًا |

#### سلوك V1 على الحدود — **مقاس**

| المدخل | القيمة المحلَّلة | التصنيف |
|---|---|---|
| `NaN` / `nan` | `nan` | **malformed ⇒ NOT_VERIFIABLE** |
| `inf` | `inf` | **malformed** |
| `-inf` | `-inf` | **malformed** |
| **`1e309`** | **`inf`** (overflow) | **malformed** — وليس عددًا كبيرًا |
| `1e-320` | `1e-320` (subnormal) | **finite ⇒ يُقارَن عاديًا** |
| `123456789012345.6` | كما هي | finite ⇒ يُقارَن |
| `0.0000001` | `1e-07` | finite ⇒ يُقارَن |

**نطاق مغلق:** هذا **تثبيت لسلوك V1 بصدق**, **وليس فتحًا لمشروع Decimal أو arbitrary precision**.

---

## 2. الاختبارات المضافة أو المتغيرة

### مضافة — CORRECTION-01
| ID | السيناريو | المتوقع |
|---|---|---|
| **D-C01-01** | GA غير فارغ · صفر عناصر مستخرَجة · **كل الغياب unsupported** | **`NOT_VERIFIABLE`** — لا FAILED |
| **D-C01-02** | GA غير فارغ · صفر مستخرَجة · **كل الغياب بإغفال معلن** | **لا FAILED بسبب missing** |
| **D-C01-03** | GA غير فارغ · صفر مستخرَجة · **عنصر واحد بلا إغفال ولا سبب استخراج** | **`FAILED — missing`** |
| **D-C01-04** | خليط: بعضه unsupported وبعضه مُغفَل معلن، **بلا فقد حقيقي** | **`PARTIAL`** |

### مضافة — CORRECTION-02
| ID | السيناريو | المتوقع |
|---|---|---|
| **D-C02-01** | **GA فيه E-05 · SVG فيه E-05 داخل `path`** | **`NOT_VERIFIABLE`** — **لا FAILED — missing** |
| **D-C02-02** | E-05 داخل `<g transform=…>` | **`NOT_VERIFIABLE`** |
| **D-C02-03** | E-05 ببدائية `rect` | **`NOT_VERIFIABLE`** |
| **D-C02-04** | **E-05 غائب تمامًا وبلا إغفال** | **`FAILED — missing`** (ضابط تمييز) |
| **D-C02-05** | `counterpart_present` يُسجَّل قبل الحكم | حقل موجود في الدليل |

### مضافة — CORRECTION-03 (Boundary)
| ID | السيناريو | المتوقع |
|---|---|---|
| **D-C03-01** | polygon بنفس النقاط **بترتيب دوراني** | **لا إعادة ترتيب تلقائية** — يُسجَّل اختلافًا |
| **D-C03-02** | polygon بترتيب معكوس | **لا reverse normalization** |
| **D-C03-03** | polygon مطابق ترتيبًا | تطابق |
| **D-C03-04** | التقرير **لا يدّعي** أن اختلاف البداية = اختلاف هندسي عام | نص متحفظ |

### مضافة — CORRECTION-04 (Boundary)
| ID | المدخل | المتوقع |
|---|---|---|
| **D-C04-01** | `NaN` | malformed ⇒ `NOT_VERIFIABLE` |
| **D-C04-02** | `inf` | malformed |
| **D-C04-03** | `-inf` | malformed |
| **D-C04-04** | **`1e309`** | **malformed (overflow → inf)** |
| **D-C04-05** | `1e-320` | **finite ⇒ يُقارَن** |
| **D-C04-06** | `123456789012345.6` | finite ⇒ يُقارَن |
| **D-C04-07** | التقرير لا يدّعي دقة عشرية غير محدودة | صياغة `parsed-value equality` |

### متغيرة
| ID | التغيير |
|---|---|
| **#13 Boundary** (R01) | حُذفت منه قاعدة «صفر عناصر ⇒ FAILED»؛ استُبدلت بـD-C01-01…04 |
| **#8 NOT_VERIFIABLE** (R01) | وُسِّع ليشمل **counterpart موجود لكن غير قابل للقراءة** (D-C02-*) |
| **#12 Boundary float** (R01) | أُعيدت صياغته إلى **parsed-value equality** + الحالات الست |
| **§7 التمثيل** (R01) | بند polygon أُعيد ككيد تعاقدي PG-1…PG-7 |

### مضافة — Mutation
| ID | الطفرة | المتوقع |
|---|---|---|
| **M-11** | تحويل `NOT_VERIFIABLE` الناتج عن unsupported إلى `FAILED — missing` | **KILLED** |
| **M-12** | تجاهل الإغفالات المعلنة عند كشف الفقد | **KILLED** |
| **M-13** | تفعيل cyclic rotation لنقاط polygon | **KILLED** |
| **M-14** | قبول `1e309` كعدد صالح | **KILLED** |

**مع الالتزام القائم:** عزل الدفاعات المكرّرة قبل كل طفرة · تصنيف صادق (KILLED/SURVIVED/CONTROL/NON-ACTIVE) · **ولا تعديل للكود لإجبار طفرة على الفشل**.

---

## 3. FINAL CONSISTENCY CHECK

| # | الخطأ الممنوع | الحاجز النافذ | مُغطّى؟ |
|---|---|---|---|
| 1 | unsupported → missing → FAILED | **الخطوة [1] تسبق [3]** + قاعدتا CORRECTION-02 | ✅ D-C02-01…04 · M-11 |
| 2 | declared omission → missing → FAILED | **الخطوة [2] تسبق [3]** | ✅ D-C01-02 · M-12 |
| 3 | NOT_VERIFIABLE → FAILED | حالة مستقلة + أسبقية FAILED **لا تشملها** | ✅ M-04 |
| 4 | NOT_VERIFIABLE → VERIFIED | `VERIFIED` يشترط **صفر حقول غير مفحوصة** | ✅ M-03 |
| 5 | representation difference → hidden normalization → VERIFIED | التطبيع **معلن حصرًا** في `normalizations_applied` · PG-2/3/4 | ✅ M-05 · M-13 |
| 6 | float parsing limitation → ادعاء دقة شاملة | صياغة **parsed-value equality** | ✅ D-C04-07 |

### تعارضات متبقية في العقد

**لا تعارض.** وأسجّل **ملاحظتين حدوديتين** لا تعارضين:

1. **نطاق مرجع المقارنة (مقاس):** عناصر استُبعدت في C3 **ليست في GA**، فحالة «missing حقيقي» **غير قابلة للتوليد من مسار C3 الحالي**. القواعد مكتوبة **بنيويًا** لتغطي صيغًا/مخرجات أخرى، وستُختبر **بمدخلات GA مُركَّبة** لا بعبث بمخرجات C3.
2. **GAP-D-01 قائم:** `host/offset/thickness/units` لا تصل إلى المخرج ⇒ **`PARTIAL` هي النتيجة المتوقعة غالبًا**, و**D لا يعالجها بإعادة الاستنتاج**.

---

## 4. GATE CARD — محدَّث

| البند | الحالة |
|---|---|
| **CORRECTION-01** | ✅ ترتيب تقييم ثلاثي إلزامي + 4 اختبارات |
| **CORRECTION-02** | ✅ أولوية الاستخراج + `counterpart_present` + 5 اختبارات |
| **CORRECTION-03** | ✅ PG-1…PG-7 قيد تعاقدي + 4 اختبارات |
| **CORRECTION-04** | ✅ parsed-value equality + 7 اختبارات حدية مقاسة |
| Final consistency (الأخطاء الستة) | ✅ كلها محجوبة بحواجز صريحة |
| DEC-D-01…07 | ✅ **بلا تغيير** |
| No-Repair · No-Generation · Isolation · D↔GA · GAP-D-01 | ✅ **بلا تغيير** |
| Mutation plan | ✅ 14 + CONTROL · تصنيف صادق |
| Regression | ✅ **1028/1028** |
| نطاق D | ✅ **لم يتوسّع** |
| كود / ملفات / تعديلات | ✅ **صفر** |

### الحالة

# **D Gate 1 = READY / AWAITING SUPERVISOR APPROVAL**

**لم أكتب كودًا ولم أنشئ ملفات تنفيذية ولم أمسّ Schema/Master/C1–C6. لن أبدأ Implementation قبل موافقتكم الصريحة.**
