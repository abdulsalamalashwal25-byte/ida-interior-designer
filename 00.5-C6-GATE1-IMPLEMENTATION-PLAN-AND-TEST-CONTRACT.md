# 00.5-C6 — GATE 1 IMPLEMENTATION PLAN & TEST CONTRACT
**تخطيط وعقد اختبار فقط — لا كود · لا ملفات تنفيذية · لا Schema · لا Master/Template · لا تعديل C1–C5 · لا D/E · لا Manifest · لا output files · لا `output_id` · لا semantics جديدة · لا توسيع نطاق · لا اختبارات منفَّذة.**

> **المرجع:** C6 Architecture **APPROVED/CLOSED** على R03 · **DEC-C6-01 = BOUNDARY/OWNERSHIP** · **DEC-C6-09 = Supervisor-approved contract**.
> **Regression baseline:** 931/931 — **baseline فقط، وليس دليلًا على صحة C6**.

---

## 1. EXECUTIVE SUMMARY

خطة تنفيذ C6 كـ**Manifest / Evidence Assembler بأقل دلالات ممكنة**: يستقبل أدلة C3/C4/C5، **يحفظ دلالات كل منتِج كما هي**, يجمّعها في بيان خارجي، ويُجري **مقارنات وعدّادات ميكانيكية معلنة فقط**.

**ثلاثة اكتشافات قياسية تحكم الخطة** (فُحصت في هذه الجولة، §7):
1. `completeness` في C4 **قاموس** لا قيمة مفردة.
2. **مفاتيح `completeness` و`class_a` غير متطابقة** — `Q-01` في `class_a` وغائب عن `completeness`.
3. **`overall = PARTIAL` قد يتعايش مع عضو `ABSTAIN`** داخل القاموس نفسه.

⇒ أي «تسوية» أو «إكمال» أو «إعادة حساب» من C6 **تزوير للدلالة**. القاعدة: **نقل حرفي**.

**الحالة المطلوبة في النهاية:** `C6 Gate 1 = IMPLEMENTATION PLAN READY / AWAITING SUPERVISOR APPROVAL`. **لم يُنفَّذ شيء بعد.**

---

## 2. SCOPE / NON-SCOPE

### Scope ✅
استقبال evidence من C3/C4/C5 · **حفظ semantics كل producer** · التجميع في Manifest خارجي · تسجيل metadata المعلنة · **مقارنات ميكانيكية محدودة ومعلنة** · **aggregate counts ميكانيكية** · إظهار الحدود و`NOT_SET` · تمرير evidence للطبقات التالية.

### Non-Scope ❌
Validator · Fidelity checker · Engineering judge · Approval engine · Governance engine · Designer · Geometry engine · Quantity engine · **Fingerprint authority** · **Identity authority**.

---

## 3. OWNERSHIP MATRIX

| الجهة | تملك | يُمنع على C6 |
|---|---|---|
| **Master** | SSOT | لا كتابة · لا قراءة هندسية |
| **C1** | الإذن والنطاق | لا إعادة تقييم |
| **C2** | التقنين · البصمات · ENV-FP · D0/D1/D2 | **لا إصدار بصمة** · لا ترقية digest |
| **C3** | الهندسة · A · GA | لا تعديل · لا إعادة حساب |
| **C4** | Quantity Schedule · **دلالة `completeness`** | **لا تحويل ولا إعادة حساب ولا اختزال** |
| **C5** | B · المعاملات · lineage | لا تعديل |
| **D** | **Fidelity** | **لا حكم** |
| **E** | **`outputs[]` · الحوكمة · الاعتماد · `output_id` الرسمي** | **لا كتابة · لا توليد هوية** |

---

## 4. INPUT CONTRACT

| المنتِج | `source_status` | `source_coverage` | `source_completeness` | حقول محفوظة إضافية |
|---|---|---|---|---|
| **C3** | `EMITTED` \| `ABSTAIN` | **`NOT_SET`** | **`NOT_SET`** | `contract` · `format` · `declared_class` (إن وُجد) · provenance · determinism · `fidelity=NOT_ASSESSED` |
| **C4** | **`NOT_SET`** | **`NOT_SET`** | كما صرّح C4 | **`source_completeness_detail`** (القاموس كما هو) · `schedule_type` · `result_kind` · `class_a` · evidence مسموحة |
| **C5** | `EMITTED` \| `ABSTAIN` | `COMPLETE` \| `PARTIAL` | **`NOT_SET`** | `contract` · `format` · `declared_class` · lineage · presentation evidence · `fidelity=NOT_ASSESSED` |

**محظور:** `completeness → status` · اختزال قاموس C4 · اشتقاق حقل غائب من حاضر.

---

## 5. OUTPUT CONTRACT LEVEL 2 (المعتمد)

### 5.1 حقول العضو الإلزامية

| الحقل | القيم المسموحة | الملكية | Inference محظور |
|---|---|---|---|
| `source_owner` | `C3` \| `C4` \| `C5` | المنتِج | — |
| `source_status` | `EMITTED` \| `ABSTAIN` \| `NOT_SET` | المنتِج | من coverage/completeness |
| `source_coverage` | `COMPLETE` \| `PARTIAL` \| `NOT_SET` | المنتِج | من status/completeness |
| `source_completeness` | `COMPLETE` \| `PARTIAL` \| `ABSTAIN` \| `NOT_SET` | المنتِج | من status/coverage |
| `producer_channel` | **`NOT_SET`** (حتى DEC-C6-11) | — | **من contract/format/تعليق/غياب class/معرفة داخلية** |
| `declared_class` | `A` \| `B` \| `C` \| `NOT_SET` | المنتِج | من format/parameters/filename/channel/contract |
| `contract` · `format` | **قيمة خام** | المنتِج | ترجمة إلى channel/class/fidelity/approval/validity |
| `approval_managed_by` | `E` | معماري | — |
| `approval_value` | `NOT_SET` أو ما مرّره **E** | **E** | أي verdict |
| `parent_ref` | مرجع \| `NOT_SET` | المنتِج/المستدعي | — |
| `parent_evidence` | `PRESENT` \| `MISSING` | مُشتق ميكانيكيًا من وجود المرجع | حكم «orphan» |
| `fidelity` | **`NOT_ASSESSED`** | **D** | أي حكم |
| `engineering_fingerprint` | **`NOT_ISSUED`** | **C2** | ترقية digest/hash/env |
| `determinism` | D0 كما ورد · D1 `NOT_ESTABLISHED` · D2 `NOT_CLAIMED` | C2 | رفع المستوى |
| `output_id` | هوية رسمية واردة \| **`NOT_SET`** | **E** | hash/filename/contract/UUID/timestamp |

### 5.2 اختياري
`source_completeness_detail` (عند تصريح المنتِج) · `omissions` · `exclusions` · `parameters_admitted/rejected` · `lineage_evidence` · `environment_evidence` · `capability` · `limitations` · الطوابع الثلاثة.

### 5.3 رأس البيان
`manifest_kind: EXTERNAL_OUTPUT_MANIFEST` · `manifest_contract_version` · `written_to_master: false` · `assembled_by: C6` · `project_id`/`master_revision` (مُمرَّران أو `NOT_SET`) · `manifest_assembly_timestamp` موسوم · `revision_view` · `aggregate_summary` · `members[]` · `notices[]`.

**ولا يُنشأ:** Manifest status/completeness/approval/fidelity/validity (§17).

---

## 6. STATUS / COVERAGE / COMPLETENESS CONTRACT

**ثلاثة حقول مستقلة دلاليًا. C6 ينقل معنى الحقل الأصلي ولا يغيّر اسمه الدلالي.** `source_status_field` **محذوف نهائيًا** (كان آلية الانزلاق في R02).

**`NOT_SET` هنا تعني حصرًا:** *«هذا المنتِج لا يُصرّح بهذا الحقل»*.

---

## 7. C4 COMPLETENESS DETAIL CONTRACT

### 7.1 القياسات الثلاثة

```
Q.completeness = {"Q-02":"PARTIAL", "Q-03":"PARTIAL", "Q-04":"COMPLETE", "overall":"PARTIAL"}
completeness keys : ['Q-02','Q-03','Q-04','overall']
class_a keys      : ['Q-01','Q-02','Q-03','Q-04']      ← Q-01 غائب عن completeness
حالة أخرى         : {'Q-02':'COMPLETE','Q-03':'ABSTAIN','Q-04':'COMPLETE','overall':'PARTIAL'}
```

### 7.2 قواعد الحفظ

| # | القاعدة |
|---|---|
| **P-1** | `source_completeness` = قيمة `overall` **كما صرّح بها C4** — بلا إعادة حساب |
| **P-2** | `source_completeness_detail` = **القاموس كما هو**, بمفاتيحه وترتيبه الدلالي |
| **P-3** | **لا توحيد مفاتيح** مع `class_a` — تفاوت المفاتيح **حقيقة مصدرية** |
| **P-4** | **لا تسوية** بين عضو `ABSTAIN` و`overall=PARTIAL` |
| **P-5** | لا اختزال · لا إعادة تسمية · لا `overall` مخترع · لا إسقاط |

---

## 8. NOT_SET CONTRACT

**يعني حصرًا:** *«المنتِج لم يصرّح بهذا الحقل / القيمة لم تصل إلى C6»*.
**لا يعني:** failed · incomplete · rejected · invalid · not applicable · unknown engineering fact.
**ولا يُخلط** بـ`UNKNOWN` (ملك B7) ولا `NOT_PRESENT` (حقيقة غياب) ولا `ABSTAIN` (قرار طبقة).

---

## 9. PRODUCER CHANNEL CONTRACT

**`producer_channel = NOT_SET`** إلا إن وصل الحقل نفسه. **لا Contract Registry قائم (مقاس)** ⇒ **DEC-C6-11 لا يُنفَّذ الآن**.
**مصادر استنتاج محظورة:** اسم `contract` · `format` · تعليق SVG · غياب `class` · معرفة C6 الداخلية · أعراف التسمية.

---

## 10. DECLARED CLASS CONTRACT

`declared_class` = **قيمة `class` المصرَّحة فقط**. C5 يصرّح `"B"`؛ **C3 لا يصرّح** ⇒ `NOT_SET`.
**محظور الاستنتاج** من: format · presentation parameters · filename · producer_channel · contract.

---

## 11. REVISION COMPARISON CONTRACT

**`revision_view` = mechanical comparison metadata.**

| القيمة | تعني حصرًا | لا تعني |
|---|---|---|
| `comparable=false` | بيانات المراجعة غير مكتملة للمقارنة الكاملة | invalid · failed · rejected · inconsistent · unapproved |
| `divergence=OBSERVED` | القيم المعلنة مختلفة | engineering conflict · geometry error · fidelity failure · approval failure |
| `divergence=NONE_OBSERVED` | القيم المعلنة متطابقة | **أن التصميم صحيح** |
| `divergence=NOT_DETERMINABLE` | لا بيانات كافية | فشل |

**محظور:** اختيار الأحدث · رفض التجميع · إخفاء الاختلاف · **دمج المراجعات في مراجعة جديدة** · إصدار verdict.
**حد مقاس:** A بلا `master_revision` بنيوي (GAP-C6-03) ⇒ `comparable=false` هو الغالب.

---

## 12–16. الحدود الخمسة

| # | الحد | القاعدة |
|---|---|---|
| **12 Approval** | `approval_managed_by=E` **architectural ownership metadata** لا project state · `approval_value=NOT_SET` يعني **«لم يصلني»** فقط | لا verdict |
| **13 Parent** | `parent_ref`/`parent_evidence` كما تصل · غياب ⇒ `NOT_SET`/`MISSING` | **لا «orphan» ولا «broken lineage»** ولا تحويل الغياب إلى engineering failure |
| **14 Fidelity** | `fidelity = NOT_ASSESSED` فقط | لا مقارنة هندسية · **provenance/element-count/وجود Manifest ليست دليل fidelity** — **D يحكم** |
| **15 Fingerprint** | `NOT_ISSUED` · D1 `NOT_ESTABLISHED` · D2 `NOT_CLAIMED` | **لا ترقية** provisional digest / env evidence / manifest hash |
| **16 Identity** | هوية رسمية واردة ⇒ تُحفظ · وإلا **`NOT_SET`** | **لا hash · لا filename · لا contract · لا UUID · لا timestamp** |

---

## 17. AGGREGATE SUMMARY CONTRACT

```
members_total · by_source_status · by_source_coverage · by_source_completeness
```
**ثلاثة عدّادات مستقلة، عدّ حرفي فقط.** لا حالة مجمّعة · لا اكتمال موحّد · لا weighted score · لا severity · لا pass/fail · لا judgment · **ولا جمع بين العدّادات**.

**وC4 يظهر في `by_source_status.NOT_SET` لأنه لا يملك الحقل — لا لأنه failed.**

**Manifest-level semantics:** لا تُنشأ في هذه المرحلة (DEC-C6-10 مؤجَّل).

---

## 18. NO-WRITE CONTRACT

**لا كتابة إلى:** Master · Schema · Template · C1–C5 artifacts · source evidence · `outputs[]`. **ولا تعديل in-place** للكائنات الواردة.

**ثلاثة إثباتات مجتمعة** (لا `open()` وحده): **behavioral no-write** (بصمات + جرد ملفات + تطابق الكائنات قبل/بعد) · **mutation/isolation** · **write-path audit** (`open(` · `.write(` · `json.dump` · `write_text` · `write_bytes` · `os.replace` · `shutil` · `pathlib` · `tempfile` · `pickle` · `os.remove` · `mkdir`).

---

## 19. IMPLEMENTATION FILE PLAN (مقترح — لم يُنشأ)

| الملف | المسؤولية | لا يملك | Dependencies مسموحة | ممنوعة |
|---|---|---|---|---|
| `scripts/c6_contract.py` | ثوابت العقد · القيم المسموحة · دلالة `NOT_SET` · Notices | أي منطق تجميع أو حكم | stdlib فقط | أي طبقة |
| `scripts/c6_preservation.py` | حفظ حقول العضو بلا إعادة تفسير · قاموس C4 | المقارنة والعدّ والتجميع | `c6_contract` · stdlib | C1–C5 · B · D · E |
| `scripts/c6_revision.py` | مقارنة المراجعات **الميكانيكية** | أي حكم | `c6_contract` · stdlib | أي طبقة |
| `scripts/c6_aggregate.py` | العدّادات الثلاثة **الميكانيكية** | أي حالة مجمّعة | `c6_contract` · stdlib | أي طبقة |
| `scripts/c6_manifest.py` | تجميع البيان النهائي | إنشاء قيم أو أحكام | الأربعة أعلاه · stdlib | C1–C5 · B · D · E |
| `tests/test_00_5_C6.py` | عقد الاختبار | — | وحدات C6 + C3/C4/C5 **كمنتِجي fixtures فقط** | — |
| `tests/run_all.py` | **سطر واحد** | — | — | — |

**مبرر استيراد C3/C4/C5 في الاختبارات فقط:** لتوليد **سجلات حقيقية** بدل fixtures مصطنعة. **الملكية لا تنتقل**: وحدات C6 نفسها **لا تستوردها**, ويُثبت ذلك باختبار عزل صريح.

---

## 20. DEPENDENCY BOUNDARY

**ممنوع في وحدات C6:** C1 validators · B-series validators · D · E · أي طبقة خارج عقد التبعية. **ولا استيراد C3/C4/C5 داخل وحدات C6** — تصل سجلاتها **كمعطيات** من المستدعي.

---

## 21. TEST MATRIX

| # | الفئة | الهدف | المدخل | النتيجة المتوقعة | شرط الفشل |
|---|---|---|---|---|---|
| **1** | **Positive** | الحقول تظهر في خاناتها | سجلات C3/C4/C5 حقيقية | C3 status · C5 coverage · C4 completeness · detail كامل | ظهور قيمة في خانة خاطئة |
| **2** | **Negative** | منع النقل الدلالي | سجل C4 | **لا `source_status`** مشتق من completeness | ظهور `source_status=PARTIAL` |
| **3** | **Boundary** | الحالات الحدية | عضو واحد · صفر أعضاء · كل الأعضاء `ABSTAIN` | تمثيل بلا حالة مجمّعة | اختراع حالة |
| **4** | **Preservation** | نقل حرفي | كل الحقول | تطابق تام مع المصدر | أي تحوير |
| **5** | **NOT_SET semantics** | الدلالة | حقول غائبة | `NOT_SET` ≠ failed/invalid/rejected | معاملته كحكم |
| **6** | **Anti-Inference** | منع الاستنتاج | contract/format/filename | `producer_channel=NOT_SET` · `declared_class=NOT_SET` لـC3 | أي استنتاج |
| **7** | **Anti-Smuggling** | AS-C6-01…24 | حالات مُصمَّمة | لا هوية/بصمة/اعتماد/اكتمال/fidelity مخترعة | أي تسريب |
| **8** | **Revision comparison** | ميكانيكية | A=X · B=Y · Q=X · وحالة ناقصة | `OBSERVED` / `NOT_DETERMINABLE` بلا حكم | اختيار الأحدث أو الرفض |
| **9** | **Aggregate isolation** | فصل العدّادات | خليط منتِجين | ثلاثة عدّادات · C4 في `status.NOT_SET` | جمع أو حالة موحّدة |
| **10** | **C4 completeness detail** | حفظ البنية | قاموس بمفاتيح متفاوتة | نقل حرفي · **لا إكمال Q-01** · لا تسوية `ABSTAIN`/`overall` | أي تعديل |
| **11** | **Identity boundary** | لا توليد | بلا `output_id` | `NOT_SET` | hash/filename/UUID |
| **12** | **Approval boundary** | لا verdict | بلا اعتماد | `managed_by=E` · `value=NOT_SET` | `NOT_APPLICABLE`/`REJECTED` |
| **13** | **Fidelity boundary** | لا حكم | أي مدخل | `NOT_ASSESSED` دائمًا | أي حكم أو مقارنة |
| **14** | **Fingerprint boundary** | لا إصدار | digest وenv موجودان | `NOT_ISSUED` · D1/D2 كما هي | ترقية |
| **15** | **Parent/provenance** | لا حكم | بلا parent | `NOT_SET`/`MISSING` | «orphan» |
| **16** | **No-write** | لا كتابة | تشغيل كامل | صفر تغيير · صفر مسار | أي كتابة |
| **17** | **Mutation/Isolation** | فعالية | §22 | حسب التصنيف | طفرة ناجية غير مُعلنة |
| **18** | **Regression** | لا انحدار | `run_all.py` | **931/931 سليمة** | أي انحدار |

---

## 22. MUTATION TEST CONTRACT

**كل طفرة تُصنَّف مسبقًا، ولا رقم مصطنع.** التصنيفات: **KILLED** · **SURVIVED** · **NON-ACTIVE / NOT APPLICABLE** · **CONTROL**.

| # | الطفرة المخطَّطة | التصنيف المتوقع |
|---|---|---|
| M-01 | السماح بنقل `completeness` إلى `status` | KILLED |
| M-02 | اختزال قاموس C4 إلى scalar | KILLED |
| M-03 | إعادة حساب `overall` من التفاصيل | KILLED |
| M-04 | استنتاج `producer_channel` من `contract` | KILLED |
| M-05 | استنتاج `declared_class` من `format` | KILLED |
| M-06 | جمع العدّادات الثلاثة | KILLED |
| M-07 | توليد `output_id` من hash | KILLED |
| M-08 | تحويل `NOT_SET` إلى `NOT_APPLICABLE` في الاعتماد | KILLED |
| M-09 | اختيار أحدث مراجعة | KILLED |
| M-10 | إضافة حقل حالة للبيان | KILLED |
| **CONTROL** | تغيير تجميلي | **CONTROL — لا ينقلب شيء** |

**التزام معلن:** أي طفرة تنجو تُعلَن **SURVIVED** بسببها، وأي طفرة غير قابلة للتفعيل تُعلَن **NON-ACTIVE** — **ولا يُغيَّر التنفيذ لتحسين الرقم** (نفس معالجة C5-M-06).

---

## 23. REGRESSION CONTRACT

**لا regression الآن.** عند بوابة التنفيذ النهائية يُشغَّل `run_all.py`، ويجب **ألا يقع انحدار** في: A · B1–B11 · C1 · C2 · C3 · C4 · C5.
**المرجع: 931/931 — baseline فقط، وليس دليلًا على صحة C6.**

---

## 24. OPEN DECISIONS / DEFERRED

| # | القرار | الحالة | يحتاجه التنفيذ الحالي؟ |
|---|---|---|---|
| **DEC-C6-05** | Canonicalization | **OPEN** | لا ⇒ **Deferred / Not Blocking** |
| **DEC-C6-06** | File Save | **OPEN** | لا — C6 يُعيد بنية والمستدعي يحفظ |
| **DEC-C6-07** | Mixed Revisions | **OPEN** | لا — التمثيل الميكانيكي كافٍ |
| **DEC-C6-08** | Quantity Schedule | **OPEN** | لا — يُدرج بنوعه المصرَّح |
| **DEC-C6-10** | Manifest-level state | **OPEN** | **لا — ولا يدخل النطاق** |
| **DEC-C6-11** | Contract Registry | **OPEN** | **لا — `producer_channel=NOT_SET`** |

**لم أغلق أيًا منها، ولم أحوّل أيًا منها إلى implementation scope.**

---

## 25. RISKS / ATTACK SURFACE

**AS-C6-01…24 سارية.** الأخطر في التنفيذ: **AS-C6-23** (field-semantics migration) · **AS-C6-24** (channel inference) · **AS-C6-21** (aggregate-status invention) · **AS-C6-22** (approval verdict smuggling) · **AS-C6-01/02** (هوية/بصمة مخترعة).
**ولا خطر جديد** نشأ عن هذه الخطة.

---

## 26. CAPABILITY STATUS

| المستوى | الحالة |
|---|---|
| **1 — Format** | ✅ JSON · المكتبة القياسية · لا تبعية خارجية |
| **2 — Approved output contract** | ✅ **معتمد من المشرف (DEC-C6-09)** |
| **3 — Runtime** | ✅ متاح — **فحص لحظي** (GAP-C-02 نشطة) |

**ولأول مرة في C6: المستويات الثلاثة مستوفاة.**

---

## 27. GATE CARD

| البند | الحالة |
|---|---|
| Responsibility contract | ✅ مثبت حرفيًا |
| Input ownership | ✅ لكل منتِج بحقوله |
| Output Contract L2 | ✅ معتمد ومفصَّل |
| status/coverage/completeness | ✅ ثلاثة حقول مستقلة · `source_status_field` محذوف |
| C4 completeness detail | ✅ **P-1…P-5** بثلاثة قياسات |
| NOT_SET | ✅ دلالة محصورة |
| producer_channel | ✅ `NOT_SET` · Anti-Inference |
| declared_class | ✅ مُصرَّح فقط |
| revision comparison | ✅ ميكانيكي بمعانٍ محصورة |
| approval · parent · fidelity · fingerprint · identity | ✅ خمسة حدود |
| Aggregate | ✅ ثلاثة عدّادات منفصلة |
| No-write | ✅ ثلاثة إثباتات |
| File plan · Dependency boundary | ✅ خمس وحدات · استيراد C3/C4/C5 **في الاختبارات فقط** مع مبرر |
| Test Matrix | ✅ 18 فئة بهدف/مدخل/نتيجة/شرط فشل |
| Mutation contract | ✅ **مصنَّف مسبقًا · بلا رقم مصطنع** |
| Regression contract | ✅ 931/931 baseline فقط |
| Open decisions | ✅ ستة **Deferred / Not Blocking** |
| كود / ملفات / اختبارات | ✅ **صفر** |

### التناقضات المعمارية الجديدة
**لا شيء.** الاكتشافات الثلاثة في §7 **تفاوتات مصدرية** تُحفظ كما هي، ومعالجتها داخل C6 **ستكون هي التناقض**.

### الحالة النهائية

# **C6 Gate 1 = IMPLEMENTATION PLAN READY / AWAITING SUPERVISOR APPROVAL**

**لم أكتب كودًا ولا اختبارات، ولم أنشئ ملفات، ولم أمسّ Schema/Master/C1–C5، ولم أبدأ D/E ولا أي Manifest implementation. لا أدّعي تنفيذًا ولا نجاح اختبارات — لم يُنفَّذ شيء بعد.**
